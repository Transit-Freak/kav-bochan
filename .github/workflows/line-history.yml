name: line-history

# "הקו בזמן" — תיעוד יומי של שינויי מסלולים ותחנות מהשוואת GTFS.
# הריצה הראשונה יוצרת בסיס (כל הגרסאות הנוכחיות); מכאן והלאה רק שינויים.

on:
  schedule:
    - cron: '30 7 * * *'   # 10:30 ישראל בקיץ — אחרי חלון הפרסום של משרד התחבורה
  push:
    branches: [main]   # הבסיס נבנה פעם אחת על main — לא על ענפי עבודה
    paths:
      - '.github/workflows/line-history.yml'
      - 'tools/linehistory.py'
      - 'tools/rebuild_lines_index.py'
      - 'tools/enrich_stop_diffs.py'
      - 'tools/fix_dest_kind.py'
      - 'tools/drop_rename_only.py'
      - 'tools/enrich_2017.py'
      - 'tools/fix_false_removed.py'
      - 'tools/mark_reverted_changes.py'
      - 'tools/build_stop_shards.py'
      - 'tools/scan_vcnt_daily.py'
      - 'tools/last_sched.py'
      - 'tools/compact_lines.py'
      # שני אלה רצים בצעד ה-commit — שינוי שבור בהם היה מתגלה רק בריצה
      # הלילית ומפיל את כל שלושת הצינורות (ציד הבאגים, סבב ב)
      - 'tools/build_kavpach_live.py'
      - 'tools/build_archive_months.py'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  # קבוצה נפרדת לכל ref: ריצת ה-push של סנכרון הענף (שמדלגת בגלל תנאי
  # main-בלבד) נכנסת לקבוצה לפני שהתנאי נבדק — בלי זה היא מבטלת את ריצת main
  group: line-history-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run:
    if: github.ref == 'refs/heads/main' || github.event_name != 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 80
    steps:
      - uses: actions/checkout@v4

      - name: Fetch GTFS
        run: |
          set -euo pipefail
          UA="Mozilla/5.0 (Windows NT 10.0; Win64; x64)"
          curl -fSL --connect-timeout 20 --max-time 1200 --retry 15 --retry-all-errors --retry-delay 60 -A "$UA" \
            -o gtfs.zip "https://gtfs.mot.gov.il/gtfsfiles/israel-public-transportation.zip"
          unzip -o gtfs.zip stops.txt stop_times.txt trips.txt routes.txt shapes.txt agency.txt calendar.txt

      - name: Diff line & stop history
        run: |
          set -euo pipefail
          # קובץ סימון ליישור חד-פעמי (תיקון גאומטריות בלי רישום אירועים)
          REBASE=0; [ -f line-history/data/.rebase ] && REBASE=1
          echo "REBASE=$REBASE"
          STOPS=stops.txt STOP_TIMES=stop_times.txt TRIPS=trips.txt ROUTES=routes.txt \
            SHAPES=shapes.txt AGENCY=agency.txt CALENDAR=calendar.txt MAIN=data-main.json \
            OUTDIR=line-history/data REBASE=$REBASE python3 tools/linehistory.py
          rm -f line-history/data/.rebase
          # ניקוי עצמי: זוג אירועים שסוגר את עצמו (רפרוף רישום סביב הפסקה)
          # נמחק — תנודת-נייר איננה שינוי (הלקח מקו 80 כפר חב"ד)
          OUTDIR=line-history/data python3 tools/collapse_wobbles.py
          # פאנל התקלות (בקשת שלמה): כל מחלקות הבעיות, נבדקות בכל סריקה
          OUTDIR=line-history/data python3 tools/qa_checks.py

      # אותה סריקה יומית אחת לכל הדפים: גם מעקב התגבורים (Gtfs_10_days)
      # רץ כאן, והתוצאות נדחפות באותו commit. כשל של שרת הרישוי לא מפיל
      # את הצינור — ההשוואה הבאה תתפוס את הפער.
      - name: מעקב תגבורים (רכבים מרובים באותה נסיעה)
        run: |
          OUTDIR=line-history/data python3 tools/scan_vcnt_daily.py || echo "שרת הרישוי לא זמין — נשלים בריצה הבאה"

      # סוג הרכב שנקבע לכל קו ברישוי משרד התחבורה (data.gov.il, יומי) — שינוי
      # נרשם בקובץ הקו (שדה veh, בלי לגעת בגרסאות המסלול) ובפיד החודשי (שלמה 06.09)
      - name: סוג הרכב ברישוי (משרד התחבורה)
        run: |
          OUTDIR=line-history/data python3 tools/linehistory_rishui.py --day "$(TZ=Asia/Jerusalem date +%F)" || echo "מאגר הרישוי לא זמין — נשלים בריצה הבאה"

      # קו שבוטל ומלאה שנה לביטולו (ולא נותר ממנו שום לו"ז) — המנגנון שואב
      # לבד מהארכיון את שעות-היציאה מיום פעילותו האחרון, ספציפית לכל קו.
      # עד 2 קבוצות-יום בריצה — ביום רגיל אפס-שתיים בודדות חוצות את סף השנה.
      - name: לו"ז אחרון לקווים שבוטלו (שנה ומעלה)
        run: |
          OUTDIR=line-history/data MAX_DAYS=2 python3 tools/last_sched.py || echo "הארכיון לא זמין — נשלים בריצה הבאה"

      # קו באג — אותה סריקה יומית אחת: קוד הזיהוי נמשך מריפו קו-באג (מקור
      # האמת שלו נשאר שם), רץ על אותו GTFS שכבר ירד כאן, והתוצאות נשמרות
      # ב-kavbug-data/; ריפו קו-באג מושך אותן בסנכרון קטן והאתר שלו נשאר
      # באותה כתובת בדיוק.
      - name: סריקת קו באג (עיקופי אוטובוסים) על אותו GTFS
        continue-on-error: true
        run: |
          set -e
          mkdir -p /tmp/kbscan kavbug-data
          for f in scan-country.js update-history.js data.js components.jsx; do
            curl -fsSL "https://raw.githubusercontent.com/Transit-Freak/kav-bug/main/$f" -o "/tmp/kbscan/$f"
          done
          npm install --prefix /tmp/kbscan fflate --no-save
          node --max-old-space-size=6144 /tmp/kbscan/scan-country.js gtfs.zip kavbug-data/country-scan
          node /tmp/kbscan/update-history.js kavbug-data/country-scan.json kavbug-data/history.json

      # איחוד הצינור: ה-GTFS שכבר ירד כאן מפורסם כ-artifact, והצינורות של
      # "הקו המדלג" ו"התחנה הבאה" נדלקים בסיום הריצה וניזונים ממנו —
      # הורדה אחת ביום במקום שלוש, וכל הכלים מתעדכנים על אותו קובץ בדיוק.
      - name: פרסום ה-GTFS לשאר הצינורות
        if: hashFiles('gtfs.zip') != ''
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: gtfs-daily
          path: gtfs.zip
          retention-days: 1
          compression-level: 0

      - name: Commit (לענף שהריץ — main בריצה המתוזמנת)
        run: |
          set -euo pipefail
          BR="${GITHUB_REF_NAME:-main}"
          rm -f gtfs.zip stops.txt stop_times.txt trips.txt routes.txt shapes.txt agency.txt calendar.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # מיזוג לפי קבצים במקום העתקת התיקייה כולה: רק הקבצים שהריצה הזו
          # שינתה נשמרים; קובץ שהשתנה בינתיים גם ב-origin — origin גובר
          # (למשל סיפור מדויק ממכונת הדיוק שנחת בזמן שהריצה רצה).
          BASE=$(git rev-parse HEAD)
          git add -A line-history/data kavbug-data kavpach-live.json
          git diff --cached --name-only --diff-filter=d -z "$BASE" -- line-history/data kavbug-data kavpach-live.json > /tmp/lh-changed
          git diff --cached --name-only --diff-filter=D -z "$BASE" -- line-history/data kavbug-data kavpach-live.json > /tmp/lh-deleted
          rm -rf /tmp/lh-out && mkdir -p /tmp/lh-out
          xargs -0 -r -I{} cp --parents {} /tmp/lh-out/ < /tmp/lh-changed
          for i in 1 2 3 4; do
            git fetch origin "$BR" && git reset --hard "origin/$BR"
            cp -r /tmp/lh-out/. .
            xargs -0 -r rm -f -- < /tmp/lh-deleted
            git diff --name-only -z "$BASE" "origin/$BR" -- line-history/data kavbug-data > /tmp/lh-theirs
            while IFS= read -r -d '' f; do
              git checkout "origin/$BR" -- "$f" 2>/dev/null || rm -f -- "$f"
            done < /tmp/lh-theirs
            OUTDIR=line-history/data python3 tools/enrich_stop_diffs.py
            # סיווג "שינוי יעד" נקבע בסורק לפי מחרוזת השם; כאן הוא מתוקן
            # לפי מק"ט תחנות הקצה. שני הכלים אידמפוטנטיים ומחזירים את
            # מצבם גם אחרי מיזוג של סריקת רקע שדרס גרסאות.
            OUTDIR=line-history/data python3 tools/fix_dest_kind.py
            # ואחרי שהתברר שרק השם השתנה — האירוע יורד מהקו לגמרי. הקו לא
            # השתנה בשום צורה, ושינוי השם מתועד בהיסטוריית התחנה.
            OUTDIR=line-history/data python3 tools/drop_rename_only.py
            OUTDIR=line-history/data python3 tools/enrich_2017.py
            # רשת ביטחון: אם בכל זאת נרשם "בוטל היום" על קו שהסורק מעולם
            # לא ראה חי, הכלי מסיר אותו. תאריך ביטול שגוי בשנים נראה כמו
            # עובדה מדודה, וזה גרוע משתיקה.
            OUTDIR=line-history/data python3 tools/fix_false_removed.py
            # רצף תחנות שהשתנה וחזר תוך ימים אינו שינוי במסלול אלא תנודה
            # בפרסום. האירוע נשאר ומקבל סימון, כי הוא באמת פורסם.
            OUTDIR=line-history/data python3 tools/mark_reverted_changes.py
            # הכלל של שלמה (02.09): תחנה "ירדה" רק אם הייתה במצב המתועד הקודם,
            # "נוספה" רק אם לא הייתה בו. כל רשימת ➕/➖ שסותרת את ההפרש בין רשימות
            # התחנות מחושבת מחדש. אידמפוטנטי; יומן ב-repair-legacy-diffs.json.
            OUTDIR=line-history/data python3 tools/repair_legacy_diffs.py --apply --all
            # הכלל של שלמה (03.09): אירוע שמדווח על שינוי תחנות בעוד הרצף המתועד
            # זהה לקודם אינו שינוי — נמחק (או הופך לתיקון שרטוט כשרק הציור השתנה).
            # אידמפוטנטי; יומן ב-repair-noop-events.json.
            OUTDIR=line-history/data python3 tools/repair_noop_events.py
            # תוכנית שירדה מהרישום ובסוף נכנסה לפעול — דחייה, לא "שינוי שלא נכנס
            # לתוקף" (שלמה 03.09). האירוע נמחק ברגע שהווריאנט מתחיל.
            OUTDIR=line-history/data python3 tools/repair_planned_entered.py
            # אירועי הארכיון יושבים בקבצי הקווים, אבל הפיד הכרונולוגי נבנה
            # בנפרד — בלי הצעד הזה אפשר לראות 2017 בתוך קו ואי אפשר לדפדף
            # אליו לפי יום.
            OUTDIR=line-history/data python3 tools/build_archive_months.py
            # שברי תחנות: קישור לתחנה טוען כמאתיים ק"ב במקום 4.5 מגה
            OUTDIR=line-history/data python3 tools/build_stop_shards.py
            OUTDIR=line-history/data python3 tools/compact_lines.py
            OUTDIR=line-history/data python3 tools/rebuild_lines_index.py
            # היסטוריה לפי תחנה: אילו קווים עצרו בכל תחנה ומתי זה השתנה
            OUTDIR=line-history/data python3 tools/build_stop_history.py
            # הדלתא לקו פח ולקו המוזהב: קווים שבוטלו, נסיעות עדכניות,
            # צמצומים — נגזר מהארכיון ונטען באתר הראשי
            OUTDIR=line-history/data python3 tools/build_kavpach_live.py
            git add -A line-history/data kavbug-data kavpach-live.json
            if git diff --cached --quiet; then
              echo "unchanged — nothing to commit"; break; fi
            git commit -m "data: line-history — שינויי מסלולים ותחנות יומיים"
            if git push origin HEAD:"$BR"; then break; fi
            echo "push rejected ($BR moved) — retry $i"; sleep 10
          done
