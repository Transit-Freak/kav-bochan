מקור: https://kavbochan.app/line-history/app.jsx
כתובת סופית: https://kavbochan.app/line-history/app.jsx
סטטוס: 200
הורד: 22.9.2026, 2:00:54 (שעון ישראל)

/* ×”×§×• ×‘×–×ž×Ÿ â€” ×”×™×¡×˜×•×¨×™×™×ª ×ž×¡×œ×•×œ×™× ×•×ª×—× ×•×ª ×ž×”×©×•×•××ª GTFS ×™×•×ž×™×ª.
   ×”× ×ª×•× ×™×: line-history/data, × ×•×¦×¨ ×¢"×™ tools/linehistory.py ×‘-GitHub Actions. */
const { useState, useEffect, useMemo, useRef } = React;
const BUILD = window.LH_BUILD || "0";

// ×›×¨×•× ×‘×× ×“×¨×•××™×“: ×”×”×—×œ×¤×” ×‘×™×Ÿ "××ª×¨ ×œ×ž×—×©×‘" ×œ"××ª×¨ ×œ× ×™×™×“" ×˜×•×¢× ×ª ×ž×—×“×© ××ª ×”×›×ª×•×‘×ª
// ×©××™×ª×” × ×›× ×¡×• ×œ×“×£ â€” ×œ× ××ª ×”×ž×¦×‘ ×”× ×•×›×—×™ (×˜××‘, ×§×• ×¤×ª×•×—) ×©×”××ª×¨ ×›×ª×‘ ×‘×©×•×¨×ª ×”×›×ª×•×‘×ª
// ×‘×œ×™ ×˜×¢×™× ×”. ×œ×›×Ÿ ×”×ž×¦×‘ ×”××—×¨×•×Ÿ × ×©×ž×¨ ×œ×œ×©×•× ×™×ª (sessionStorage) ×•×ž×©×•×—×–×¨ ×›×©×˜×¢×™× ×”
// ×ž×—×“×© × ×•×—×ª×ª ×¢×œ ××—×ª ×”×›×ª×•×‘×•×ª ×”×ž×§×•×¨×™×•×ª (×©×œ×ž×” 07.09: "×ž×—×–×™×¨ ××•×ª×š ×œ×“×£ ×”×§×•×“×").
(function () {
  try {
    const K = "kbNav", now = location.href, base = (u) => String(u).split("#")[0];
    let s = null; try { s = JSON.parse(sessionStorage[K] || "null"); } catch (e) { /* ignore */ }
    const nav = performance.getEntriesByType ? performance.getEntriesByType("navigation")[0] : null;
    const reload = nav ? nav.type === "reload" : !!(performance.navigation && performance.navigation.type === 1);
    const origs = ((s && Array.isArray(s.origs)) ? s.origs : []).filter((u) => base(u) === base(now));
    // ×”×”×—×œ×¤×” "××ª×¨ ×œ×ž×—×©×‘"/"××ª×¨ ×œ× ×™×™×“" ×œ× ×ª×ž×™×“ × ×¨×©×ž×ª ×›-reload â€” ×œ×¤×¢×ž×™× ×›× ×™×•×•×˜
    // ×¨×’×™×œ ×‘×œ×™ ×ž×¤× ×”, ×•××– × ×—×ª× ×• ×¢×œ ×”×›×ª×•×‘×ª ×”×ž×§×•×¨×™×ª (×œ×ž×©×œ ×§×™×©×•×¨ ×œ×§×• 10) ×•×œ×
    // ×¢×œ ×”×ž×¡×š ×”××—×¨×•×Ÿ. ×ž×¦×‘ ×©× ×©×ž×¨ ×‘×“×§×•×ª ×”××—×¨×•× ×•×ª ×‘××•×ª×” ×œ×©×•× ×™×ª ×ž×¡×¤×™×§ ×‘×©×‘×™×œ
    // ×œ×©×—×–×¨ ×’× ××– (×©×œ×ž×” 18.09: "×ž×§×¤×™×¥ ××•×ª×š ×œ×§×• 10 ×•×œ× × ×•×ª×Ÿ ×œ×—×–×•×¨ ×œ×ª×¤×¨×™×˜").
    const recent = !!(s && s.ts && Date.now() - s.ts < 15 * 60 * 1000);
    const toggled = !reload && recent && !document.referrer && (!nav || nav.type === "navigate");
    if ((reload || toggled) && s && s.url && s.url !== now && base(s.url) === base(now) && origs.includes(now)) history.replaceState(history.state, "", s.url);
    const note = () => { if (!origs.includes(location.href)) origs.push(location.href); if (origs.length > 30) origs.splice(0, origs.length - 30); };
    const save = () => { try { sessionStorage[K] = JSON.stringify({ origs, url: location.href, ts: Date.now() }); } catch (e) { /* ignore */ } };
    note();
    const push0 = history.pushState.bind(history), rep0 = history.replaceState.bind(history);
    history.pushState = function (st, t, u) { push0(st, t, u); note(); save(); };
    history.replaceState = function (st, t, u) { rep0(st, t, u); save(); };
    window.addEventListener("popstate", save);
    window.addEventListener("hashchange", () => { note(); save(); });
    save();
  } catch (e) { /* ignore */ }
})();

// ××™×—×•×“ ×œ×¦×•×¨×š ×”×¡×¨×’×œ ×‘×œ×‘×“: ×©×œ×•×© ×“×¨×’×•×ª ×©×œ ×©×™× ×•×™ ×ª×—× ×•×ª ×”×Ÿ ×©××œ×” ××—×ª, ×•×›×š ×’×
// ×”××¨×›×”/×§×™×¦×•×¨/×”×—×œ×¤×ª ×§×¦×”. "×ª×™×¢×•×“ ×¨××©×•×Ÿ" ×•"×¦×™×œ×•× ×ž×”××¨×›×™×•×Ÿ" ××™× × ×©×™× ×•×™×™× ××œ×
// × ×§×•×“×•×ª ×¤×ª×™×—×”, ×•×œ×›×Ÿ ×”× ×™×—×“.
const KGROUP = {
  "stops-add": "stops", "stops-del": "stops",
  extend: "terminal", shorten: "terminal",
  snapshot: "baseline",
};
const KGLABEL = { stops: "×©×™× ×•×™ ×ª×—× ×•×ª", terminal: "×©×™× ×•×™ ×§×¦×” ×”×ž×¡×œ×•×œ",
                  baseline: "× ×§×•×“×ª ×¤×ª×™×—×”" };
const KINDS = {
  baseline:    { label: "×ª×™×¢×•×“ ×¨××©×•×Ÿ", color: "#64748b" },
  snapshot:    { label: "×¦×™×œ×•× ×ž×”××¨×›×™×•×Ÿ", color: "#64748b" },
  new:         { label: "×•×¨×™×× ×˜ ×—×“×©", color: "#15803d" },
  route:       { label: "×©×™× ×•×™ ×ž×¡×œ×•×œ", color: "#7c3aed" },
  redraw:      { label: "×ª×™×§×•×Ÿ ×©×¨×˜×•×˜", color: "#0e7490" },
  terminal:    { label: "×©×™× ×•×™ ×§×¦×” ×”×ž×¡×œ×•×œ", color: "#a21caf" },
  extend:      { label: "×”××¨×›×ª ×§×•", color: "#15803d" },
  shorten:     { label: "×§×™×¦×•×¨ ×§×•", color: "#c2410c" },
  "stops-add": { label: "×ª×—× ×•×ª × ×•×¡×¤×•", color: "#3f6212" },
  "stops-del": { label: "×ª×—× ×•×ª ×™×¨×“×•", color: "#be123c" },
  stops:       { label: "×©×™× ×•×™ ×ª×—× ×•×ª", color: "#b45309" },
  operator:    { label: "×”×—×œ×¤×ª ×ž×¤×¢×™×œ", color: "#0f766e" },
  dest:        { label: "×©×™× ×•×™ ×™×¢×“", color: "#9333ea" },
  renum:       { label: "×©×™× ×•×™ ×ž×¡×¤×¨", color: "#be185d" },
  renamed:     { label: "×©×™× ×•×™ ×©× ×ª×—× ×ª ×§×¦×”", color: "#b45309" },
  mode:        { label: "×©×™× ×•×™ ×¡×•×’ ×”×§×•", color: "#0369a1" },
  access:      { label: "×©×™× ×•×™ × ×’×™×©×•×ª", color: "#0f766e" },
  vehicle:     { label: "×©×™× ×•×™ ×¡×•×’ ×”×¨×›×‘", color: "#7c3aed" },
  ltype:       { label: "×©×™× ×•×™ ×¡×•×’ ×”×§×•", color: "#0e7490" },
  returned:    { label: "×‘×•×˜×œ ×•×—×–×¨", color: "#f59e0b" },
  board:       { label: "×©×™× ×•×™ ×¢×œ×™×™×”/×™×¨×™×“×”", color: "#854d0e" },
  platform:    { label: "×©×™× ×•×™ ×¨×¦×™×£", color: "#0e7490" },
  "planned-dropped": { label: "×ª×•×›× ×Ÿ ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ", color: "#9f1239" },
  "planned-new": { label: "×§×• ×©×¤×•×¨×¡× ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ", color: "#9f1239" },
  "planned-route": { label: "×©×™× ×•×™ ×ª×—× ×•×ª ×©×¤×•×¨×¡× ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ", color: "#7f1d1d" },
  removed:     { label: "×‘×•×˜×œ", color: "#dc2626" },
  "removed-year": { label: "×‘×•×˜×œ â€” ×ž×¢×œ ×©× ×” ×œ× ×—×–×¨", color: "#7f1d1d" },
  freq:        { label: "×©×™× ×•×™ ×ž×¡×¤×¨ ×”×¨×›×‘×™× ×‘××•×ª×” × ×¡×™×¢×”", color: "#b45309" },
  sched:       { label: "×©×™× ×•×™ ×œ×•\"×–", color: "#4338ca" },
  times:       { label: "×”×œ×•\"×– ×”××—×¨×•×Ÿ", color: "#0e7490" },
};
// ×”×’×“×¨×ª ×”×ž×©×ª×ž×©: "×©×™× ×•×™ ×ž×¡×¤×¨ ×”×¨×›×‘×™×" = ×¨×§ ×ª×’×‘×•×¨ (×©× ×™ ×¨×›×‘×™× ×‘××•×ª×” ×“×§×”
// ×©×”×©×ª× ×•). ×›×œ ×©××¨ ×©×™× ×•×™×™ ×”×›×ž×•×ª/×©×¢×•×ª â€” ×ª×—×ª ×©×™× ×•×™ ×œ×•"×–.
function evKind(v) {
  if (v.k === "freq" && !/×ª×’×‘×•×¨/.test(v.note || "")) return "sched";
  return v.k;
}

function plKindOf(x) { return x.pk || (/×”×•×•×¨×™×× ×˜/.test(x.note || "") ? "new" : "route"); }
// ×ª×•×›× ×™×ª ×©×œ× ×™×¦××” ×œ×¤×•×¢×œ â€” ×ž×” ×”×™× ×”×™×™×ª×” ×ž×©× ×” ×•×ž×” ×§×¨×” ×‘×ž×§×•×ž×”. ×”×§×˜×’×•×¨×™×” × ×•×¢×“×”
// ×œ×ž×” ×©×œ× × ×›× ×¡ ×œ×¤×¢×•×œ ×‘×—×™×™× (×©×œ×ž×” 03.09): ×ª×•×›× ×™×ª ×©×‘×¡×•×£ × ×›× ×¡×” × ×ž×—×§×ª ×‘×¨×™×¦×”
// ×”×™×•×ž×™×ª (tools/repair_planned_entered.py). ×©×œ×ž×” 05.09: "×œ× ×ž×¢× ×™×™×Ÿ ××•×ª×™ ×ž×¡×¤×¨
// ×”×ª×—× ×•×ª ××œ× ×× ×”×™×” ×©×™× ×•×™ ×‘×™×Ÿ ×”×’×¨×¡×” ×”×¡×•×¤×™×ª ×œ×’×¨×¡×” ×©×œ ×¢×›×©×™×•", "×ª×¦×™×™×Ÿ ×× ×–×”
// × ×›× ×¡ ×‘×ª××¨×™×š ××—×¨", "×ž×” ×”×™×” ×”×ž×¡×œ×•×œ ×”×©×•× ×”". ×œ×›×Ÿ:
//   base      ×”×ž×¡×œ×•×œ ×©×”×§×• × ×¡×¢ ×‘×• ×œ×¤× ×™ ×”×ª×•×›× ×™×ª (×”×ª×™×¢×•×“ ×”×’×™××•×ž×˜×¨×™ ×”××—×¨×•×Ÿ ×œ×¤× ×™×”)
//   removedAt ×•×¨×™×× ×˜ ×©× ×¡×¢, ×™×¨×“, ×•×ª×•×›× ×Ÿ ×œ×—×–×•×¨ â€” ×ž×ª×™ ×™×¨×“
//   entered   ×’×¨×¡×” ×ž××•×—×¨×ª ×¢× ×‘×“×™×•×§ ×¨×¦×£ ×”×ª×—× ×•×ª ×©×ª×•×›× ×Ÿ: × ×›× ×¡ ×‘×ª××¨×™×š ××—×¨
//   changed   ×ž×” ×©× ×›× ×¡ ×‘×ž×§×•×: ×”×’×¨×¡×” ×”×¨××©×•× ×” ××—×¨×™ ×”×ª×•×›× ×™×ª ×©×¨×¦×£ ×”×ª×—× ×•×ª ×‘×” ×©×•× ×”
//             ×ž×”×‘×¡×™×¡ (×‘-'new' â€” ×©×”×•×•×¨×™×× ×˜ ×”×ª×—×™×œ ×‘×”), ×‘×ª×•×š ×—×¦×™ ×©× ×” ×ž×”×ž×•×¢×“.
//             ×©×™× ×•×™ ×©× ×›× ×¡ ×©× ×” ××—×¨ ×›×š ××™× ×• "×‘×ž×§×•×" â€” ×”×•× ××™×¨×•×¢ ×¨×’×™×œ ×‘×¦×™×¨.
//   goneAt    ×”×•×•×¨×™×× ×˜ ×¢×¦×ž×• ×™×¨×“ ×ž×”×¨×™×©×•× ×–×ž×Ÿ ×§×¦×¨ ××—×¨×™ ×•××™× ×• × ×•×¡×¢ ×”×™×•×
//   vsBase    ×ž×” ×”×ª×•×›× ×™×ª ×”×™×™×ª×” ×ž×©× ×” ×œ×¢×•×ž×ª ×”×ž×¡×œ×•×œ ×©× ×¡×¢ (× ×•×¡×¤×• / ×™×¨×“×•)
//   vsPlan    ×‘×ž×” ×”×ž×¡×œ×•×œ ×©× ×›× ×¡ ×‘×¤×•×¢×œ (changed) ×©×•× ×” ×ž×”×ª×•×›× ×™×ª
// ×”×”×©×•×•××•×ª ×”×§×•×“×ž×•×ª ××ž×¨×• "×ž×” ×©× ×›× ×¡ ××—×¨ ×›×š ×”×™×” ×©×•× ×”" ×’× ×›×©×ž×” ×©"× ×›× ×¡" ×”×™×” ×¦×™×œ×•×
// ×©×œ ×”×ž×¡×œ×•×œ ×”×™×©×Ÿ ×œ×œ× ×©×™× ×•×™ (×§×• 2 ××™×œ×ª, 2025) â€” ×¢×›×©×™×• ×–×” "×”×ž×¡×œ×•×œ × ×©××¨ ×›×¤×™ ×©×”×™×”".
const PL_WINDOW = 183;
function stopsDiff(a, b) {
  const ac = new Set((a || []).map((s) => String(s[0]))), bc = new Set((b || []).map((s) => String(s[0])));
  return { add: (a || []).filter((s) => !bc.has(String(s[0]))), rem: (b || []).filter((s) => !ac.has(String(s[0]))) };
}
function plannedInfo(x, i, vs) {
  const kind = plKindOf(x), plan = x.pstops || [];
  const key = (st) => (st || []).map((s) => String(s[0])).join("|");
  const pk = key(plan);
  let base = null, bi = -1;
  for (let j = i - 1; j >= 0; j--) if ((vs[j].stops || []).length) { base = vs[j]; bi = j; break; }
  let removedAt = null;
  for (let j = i - 1; j > bi; j--) if (vs[j].k === "removed") { removedAt = vs[j].d; break; }
  const bk = base ? key(base.stops) : null;
  const until = x.ps ? new Date(x.ps).getTime() + PL_WINDOW * 864e5 : null;
  const soon = (d) => !until || new Date(d).getTime() <= until;
  let entered = null, changed = null;
  for (let j = i + 1; j < vs.length; j++) {
    const w = vs[j];
    if (!(w.stops || []).length) continue;
    const wk = key(w.stops);
    if (plan.length && wk === pk) { entered = w; break; }
    if (!changed && soon(w.d) && (kind === "new" || wk !== bk)) changed = w;
  }
  if (entered) changed = null;
  // ×”×•×•×¨×™×× ×˜ ×¢×¦×ž×• ×™×¨×“ ×•×œ× ×—×–×¨: ×”×’×¨×¡×” ×”××—×¨×•× ×” ×‘×§×•×‘×¥ ×”×™× ×”×‘×™×˜×•×œ, ×–×ž×Ÿ ×§×¦×¨ ××—×¨×™
  // ×”×ª×•×›× ×™×ª. ×‘×™×˜×•×œ ×–×ž× ×™ (×™×¨×“ ×•×—×–×¨, ×§×• 2 ××™×œ×ª 2025) ××™× ×• × ×—×©×‘.
  const last = vs[vs.length - 1];
  const goneAt = !entered && !changed && last && last.k === "removed" && last.d > x.d && soon(last.d) ? last.d : null;
  const vsBase = base ? stopsDiff(plan, base.stops) : null;
  return { kind, plan, base, removedAt, entered, changed, goneAt, vsBase,
           sameAsBase: !!vsBase && !vsBase.add.length && !vsBase.rem.length,
           vsPlan: changed ? stopsDiff(changed.stops, plan) : null };
}
// ×©×•×¨×ª ×”×ž×¦×‘ ×©×œ ×ª×•×›× ×™×ª ×©×œ× ×™×¦××” ×œ×¤×•×¢×œ â€” ×ž×” ×§×¨×” ×‘×¡×•×£
function PlanStatus({ p }) {
  if (p.kind === "new") {
    const w = p.entered || p.changed;
    if (w) return <>×”×•×•×¨×™×× ×˜ ×”×ª×—×™×œ ×‘×¡×•×£ ×‘-<b>{fmtD(w.d)}</b>{p.entered ? " ×‘××•×ª×• ×ž×¡×œ×•×œ" : " ×‘×ž×¡×œ×•×œ ×©×•× ×”"}</>;
    if (p.base) return <>×”×•×•×¨×™×× ×˜ {p.removedAt ? <>× ×¡×¢ ×¢×“ <b>{fmtD(p.removedAt)}</b></> : <>×ª×•×¢×“ × ×•×¡×¢ ×œ××—×¨×•× ×” ×‘-<b>{fmtD(p.base.d)}</b></>}, ×ª×•×›× ×Ÿ ×œ×—×–×•×¨ ×•×œ× ×—×–×¨ ×¢×“ ×”×™×•×</>;
    return <>×”×•×•×¨×™×× ×˜ ×œ× × ×¡×¢ ×ž×¢×•×œ×</>;
  }
  if (p.entered) return <>×”×©×™× ×•×™ × ×›× ×¡ ×‘×¡×•×£ ×‘-<b>{fmtD(p.entered.d)}</b></>;
  if (p.changed) return <>×”×©×™× ×•×™ ×œ× × ×›× ×¡ ×œ×¤×¢×•×œ; ×ž×” ×©× ×›× ×¡ ×‘-<b>{fmtD(p.changed.d)}</b> ×”×™×” ×©×•× ×”</>;
  if (p.goneAt) return <>×”×©×™× ×•×™ ×œ× × ×›× ×¡ ×œ×¤×¢×•×œ; ×”×§×• ×¢×¦×ž×• ×™×¨×“ ×ž×”×¨×™×©×•× ×‘-<b>{fmtD(p.goneAt)}</b></>;
  return <>×”×©×™× ×•×™ ×œ× × ×›× ×¡ ×œ×¤×¢×•×œ ×¢×“ ×”×™×•×{p.base ? ", ×”×ž×¡×œ×•×œ × ×©××¨ ×›×¤×™ ×©×”×™×”" : ""}</>;
}
// ×©×•×¨×•×ª ×”×¤×™×¨×•×˜ â€” ×ž×” ×”×ª×•×›× ×™×ª ×”×™×™×ª×” ×ž×©× ×”, ×•×‘×ž×” ×ž×” ×©× ×›× ×¡ ×‘×¤×•×¢×œ ×©×•× ×” ×ž×ž× ×”.
// ×ž×©×•×ª×£ ×œ×›×¨×˜×™×¡ ×‘×¦×™×¨ ×•×œ×›×¨×˜×™×¡ ×”×¨××©×™; ×‘×œ×™ ×¡×¤×™×¨×ª ×ª×—× ×•×ª (×©×œ×ž×” 05.09).
function PlanLines({ p, max }) {
  // ×©× ×•×ž×§"×˜, ×›×ž×• ×‘×©××¨ ×›×¨×˜×™×¡×™ ×”××™×¨×•×¢×™× (×©×œ×ž×” 05.09: "×ª×•×¡×™×£ ×ž×¡×¤×¨ ×ª×—× ×”")
  const names = (arr) => arr.slice(0, max).map((s) => `${s[1]} (${s[0]})`).join(", ") + (arr.length > max ? ` ×•×¢×•×“ ${arr.length - max}` : "");
  const plan = p.plan || [];
  return (<>
    {p.vsBase ? (p.sameAsBase
      ? <div>ðŸŸ° {p.kind === "new" ? "×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ ×–×”×” ×œ×ž×¡×œ×•×œ ×©×‘×• × ×¡×¢ ×§×•×“×" : "×¨×¦×£ ×”×ª×—× ×•×ª ×©×ª×•×›× ×Ÿ ×–×”×” ×œ×ž×¡×œ×•×œ ×©× ×¡×¢ â€” ×”×©×™× ×•×™ ×”×™×” ×‘×©×¨×˜×•×˜ ×‘×œ×‘×“"}</div>
      : <>
        {p.vsBase.add.length > 0 && <div>âž• ×‘×ª×•×›× ×™×ª × ×•×¡×¤×•: {names(p.vsBase.add)}</div>}
        {p.vsBase.rem.length > 0 && <div>âž– ×‘×ª×•×›× ×™×ª ×™×¨×“×•: {names(p.vsBase.rem)}</div>}
      </>)
      : plan.length > 1 ? <div>ðŸ—ºï¸ ×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ: ×ž{plan[0][1]} ×¢×“ {plan[plan.length - 1][1]}</div> : null}
    {p.vsPlan && (p.vsPlan.add.length || p.vsPlan.rem.length) ? (
      <div>×œ×¢×•×ž×ª ×”×ª×•×›× ×™×ª, ×‘×ž×¡×œ×•×œ ×©× ×›× ×¡ ×‘-{fmtD(p.changed.d)}:
        {p.vsPlan.add.length > 0 && <> âž• {names(p.vsPlan.add)}</>}
        {p.vsPlan.add.length > 0 && p.vsPlan.rem.length > 0 ? " Â·" : ""}
        {p.vsPlan.rem.length > 0 && <> âž– {names(p.vsPlan.rem)}</>}
      </div>) : null}
  </>);
}
// ×‘×™×˜×•×œ ×©× ×©××¨ ×‘×ª×•×§×£ ×ž×¢×œ ×©× ×” (×”×’×¨×¡×” ×”××—×¨×•× ×” ×”×™× removed ×•×™×©× ×” ×ž×©× ×”) ×ž×§×‘×œ ×§×˜×’×•×¨×™×” ×ž×©×œ×•
function dispKind(x, i, vs) {
  // "×”××—×¨×•×Ÿ" â€” ×‘×œ×™ ××™×¨×•×¢×™× × ×’×–×¨×™× (×¡×•×’ ×¨×›×‘ ×‘×¨×™×©×•×™) ×©×¢×œ×•×œ×™× ×œ×‘×•× ××—×¨×™ ×”×‘×™×˜×•×œ
  if (x.k === "removed" && vs.slice(i + 1).every((v) => v.syn) && (Date.now() - new Date(x.d)) / 864e5 >= 365) return "removed-year";
  if (x.k === "planned-dropped") return plKindOf(x) === "new" ? "planned-new" : "planned-route";
  return evKind(x);
}
// ××•×ª×• ×›×œ×œ ×‘×¨×ž×ª ×”××™× ×“×§×¡ (lk/ld = ×”×¨×©×•×ž×” ×”××—×¨×•× ×” ×©×œ ×”×•×•×¨×™×× ×˜)
function isRemovedYear(l) {
  return l.lk === "removed" && (Date.now() - new Date(l.ld)) / 864e5 >= 365;
}
// ×§×˜×’×•×¨×™×•×ª ×”×‘×—×™×¨×” â€” ×ž×—×•×œ×§×•×ª ×œ×§×‘×•×¦×•×ª, ×‘×œ×™ ×—×¤×™×¤×•×ª: ×©×œ×•×© ×§×˜×’×•×¨×™×•×ª ×‘×™×˜×•×œ
// × ×¤×¨×“×•×ª (×ž×¢×œ ×©× ×” / ×¤×—×•×ª ×ž×©× ×” / ×—×–×¨), ×•×ª×•×•×™×•×ª ×©×ž×¡×‘×™×¨×•×ª ××ª ×”×”×‘×“×œ.
// ×”×§×˜×’×•×¨×™×•×ª ××•×—×“×• ××™×¤×” ×©×”×”×¤×¨×“×” ×”×™×™×ª×” ×©×œ× ×• ×•×œ× ×©×œ ×”×¢×•×œ×:
// Â· "×ž×¢×œ ×©× ×”" ×•"×¤×—×•×ª ×ž×©× ×”" ×”×Ÿ ××•×ª×• ×“×‘×¨ â€” ×§×• ×ž×‘×•×˜×œ. ×ž×©×š ×”×–×ž×Ÿ ×›×ª×•×‘ ×‘×ª××¨×™×š,
//   ×•××™×Ÿ ×¡×™×‘×” ×œ×—×™×™×‘ ×¡×™×ž×•×Ÿ ×©×ª×™ ×ª×™×‘×•×ª ×›×“×™ ×œ×¨××•×ª ×§×•×•×™× ×ž×‘×•×˜×œ×™×.
// Â· ×”××¨×›×”/×§×™×¦×•×¨/×©×™× ×•×™ ×§×¦×” ×”×•×¤×¨×“×• ×œ×¤×™ ×¡×£ ×©×¨×™×¨×•×ª×™ ×©×œ ×©×œ×•×© ×ª×—× ×•×ª: ×§×• ×©×§×™×‘×œ
//   ×©×ª×™×™× ×‘×§×¦×” × ×¤×œ ×œ×§×˜×’×•×¨×™×” ××—×ª ×•××—×“ ×©×§×™×‘×œ ×©×œ×•×© ×œ××—×¨×ª. ×©×œ×•×©×ª×Ÿ "×”×§×¦×” ×–×–",
//   ×•×”×”×‘×—× ×” × ×©××¨×ª ×›×ª×•×‘×” ×‘×ª×•×š ×”××™×¨×•×¢.
// Â· "×ª×™×§×•×Ÿ ×©×¨×˜×•×˜" ×”×•× ×”×§×˜×’×•×¨×™×” ×”×’×“×•×œ×” ×‘×™×•×ª×¨ ×•××™×Ÿ ×‘×” ×©×™× ×•×™ ×œ× ×•×¡×¢ â€” ×ž×©×¨×“
//   ×”×ª×—×‘×•×¨×” ×¦×™×™×¨ ×ž×—×“×© ×‘×œ×™ ×©××£ ×ª×—× ×” ×–×–×”. ×”×•×¦××” ×œ×§×‘×•×¦×” ×˜×›× ×™×ª × ×¤×¨×“×ª.
// ×©×œ×•×© ×§×˜×’×•×¨×™×•×ª ×”×ª×—× ×•×ª × ×©××¨×•: ×”×”×‘×“×œ ×‘×™×Ÿ ×ª×—× ×” ×©× ×•×¡×¤×” ×œ×ª×—× ×” ×©×™×¨×“×” ×”×•×
// ×‘×“×™×•×§ ×ž×” ×©×ž×—×¤×©×™×, ×•××™×—×•×“×Ÿ ×”×™×” ×—×•×¡×š ×©×•×¨×” ×•×¢×•×œ×” ×‘×ž×™×“×¢.
const CAT_GROUPS = [
  { title: "×‘×™×˜×•×œ×™×", items: ["removed-year", "removed-now", "removed-past"] },
  { title: "×©×™× ×•×™×™ ×ž×¡×œ×•×œ", items: ["route", "endpoint"] },
  { title: "×©×™× ×•×™×™ ×ª×—× ×•×ª", items: ["stops", "stops-add", "stops-del"] },
  { title: "×ª×“×™×¨×•×ª ×•×œ×•×— ×–×ž× ×™×", items: ["freq", "sched"] },
  { title: "×¨×™×©×•× ×•×¤×¨×˜×™×", items: ["new", "operator", "dest", "renum", "mode", "platform", "vehicle", "ltype"] },
  { title: "×©×™× ×•×™×™× ×©×œ× × ×›× ×¡×• ×œ×¤×¢×•×œ", items: ["planned-new", "planned-route"] },
  { title: "×©×™× ×•×™×™× ×˜×›× ×™×™×", items: ["redraw"] },
];
const CAT_LABELS = {
  "removed-year": "×ž×‘×•×˜×œ â€” ×ž×¢×œ ×©× ×” ×œ× ×—×–×¨",
  "removed-now": "×ž×‘×•×˜×œ ×›×¨×’×¢ â€” ×¤×—×•×ª ×ž×©× ×”",
  "removed-past": "×‘×•×˜×œ ×‘×¢×‘×¨ ×•×—×–×¨ ×œ×¤×¢×•×œ",
  route: "×©×™× ×•×™ ×ž×¡×œ×•×œ (×¦×™×•×¨ ×•×’× ×ª×—× ×•×ª)",
  endpoint: "×©×™× ×•×™ ×§×¦×” ×”×§×• (×”××¨×›×”, ×§×™×¦×•×¨ ××• ×”×—×œ×¤×ª ×§×¦×”)",
  redraw: "×ª×™×§×•×Ÿ ×©×¨×˜×•×˜ â€” ×”×ª×—× ×•×ª ×œ× ×”×©×ª× ×•",
  stops: "×”×•×—×œ×¤×• ×ª×—× ×•×ª (× ×•×¡×¤×• ×•×’× ×™×¨×“×•)",
  "stops-add": "×¨×§ × ×•×¡×¤×• ×ª×—× ×•×ª",
  "stops-del": "×¨×§ ×™×¨×“×• ×ª×—× ×•×ª",
  new: "×•×¨×™×× ×˜ ×—×“×© ×‘×¨×™×©×•×",
  operator: "×”×—×œ×¤×ª ×ž×¤×¢×™×œ",
  dest: "×©×™× ×•×™ ×™×¢×“",
  renum: "×©×™× ×•×™ ×ž×¡×¤×¨ ×§×•",
  mode: "×©×™× ×•×™ ×¡×•×’ ×”×§×• (×œ×ž×©×œ ×¨×’×™×œ â†” ×œ×¤×™ ×“×¨×™×©×”)",
  platform: "×©×™× ×•×™ ×¨×¦×™×£ â€” ×”×§×• ×¢×‘×¨ ×œ×¨×¦×™×£ ××—×¨ ×‘×ž×¡×•×£",
  vehicle: "×©×™× ×•×™ ×¡×•×’ ×”×¨×›×‘",
  ltype: "×©×™× ×•×™ ×¡×•×’ ×”×§×• â€” ×¢×™×¨×•× ×™ / ××–×•×¨×™ / ×‘×™× ×¢×™×¨×•× ×™, ××• ×™×™×—×•×“×™×•×ª: ×ª×œ×ž×™×“×™× / ×œ×™×œ×” / ×ž×–×™×Ÿ",
  // × ×™×¡×•×— ×§×¦×¨ (×©×œ×ž×” 05.09: "×–×” ××¨×•×š ×•×ž×¡×•×¨×‘×œ"). ×”×›×œ×œ ×”×ž×œ× ×›×ª×•×‘ ×¢×œ ×”××™×¨×•×¢ ×¢×¦×ž×•.
  "planned-dropped": "×ª×•×›× ×Ÿ ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ â€” ×™×¨×“ ×ž×”×¨×™×©×•× ×œ×¤× ×™ ×ª××¨×™×š ×”×”×ª×—×œ×”",
  "planned-new": "×§×• ×©×¤×•×¨×¡× ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ â€” ×™×¨×“ ×ž×”×¨×™×©×•× ×œ×¤× ×™ ×©×”×ª×—×™×œ",
  "planned-route": "×©×™× ×•×™ ×ª×—× ×•×ª ×©×¤×•×¨×¡× ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ â€” ×™×¨×“ ×ž×”×¨×™×©×•× ×œ×¤× ×™ ×©× ×›× ×¡",
  freq: "×©×™× ×•×™ ×ž×¡×¤×¨ ×”×¨×›×‘×™× ×‘××•×ª×” × ×¡×™×¢×” (×ª×’×‘×•×¨)",
  sched: "×©×™× ×•×™ ×©×¢×•×ª ×”×™×¦×™××” (×œ×•\"×–)",
};
const CAT_COLORS = { "removed-now": "#dc2626", "removed-past": "#f59e0b",
                     endpoint: "#a21caf" };
function catColor(k) { return CAT_COLORS[k] || (KINDS[k] || {}).color || "#64748b"; }
const ENDPOINT_KINDS = ["extend", "shorten", "terminal"];
// ×©×œ×•×© ×§×˜×’×•×¨×™×•×ª ×”×‘×™×˜×•×œ ×–×¨×•×ª ×–×• ×œ×–×•: ×§×• ×©×œ× ×—×–×¨ ×ž×¢×œ ×©× ×” ×”×•× ×ž×‘×•×˜×œ ×‘×¤×•×¢×œ,
// ×•××™×œ×• ×‘×™×˜×•×œ ×˜×¨×™ ×¢×•×“ ×¢×©×•×™ ×œ×”×ª×‘×¨×¨ ×›×”×¤×¡×§×”. ×”×”×‘×—× ×” × ×©××¨×” ×œ×‘×§×©×ª ×”×ž×©×ª×ž×©.
function catMatch(l, k) {
  if (k === "removed-year") return isRemovedYear(l);
  if (k === "removed-now") return l.lk === "removed" && !isRemovedYear(l);
  if (k === "removed-past") return l.lk !== "removed" && (l.ks || []).includes("removed");
  if (k === "endpoint") return ENDPOINT_KINDS.some((x) => (l.ks || []).includes(x));
  return (l.ks || []).includes(k);
}
// ××•×ª×• ×›×œ×œ ×‘×¨×ž×ª ×”××™×¨×•×¢ ×”×‘×•×“×“ ×‘×¢×ž×•×“ ×”×§×•: ×”×× ×”××™×¨×•×¢ ×”×–×” ×©×™×™×š ×œ×§×˜×’×•×¨×™×” ×©×œ
// ×”×—×™×¤×•×©. ×ž×©×ž×© ×›×©× ×›× ×¡×™× ×œ×§×• ×ž×ª×•×š ×—×™×¤×•×© ×œ×¤×™ ×§×˜×’×•×¨×™×” â€” ×”×¡×™× ×•×Ÿ ×‘×¢×ž×•×“ × ×“×œ×§
// ××•×˜×•×ž×˜×™×ª ×¢×œ ××•×ª×” ×§×˜×’×•×¨×™×” (×©×œ×ž×” 06.09), ×•××¤×©×¨ ×œ×©× ×•×ª ××•×ª×• ×‘×¡×¨×’×œ.
function evInCat(x, i, vs, k) {
  const dk = dispKind(x, i, vs);
  if (k === "endpoint") return ENDPOINT_KINDS.includes(dk);
  if (k === "removed-year") return dk === "removed-year";
  if (k === "removed-now") return dk === "removed";
  if (k === "removed-past") return dk === "removed" || dk === "returned";
  if (k === "sched") return dk === "sched" || dk === "freq";
  if (k === "stops" || k === "stops-add" || k === "stops-del") {
    if (dk === k) return true;
    // ×›×ž×• ×‘××™× ×“×§×¡: ×©×™× ×•×™ ×ž×¡×œ×•×œ ×©× ×•×¡×¤×•/×™×¨×“×• ×‘×• ×ª×—× ×•×ª × ×¡×¤×¨ ×’× ×›×©×™× ×•×™ ×ª×—× ×•×ª
    if ((x.src === "ob" || x.gd) && dk !== "removed") {
      const a = x.add && x.add.length, r = x.rem && x.rem.length;
      return k === "stops" ? (a && r) : k === "stops-add" ? (a && !r) : (r && !a);
    }
    return false;
  }
  return dk === k;
}
const REMOVAL_CATS = new Set(["removed-year", "removed-now", "removed-past"]);
/* ××™×¨×•×¢×™ "×©×™× ×•×™ ×¨×¦×™×£" ×™×©× ×™× (k=platform ×‘×œ×™ pv) ×ž×•×¡×ª×¨×™×: ×¢×“ 07.09 ×”×¡×•×¨×§ ×§×¨×¡
   ××ª ×›×œ ×©×•×¨×•×ª ×”×¨×¦×™×¤×™× ×©×œ ×ž×¡×•×£ (×‘×§×•×‘×¥ ×”×ž×©×¨×“ ×™×© ×©×•×¨×” ×œ×›×œ ×¨×¦×™×£, ××•×ª×• ×ž×§"×˜)
   ×œ×ž×¡×¤×¨ ××—×“, ×•×”×ž×¡×¤×¨ "×§×¤×¥" 12â†’17â†’8â†’16â†’3 ×‘×¨××©×œ"×¦ ×‘×œ×™ ×©×¨×¦×™×£ × ×•×¡×£ ××• ×‘×•×˜×œ.
   ×”××™×¨×•×¢×™× ×”×—×“×©×™× (pv=2, tools/platforms.py) × ×‘× ×™× ×ž×”×©×•×¨×•×ª ×¢×¦×ž×Ÿ â€” ×¨×¦×™×£
   ×©×§×™×‘×œ ×§×•×•×™× / × ×©××¨ ×‘×œ×™ ×§×•×•×™× ×‘×ª×—× ×”, ×•×§×• ×©×¢×‘×¨ ×¨×¦×™×£ â€” ×•×ž×•×¦×’×™×. */
const hiddenEv = (e) => e && e.k === "platform" && !e.pv;
const SKINDS = {
  new:     { label: "×—×“×©×”", color: "#15803d" },
  del:     { label: "×‘×•×˜×œ×”", color: "#dc2626" },
  renamed: { label: "×©×™× ×•×™ ×©×", color: "#b45309" },
  moved:   { label: "×”×–×–×ª ×ž×™×§×•×", color: "#2563eb" },
  city:    { label: "×©×™× ×•×™ ×¢×™×¨", color: "#b91c1c" },
  pubdest: { label: "×ª×—× ×ª ×™×¢×“ ×œ×¤×¨×¡×•×", color: "#7e22ce" },
  platform: { label: "×¨×¦×™×£ × ×•×¡×£/×‘×•×˜×œ", color: "#0e7490" },
  // ×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ ×™×•× ×™ 2012 (GTFS ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×“×¨×š OpenStreetMap, changeset 12028672):
  // ××™×¨×•×¢ ××—×“ ×œ×›×œ ×ª×—× ×” ×©×”×™×™×ª×” ×‘×¨×™×©×•× ××– â€” ×©×, ×›×ª×•×‘×ª ×•×ž×™×§×•× ×©×œ 2012 (×©×œ×ž×” 22.09)
  gtfs2012: { label: "×‘×¨×™×©×•× 2012", color: "#78350f" },
};
// ××™×¨×•×¢×™ 2012 × ×‘× ×™× ×‘×“×¤×“×¤×Ÿ ×ž×§×•×‘×¥ ×”×¨×™×©×•× (magihim-2012/data/stops-2012.json, ××• ×”×©×‘×¨ ×œ×¤×™
// ×§×™×“×•×ž×ª ×”×ž×§"×˜), ×›×“×™ ×œ× ×œ×’×¢×ª ×‘×§×•×¨×•×ª ×”×—×™×™× ×©×”×¦× ×¨×ª ×ž×ª×—×–×§×ª. dist: ×”×ž×¨×—×§ ×œ×ž×™×§×•× ×”××—×¨×•×Ÿ
// ×©×™×“×•×¢ ×œ× ×• ×ž×§×•×¨×•×ª ×”×—×™×™× ×©×œ ××•×ª×” ×ª×—× ×”, ×›×©×™×©.
function distM(a, b) {
  const ky = 110540, kx = 111320 * Math.cos((a[0] * Math.PI) / 180);
  return Math.hypot((a[0] - b[0]) * ky, (a[1] - b[1]) * kx);
}
// ×œ×›×œ ×ª×—× ×”: ××™×¨×•×¢ "×‘×¨×™×©×•× 2012", ×•××—×¨×™×• ×ž×” ×©×”×©×ª× ×” ×‘××•×ª×• ×ž×§"×˜ ×‘×™×Ÿ 2012 ×œ×ª×—×™×œ×ª
// ×”×ª×™×¢×•×“ ×©×œ× ×• (×ž×¨×¥ 2017): ×©×™× ×•×™ ×©×, ×”×–×–×” (30 ×ž×³ ×•×ž×¢×œ×”), ××• ×‘×™×˜×•×œ (×”×ž×§"×˜ ××™× ×•
// ×‘×¨×™×©×•× ×ž-2017 ×•××™×œ×š). ×”××™×¨×•×¢×™× ×”××œ×” ×ž×ª×•××¨×›×™× "×‘×™×Ÿ 2012 ×œ-2017" â€” ×œ× ×™×“×•×¢ ×ž×ª×™
// ×‘×“×™×•×§ (b12), ×•×”× ×ž×ž×•×™× ×™× ××—×¨×™ 2012 ×•×œ×¤× ×™ 2017 (×©×œ×ž×” 22.09).
function events2012(snap) {
  const out = [];
  for (const c in snap) {
    const [n, la, lo, addr, , fn, fla, flo, dist, ren] = snap[c];
    out.push({ c, d: "2012-06-26", k: "gtfs2012", n, t: addr, la, lo });
    if (fn == null) { out.push({ c, d: "2016-12-31", b12: 1, k: "del", n, la, lo }); continue; }
    // ××•×ª×• ×ž×§"×˜ ×‘×ž×¨×—×§ ×§×™×œ×•×ž×˜×¨ ×•×ž×¢×œ×”: ×–×• ×œ× ×”×–×–×” ××œ× ×ž×§"×˜ ×©×”×•×§×¦×” ×ž×—×“×© ×œ×ª×—× ×” ××—×¨×ª
    // (×œ×ž×©×œ 59695: "×‘×™×ª ×œ×—× ×”×’×œ×™×œ×™×ª ×’×³" ×‘-2012, "×¦×•×ž×ª × ×”×¨×™×” ×ž×–×¨×—" ×ž-2017, 31 ×§"×ž) â€”
    // ×”×ª×—× ×” ×©×œ 2012 ×‘×•×˜×œ×”, ×•×”×ž×§"×˜ ×©×™×ž×© ××—×¨ ×›×š ×ª×—× ×” ××—×¨×ª (×©×œ×ž×” 22.09)
    if (dist >= 1000) { out.push({ c, d: "2016-12-31", b12: 1, k: "del", n, la, lo, reused: fn, rdist: dist }); continue; }
    if (ren) out.push({ c, d: "2016-12-31", b12: 1, k: "renamed", on: n, nn: fn, n: fn, la: fla, lo: flo });
    if (dist >= 30) out.push({ c, d: "2016-12-31", b12: 1, k: "moved", n: fn, la: fla, lo: flo, ola: la, olo: lo, dist });
  }
  return out;
}

// ×¤×¢× ×•×— polyline (precision 5)
// ×¤×•×¨×ž×˜ ×§×•×‘×¥ ×“×—×•×¡ (×—×™×¡×›×•×Ÿ ~30MB, ×‘×§×©×ª ×”×ž×©×ª×ž×©): ×ª×—× ×•×ª ×•×ž×¡×œ×•×œ×™× × ×©×ž×¨×™×
// ×¤×¢× ××—×ª ×‘×ž××’×¨×™ pool/spool ×•×”×’×¨×¡××•×ª ×ž×¤× ×•×ª ×‘××™× ×“×§×¡; ×›××Ÿ ×¤×•×ª×—×™× ×—×–×¨×”
// ×œ×¦×•×¨×” ×”×ž×œ××” â€” ×©××¨ ×”×§×•×“ ×œ× ×™×•×“×¢ ×©×”×§×•×‘×¥ ×”×™×” ×“×—×•×¡. ×ª××•×-×œ××—×•×¨ ×œ×§×‘×¦×™× ×™×©× ×™×.
function materializeLf(lf) {
  if (!lf) return lf;
  // ×’×¨×¡××•×ª "×©×™× ×•×™ ×¨×¦×™×£" ×”×™×©× ×•×ª ×ž×¡×•×ž× ×•×ª ×ž×•×¡×ª×¨×•×ª (hid) ××‘×œ × ×©××¨×•×ª ×‘×ž×¢×¨×š: ×”×Ÿ
  // × ×•×©××•×ª ××ª ×¨×¦×£ ×”×ª×—× ×•×ª ×©×œ ×–×ž× ×Ÿ, ×•×”×ž×¤×” ×ž×©×•×•×” ×œ×’×¨×¡×” ×”×§×•×“×ž×ª â€” ×”×¡×¨×ª×Ÿ ×ž×”×ž×¢×¨×š
  // ×©×™× ×ª×” ××ª ×”×”×©×•×•××”, ×•×ª×—× ×•×ª ×©× ×•×¡×¤×• (×§×• 38 ×¨×—×•×‘×•×ª, 15.02.2024) ××™×‘×“×• ××ª
  // ×”×¡×™×ž×•×Ÿ ×”×™×¨×•×§ (×©×œ×ž×” 07.09). ×”×¡×™× ×•×Ÿ × ×¢×©×” ×¨×§ ×‘×ª×¦×•×’×” (×¨××• hiddenEv).
  (lf.versions || []).forEach((v) => { if (hiddenEv(v)) v.hid = true; });
  const pool = lf.pool, spool = lf.spool;
  (lf.versions || []).forEach((v) => {
    if (pool && Array.isArray(v.stops) && v.stops.length && typeof v.stops[0] === "number")
      v.stops = v.stops.map((i) => pool[i]);
    if (typeof v.shp === "number") v.shp = (spool && v.shp > 0 && spool[v.shp]) || "";
  });
  delete lf.pool; delete lf.spool;
  // ××™×¨×•×¢×™× × ×’×–×¨×™× (syn) ×©× ×›× ×¡×™× ×œ×¦×™×¨ ×”×–×ž×Ÿ ×›×§×˜×’×•×¨×™×•×ª ×ž×©×œ×”×, ×‘×œ×™ ×’××•×ž×˜×¨×™×”
  // ×•×‘×œ×™ ×œ×’×¢×ª ×‘×’×¨×¡××•×ª ×”×ž×¡×œ×•×œ (×©×œ×ž×” 06.09: "×§×˜×’×•×¨×™×” × ×¤×¨×“×ª, ×œ× ×—×œ×§ ×ž×”×˜×§×¡×˜"):
  // Â· "×©×™× ×•×™ ×¡×•×’ ×¨×›×‘" â€” ×œ×›×œ ×©×™× ×•×™ ×‘×ž××’×¨ ×”×¨×™×©×•×™ ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” (veh =
  //   [[×ª××¨×™×š, ×¡×•×’, ×’×•×“×œ], â€¦]; ×”×ž×¦×‘ ×”× ×•×›×—×™ ×›×ª×•×‘ ×‘×©×•×¨×ª ×”×¤×¨×˜×™×, "×ž×™× ×™×‘×•×¡ ×¢×™×¨×•× ×™ × ×’×™×©").
  //   "××•×˜×•×‘×•×¡" ×œ×‘×“ ×œ× ××•×ž×¨ ×›×œ×•×, ××– ×”×§×˜×’×•×¨×™×•×ª ×©×œ ×”×ž×©×¨×“ ×ž×•×¦×’×•×ª ×›×’×•×“×œ:
  //   ××•×˜×•×‘×•×¡ ×¨×’×™×œ / ××•×˜×•×‘×•×¡ ×ž×¤×¨×§×™ / ×ž×™×“×™×‘×•×¡ / ×ž×™× ×™×‘×•×¡, ×•"×œ× ×ž×•×’×“×¨" = ×œ× × ×§×‘×¢.
  // Â· "×‘×•×˜×œ ×•×—×–×¨" â€” ×›×¨×˜×™×¡ ×‘×™×•× ×©×”×§×• ×—×–×¨ ×œ×¨×™×©×•× ××—×¨×™ ×ª×§×•×¤×ª ×‘×™×˜×•×œ, ×‘×ž×§×•× ×¤×¡
  //   ×˜×§×¡×˜ ×‘×›×•×ª×¨×ª. ×§×• ×©×¢×“×™×™×Ÿ ×ž×‘×•×˜×œ × ×©××¨ ×¢× ×›×¨×˜×™×¡ "×‘×•×˜×œ" ×•×”×•×“×¢×ª ×”×¡×˜×˜×•×¡.
  if (!lf._synMerged) {
    // ××•×ª×• × ×™×¡×•×— ×›×ž×• ×‘×¤×™×“ (tools/linehistory_rishui.py: desc/note_for): ×ª×ž×™×“ ×’×•×“×œ
    // ×•×¡×•×’ ×™×—×“ â€” "××•×˜×•×‘×•×¡ ×¢×™×¨×•× ×™", "×ž×™× ×™×‘×•×¡ ×¢×™×¨×•× ×™", "××•×˜×•×‘×•×¡ ×ž×¤×¨×§×™ ×‘×™× ×¢×™×¨×•× ×™" â€”
    // ×‘×œ×™ "×¨×’×™×œ" ×•×‘×œ×™ "×‘×’×•×“×œ ×ž×œ×" (×©×œ×ž×” 06â€“07.09)
    const UND = "×œ× ×ž×•×’×“×¨";
    const desc = (s, t) => {
      if (!s || s === UND) return UND;
      if (s === "×ž×¤×¨×§×™") return ["××•×˜×•×‘×•×¡ ×ž×¤×¨×§×™", t].filter(Boolean).join(" ");
      return [s, t].filter(Boolean).join(" ");
    };
    const real = (lf.versions || []).filter((v) => !v.syn);
    const veh = lf.veh || [], evs = [];
    for (let i = 1; i < veh.length; i++) {
      const [d, t, s] = veh[i], [, pt, ps] = veh[i - 1];
      let note;
      // ×ª×‘× ×™×ª ××—×ª â€” "×”×™×” â† × ×”×™×”" â€” ×’× ×›×©×¦×“ ××—×“ "×œ× ×ž×•×’×“×¨" (×©×œ×ž×” 07.09, ×§×• 292)
      if (s === UND && ps !== UND) note = `×¡×•×’ ×”×¨×›×‘ ×‘×¨×™×©×•×™ ×©×•× ×”: ${desc(ps, pt)} â† ×œ× ×ž×•×’×“×¨ (×”×ž×©×¨×“ ×›×‘×¨ ×œ× ×§×•×‘×¢ ×œ×§×• ×¡×•×’ ×¨×›×‘)`;
      else if (ps === UND && s !== UND) note = `×¡×•×’ ×”×¨×›×‘ ×‘×¨×™×©×•×™ ×©×•× ×”: ×œ× ×ž×•×’×“×¨ â† ${desc(s, t)} (×¢×“ ×¢×›×©×™×• ×”×ž×©×¨×“ ×œ× ×§×‘×¢ ×œ×§×• ×¡×•×’ ×¨×›×‘)`;
      else if (ps !== s) note = `×¡×•×’ ×”×¨×›×‘ ×‘×¨×™×©×•×™ ×©×•× ×”: ${desc(ps, pt)} â† ${desc(s, t)}`;
      else note = `×¡×•×’ ×”×§×• ×‘×¨×™×©×•×™ ×©×•× ×”: ${pt || UND} â† ${t || UND} (×”×¨×›×‘: ${desc(s, "")})`;
      evs.push({ d, k: "vehicle", syn: true, stops: [], shp: "", note });
    }
    // Â· "×©×™× ×•×™ ×¡×•×’ ×”×§×•" â€” ×¡×•×’ ×”×§×• ×ž×¨×©×™×ž×ª ×”××©×›×•×œ×•×ª ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” (×¢×™×¨×•× ×™/××–×•×¨×™/
    //   ×‘×™× ×¢×™×¨×•× ×™) ×•×™×™×—×•×“×™×•×ª ×ž×§×•×‘×¥ ×”× ×•×¡×¢×™× (×¡×“×™×¨/×ª×œ×ž×™×“×™×/×œ×™×œ×”/×ž×–×™× ×™×); lt =
    //   [[×ª××¨×™×š, ×¡×•×’, ×™×™×—×•×“×™×•×ª, ××©×›×•×œ], â€¦] (tools/linehistory_ltype.py, ×©×œ×ž×” 07.09).
    //   ×™×™×—×•×“×™×•×ª ×¨×™×§×” = ×œ× ×”×™×™×ª×” ×™×“×•×¢×” ××– â€” ×œ× ×ž×©×•×•×™×.
    const UNN = { "×§×•×•×™× ×ž×–×™× ×™×": "×ž×–×™×Ÿ" };
    const lt = lf.lt || [];
    for (let i = 1; i < lt.length; i++) {
      const [d, t, u] = lt[i], [, pt, pu] = lt[i - 1], parts = [];
      if (t && pt && t !== pt) parts.push(`×¡×•×’ ×”×§×• ×‘×¨×©×™×ž×ª ×”××©×›×•×œ×•×ª ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×©×•× ×”: ${pt} â† ${t}`);
      if (u && pu && u !== pu) parts.push(`×™×™×—×•×“×™×•×ª ×”×§×• ×‘×§×•×‘×¥ ×”× ×•×¡×¢×™× ×©×œ ×”×ž×©×¨×“ ×©×•× ×ª×”: ${UNN[pu] || pu} â† ${UNN[u] || u}`);
      if (parts.length) evs.push({ d, k: "ltype", syn: true, stops: [], shp: "", note: parts.join(" Â· ") });
    }
    // ×”×ª×§×•×¤×” × ×’×ž×¨×ª ×‘×’×¨×¡×” ×”×‘××” ×ž×›×œ ×¡×•×’, ×œ× ×¨×§ ×‘"×•×¨×™×× ×˜ ×—×“×©" (×“×™×•×•×— ×©×œ×ž×” 03.09,
    // ×§×• 6 ×¨×”×˜: ×”×¡×¨×™×§×” ×¨×©×ž×” ×—×–×¨×” ×›"×©×™× ×•×™ ×ž×¡×œ×•×œ" ×‘×œ×™ ××™×¨×•×¢ new)
    for (let i = 0; i < real.length; i++) {
      if (real[i].k !== "removed") continue;
      const nx = real[i + 1];
      if (!nx || nx.k === "removed") continue;
      const days = gapDays(real[i].d, nx.d);
      const span = days >= 60 ? Math.round(days / 30.44) + " ×—×•×“×©×™×" : days + " ×™×ž×™×";
      evs.push({ d: nx.d, k: "returned", syn: true, stops: [], shp: "", src: nx.src,
        note: `×”×§×• ×œ× ×”×™×” ×‘×¨×™×©×•× ×ž-${fmtD(real[i].d)} ×¢×“ ${fmtD(nx.d)} (${span}) â€” ×•××– ×—×–×¨ ×œ×¤×¢×•×œ` });
    }
    if (evs.length) {
      lf.versions = [...(lf.versions || []), ...evs]
        .sort((a, b) => (a.d < b.d ? -1 : a.d > b.d ? 1 : (a.syn ? 1 : 0) - (b.syn ? 1 : 0)));
    }
    lf._synMerged = true;
  }
  return lf;
}

function decodeShape(str) {
  const pts = []; let i = 0, la = 0, lo = 0;
  while (i < str.length) {
    for (const which of [0, 1]) {
      let b, shift = 0, result = 0;
      do { b = str.charCodeAt(i++) - 63; result |= (b & 0x1f) << shift; shift += 5; } while (b >= 0x20);
      const d = (result & 1) ? ~(result >> 1) : (result >> 1);
      if (which === 0) la += d; else lo += d;
    }
    pts.push([la / 1e5, lo / 1e5]);
  }
  return pts;
}

function fsafe(rd) { return rd.replace(/#/g, "H").replace(/\//g, "_"); }
function fmtD(d) { return (d || "").split("-").reverse().join("."); }
// ×¡×™×•×ž×ª ×”×ª××¨×™×›×™× "(2025-07-10 â† 2025-07-11)" ×‘×”×¢×¨×•×ª ×”×¡×¨×™×§×” ×ž×•×¡×ª×¨×ª
// ×‘×ª×¦×•×’×” (×‘×§×©×ª ×©×œ×ž×”): ×ª××¨×™×š ×”×‘×™×¦×•×¢ ×›×‘×¨ ×›×ª×•×‘ ×‘×›×•×ª×¨×ª ×”××™×¨×•×¢ â€” ×–×”×•
function noteFix(s) {
  return String(s || "").replace(/\s*\(\d{4}-\d{2}-\d{2} â† \d{4}-\d{2}-\d{2}\)/g, "");
}
function fmtM(d) { const p = (d || "").split("-"); return p[1] + "." + p[0]; }
function gapDays(a, b) { return Math.round((new Date(b) - new Date(a)) / 864e5); }
// ×—×™×¤×•×© ×¡×œ×—× ×™ ×œ×’×¨×©×™×™×: ×‘× ×ª×•× ×™× ×›×ª×•×‘ ×¨×©×œ''×¦ (×©× ×™ ×’×¨×©×™×) ×•×”×ž×©×ª×ž×© ×ž×§×œ×™×“
// ×¨×©×œ"×¦ ××• ×¨×©×œ×´×¦ â€” ×›×œ ×¡×™×ž× ×™ ×”×’×¨×©/×’×¨×©×™×™× ×ž×•×¡×¨×™× ×ž×©× ×™ ×¦×™×“×™ ×”×”×©×•×•××”
// ×ž×§"×˜ ×›×ž×• 21011-1-# ×ž×ª×”×¤×š ×‘×˜×§×¡×˜ ×¢×‘×¨×™ ×œ-"#-21011-1", ×›×™ '#' ×‘×¡×•×£ ×¨×¦×£ ×¡×¤×¨×•×ª
// × ×—×©×‘ ×ª×• × ×™×˜×¨×œ×™ ×•×ž×§×‘×œ ××ª ×›×™×•×•×Ÿ ×”×¤×¡×§×” (×©×œ×ž×” 07.09). ×‘×ª×¦×•×’×” ×¢×•×–×¨ dir=ltr, ××‘×œ
// ×˜×§×¡×˜ ×©×ž×¢×ª×™×§×™× ×œ×•×•××˜×¡××¤ ×ž××‘×“ ××•×ª×• â€” ×œ×›×Ÿ ×‘×ª×•×š ×”×˜×§×¡×˜ ×¢×¦×ž×• ×™×© ×ª×•×•×™ ×›×™×•×•×Ÿ
// ×‘×œ×ª×™-× ×¨××™× (LREâ€¦PDF) ×©× ×•×¡×¢×™× ×™×—×“ ×¢× ×”×”×¢×ª×§×” ×•×ž×—×–×™×§×™× ××ª ×”×¡×“×¨ ×‘×›×œ ×ž×§×•×.
const rdTxt = (rd) => "\u202A" + String(rd || "") + "\u202C";
// ×—×™×¤×•×©: ×ž×§"×˜ ×©×”×•×“×‘×§ ×¢× ×ª×•×•×™ ×”×›×™×•×•×Ÿ ×”××œ×” ×—×™×™×‘ ×¢×“×™×™×Ÿ ×œ×”×ª××™×
const sQ = (t) => String(t || "").replace(/[×³×´'"\u200e\u200f\u202a-\u202e\u2066-\u2069]+/g, "");
// ×§×•×‘×¦×™ ×”× ×ª×•× ×™× ×ž×ª×¢×“×›× ×™× ×™×•×ž×™×ª ×ª×—×ª ××•×ª×” ×›×ª×•×‘×ª. ×—×•×ª×ž×ª-×™×•× ×‘×›×ª×•×‘×ª ×”×—×˜×™××”
// ××ª ×”×ž×˜×ž×•×Ÿ ×¤×¢× ×‘×™×•× ×’× ×œ×§×‘×¦×™× ×”×™×¡×˜×•×¨×™×™× ×©×œ× ×”×©×ª× ×•, ×•×‘×—×œ×§ ×ž×”×§×‘×¦×™×
// (?v=BUILD ×‘×œ×‘×“) ×”×•×’×©×” ×’×¨×¡×” ×©×œ ××ª×ž×•×œ. cache:no-cache ×ž××œ×¥ ×‘×“×™×§×ª
// ×˜×¨×™×•×ª ×ž×•×œ ×”×©×¨×ª: ×§×•×‘×¥ ×©×œ× ×”×©×ª× ×” ×—×•×–×¨ 304 ×–×¢×™×¨, ×§×•×‘×¥ ×©×”×©×ª× ×” ×ž×’×™×¢ ×˜×¨×™.
const dfetch = (p) => fetch(p + "?v=" + BUILD, { cache: "no-cache" });

// ×“×™×•×§ ×”×ª××¨×™×š × ×§×‘×¢ ×œ×¤×™ ×”×ž×¨×•×•×— ×‘×™×Ÿ ×©× ×™ ×¦×™×œ×•×ž×™ ×”××¨×›×™×•×Ÿ ×©×‘×™× ×™×”× ××•×ª×¨ ×”×©×™× ×•×™.
// ×‘××¨×›×™×•×Ÿ ×©×œ 2020 ×”×¦×™×œ×•×ž×™× ×™×•×ž×™×™× ×•×”×ª××¨×™×š ×ž×“×•×™×§; ×‘-2017 ×”× ×‘×ž×¨×—×§ ×©×‘×•×¢×™×™×,
// ×•××– ×™×•× ×ž×“×•×™×§ ×”×•× ×”×ž×¦××” â€” ×‘×ž×§×¨×” ×›×–×” × ×›×ª×‘ ×”×—×•×“×© ×‘×œ×‘×“. (×‘×§×©×ª ×”×ž×©×ª×ž×©.)
function evDate(v) {
  const sd = v.sd || (/(\d{4}-\d{2}-\d{2}) ×œ-(\d{4}-\d{2}-\d{2})/.exec(v.note || "") || [])[1];
  if (!sd || sd === v.d) return { txt: fmtD(v.d), exact: true };
  const g = gapDays(sd, v.d);
  // ×¢×“ ×©×œ×•×©×” ×™×ž×™× ×”×ª××¨×™×š × ×©××¨: ×”×•× × ×›×•×Ÿ ×¢×“ ×™×•×-×™×•×ž×™×™×, ×•×¡×™×ž×•×Ÿ ×©×œ ×¢×©×¨×•×ª
  // ××œ×¤×™ ××™×¨×•×¢×™× ×›"×œ× ×™×“×•×¢" ×‘×’×œ×œ ×–×” ×”×™×” ×”×•×¤×š ××ª ×”×¦×™×¨ ×œ×‘×œ×ª×™ ×§×¨×™× ×‘×œ×™
  // ×œ×”×•×¡×™×£ ××ž×ª. ×ž×¢×‘×¨ ×œ×–×” ×”×™×•× ×”×•× × ×™×—×•×©, ×•××– × ×›×ª×‘ ×”×—×•×“×© ×‘×œ×‘×“.
  if (g <= 3) return { txt: fmtD(v.d), exact: true,
                       tip: `××•×ª×¨ ×‘×™×Ÿ ${fmtD(sd)} ×œ-${fmtD(v.d)}` };
  if (fmtM(sd) === fmtM(v.d)) return { txt: fmtM(v.d), exact: false, tip: `××•×ª×¨ ×‘×™×Ÿ ${fmtD(sd)} ×œ-${fmtD(v.d)} â€” ${g} ×™×ž×™× ×‘×™×Ÿ ×¦×™×œ×•×ž×™ ×”××¨×›×™×•×Ÿ, ×•×œ×›×Ÿ ×”×™×•× ×”×ž×“×•×™×§ ××™× ×• ×™×“×•×¢` };
  return { txt: `${fmtM(sd)}â€“${fmtM(v.d)}`, exact: false, tip: `××•×ª×¨ ×‘×™×Ÿ ${fmtD(sd)} ×œ-${fmtD(v.d)} â€” ${g} ×™×ž×™× ×‘×™×Ÿ ×¦×™×œ×•×ž×™ ×”××¨×›×™×•×Ÿ, ×•×œ×›×Ÿ ×”×™×•× ×”×ž×“×•×™×§ ××™× ×• ×™×“×•×¢` };
}
function esc(s) { return String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"); }

/* ---------- ××™×ª×•×¨ ×”×§×˜×¢×™× ×©×©×•× ×• ×‘×™×Ÿ ×©×ª×™ ×’××•×ž×˜×¨×™×•×ª ----------
   ×œ×›×œ × ×§×•×“×” ×‘×’×¨×¡×” ××—×ª ×ž×—×©×‘×™× ××ª ×”×ž×¨×—×§ (×‘×ž×˜×¨×™×) ×œ×§×˜×¢ ×”×§×¨×•×‘ ×‘×™×•×ª×¨ ×‘×’×¨×¡×”
   ×”×©× ×™×™×”; ×¨×¦×£ × ×§×•×“×•×ª ×¨×—×•×§×•×ª = ×§×˜×¢ ×©×©×•× ×”. ×¢×ž×™×“ ×’× ×›×©×›×œ ×”×ž×¡×œ×•×œ × ×“×’× ×ž×—×“×©
   ×‘× ×§×•×“×•×ª ××—×¨×•×ª (×”×ž×¨×—×§ × ×©××¨ ~0 ×‘×§×˜×¢×™× ×©×œ× ×–×–×• ×‘××ž×ª). */
function segDiff(cur, prev) {
  if (!cur || cur.length < 2 || !prev || prev.length < 2) return null;
  const ky = 110540, kx = 111320 * Math.cos((cur[0][0] * Math.PI) / 180);
  const M = (p) => [p[1] * kx, p[0] * ky];
  /* ×“×’×™×ž×” ×¦×¤×•×¤×” (×›×œ ~12 ×ž') ×©×œ ×©× ×™ ×”×©×¨×˜×•×˜×™× ×œ×¤× ×™ ×”×”×©×•×•××”: ×§×˜×¢ ×™×©×¨ ××¨×•×š
     ×‘×œ×™ ×§×•×“×§×•×“×™× ×‘××ž×¦×¢ â€” ×›×ž×• ×—×¦×™×™×ª ×›×™×›×¨ ×‘×§×• ×™×©×¨ â€” ×œ× × ×“×’× ×§×•×“× ×‘×›×œ×œ,
     ×•×”×ª×•×•××™ ×”×™×©×Ÿ ×©×¢×‘×¨ ×‘×ª×•×š ×”×›×™×›×¨ ×œ× ×¡×•×ž×Ÿ ×‘××“×•× (×¦×™×œ×•×ž×™ ×©×œ×ž×”, ×§×• 26,
     ×¦×•×ž×ª ×‘×¨×•×¨ ×—×™×œ ×•×¦×•×ž×ª ××•×¨ ×”× ×¨). ×”× ×§×•×“×•×ª ×”× ×“×’×ž×•×ª ×™×•×©×‘×•×ª ×‘×“×™×•×§ ×¢×œ
     ×”×§×• ×”×ž×§×•×¨×™ â€” ×©×•× ×’××•×ž×˜×¨×™×” ×œ× ×ž×•×ž×¦××ª. */
  function densify(Praw, Pm) {
    const R = [Praw[0]], Q = [Pm[0]];
    for (let i = 1; i < Praw.length; i++) {
      const a = Pm[i - 1], b = Pm[i];
      const n = Math.min(60, Math.floor(Math.hypot(b[0] - a[0], b[1] - a[1]) / 12));
      for (let k = 1; k <= n; k++) {
        const t = k / (n + 1);
        R.push([Praw[i - 1][0] + (Praw[i][0] - Praw[i - 1][0]) * t,
                Praw[i - 1][1] + (Praw[i][1] - Praw[i - 1][1]) * t]);
        Q.push([a[0] + (b[0] - a[0]) * t, a[1] + (b[1] - a[1]) * t]);
      }
      R.push(Praw[i]); Q.push(Pm[i]);
    }
    return [R, Q];
  }
  const [cd, A] = densify(cur, cur.map(M));
  const [pd, B] = densify(prev, prev.map(M));
  const CS = 60; // ×’×•×“×œ ×ª× ×”×¨×©×ª ×‘×ž×˜×¨×™×
  function buildGrid(S) {
    const g = new Map();
    for (let i = 0; i < S.length - 1; i++) {
      const x0 = Math.min(S[i][0], S[i + 1][0]) - 20, x1 = Math.max(S[i][0], S[i + 1][0]) + 20;
      const y0 = Math.min(S[i][1], S[i + 1][1]) - 20, y1 = Math.max(S[i][1], S[i + 1][1]) + 20;
      for (let gx = Math.floor(x0 / CS); gx <= Math.floor(x1 / CS); gx++)
        for (let gy = Math.floor(y0 / CS); gy <= Math.floor(y1 / CS); gy++) {
          const k = gx + ":" + gy;
          if (!g.has(k)) g.set(k, []);
          g.get(k).push(i);
        }
    }
    return g;
  }
  function segd(p, s0, s1) {
    const dx = s1[0] - s0[0], dy = s1[1] - s0[1];
    const l2 = dx * dx + dy * dy;
    let t = l2 ? ((p[0] - s0[0]) * dx + (p[1] - s0[1]) * dy) / l2 : 0;
    t = Math.max(0, Math.min(1, t));
    const x = s0[0] + t * dx - p[0], y = s0[1] + t * dy - p[1];
    return Math.sqrt(x * x + y * y);
  }
  function dists(P, S, g) {
    return P.map((p) => {
      const gx = Math.floor(p[0] / CS), gy = Math.floor(p[1] / CS);
      let best = Infinity;
      for (let dx = -1; dx <= 1; dx++) for (let dy = -1; dy <= 1; dy++) {
        const segs = g.get((gx + dx) + ":" + (gy + dy));
        if (segs) for (const i of segs) { const d = segd(p, S[i], S[i + 1]); if (d < best) best = d; }
      }
      return best;
    });
  }
  function runsIdx(ds, th) {
    const idx = [];
    ds.forEach((d, i) => { if (d > th) idx.push(i); });
    if (!idx.length) return [];
    const out = [];
    let s = idx[0], e = idx[0];
    for (let i = 1; i < idx.length; i++) {
      // ×’×™×©×•×¨ ×”×“×•×§ (3 × ×§×•×“×•×ª): ×’×™×©×•×¨ ×¨×—×‘ ×”×“×‘×™×§ ×¨×¡×™×¡×™-×¨×¢×© ×œ×¨×¦×£ ×©×œ ×ž××•×ª
      // ×ž×˜×¨×™× ×•×”×¡×™×ž×•×Ÿ × ×¨××” "×‘×¢×¨×š" (×“×™×•×•×— ×©×œ×ž×”, ×§×• 26 â€” ×”×›×™×›×¨ ×”×—×“×©×”)
      if (idx[i] - e <= 3) e = idx[i];
      else { out.push([s, e]); s = e = idx[i]; }
    }
    out.push([s, e]);
    return out;
  }
  const gB = buildGrid(B), gA = buildGrid(A);
  const da = dists(A, B, gB), db = dists(B, A, gA);
  // ×¡×£ ×ž×¡×ª×’×œ: ×©× ×™ ×©×¨×˜×•×˜×™× ×©×“×•×œ×œ×• ××—×¨×ª "×¨×•×¢×“×™×" ×–×” ×¡×‘×™×‘ ×–×” ×œ×›×œ ×”××•×¨×š,
  // ×•×¢× ×¡×£ ×§×‘×•×¢ ×›×œ ×”×ž×¡×œ×•×œ ×”×•×“×’×© ×›××™×œ×• ×”×•×—×œ×£ (×“×™×•×•×— ×©×œ×ž×”, ×§×• 26). ×”×¡×£
  // ×¢×•×œ×” ×ž×¢×œ ×¨×¢×©-×”×‘×¡×™×¡ ×©×œ ×”×”×©×•×•××” ×›×š ×©×¨×§ ×”×¡×˜×™×™×” ×”××ž×™×ª×™×ª ×ž×•×“×’×©×ª
  const med = (arr) => {
    const s = [...arr].filter((x) => isFinite(x)).sort((x, y) => x - y);
    return s.length ? s[Math.floor(s.length / 2)] : 0;
  };
  const noise = med(da.concat(db));
  // ×‘×œ×™ ×¨×£ ×–×™×”×•×™ (×‘×§×©×ª ×©×œ×ž×”: "×©×œ× ×ª×”×™×” ×”×’×‘×œ×” â€” ×©×¤×©×•×˜ ×™×¡×ž×Ÿ ××ª ×”×§×˜×¢"):
  // ×”×¡×£ ×”×•× ×¨×§ ×“×™×•×§ ×”×¦×™×•×¨ â€” 5 ×ž'. ×›×œ ×ž×§×•× ×©×©× ×™ ×”×©×¨×˜×•×˜×™× ××™× × ××•×ª×• ×§×•
  // ×‘×• ×ž×¡×•×ž×Ÿ. ×”×¨×›×™×‘ ×”×ž×¡×ª×’×œ ×ž×’×Ÿ ×¨×§ ×ž×¦×ž×“ ×©×¨×˜×•×˜×™× ×™×©×Ÿ ×•×¨×•×¢×“.
  let th = Math.max(5, noise * 2.5 + 3);
  let curRuns = runsIdx(da, th), prevRuns = runsIdx(db, th);
  if (!curRuns.length && !prevRuns.length) {
    th = Math.max(3, noise * 2.5 + 2);
    curRuns = runsIdx(da, th); prevRuns = runsIdx(db, th);
  }
  if (!curRuns.length && !prevRuns.length) return null;
  /* ×”×¨×—×‘×” ×¢×“ × ×§×•×“×ª ×”××™×—×•×™ (×‘×§×©×ª ×©×œ×ž×”: "×”×§×˜×¢ ×©×ž×¦×•×™×¨ ×‘×¦×‘×¢ ×”×—×œ×© â€” ×©×™×•×“×’×©
     ×›×•×œ×•"): ×¨×¦×£ ×©× ×ª×¤×¡ ×ž×¢×œ ×”×¡×£ ×ž×ª×¨×—×‘ ×œ××•×¨×š ×”×§×• ×©×œ×• ×¢×“ ×”×ž×§×•× ×©×‘×• ×©× ×™
     ×”×©×¨×˜×•×˜×™× ×‘××ž×ª ×ž×ª××—×“×™× (×ž×ª×—×ª ×œ-LOW), ×›×š ×©×”×”×“×’×©×” ×ž×›×¡×” ××ª ×›×œ ×”×§×˜×¢
     ×©× ×¨××” × ×¤×¨×“ ×‘×¢×™×Ÿ â€” ×œ× ×¨×§ ××ª ×œ×™×‘×• ×©×—×¦×” ××ª ×”×¡×£. */
  const LOW = Math.max(3, th - 2);
  const grow = (runs, ds) => {
    const n = ds.length;
    const sep = new Array(n);
    for (let i = 0; i < n; i++) sep[i] = ds[i] > LOW;
    // ×›×©×”×§×• ×”×™×©×Ÿ ×—×•×¦×” ××ª ×”×—×“×© ×”×ž×¨×—×§ ×¦×•× ×— ×œ×¨×’×¢ ×œ××¤×¡ â€” ×—×•×¨ ×©×œ ×¢×“ 2 ×“×’×™×ž×•×ª
    // (~24 ×ž') ×‘× ×§×•×“×ª ×”×”×¦×˜×œ×‘×•×ª ×œ× ×§×•×˜×¢ ××ª ×”×¨×¦×£, ×•×”×–× ×‘ ×©×ž×¢×‘×¨ ×œ×” ×ž×•×“×’×© ×’×
    for (let i = 1; i < n - 1; i++) {
      if (!sep[i] && sep[i - 1] && (sep[i + 1] || (i + 2 < n && sep[i + 2]))) sep[i] = true;
    }
    const out = [];
    for (let [a, b] of runs) {
      while (a > 0 && sep[a - 1]) a--;
      while (b < n - 1 && sep[b + 1]) b++;
      if (out.length && a <= out[out.length - 1][1] + 3) out[out.length - 1][1] = Math.max(out[out.length - 1][1], b);
      else out.push([a, b]);
    }
    return out;
  };
  curRuns = grow(curRuns, da);
  prevRuns = grow(prevRuns, db);
  /* ×”×¡×™×ž×•×Ÿ ×ž×“×•×™×§ ×œ×¤×™ ×§×•××•×¨×“×™× ×˜×•×ª ×”×©×¨×˜×•×˜×™× ×‘×œ×‘×“ (×“×¨×™×©×ª ×©×œ×ž×”, ×§×• 26
     ×©×“×¨×•×ª â€” "×œ× ×œ× ×—×© ×‘×¢×¨×š"): ×œ×›×œ ×§×˜×¢ ×©×©×•× ×” ×‘×¦×“ ××—×“ × ×—×ª×š ×’× ×”×¦×“ ×”×©× ×™
     ×‘×“×™×•×§ ×‘×™×Ÿ ×”×”×˜×œ×•×ª ×”×’×™××•×ž×˜×¨×™×•×ª ×©×œ ×§×¦×•×ª ×”×§×˜×¢ ×¢×œ ×”×¤×•×œ×™×œ×™×™×Ÿ ×©×œ×•. ×›×š
     ×”××“×•× ×¢×•×§×‘ ××—×¨×™ ×”×©×¨×˜×•×˜ ×”×™×©×Ÿ ×¢×¦×ž×• ×ž× ×§×•×“×ª ×”×”×ª×¤×¦×œ×•×ª ×•×¢×“ × ×§×•×“×ª
     ×”×”×ª××—×“×•×ª, ×•×”×™×¨×•×§ ××—×¨×™ ×”×—×“×© â€” ×‘×œ×™ ×œ×”×ž×¦×™× ×ª×•×•××™ ×•×‘×œ×™ ×§×™×˜×•×¢. */
  /* ×§×˜×¢-×”× ×’×“ ×”×ž×“×•×™×§: ×§×¦×•×ª ×”×¨×¦×£ ×ž×•×˜×œ×™× ×¢×œ ×”×¤×•×œ×™×œ×™×™×Ÿ ×”×©× ×™, ×•×›×©×ž×¡×œ×•×œ
     ×¢×•×‘×¨ ×‘××•×ª×• ×›×‘×™×© ×¤×¢×ž×™×™× (×”×œ×•×š-×•×©×•×‘) ×™×© ×œ×›×œ ×§×¦×” ×›×ž×” ×”×˜×œ×•×ª ××¤×©×¨×™×•×ª â€”
     × ×‘×—×¨ ×¦×ž×“ ×”×”×˜×œ×•×ª ×©×ž×ž×–×¢×¨ ××ª ×”××•×¨×š ×”×›×œ×•× ×‘×™× ×™×”×Ÿ. ×‘×—×™×¨×” ×œ×¤×™ ×¡×“×¨-×œ××•×¨×š-
     ×”×§×• ×›×œ××” ×‘×§×• 26 ×œ×•×œ××” ×©×œ 4 ×§"×ž ×©×§×™×™×ž×ª ×‘×©× ×™ ×”×©×¨×˜×•×˜×™× ×•×”×¦×™×’×” ××•×ª×”
     ×›××™×œ×• ×‘×•×˜×œ×”. ×’× ×¢×›×©×™×•: ×× ×”××•×¨×š ×”×›×œ×•× ×œ× ×¤×¨×•×¤×•×¨×¦×™×•× ×œ×™ ×œ×¨×¦×£ â€” ××™×Ÿ
     ×ª×•×•××™ ×ž×§×‘×™×œ ××ž×™×ª×™ (×”××¨×›×” ×ž×¢×‘×¨ ×œ×§×¦×”) ×•×œ× ×ž×ž×¦×™××™× ×›×œ×•×. */
  const cumOf = (S) => { const c = [0]; for (let i = 1; i < S.length; i++) c.push(c[i - 1] + Math.hypot(S[i][0] - S[i - 1][0], S[i][1] - S[i - 1][1])); return c; };
  const cumA = cumOf(A), cumB = cumOf(B);
  const lenAt = (c, pos) => { const i = Math.max(0, Math.min(Math.floor(pos), c.length - 2)); return c[i] + (c[i + 1] - c[i]) * (pos - i); };
  function candsOn(q, Sm) {   // ××©×›×•×œ×•×ª ×”×”×˜×œ×•×ª ×”×§×¨×•×‘×•×ª (××—×“ ×œ×›×œ ×ž×¢×‘×¨ ×©×œ ×”×›×‘×™×©)
    const raw = [];
    let dmin = Infinity;
    for (let i = 0; i < Sm.length - 1; i++) {
      const s0 = Sm[i], s1 = Sm[i + 1];
      const dx = s1[0] - s0[0], dy = s1[1] - s0[1];
      const l2 = dx * dx + dy * dy;
      let t = l2 ? ((q[0] - s0[0]) * dx + (q[1] - s0[1]) * dy) / l2 : 0;
      t = Math.max(0, Math.min(1, t));
      const x = s0[0] + t * dx - q[0], y = s0[1] + t * dy - q[1];
      const dd = Math.sqrt(x * x + y * y);
      if (dd < dmin) dmin = dd;
      raw.push({ d: dd, pos: i + t });
    }
    const lim = Math.min(250, Math.max(dmin * 2, dmin + 30));
    const near = raw.filter((c) => c.d <= lim);
    const out = [];
    for (const c of near) {
      if (out.length && c.pos - out[out.length - 1].pos <= 3) {
        if (c.d < out[out.length - 1].d) out[out.length - 1] = { d: c.d, pos: c.pos };
      } else out.push({ d: c.d, pos: c.pos });
    }
    return out;
  }
  const counterIv = (run, Pm, cumP, Sm, cumS) => {
    const a0 = Math.max(0, run[0] - 1), b0 = Math.min(Pm.length - 1, run[1] + 1);
    const c0 = candsOn(Pm[a0], Sm), c1 = candsOn(Pm[b0], Sm);
    const runLen = lenAt(cumP, b0) - lenAt(cumP, a0);
    // ×”×¦×ž×“ ×”× ×‘×—×¨: ×”××•×¨×š ×”×›×œ×•× ×”×›×™ ×“×•×ž×” ×œ××•×¨×š ×”×¨×¦×£ ×¢×¦×ž×• (×ž×–×¢×•×¨ ×˜×”×•×¨ ×§×¨×¡
    // ×œ××¤×¡ ×›×©×©× ×™ ×”×§×¦×•×•×ª × ×¤×œ×• ×¢×œ ××•×ª×” × ×§×•×“×ª ×¦×•×ž×ª), ×¢× ×©×•×‘×¨-×©×•×•×™×•×Ÿ ×œ×§×¨×‘×”
    let best = null;
    for (const u of c0) for (const v of c1) {
      const span = Math.abs(lenAt(cumS, v.pos) - lenAt(cumS, u.pos));
      const score = Math.abs(span - runLen) + (u.d + v.d) * 2;
      if (!best || score < best.score) best = { score, span, lo: Math.min(u.pos, v.pos), hi: Math.max(u.pos, v.pos) };
    }
    if (!best) return null;
    if (best.span > runLen * 3 + 300 || best.span < runLen * 0.15 - 60) return null;
    return [best.lo, best.hi];
  };
  const ivA = curRuns.map(([a, b]) => [Math.max(0, a - 1), Math.min(A.length - 1, b + 1)]);
  const ivB = prevRuns.map(([a, b]) => [Math.max(0, a - 1), Math.min(B.length - 1, b + 1)]);
  for (const r of curRuns) { const iv = counterIv(r, A, cumA, B, cumB); if (iv) ivB.push(iv); }
  for (const r of prevRuns) { const iv = counterIv(r, B, cumB, A, cumA); if (iv) ivA.push(iv); }
  const mergeIv = (iv) => {
    const s = iv.filter(([x, y]) => y - x > 0.05).sort((u, v) => u[0] - v[0]);
    const out = [];
    for (const r of s) {
      if (out.length && r[0] <= out[out.length - 1][1] + 2) out[out.length - 1][1] = Math.max(out[out.length - 1][1], r[1]);
      else out.push([r[0], r[1]]);
    }
    return out;
  };
  const lerp = (a, b, t) => [a[0] + (b[0] - a[0]) * t, a[1] + (b[1] - a[1]) * t];
  const slice = (P, [x0, x1]) => {
    const i0 = Math.floor(x0), i1 = Math.min(Math.floor(x1), P.length - 1);
    const t0 = x0 - i0, t1 = x1 - i1;
    const out = [t0 > 0.001 && i0 < P.length - 1 ? lerp(P[i0], P[i0 + 1], t0) : P[i0]];
    for (let i = i0 + 1; i <= i1; i++) out.push(P[i]);
    if (t1 > 0.001 && i1 < P.length - 1) out.push(lerp(P[i1], P[i1 + 1], t1));
    return out;
  };
  const curSegs = mergeIv(ivA).map((r) => slice(cd, r)).filter((r) => r.length > 1);
  const prevSegs = mergeIv(ivB).map((r) => slice(pd, r)).filter((r) => r.length > 1);
  const derived = curRuns.length && !prevRuns.length ? "prev"
    : prevRuns.length && !curRuns.length ? "cur" : null;
  return { curSegs, prevSegs, derived };
}

/* ---------- ×ž×¤×ª ×œ×¤× ×™/××—×¨×™ ---------- */
/* ×˜×‘×œ×ª "×œ×¤× ×™ / ××—×¨×™" ×œ×œ×•×— ×”×™×¦×™××•×ª â€” ×œ××™×¨×•×¢×™ ×ª×“×™×¨×•×ª/×œ×•"×– ×©× ×©×ž×¨×• ×¢×
   ×”×¨×©×™×ž×•×ª ×”×ž×œ××•×ª (tl/tn). ×¨×™×‘×•×™ ×‘××•×ª×” ×©×¢×” = ×ª×’×‘×•×¨, ×•×ž×•×¦×’ ×›×ž×•× ×”. */
function TimesDiff({ tl, tn }) {
  const [open, setOpen] = useState(true);   // × ×¤×ª×—×ª ×ž×™×“ ×¢× ×”×œ×—×™×¦×” ×¢×œ ×”××™×¨×•×¢
  const rows = useMemo(() => {
    const cnt = (s) => {
      const c = new Map();
      (s ? s.split(",") : []).forEach((t) => c.set(t, (c.get(t) || 0) + 1));
      return c;
    };
    const a = cnt(tl), b = cnt(tn);
    const times = [...new Set([...a.keys(), ...b.keys()])].sort();
    const out = [], oldOnly = [], newOnly = [];
    times.forEach((t) => {
      const x = a.get(t) || 0, y = b.get(t) || 0;
      if (x > 0 && y > 0) {
        if (x === y) out.push({ key: t, before: x > 1 ? `${t} (${x}Ã—)` : t, cls: "same" });
        else out.push({ key: t, before: `${t} (${x}Ã—)`, after: `${t} (${y}Ã—)`, cls: "changed" });
      } else if (x > 0) oldOnly.push(t);
      else newOnly.push(t);
    });
    // ×”×ª××ž×ª ×™×¦×™××•×ª ×©×–×–×•: ×©×¢×” ×©×™×¨×“×” ×ž×•×œ ×©×¢×” ×§×¨×•×‘×” ×©× ×•×¡×¤×” (×¢×“ 20 ×“×§' ×”×¤×¨×©)
    const mins = (t) => +t.slice(0, 2) * 60 + +t.slice(3, 5);
    let i = 0, j = 0;
    while (i < oldOnly.length || j < newOnly.length) {
      const o = oldOnly[i], n = newOnly[j];
      if (o != null && n != null && Math.abs(mins(o) - mins(n)) <= 20) {
        out.push({ key: o + n, before: o, after: n, cls: "moved" }); i++; j++;
      } else if (o != null && (n == null || o < n)) {
        out.push({ key: o + "-", before: o, after: "â€”", cls: "removed" }); i++;
      } else {
        out.push({ key: "-" + n, before: "â€”", after: n, cls: "added" }); j++;
      }
    }
    out.sort((r, s) => (r.before !== "â€”" ? r.before : r.after).localeCompare(s.before !== "â€”" ? s.before : s.after));
    return out;
  }, [tl, tn]);
  const nOld = tl ? tl.split(",").length : 0, nNew = tn ? tn.split(",").length : 0;
  const changed = rows.filter((r) => r.cls !== "same").length;
  return (
    <div className="tdiff">
      <button className="tdiff-btn" aria-expanded={open} onClick={() => setOpen(!open)}
        title="×›×œ ×”×™×¦×™××•×ª ×©×•×¨×”-×©×•×¨×”: ×”×©×¢×” ×œ×¤× ×™ ×ž×•×œ ×”×©×¢×” ××—×¨×™; ×©×¢×” ×©×œ× ×”×©×ª× ×ª×” ×ž×•×¦×’×ª ×¤×¢× ××—×ª">
        ðŸ“Š {open ? "×”×¡×ª×¨ ××ª" : "×”×¦×’ ××ª"} ×˜×‘×œ×ª ×”×œ×¤× ×™/××—×¨×™ ×”×ž×œ××” ({nOld} â† {nNew} ×™×¦×™××•×ª Â· {changed} ×”×©×ª× ×•)
      </button>
      {open && (
        <div className="tdiff-wrap">
          <table className="tdiff-tbl">
            <thead><tr><th>×œ×¤× ×™</th><th>××—×¨×™</th></tr></thead>
            <tbody>
              {rows.map((r) => (
                <tr key={r.key} className={"td-" + r.cls}>
                  {r.cls === "same"
                    ? <td className="num" colSpan={2}>{r.before}</td>
                    : <><td className="num">{r.before}</td><td className="num">{r.after}</td></>}
                </tr>
              ))}
            </tbody>
          </table>
          <div className="tdiff-leg">×©×•×¨×” ×ž×ž×•×–×’×ª = ×”×™×¦×™××” ×œ× ×”×©×ª× ×ª×” Â· ×›×ª×•× = ×”×™×¦×™××” ×–×–×” (×”×©×¢×” ×”×™×©× ×” ×ž×•×œ ×”×—×“×©×”) ××• ×ª×’×‘×•×¨ ×©×”×©×ª× ×” Â· ××“×•× = ×™×¦×™××” ×©×™×¨×“×” Â· ×™×¨×•×§ = ×™×¦×™××” ×©× ×•×¡×¤×” Â· 2Ã— = ×©× ×™ ××•×˜×•×‘×•×¡×™× ×‘××•×ª×” ×©×¢×”</div>
        </div>
      )}
    </div>
  );
}


// ×§×• ×ž×¢×’×œ×™ ×¢×•×¦×¨ ×‘××•×ª×” ×ª×—× ×” ×‘×©× ×™ ×§×˜×¢×™ ×”×ž×¡×œ×•×œ â€” ×‘×¨×©×™×ž×•×ª âž•/âž– ×”×©× ×”×•×¤×™×¢
// ×¤×¢×ž×™×™× ×•× ×¨××” ×›×ž×• ×‘××’ (×§×• 114 ×“×Ÿ). ×ž××—×“×™× ×•×ž×¦×™×™× ×™× Ã—N ×‘×ž×§×•× ×œ×©×›×¤×œ.
const dedupCount = (arr) => {
  const m = new Map();
  (arr || []).forEach((x) => { const k = typeof x === "string" ? x : x[0] + "|" + x[1]; const e = m.get(k); if (e) e.n += 1; else m.set(k, { x, n: 1 }); });
  return [...m.values()];
};
function DiffMap({ cur, prev, approx, prevApprox, curStops, prevStops, addedCodes, stops12, shape12, remPins, sg, planned, plats }) {
  const ref = useRef(null);
  const mapRef = useRef(null);
  // ×§×˜×¢×™-×©×™× ×•×™ ×©×©×•×œ×¤×• ×ž×”××¨×›×™×•×Ÿ (v.sg) â€” ×”×’××•×ž×˜×¨×™×” ×”××ž×™×ª×™×ª ×©×œ ×ž×” ×©×™×¨×“
  // ×•×ž×” ×©× ×•×¡×£ ×’× ×›×©×œ×’×¨×¡××•×ª ××™×Ÿ ×©×¨×˜×•×˜ ×ž×œ× (×‘×§×©×ª ×©×œ×ž×”: ×¨×§ ×”×§×˜×¢ ×©×©×•× ×”)
  const sgO = useMemo(() => (sg && sg.o ? sg.o.map(decodeShape).filter((r) => r.length > 1) : null), [sg]);
  const sgN = useMemo(() => (sg && sg.n ? sg.n.map(decodeShape).filter((r) => r.length > 1) : null), [sg]);
  // ×”×§×˜×¢×™× ×©×©×•× ×• + ×”×ª×—× ×•×ª ×©×©×•× ×• â€” ×”×™×¢×“ ×©×œ ×ž×¦×‘ "×”×ª×ž×§×“×•×ª" (×‘×§×©×ª ×”×ž×©×ª×ž×©:
  // ×‘×ª×™×§×•×Ÿ ×‘××’ ×œ×¨××•×ª ×¨×§ ××ª ×”×§×˜×¢ ×©×”×©×ª× ×”, ×œ× ××ª ×›×œ ×”×ž×¡×œ×•×œ).
  // ×”×©×•×•××ª ×§×˜×¢×™× ×¨×§ ×›×©×©× ×™ ×”×¦×“×“×™× ×ž×“×•×™×§×™× â€” ×§×• ×ž×§×•×¨×‘ ×‘×™×Ÿ ×ª×—× ×•×ª ×™×™×ª×Ÿ ×¨×¢×©
  const diff = useMemo(() => (prev && !approx && !prevApprox ? segDiff(cur, prev) : null), [cur, prev, approx, prevApprox]);
  const chStops = useMemo(() => {
    const pts = [];
    const curCodes = new Set((curStops || []).map((s) => s[0]));
    if (addedCodes && (curStops || []).length) {
      pts.push(...(curStops || []).filter((s) => addedCodes.has(s[0])).map((s) => [s[2], s[3]]));
    } else if (prevStops) {
      const prevCodes = new Set((prevStops || []).map((s) => s[0]));
      pts.push(...(curStops || []).filter((s) => !prevCodes.has(s[0])).map((s) => [s[2], s[3]]));
    }
    // ×’× ×”×ª×—× ×•×ª ×©×™×¨×“×• ×”×Ÿ ×—×œ×§ ×ž×”×©×™× ×•×™ â€” ×‘×œ×¢×“×™×”×Ÿ ×”×ž×™×§×•×“ ×”×ª×›×•×•×¥ ×œ×ª×—× ×”
    // ×©× ×•×¡×¤×” ×‘×œ×‘×“ ×•×”×©××™×¨ ××ª ×”×™×¨×™×“×•×ª ×ž×—×•×¥ ×œ×ž×¡×š (×“×™×•×•×— ×©×œ×ž×”)
    pts.push(...(prevStops || []).filter((s) => !curCodes.has(s[0])).map((s) => [s[2], s[3]]));
    pts.push(...(remPins || []).map((p) => [p[2], p[3]]));
    return pts;
  }, [curStops, prevStops, addedCodes, remPins]);
  const focusPts = useMemo(() => {
    const pts = [];
    if (diff) { diff.curSegs.forEach((s2) => pts.push(...s2)); diff.prevSegs.forEach((s2) => pts.push(...s2)); }
    (sgO || []).forEach((r) => pts.push(...r));
    (sgN || []).forEach((r) => pts.push(...r));
    pts.push(...chStops);
    return pts;
  }, [diff, chStops, sgO, sgN]);
  const [focus, setFocus] = useState(true);
  const canFocus = focusPts.length > 0;
  useEffect(() => {
    if (!ref.current) return;
    const coarse = window.matchMedia && window.matchMedia("(pointer: coarse)").matches;
    const map = L.map(ref.current, { scrollWheelZoom: !coarse });
    mapRef.current = map;
    L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">OpenStreetMap</a>', maxZoom: 19,
    }).addTo(map);
    const focused = canFocus && focus;
    const pts12 = (stops12 || []).map((s) => [s[1], s[2]]);
    const pinPts = (remPins || []).map((p) => [p[2], p[3]]);
    const all = focused ? focusPts : cur.concat(prev || []).concat(pts12).concat(shape12 || []).concat(pinPts);
    map.fitBounds(L.latLngBounds(all.length ? all : [[32.08, 34.78]]).pad(focused ? 0.35 : 0.1), { maxZoom: 16 });
    // ×‘×ž×¦×‘ ×”×ª×ž×§×“×•×ª ×©×›×‘×•×ª-×”×¨×§×¢ ×›×ž×¢×˜ ×©×§×•×¤×•×ª: ×”×ž×§×•×•×§×• ×”××“×•× ×”×¢×“×™×Ÿ ×©×ž×¦×•×™×¨
    // ×œ××•×¨×š ×›×œ ×”×ž×¡×œ×•×œ × ×§×¨× ×‘×˜×¢×•×ª ×›"×©×™× ×•×™ ×œ× ×ž×¡×•×ž×Ÿ" ×‘×§×˜×¢×™× ×©×‘×”× ×©× ×™
    // ×”×©×¨×˜×•×˜×™× ×–×”×™× ×•×”×•× ×ž×¦×™×¥ ×ž×ª×—×ª ×œ×™×¨×•×§ (×“×™×•×•×— ×©×œ×ž×”)
    if (prev && prev.length > 1) {
      // ×ª×•×›× ×™×ª ×©×œ× ×™×¦××” ×œ×¤×•×¢×œ: ×”×ž×¡×œ×•×œ ×‘×¤×•×¢×œ ×”×•× ×¨×§×¢ ××¤×•×¨, ×œ× "×ž×¡×œ×•×œ ×§×•×“×" ××“×•×
      L.polyline(prev, planned
        ? { color: "#475569", weight: 4, opacity: 0.6 }
        : { color: "#dc2626", weight: focused ? 2 : 4, opacity: focused ? 0.12 : 0.75, dashArray: "8 7" }).addTo(map);
    }
    if (cur.length > 1) {
      L.polyline(cur, planned
        ? { color: "#9f1239", weight: 5, opacity: 0.9, dashArray: "10 8" }   // ×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ ×•×œ× × ×¡×¢ â€” ×§×• ×ž×§×•×•×§×• ×‘×™×Ÿ ×”×ª×—× ×•×ª
        : approx
        ? { color: "#7c3aed", weight: 4, opacity: 0.8, dashArray: "7 9" }   // ×§×• ×ž×§×•×¨×‘ ×‘×™×Ÿ ×ª×—× ×•×ª
        : { color: prev ? "#16a34a" : "#4c1d95", weight: focused ? 3 : 5, opacity: focused ? 0.25 : 0.9 }).addTo(map);
    }
    if (focused && diff && !planned) {
      diff.prevSegs.forEach((s2) => L.polyline(s2, { color: "#dc2626", weight: 6, opacity: 0.95, dashArray: "9 8" }).addTo(map));
      diff.curSegs.forEach((s2) => L.polyline(s2, { color: "#16a34a", weight: 7, opacity: 0.95 }).addTo(map));
    }
    if (!diff && (sgO || sgN)) {
      (sgO || []).forEach((r) => L.polyline(r, { color: "#dc2626", weight: 6, opacity: 0.95, dashArray: "9 8" }).addTo(map));
      (sgN || []).forEach((r) => L.polyline(r, { color: "#16a34a", weight: 7, opacity: 0.95 }).addTo(map));
    }
    // ×ž×¡×œ×•×œ 2012 â€” ×§×• ×—×•× ×ž×§×•×•×§×•. ×›×©×™×© ×ž×¡×œ×•×œ ×ž×©×•×¢×¨ ×¢×œ ×”×›×‘×™×©×™× (shape12,
    // tools/shape_2012.py â€” ×‘×§×©×ª ×©×œ×ž×” 05.09) ×”×•× ×ž×¦×•×™×¨; ××—×¨×ª ×§×• ×™×©×¨ ×“×¨×š
    // ×”×ª×—× ×•×ª ×©×”×•×¦×œ×‘×• ×œ×ž×§"×˜ (×ž×™×§×•× ×œ×¤×™ ×”×ž××’×¨ ×©×œ ×”×™×•×)
    if (shape12 && shape12.length > 1) {
      L.polyline(shape12, { color: "#78350f", weight: 4, opacity: 0.8, dashArray: "8 6" }).addTo(map);
    } else if (pts12.length > 1) {
      L.polyline(pts12, { color: "#78350f", weight: 3, opacity: 0.75, dashArray: "3 7" }).addTo(map);
    }
    if (pts12.length) {
      stops12.forEach((s) => {
        L.circleMarker([s[1], s[2]], { radius: 4, color: "#78350f", weight: 2, fillColor: "#fff", fillOpacity: 1 })
          .addTo(map).bindPopup(`<b>${esc(s[0])}</b><br><span class="pst">×ž×¡×œ×•×œ 2012</span>` + (s[4] ? `<br><span class="pcode">×ž×§×´×˜ ${esc(String(s[4]))}</span>` : ""), { className: "lh-pop", offset: [0, -4] });
      });
    }
    const curCodes = new Set((curStops || []).map((s) => s[0]));
    const prevCodes = new Set((prevStops || []).map((s) => s[0]));
    // ×©× ×”×ª×—× ×” × ×¤×ª×— ×‘×—×œ×•×Ÿ ×§×•×¤×¥ (popup) â€” ×”×•× ×ž×•×¦×ž×“ ×œ×¢×•×’×Ÿ ×©×œ ×”×ª×—× ×” ×•×”×ž×¤×”
    // ×–×–×” ××œ×™×• ×œ×‘×“, ××– ×”×©× ×ª×ž×™×“ ×ž×•×¦×’ ×‘×ž×§×•× ×”× ×›×•×Ÿ ×’× ×‘×§×¦×” ×”×ž×¤×” ×•×‘× ×™×™×“.
    // ×”××™×‘×¨ ×”×—×ž×™×©×™ ×‘×ª×—× ×” ×”×•× ×ž×’×‘×œ×ª ×¢×œ×™×™×”/×™×¨×™×“×” ×ž×”×¤×™×“: ××—×ª ×ž×›×œ ×ª×©×¢ ×¢×¦×™×¨×•×ª
    // ×ž×•×’×‘×œ×ª ×›×š, ×•×‘×©×•× ×ž×§×•× ×œ× ×›×ª×•×‘ ×œ× ×•×¡×¢ ×©×‘×ª×—× ×” ×”×–×• ×¨×§ ×ž×•×¨×™×“×™×.
    // 1 = ××™×Ÿ ×”×•×¨×“×” (×”×¢×œ××” ×‘×œ×‘×“), 2 = ××™×Ÿ ×”×¢×œ××” (×”×•×¨×“×” ×‘×œ×‘×“) â€” ×›×¤×™ ×©×”×¦× ×¨×ª
    // ×ž×§×•×“×“×ª (tools/linehistory.py). ×”×ž×™×¤×•×™ ×”×™×” ×”×¤×•×š ×•×”×¦×™×’ "×”×¢×œ××” ×‘×œ×‘×“"
    // ×¢×œ ×ª×—× ×•×ª ×¡×•×¤×™×•×ª (×“×™×•×•×— ×©×œ×ž×”, ×ª×—× ×” 787)
    const PD = { 1: "××™×¡×•×£ × ×•×¡×¢×™× ×‘×œ×‘×“ (×‘×œ×™ ×”×•×¨×“×”)", 2: "×”×•×¨×“×ª × ×•×¡×¢×™× ×‘×œ×‘×“ (×‘×œ×™ ××™×¡×•×£)", 3: "×œ× ×¢×•×¦×¨ ×œ× ×•×¡×¢×™×" };
    const popHtml = (s, status) =>
      `<b>${esc(s[1])}</b>${status ? `<br><span class="pst">${status}</span>` : ""}` +
      (PD[s[4]] ? `<br><span class="pst">â›” ${PD[s[4]]}</span>` : "") +
      // ×”×¨×¦×™×£ ×©×œ ×”×•×•×¨×™×× ×˜ ×‘×ª×—× ×” ×”×–×• ×”×™×•× (platforms.json) â€” ×¨×§ ×‘×ž×¡×•×¤×™× ×¢× ×¨×¦×™×¤×™×
      (plats && plats[String(s[0])] ? `<br><span class="pst">ðŸ›¤ï¸ ×¨×¦×™×£ ${esc(String(plats[String(s[0])]))}</span>` : "") +
      `<br><span class="pcode">×ž×§×´×˜ ×ª×—× ×” ${esc(s[0])}</span>`;
    (curStops || []).forEach((s) => {
      // ×”×’×¨×¡×” ×”×§×•×“×ž×ª ×¢×©×•×™×” ×œ×”×™×•×ª ×©×™× ×•×™ ×ª×“×™×¨×•×ª ×‘×œ×™ ×¨×¦×£ ×ª×—× ×•×ª, ×•××– ××™×Ÿ ×ž×•×œ ×ž×”
      // ×œ×”×©×•×•×ª. ×¨×©×™×ž×ª ×”×ª×—× ×•×ª ×©× ×•×¡×¤×• ×›×‘×¨ ×—×•×©×‘×” ×‘×¦× ×¨×ª ×•× ×©×ž×¨×” ×¢×œ ×”×’×¨×¡×” â€” ×”×™×
      // ×”×ž×§×•×¨ ×”××ž×™×Ÿ ×œ×¡×™×ž×•×Ÿ, ×•×œ× ×”×©×•×•××” ×ž×•×œ ×’×¨×¡×” ×©××™×Ÿ ×‘×” ×’××•×ž×˜×¨×™×”.
      const isNew = addedCodes ? addedCodes.has(s[0]) : (prevStops && !prevCodes.has(s[0]));
      const m = L.circleMarker([s[2], s[3]], {
        radius: isNew ? 8 : 5, color: isNew ? "#fff" : "#4c1d95", weight: 2,
        fillColor: isNew ? "#16a34a" : "#fff", fillOpacity: 1, opacity: focused && !isNew ? 0.4 : 1,
      }).addTo(map)
        .bindPopup(popHtml(s, isNew ? "ðŸŸ¢ ×ª×—× ×” ×©× ×•×¡×¤×” ×‘×’×¨×¡×” ×–×•" : ""), { className: "lh-pop", offset: [0, -4] });
      // ×©× ×”×ª×—× ×” ×ž×•×¦×’ ×¨×§ ×‘×œ×—×™×¦×” (popup ×¦×ž×•×“ ×œ×ª×—× ×”) â€” ×ª×•×•×™×•×ª ×¨×™×—×•×£ ×‘×•×˜×œ×•
      // ×œ×’×ž×¨×™: ×”×Ÿ × ×ª×§×¢×• ×¤×ª×•×—×•×ª ×•×”×¦×™×’×• ×©× ×›×¤×•×œ/×™×©×Ÿ ×‘×ž×§×•× ××—×¨ ×¢×œ ×”×ž×¤×”
    });
    (prevStops || []).forEach((s) => {
      if (curCodes.has(s[0])) return;
      const m = L.circleMarker([s[2], s[3]], { radius: 8, color: "#dc2626", weight: 3, fillColor: "#fff", fillOpacity: 1 })
        .addTo(map)
        .bindPopup(popHtml(s, "ðŸ”´ ×ª×—× ×” ×©×™×¨×“×” ×ž×”×§×• ×‘×’×¨×¡×” ×–×•"), { className: "lh-pop", offset: [0, -4] });
    });
    // ×ª×—× ×•×ª ×©×™×¨×“×• ×©××™× ×Ÿ ×‘××£ ×¨×©×™×ž×” ×‘×§×•×‘×¥ (×”×¦× ×¨×ª ×¤×¢× ×—×” ×ž×§"×˜+×ž×™×§×•× ×ž×¦×™×œ×•×
    // ×”××¨×›×™×•×Ÿ, v.nc) â€” ×‘×œ×¢×“×™×”×Ÿ ×”×ª×—× ×” ×©×™×¨×“×” ×¤×©×•×˜ ×œ× ×”×•×¤×™×¢×” ×‘×ž×¤×”
    (remPins || []).forEach((p) => {
      if (curCodes.has(p[0]) || prevCodes.has(p[0])) return;
      L.circleMarker([p[2], p[3]], { radius: 8, color: "#dc2626", weight: 3, fillColor: "#fff", fillOpacity: 1 })
        .addTo(map)
        .bindPopup(popHtml([p[0], p[1]], "ðŸ”´ ×ª×—× ×” ×©×™×¨×“×” ×ž×”×§×• ×‘×’×¨×¡×” ×–×•"), { className: "lh-pop", offset: [0, -4] });
    });
    return () => { mapRef.current = null; map.remove(); };
  }, [cur, prev, curStops, prevStops, addedCodes, focus, diff, chStops, focusPts, canFocus, stops12, shape12, remPins, sgO, sgN, plats]);
  // ×¡×™×›×•× ×˜×§×¡×˜×•××œ×™ ×œ×ž×™ ×©×œ× ×¨×•××” ××ª ×”×ž×¤×” â€” ×”×ž×¡×¤×¨×™× ×›×‘×¨ ×ž×—×•×©×‘×™× ×ž×ž×™×œ×
  const nAdd = (curStops || []).filter((s) => addedCodes && addedCodes.has(s[0])).length;
  const curC = new Set((curStops || []).map((s) => s[0]));
  const nRem = (prevStops || []).filter((s) => !curC.has(s[0])).length;
  const mapLabel = "×ž×¤×ª ×”×ž×¡×œ×•×œ" + (prev ? " ×‘×”×©×•×•××” ×œ×’×¨×¡×” ×”×§×•×“×ž×ª" : "") +
    (nAdd ? " Â· " + nAdd + " ×ª×—× ×•×ª × ×•×¡×¤×•" : "") + (nRem ? " Â· " + nRem + " ×ª×—× ×•×ª ×™×¨×“×•" : "");
  return (
    <div className="mapwrap">
      <div className="map" ref={ref} role="img" aria-label={mapLabel} />
      {canFocus && (
        <button className="focusbtn" title="×”×—×œ×¤×” ×‘×™×Ÿ ×ª×¦×•×’×ª ×›×œ ×”×ž×¡×œ×•×œ ×œ×‘×™×Ÿ ×”×ª×§×¨×‘×•×ª ×¨×§ ×œ×§×˜×¢ ×©×‘×• ×”×™×” ×”×©×™× ×•×™" onClick={() => setFocus(!focus)}>
          {focus ? "ðŸ—ºï¸ ×›×œ ×”×ž×¡×œ×•×œ" : "ðŸ” ×¨×§ ×”×§×˜×¢ ×©×©×•× ×”"}
        </button>
      )}
      {/* ×›×©×¨×§ ×¦×“ ××—×“ ×—×¨×’ ×ž×”×¡×£ (×ª×™×§×•×Ÿ ×©×¨×˜×•×˜ ×§×˜×Ÿ) â€” ×”×¦×“ ×”×©× ×™ ×”×•× ×—×™×ª×•×š
          ×ž×“×•×™×§ ×©×œ ×”×©×¨×˜×•×˜ ×©×œ×• ×‘×™×Ÿ × ×§×•×“×•×ª ×”×”×ª×¤×¦×œ×•×ª; ×•×›×©×”×ª×•×•××™ ×”×™×©×Ÿ ×¢×‘×¨
          ×¦×ž×•×“ (×›×™×›×¨ ×—×“×©×” ×‘×§×• 26 â€” ×”×§×• ×”×™×©×Ÿ ×ž×©×™×§ ×œ×”) ××™×Ÿ ×‘×›×œ×œ ×ž×” ×œ×¡×ž×Ÿ
          ×‘××“×•×, ×•××•×ž×¨×™× ××ª ×–×” ×‘×ž×§×•× ×œ×”×ž×¦×™× ×§×˜×¢ (×“×¨×™×©×ª ×©×œ×ž×”) */}
      {diff && diff.derived === "prev" && diff.prevSegs.length > 0 && (
        <div className="mut">â„¹ï¸ ×”×§×˜×¢ ×”××“×•× ×ž×¡×ž×Ÿ ××ª ×”×ª×•×•××™ ×”×™×©×Ÿ ×‘×ž×§×•× ×©×‘×• ×”×©×¨×˜×•×˜ ×ª×•×§×Ÿ â€” ×—×ª×•×š ×ž×”×©×¨×˜×•×˜ ×”×™×©×Ÿ ×¢×¦×ž×•, ×‘×“×™×•×§ ×‘×™×Ÿ × ×§×•×“×•×ª ×”×”×ª×¤×¦×œ×•×ª ×ž×”×ª×•×•××™ ×”×—×“×©; ×”×™×¨×•×§ ×”×•× ×”×ª×™×§×•×Ÿ.</div>
      )}
      {diff && diff.derived === "prev" && diff.prevSegs.length === 0 && (
        <div className="mut">â„¹ï¸ ×›××Ÿ × ×•×¡×£ ×ª×•×•××™ ×—×“×© (×”×™×¨×•×§) â€” ×”×ª×•×•××™ ×”×™×©×Ÿ ×‘×ž×§×•× ×©×‘×• ×”×©×¨×˜×•×˜ ×ª×•×§×Ÿ ×¢×‘×¨ ×¦×ž×•×“ ×œ×—×“×©, ×‘×œ×™ ×¡×˜×™×™×” ×ž×©×œ×•, ×•×œ×›×Ÿ ××™×Ÿ ×§×˜×¢ ××“×•×.</div>
      )}
      {diff && diff.derived === "cur" && diff.curSegs.length > 0 && (
        <div className="mut">â„¹ï¸ ×”×§×˜×¢ ×”×™×¨×•×§ ×ž×¡×ž×Ÿ ××ª ×”×ª×•×•××™ ×”×—×“×© ×‘×ž×§×•× ×©×‘×• ×”×©×¨×˜×•×˜ ×ª×•×§×Ÿ â€” ×—×ª×•×š ×ž×”×©×¨×˜×•×˜ ×”×—×“×© ×¢×¦×ž×•, ×‘×“×™×•×§ ×‘×™×Ÿ × ×§×•×“×•×ª ×”×”×ª×¤×¦×œ×•×ª ×ž×”×ª×•×•××™ ×”×™×©×Ÿ; ×”××“×•× ×”×•× ×ž×” ×©×™×¨×“.</div>
      )}
      {diff && diff.derived === "cur" && diff.curSegs.length === 0 && (
        <div className="mut">â„¹ï¸ ×›××Ÿ ×™×¨×“ ×ª×•×•××™ (×”××“×•×) â€” ×”×ª×•×•××™ ×”×—×“×© ×‘×ž×§×•× ×©×‘×• ×”×©×¨×˜×•×˜ ×ª×•×§×Ÿ ×¢×•×‘×¨ ×¦×ž×•×“ ×œ×™×©×Ÿ, ×‘×œ×™ ×¡×˜×™×™×” ×ž×©×œ×•, ×•×œ×›×Ÿ ××™×Ÿ ×§×˜×¢ ×™×¨×•×§.</div>
      )}
      {diff && !diff.prevSegs.length && !diff.curSegs.length && (
        <div className="mut">â„¹ï¸ ×©× ×™ ×”×©×¨×˜×•×˜×™× ×›×ž×¢×˜ ×—×•×¤×¤×™× (×”×”×‘×“×œ ×§×˜×Ÿ ×ž×¢×©×¨×•×ª ×ž×˜×¨×™×) â€” ×œ×›×Ÿ ××™×Ÿ ×§×˜×¢ ××“×•× ××• ×™×¨×•×§ ×ž×•×“×’×©; ×”×§×• ×”××“×•× ×”×ž×§×•×•×§×• ×ž×¡×ª×ª×¨ ×ž×ª×—×ª ×œ×™×¨×•×§.</div>
      )}
    </div>
  );
}

/* ×©×•×¨×ª ×§×• ×›×§×™×©×•×¨ ××ž×™×ª×™: ×§×œ×™×§ ×¨×’×™×œ × ×©××¨ ×‘×ª×•×š ×”××¤×œ×™×§×¦×™×”, Ctrl/×§×œ×™×§Ö¾××ž×¦×¢×™
   ×¤×•×ª×—×™× ××ª ×“×£ ×”×§×• ×‘×›×¨×˜×™×¡×™×™×” ×—×“×©×” (×œ×›×œ ×§×• ×™×© ×›×ª×•×‘×ª ×ž×©×œ×• ××—×¨×™ ×”-#) */
const lineHref = (r) => "#" + encodeURIComponent(r);
// ×œ×§×• ×™×© ×›×ª×•×‘×ª ×ž×©×œ×• ×•×œ×ª×—× ×” ×œ× ×”×™×™×ª×” â€” ××™ ××¤×©×¨ ×”×™×” ×œ×©×œ×•×— ×œ×ž×™×©×”×• ×©×™× ×•×™
// ×‘×ª×—× ×” ×ž×¡×•×™×ž×ª, ×¨×§ ×œ×”×¢×ª×™×§ ×œ×• ×˜×§×¡×˜. â€Ž#stop=<×ž×§"×˜>â€Ž ×¤×•×ª×— ××ª ×”×ª×—× ×” ×¢× ×›×œ
// ×§×•×¨×•×ª ×”×—×™×™× ×©×œ×”.
const stopHref = (c) => "#stop=" + encodeURIComponent(c);
const plainClick = (e) => !(e.ctrlKey || e.metaKey || e.shiftKey || e.altKey);

/* ×ª×’×™×ª ×¢× ×”×¡×‘×¨ ×©× ×¤×ª×— ×‘×”×§×©×”: title ×ž×•×¦×’ ×¨×§ ×‘×¨×™×—×•×£ ×¢×›×‘×¨, ×•×‘×˜×œ×¤×•×Ÿ ××™×Ÿ
   ×¨×™×—×•×£ â€” ×¨×•×‘ ×”×§×”×œ ×œ× ×¨××” ××ª ×”×”×¡×‘×¨×™× ×”××œ×” ×ž×¢×•×œ×. ×”×§×©×” ×¢×œ ×”×ª×’×™×ª ×¤×•×ª×—×ª
   ××ª ××•×ª×• ×”×¡×‘×¨ ×›×©×•×¨×” ×§×˜× ×” ×ž×ª×—×ª×™×”, ×•×”×§×©×” × ×•×¡×¤×ª ×¡×•×’×¨×ª. */
function TipTag({ cls, tip, children }) {
  const [open, setOpen] = useState(false);
  // ×›×©××•×ª×• ×¨×›×™×‘ ×ž×§×‘×œ ×”×¡×‘×¨ ××—×¨ (×”×—×œ×¤×ª ×—×•×“×©/×¡×™× ×•×Ÿ ×¢× key ×–×”×”) â€” ×”×”×¡×‘×¨
  // ×”×¤×ª×•×— × ×¡×’×¨, ××—×¨×ª ×”×•× × ×©××¨ "×ª×§×•×¢" ×œ×™×“ ×ª×•×›×Ÿ ×©×›×‘×¨ ×”×ª×—×œ×£
  useEffect(() => { setOpen(false); }, [tip]);
  return (
    <>
      <span className={(cls || "") + " tiptag"} title={tip} role="button" tabIndex={0}
        aria-expanded={open}
        onClick={(e) => { e.preventDefault(); e.stopPropagation(); setOpen(!open); }}
        onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); e.stopPropagation(); setOpen(!open); } }}>
        {children}</span>
      {open && <span className="tipnote" onClick={(e) => { e.preventDefault(); e.stopPropagation(); }}>â„¹ï¸ {tip}</span>}
    </>
  );
}

/* ×”×•×“×¢×ª ×›×©×œ ×¨×©×ª ××—×™×“×” ×¢× ×›×¤×ª×•×¨ × ×™×¡×™×•×Ÿ ×—×•×–×¨ â€” ×‘×ž×§×•× "××™×Ÿ × ×ª×•× ×™×" ×ž×˜×¢×” */
function NetErr({ onRetry }) {
  return (
    <div className="empty">ðŸ“¡ ×”× ×ª×•× ×™× ×œ× ×™×¨×“×• â€” ×›× ×¨××” ×ª×§×œ×ª ×¨×©×ª ×¨×’×¢×™×ª.{" "}
      <button className="morebtn" onClick={onRetry}>â†» × ×¡×• ×©×•×‘</button></div>
  );
}

/* ×—×™×¤×•×© ×©× ×©×ž×¨ ×‘×™×Ÿ × ×™×•×•×˜×™×: ×—×–×¨×” ×ž×¢×ž×•×“ ×§×• ×œ× ×ž×•×—×§×ª ××ª ×ž×” ×©×”×•×§×œ×“ */
function usePersistedQ(key) {
  const [q, setQ] = useState(() => { try { return sessionStorage.getItem(key) || ""; } catch (e) { return ""; } });
  useEffect(() => { try { sessionStorage.setItem(key, q); } catch (e) { /* ×“×¤×“×¤×Ÿ ×—×•×¡× ××—×¡×•×Ÿ */ } }, [key, q]);
  return [q, setQ];
}
// ×¢×¨×š ×©×ž×ª×¢×“×›×Ÿ ×¨×§ ××—×¨×™ ×”×¤×¡×§×” ×‘×”×§×œ×“×”: ×”×¡×™× ×•×Ÿ ×©×œ "×›×œ ×”×ª×§×•×¤×”" ×‘×˜××‘ ×”×ª×—× ×•×ª ×ž×¨× ×“×¨
// ×ž××•×ª ×©×•×¨×•×ª, ×•×›×œ ×ª×• ×”×¨×™×¥ ××•×ª×• ×ž×—×“×© â€” ×‘×˜×œ×¤×•×Ÿ ×–×” ××™×˜×™ (×©×œ×ž×” 06.09)
function useDebounced(v, ms) {
  const [d, setD] = useState(v);
  useEffect(() => { const t = setTimeout(() => setD(v), ms); return () => clearTimeout(t); }, [v, ms]);
  return d;
}

/* ×¢×•×’×Ÿ 2012: rd -> ×ª×§×¦×™×¨ ×”×ž×¡×œ×•×œ ×“××– (× ×˜×¢×Ÿ ×¤×¢× ××—×ª ×œ×›×œ ×”×“×¤×“×•×£) */
let ANC2012 = null;
let ANC_SET = new Set();   // ×”×ž×§"×˜×™× ×©×”×•×¦×œ×‘×• â€” ×œ×¡×™× ×•×Ÿ "2012" ×‘×—×™×¤×•×©
/* months.json × ×˜×¢×Ÿ ×¤×¢× ××—×ª ×œ×‘×™×§×•×¨: ×©×œ×•×© ×§×•×ž×¤×•× × ×˜×•×ª (×ž×¡×š ×”×‘×™×ª, ×”×¤×™×“
   ×”×™×•×ž×™ ×•×˜××‘ ×”×ª×—× ×•×ª) ×‘×™×§×©×• ××•×ª×• ×›×œ ××—×ª ×‘× ×¤×¨×“. ×›×©×œ ×ž× ×§×” ××ª ×”×ž×˜×ž×•×Ÿ ×›×“×™
   ×©×›×¤×ª×•×¨ "× ×¡×• ×©×•×‘" ×‘××ž×ª ×™× ×¡×” ×©×•×‘. */
let MONTHS_P = null;
const getMonths = () => MONTHS_P || (MONTHS_P = dfetch("data/months.json")
  .then((r) => r.json())
  .catch((e) => { MONTHS_P = null; throw e; }));

/* ×¨×¦×™×¤×™× (platforms.json v2, tools/platforms.py â€” × ×›×ª×‘ ×‘×›×œ ×¨×™×¦×” ×™×•×ž×™×ª ×ž×”×©×•×¨×•×ª
   ×©×œ ×”×¨×¦×™×¤×™× ×‘×§×•×‘×¥ ×”×ª×—× ×•×ª ×©×œ ×”×ž×©×¨×“): st â€” ×œ×›×œ ×ž×§"×˜ ×”×¨×¦×™×¤×™× ×©×™×© ×‘×”× × ×¡×™×¢×•×ª
   ×‘×ª×•×§×£ ×•×ž×™ ×¢×•×¦×¨ ×‘×›×œ ××—×“; rd â€” ×œ×›×œ ×•×¨×™×× ×˜ ×”×¨×¦×™×£ ×©×œ×• ×‘×›×œ ×ª×—× ×”. × ×˜×¢×Ÿ ×¤×¢× ××—×ª;
   ×›×©×œ ××• ×§×•×‘×¥ ×™×©×Ÿ = ×‘×œ×™ ×¨×¦×™×¤×™×, ×‘×œ×™ ×œ×©×‘×•×¨ ××ª ×”×“×£. */
let PLAT_P = null;
const getPlatforms = () => PLAT_P || (PLAT_P = dfetch("data/platforms.json")
  .then((r) => (r.ok ? r.json() : null))
  .then((d) => (d && d.v === 2 ? d : { st: {}, rd: {} }))
  .catch(() => ({ st: {}, rd: {} })));
function usePlatforms() {
  const [p, setP] = useState(null);
  useEffect(() => { let on = true; getPlatforms().then((d) => { if (on) setP(d); }); return () => { on = false; }; }, []);
  return p;
}
const platKey = (p) => (/^\d+$/.test(p) ? [0, parseInt(p, 10)] : [1, p]);
const platSort = (a, b) => { const x = platKey(a), y = platKey(b); return x[0] - y[0] || (x[0] === 0 ? x[1] - y[1] : String(x[1]).localeCompare(String(y[1]))); };

const getAnchors2012 = () =>
  ANC2012 || (ANC2012 = dfetch("data/anchor-2012.json")
    .then((r) => (r.ok ? r.json() : { anchors: {} }))
    .then((d) => { ANC_SET = new Set(Object.keys(d.anchors || {})); return d; })
    .catch(() => ({ anchors: {} })));

/* ---------- ×¢×ž×•×“ ×§×• ---------- */
/* ---------- ×”×©×•×•××” ×‘×™×Ÿ ×—×œ×•×¤×•×ª ---------- */
// ×¢×“ ×¢×›×©×™×• ××¤×©×¨ ×”×™×” ×œ×”×©×•×•×ª ×’×¨×¡×” ×©×œ ×§×• ×œ×’×¨×¡×” ×§×•×“×ž×ª ×©×œ×•, ××‘×œ ×œ× ×—×œ×•×¤×”
// ×œ×—×œ×•×¤×”: ×ž×™ ×©×¨×¦×” ×œ×“×¢×ª ×‘×ž×” ×›×™×•×•×Ÿ 1 ×©×•× ×” ×ž×›×™×•×•×Ÿ 2, ××• ×”"×¨××©×™×ª" ×ž×”×—×œ×•×¤×”,
// ×”×™×” ×¦×¨×™×š ×œ×¤×ª×•×— ×©× ×™ ×¢×ž×•×“×™× ×•×œ×”×—×–×™×§ ××ª ×©×ª×™ ×”×¨×©×™×ž×•×ª ×‘×¨××©.
function AltCompare({ rd, altRd, label, onClose }) {
  const [a, setA] = useState(null);
  const [b, setB] = useState(null);
  useEffect(() => {
    let ok = true;
    setA(null); setB(null);
    // ××•×ª×” ×›×ª×•×‘×ª ×‘×“×™×•×§ ×›×ž×• ×‘×¢×ž×•×“ ×”×§×• â€” ××—×¨×ª ××•×ª×• ×§×•×‘×¥ ×™×¨×“ ×¤×¢×ž×™×™× ×ª×—×ª
    // ×©×ª×™ ×—×•×ª×ž×•×ª ×©×•× ×•×ª, ×•×‘×ž×§×¨×” ×’×¨×•×¢ ×—×¦×™ ×ž×¡×š ×”×¦×™×’ × ×ª×•× ×™× ×©×œ ××ª×ž×•×œ
    const get = (r) => dfetch("data/lines/" + fsafe(r) + ".json")
      .then((x) => (x.ok ? x.json() : null)).then(materializeLf);
    get(rd).then((d) => ok && setA(d)).catch(() => ok && setA(false));
    get(altRd).then((d) => ok && setB(d)).catch(() => ok && setB(false));
    return () => { ok = false; };
  }, [rd, altRd]);
  if (a === null || b === null) return <div className="altcmp">×˜×•×¢×Ÿâ€¦</div>;
  if (!a || !b) return <div className="altcmp">××—×ª ×”×—×œ×•×¤×•×ª ×œ× × ×˜×¢× ×”.</div>;
  // ×”×ž×¦×‘ ×”× ×•×›×—×™ ×©×œ ×›×œ ×—×œ×•×¤×”: ×”×’×¨×¡×” ×”××—×¨×•× ×” ×©×™×© ×‘×” ×¨×¦×£ ×ª×—× ×•×ª
  const last = (lf) => {
    const vs = (lf.versions || []).filter((v) => (v.stops || []).length);
    return vs[vs.length - 1] || null;
  };
  const va = last(a), vb = last(b);
  if (!va || !vb) return <div className="altcmp">×œ××—×ª ×”×—×œ×•×¤×•×ª ××™×Ÿ ×¨×¦×£ ×ª×—× ×•×ª ×ž×ª×•×¢×“.</div>;
  const ca = va.stops.map((x) => x[0]), cb = vb.stops.map((x) => x[0]);
  const sa = new Set(ca), sb = new Set(cb);
  const onlyA = va.stops.filter((x) => !sb.has(x[0]));
  const onlyB = vb.stops.filter((x) => !sa.has(x[0]));
  const both = ca.filter((x) => sb.has(x)).length;
  const same = ca.length === cb.length && ca.every((x, i) => x === cb[i]);
  return (
    <div className="altcmp">
      <div className="altcmphead">
        <b>×”×©×•×•××ª ×—×œ×•×¤×•×ª</b> Â· {a.dest || rd} <span className="mut">×ž×•×œ</span> {label}
        <button className="cmpx" onClick={onClose}>âœ• ×¡×™×•×</button>
      </div>
      <div className="altstat">
        <span>{both.toLocaleString()} ×ª×—× ×•×ª ×ž×©×•×ª×¤×•×ª</span>
        <span className="onlya">{onlyA.length.toLocaleString()} ×¨×§ ×›××Ÿ</span>
        <span className="onlyb">{onlyB.length.toLocaleString()} ×¨×§ ×‘×—×œ×•×¤×” ×”×©× ×™×™×”</span>
        <span className="mut">{fmtD(va.d)} ×ž×•×œ {fmtD(vb.d)}</span>
      </div>
      {same && <div className="mut">×¨×¦×£ ×”×ª×—× ×•×ª ×–×”×” ×‘×©×ª×™×”×Ÿ â€” ×”×”×‘×“×œ ×”×•× ×‘×›×™×•×•×Ÿ ×”× ×¡×™×¢×” ×‘×œ×‘×“.</div>}
      <DiffMap cur={decodeShape(va.shp || "")} prev={decodeShape(vb.shp || "")}
        approx={!va.shp} prevApprox={!vb.shp} curStops={va.stops} prevStops={vb.stops} />
      <div className="legend">
        <span><i style={{ borderColor: "#4c1d95" }} /> ×”×—×œ×•×¤×” ×”×¤×ª×•×—×”</span>
        <span><i style={{ borderColor: "#16a34a" }} /> ×”×—×œ×•×¤×” ×œ×”×©×•×•××”</span>
      </div>
      {(onlyA.length > 0 || onlyB.length > 0) && (
        <div className="cmplist">
          {onlyA.length > 0 && <div className="ad">×¨×§ ×›××Ÿ: {onlyA.map((x) => `${x[1]} (${x[0]})`).join(", ")}</div>}
          {onlyB.length > 0 && <div className="rm">×¨×§ ×‘×—×œ×•×¤×” ×”×©× ×™×™×”: {onlyB.map((x) => `${x[1]} (${x[0]})`).join(", ")}</div>}
        </div>
      )}
    </div>
  );
}

/* ×˜×‘×œ×ª ×”×œ×•"×– ×”×ž×œ× ×©×œ ×”×—×œ×•×¤×” (×‘×§×©×ª ×©×œ×ž×”): ×›×œ ×©×¢×•×ª ×”×™×¦×™××” ×œ×¤×™ ×™×•×,
   ×ž×¤×¨×¡×•× ×”×¨×™×©×•×™ ×©×œ 10 ×”×™×ž×™× ×”×§×¨×•×‘×™× â€” ××•×ª×• ×§×•×‘×¥ ×©×ž×–×™×Ÿ ××ª ×ž×¢×§×‘
   ×”×ª×’×‘×•×¨×™×, ×•×œ×›×Ÿ ×©×¢×” ×©×ž×ª×•×›× × ×™× ×‘×” 2+ ××•×˜×•×‘×•×¡×™× ×ž×¡×•×ž× ×ª ×‘×” ×‘×ž×¤×•×¨×©.
   ×™×•×©×‘×ª ×‘×›×¨×˜×™×¡ ×”×¦×“ ×•×œ×›×Ÿ ×ž×•×¦×’×ª ×‘×›×œ ×’×¨×¡×” ×©× ×‘×—×¨×ª â€” ×’× ×©×™× ×•×™ ×ž×¡×œ×•×œ
   ×•×›×œ ×©×™× ×•×™ ×©××™× ×• ×©×™× ×•×™ ×œ×•"×–. */
const SCHED_DAYS = ["×", "×‘", "×’", "×“", "×”", "×•", "×©"];
const SCHED_LBL = { "×": "×¨××©×•×Ÿ", "×‘": "×©× ×™", "×’": "×©×œ×™×©×™", "×“": "×¨×‘×™×¢×™", "×”": "×—×ž×™×©×™", "×•": "×©×™×©×™", "×©": "×©×‘×ª" };
// ×”×™×•× ×‘×©×‘×•×¢ ×©××™×¨×•×¢ ×œ×•"×– × ×’×¢ ×‘×• â€” × ×©×œ×£ ×ž× ×•×¡×— ×”×”×¢×¨×” ("×™×ž×™ ×¨××©×•×Ÿ" / "×©×‘×ª")
const schedDayOf = (note) => {
  const m = /\((?:×™×ž×™ )?([×-×ª]+)/.exec(note || "");
  return { "×¨××©×•×Ÿ": "×", "×©× ×™": "×‘", "×©×œ×™×©×™": "×’", "×¨×‘×™×¢×™": "×“",
           "×—×ž×™×©×™": "×”", "×©×™×©×™": "×•", "×©×‘×ª": "×©" }[m && m[1]] || null;
};
function SchedBox({ rd, vs, selD, isLast }) {
  const [d, setD] = useState(null);
  useEffect(() => {
    let ok = true;
    setD(null);
    dfetch("data/sched/" + fsafe(rd).slice(0, 2) + ".json")
      .then((r) => { if (!r.ok) throw new Error(r.status); return r.json(); })
      .then((j) => { if (ok) setD(j.lines && j.lines[rd] ? { g: j.g, days: j.lines[rd] } : false); })
      .catch(() => { if (ok) setD(false); });
    return () => { ok = false; };
  }, [rd]);
  // ××™×Ÿ ×œ×•"×– ×œ×©×‘×•×¢ ×”×§×¨×•×‘ = ×”×—×œ×•×¤×” ××™× ×” ×¤×¢×™×œ×” ×›×¨×’×¢ â€” ×•×–×” × ××ž×¨, ×œ× × ×©×ª×§ (×©×œ×ž×” 07.09:
  // "×¤×¢×™×œ" ×”×•× ×¨×§ ×ž×™ ×©×™×© ×œ×• ×œ×•"×– ×œ×©×‘×•×¢ ×”×§×¨×•×‘). ×§×• ×©×›×‘×¨ ×ž×¡×•×ž×Ÿ ×ž×‘×•×˜×œ ×œ× ×¦×¨×™×š ××ª ×–×”.
  if (d === false) {
    const real = (vs || []).filter((v) => !v.syn && v.k !== "planned-dropped");
    if (real.length && real[real.length - 1].k === "removed") return null;
    return <div className="gapwarn">â¸ï¸ ××™×Ÿ ×œ×—×œ×•×¤×” ×”×–×• ×œ×•×´×– ×œ×©×‘×•×¢ ×”×§×¨×•×‘ (×œ×¤×™ ×¤×¨×¡×•× ×”×¨×™×©×•×™ ×œ-10 ×”×™×ž×™× ×”×§×¨×•×‘×™×) â€” ×”×™× ××™× ×” ×¤×¢×™×œ×” ×›×¨×’×¢.</div>;
  }
  if (!d) return null;
  // ×”×œ×•"×– ×©×œ ××– (×‘×§×©×ª ×©×œ×ž×”): ×›×©× ×‘×—×¨×” ×’×¨×¡×” ×™×©× ×” ×ž×©×—×–×¨×™× ××—×•×¨×” ×ž×”×œ×•"×– ×©×œ
  // ×”×™×•× â€” ×›×œ ××™×¨×•×¢ ×œ×•"×– ×©×ž××•×—×¨ ×ž×”×’×¨×¡×” ×ž×•×—×–×¨ ×œ×©×¢×•×ª ×”×™×©× ×•×ª ×©×œ×• (tl).
  // ×ž×”×—×“×© ×œ×™×©×Ÿ, ×›×š ×©×”××™×¨×•×¢ ×”×§×¨×•×‘ ×‘×™×•×ª×¨ ×œ×’×¨×¡×” ×§×•×‘×¢ ××—×¨×•×Ÿ = ×”×ž×¦×‘ ×‘×–×ž× ×”.
  const past = !isLast && selD;
  let days = d.days, touched = new Set();
  if (past) {
    days = {};
    for (const b of SCHED_DAYS) days[b] = (d.days[b] || []).map((t) => Array.isArray(t) ? t[0] : t);
    (vs || []).filter((v) => (v.k === "sched" || v.k === "freq") && v.tl != null && v.d > selD)
      .sort((a, b) => b.d.localeCompare(a.d))
      .forEach((v) => {
        const L = schedDayOf(v.note);
        if (L) { days[L] = String(v.tl || "").split(",").map((s) => s.trim()).filter(Boolean); touched.add(L); }
      });
    // ×¨×§ ×ž×” ×©×ª×•×¢×“ ×‘××ž×ª: ×™×ž×™× ×©×œ× ×ª×•×¢×“ ×‘×”× ×©×™× ×•×™ ×œ×•"×– ×ž××– â€” ××™×Ÿ ×ž×™×“×¢ ×¢×œ ××•×ª×” ×ª×§×•×¤×”,
    // ×•×œ× "×›× ×¨××” ×›×ž×• ×”×™×•×" (×©×œ×ž×” 13.09). ×›×©××™×Ÿ ×©×•× ×™×•× ×›×–×” â€” ×”×œ×•"×– ×œ× ×ž×•×¦×’ ×‘×›×œ×œ.
    for (const b of SCHED_DAYS) if (!touched.has(b)) days[b] = [];
  }
  if (past && !touched.size) return null;
  const hasTb = !past && SCHED_DAYS.some((b) => (days[b] || []).some((t) => Array.isArray(t)));
  return (
    <details className="schedbox">
      <summary>ðŸ• {past ? `×”×œ×•"×– ×›×¤×™ ×©×”×™×” ×‘×’×¨×¡×ª ${selD.split("-").reverse().join(".")}` : '×”×œ×•"×– ×”×ž×œ× ×©×œ ×”×—×œ×•×¤×” â€” ×›×œ ×©×¢×•×ª ×”×™×¦×™××”'}{hasTb ? " Â· ×™×© ×ª×’×‘×•×¨×™×" : ""}</summary>
      <div className="schednote">
        {/* ×”×”×¢×¨×” ×¢×œ ×”×©×—×–×•×¨ ×œ× ×”×•×‘× ×” (×©×œ×ž×” 07.09: "×ž×” ×”×”×¢×¨×” ×”×ž×•×–×¨×” ×”×–×•?") â€” ×¢×›×©×™×•
            ×‘×ž×™×œ×™× ×¤×©×•×˜×•×ª, ×•×‘×œ×™ ×œ×“×‘×¨ ×¢×œ âœ± ×›×©××™×Ÿ ××£ ×™×•× ×›×–×” ×‘×˜×‘×œ×” */}
        {past ? <>
          ×ž×•×¦×’×•×ª ×¨×§ ×”×©×¢×•×ª ×©×ª×•×¢×“×•: ×‘×™×ž×™× ×¢× âœ± × ×¨×©× ×©×™× ×•×™ ×œ×•"×– ××—×¨×™ ×”×’×¨×¡×” ×”×–×•, ×•×œ×›×Ÿ
          ××œ×” ×”×©×¢×•×ª ×©×”×™×• ×œ×¤× ×™ ×”×©×™× ×•×™.
          {SCHED_DAYS.some((b) => !touched.has(b)) ? " ×‘×©××¨ ×”×™×ž×™× ××™×Ÿ ×ž×™×“×¢ ×¢×œ ×”×œ×•\"×– ×‘×ª××¨×™×š ×”×ž×‘×•×§×©." : ""}
        </> : <>
          ×ž×¤×¨×¡×•× ×”×¨×™×©×•×™ ×œ-10 ×”×™×ž×™× ×”×§×¨×•×‘×™× (× ×›×•×Ÿ ×œ-{d.g.split("-").reverse().join(".")}) â€”
          ×–×” ×”×œ×•"×– ×”× ×•×›×—×™.
          {/* ×”×”×¡×‘×¨ ×¢×œ Ã—N ×¨×§ ×›×©×™×© ×‘××ž×ª ×©×¢×” ×›×–××ª ×‘×œ×•"×– (×©×œ×ž×” 06.09) */}
          {hasTb ? <> ×©×¢×” ×ž×•×“×’×©×ª ×¢× Ã—N ×¤×™×¨×•×©×” ×©-N ××•×˜×•×‘×•×¡×™× ×™×•×¦××™× ×‘××•×ª×” ×©×¢×” (×ª×’×‘×•×¨).</> : null}
        </>}
      </div>
      <table className="schedtbl"><tbody>
        {(() => {
          // ×™×ž×™× ×¢× ××•×ª×• ×œ×•"×– ×‘×“×™×•×§ ×ž×ª××—×“×™× ×œ×©×•×¨×” ××—×ª, ×’× ×›×©××™× × ×¢×•×§×‘×™×:
          // "×¨××©×•×Ÿâ€“×—×ž×™×©×™", "×¨××©×•×Ÿ ×•×—×ž×™×©×™", "×¨××©×•×Ÿâ€“×©×œ×™×©×™ ×•×—×ž×™×©×™" (×©×œ×ž×” 06.09)
          const groups = new Map();
          for (const b of SCHED_DAYS) {
            const ts = days[b] || [];
            if (!ts.length) continue;
            const key = JSON.stringify(ts);
            if (!groups.has(key)) groups.set(key, { ts, days: [] });
            groups.get(key).days.push(b);
          }
          const rows = [...groups.values()];
          const lbl = (ds) => {
            const items = [];
            for (let i = 0; i < ds.length;) {
              let j = i;
              while (j + 1 < ds.length && SCHED_DAYS.indexOf(ds[j + 1]) === SCHED_DAYS.indexOf(ds[j]) + 1) j++;
              if (j - i >= 2) items.push(SCHED_LBL[ds[i]] + "â€“" + SCHED_LBL[ds[j]]);
              else for (let k = i; k <= j; k++) items.push(SCHED_LBL[ds[k]]);
              i = j + 1;
            }
            return items.length === 1 ? items[0] : items.slice(0, -1).join(", ") + " ×•" + items[items.length - 1];
          };
          return rows.map((r) => (
            <tr key={r.days.join("")}>
              <th>{lbl(r.days)}{past && r.days.some((b) => touched.has(b)) ? " âœ±" : ""}</th>
              <td>{r.ts.map((t, i) => Array.isArray(t)
                ? <span key={i} className="tchip tb" title={`${t[1]} ××•×˜×•×‘×•×¡×™× ×™×•×¦××™× ×‘×©×¢×” ×–×• (×ª×’×‘×•×¨)`}>{t[0]} Ã—{t[1]}</span>
                : <span key={i} className="tchip">{t}</span>)}</td>
            </tr>
          ));
        })()}
      </tbody></table>
    </details>
  );
}

// --- ×”×ª×¨××•×ª ×“×¤×“×¤×Ÿ (OneSignal) â€” ×¤×¢×™×œ ×¨×§ ×›×©×ž×–×”×” ×”××¤×œ×™×§×¦×™×” ×ž×•×–×Ÿ ×‘-index.html ---
const PUSH_ON = typeof window !== "undefined" && !!window.KB_ONESIGNAL_APP_ID;
// ×ª×’ ×¢×™×¨: ×ž×¤×ª×—×•×ª ×ª×’×™× ×—×™×™×‘×™× ×œ×”×™×•×ª ASCII â€” ×”××© ×™×¦×™×‘ ×©×œ ×©× ×”×¢×™×¨ (djb2â†’base36),
// ×–×”×” ×œ×—×™×©×•×‘ ×‘×¦×“ ×”×©×•×œ×— (tools/send_push.py)
const cityTag = (name) => { let h = 5381; const s = String(name || "").trim(); for (let i = 0; i < s.length; i++) h = ((h * 33) ^ s.charCodeAt(i)) >>> 0; return "c" + h.toString(36); };
// ×ž×¦×‘ ×”×”×ª×¨××•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×” (×©×œ×ž×” 07.09: "×›×œ ×¤×¢× ×©×•××œ ××™×©×•×¨ ×•×œ× ×ž×’×™×¢×” ×”×•×“×¢×”"):
// ×‘××™×™×¤×•×Ÿ ×”×ª×¨××•×ª ×¢×•×‘×“×•×ª ×¨×§ ×ž×”××ª×¨ ×©× ×•×¡×£ ×œ×ž×¡×š ×”×‘×™×ª; ×“×¤×“×¤×Ÿ ×©×—×¡× â€” ×¦×¨×™×š ×œ×¤×ª×•×—
// ×‘×”×’×“×¨×•×ª ×”××ª×¨. ×”×”×¨×©××” ×ž×ª×‘×§×©×ª ×¨×§ ×›×©××™×Ÿ, ×•×”×ž× ×•×™ ×ž×•×¤×¢×œ ×× ×›×‘×”.
const pushSupport = () => {
  try {
    const ua = navigator.userAgent || "";
    const ios = /iPhone|iPad|iPod/.test(ua) || (navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1);
    const standalone = (window.matchMedia && window.matchMedia("(display-mode: standalone)").matches) || navigator.standalone === true;
    if (ios && !standalone) return { ok: false, why: "×‘××™×™×¤×•×Ÿ ×•×‘××™×™×¤×“ ×”×ª×¨××•×ª ×¢×•×‘×“×•×ª ×¨×§ ××—×¨×™ ×©×ž×•×¡×™×¤×™× ××ª ×”××ª×¨ ×œ×ž×¡×š ×”×‘×™×ª (×›×¤×ª×•×¨ ×”×©×™×ª×•×£ â† \"×”×•×¡×£ ×œ×ž×¡×š ×”×‘×™×ª\"), ×¤×•×ª×—×™× ××•×ª×• ×ž×©× ×•× ×¨×©×ž×™× ×©×•×‘" };
    if (!("Notification" in window) || !("PushManager" in window)) return { ok: false, why: "×”×“×¤×“×¤×Ÿ ×”×–×” ×œ× ×ª×•×ž×š ×‘×”×ª×¨××•×ª. ×‘×›×¨×•×, ×¡×ž×¡×•× ×’ ××™× ×˜×¨× ×˜ ××• ×¤×™×™×¨×¤×•×§×¡ ×–×” ×¢×•×‘×“" };
    if (Notification.permission === "denied") return { ok: false, why: "×”×“×¤×“×¤×Ÿ ×—×•×¡× ×”×ª×¨××•×ª ×ž×”××ª×¨. ×œ××¤×©×¨: ×œ×—×™×¦×” ×¢×œ ×¡×ž×œ ×”×ž× ×¢×•×œ ×œ×™×“ ×”×›×ª×•×‘×ª â† ×”×ª×¨××•×ª â† ××¤×©×¨, ×•××– ×œ×”×™×¨×©× ×©×•×‘" };
  } catch (e) { /* ignore */ }
  return { ok: true };
};
async function ensurePush(OS) {
  const sup = pushSupport();
  if (!sup.ok) return sup;
  if (!OS.Notifications.permission) await OS.Notifications.requestPermission();
  if (!OS.Notifications.permission) return { ok: false, why: "×”×”×¨×©××” ×œ×”×ª×¨××•×ª ×œ× ××•×©×¨×”. ×›×©×”×“×¤×“×¤×Ÿ ×©×•××œ â€” \"××¤×©×¨\", ×œ× ×œ×¡×’×•×¨ ××ª ×”×©××œ×”" };
  const ps = OS.User.PushSubscription;
  if (ps && ps.optedIn === false) await ps.optIn();
  if (!(ps && ps.id)) return { ok: false, why: "×”×“×¤×“×¤×Ÿ ×œ× ×™×¦×¨ ×ž× ×•×™ ×œ×”×ª×¨××•×ª (×‘×‘×¨×™×™×‘: ×œ×”×“×œ×™×§ \"Use Google services for push messaging\" ×‘×”×’×“×¨×•×ª)" };
  return { ok: true, id: ps.id };
}
// ××™×ž×•×ª ×ž×•×œ ×”×©×¨×ª ×©×œ ×¡×¤×§ ×”×”×ª×¨××•×ª ×•×ª×™×§×•×Ÿ ×™×©×™×¨ â€” ×‘×œ×™ ×œ×¡×ž×•×š ×¢×œ ×ª×•×¨ ×”×¤×¢×•×œ×•×ª ×©×œ
// ×”-SDK ×‘×“×¤×“×¤×Ÿ. ××¦×œ ×©×œ×ž×” (07.09) ×”×ž× ×•×™ × ×•×¦×¨, ×”×“×£ ×”×¨××” "âœ“ × ×©×ž×¨ â€” 5 ×¢×¨×™×",
// ×•×‘×©×¨×ª ×œ× × ×¨×©× ××£ ×ª×’. ×”× ×ª×™×‘×™× ×›××Ÿ ×”× ××•×ª× × ×ª×™×‘×™× ×©×”-SDK ×¢×¦×ž×• ×§×•×¨× ×ž×”×“×¤×“×¤×Ÿ
// (×ž×–×”×” ×”××¤×œ×™×§×¦×™×” ×‘×œ×‘×“, ×‘×œ×™ ×ž×¤×ª×—). ×ž×—×–×™×¨ {have, fixed, ok} ××• {err}.
const TAG_RE = /^(kb|c[0-9a-z]+|l\d+|freq|kg_\w+)$/;
async function syncServer(OS, want) {
  const sub = OS.User && OS.User.PushSubscription && OS.User.PushSubscription.id;
  if (!sub) return { err: "××™×Ÿ ×ž× ×•×™ ×‘×“×¤×“×¤×Ÿ ×”×–×”" };
  const api = "https://api.onesignal.com/apps/" + window.KB_ONESIGNAL_APP_ID;
  const j = async (path, opt, hdr) => {
    const r = await fetch(api + path, { ...(opt || {}), headers: { "content-type": "application/json", ...(hdr ? { "OneSignal-Subscription-Id": sub } : {}) } });
    if (!r.ok) throw new Error("HTTP " + r.status);
    return r.json();
  };
  const jj = async (path, opt) => { try { return await j(path, opt, true); } catch (e) { return j(path, opt, false); } };
  const wait = (ms) => new Promise((ok) => setTimeout(ok, ms));
  try {
    let idn = null;
    for (let i = 0; i < 3 && !(idn && idn.identity && idn.identity.onesignal_id); i++) {
      if (i) await wait(3000);
      try { idn = await jj("/subscriptions/" + sub + "/user/identity"); } catch (e) { idn = null; }
    }
    const oid = idn && idn.identity && idn.identity.onesignal_id;
    if (!oid) return { err: "×”×ž× ×•×™ ×¢×“×™×™×Ÿ ×œ× ×ž×•×›×¨ ×‘×©×¨×ª â€” × ×¡×• ×©×•×‘ ×‘×¢×•×“ ×¨×’×¢" };
    const tagsOf = (u) => ((u && u.properties && u.properties.tags) || {});
    const diff = (h) => {
      const d = {};
      for (const k in want) if (h[k] !== want[k]) d[k] = want[k];
      for (const k in h) if (h[k] !== "" && !(k in want) && TAG_RE.test(k)) d[k] = "";   // "" = ×ž×—×™×§×ª ×ª×’
      return d;
    };
    let have = tagsOf(await jj("/users/by/onesignal_id/" + oid));
    let d = diff(have), fixed = false;
    if (Object.keys(d).length) {
      await jj("/users/by/onesignal_id/" + oid, { method: "PATCH", body: JSON.stringify({ properties: { tags: d } }) });
      have = tagsOf(await jj("/users/by/onesignal_id/" + oid));
      fixed = true; d = diff(have);
    }
    return { have, fixed, ok: !Object.keys(d).length };
  } catch (e) { return { err: (e && e.message) || String(e) }; }
}
// ×ž×¦×‘ ×œ×ª×¦×•×’×”: {loading, ok, why, id, srv}. ×›×©×”×›×•×œ ×ª×§×™×Ÿ â€” ×”×ª×’×™× ×ž×”×“×¤×“×¤×Ÿ ×”×–×”
// × ×©×œ×—×™× ×©×•×‘ ×œ×¡×¤×§ (resync) ×•×ž××•×ž×ª×™× ×ž×•×œ ×”×©×¨×ª (×©×œ×ž×” 07.09)
function readPushState(cb, resync) {
  const sup = pushSupport();
  if (!sup.ok) { cb({ ...sup, loading: false }); return; }
  let got = false;
  window.OneSignalDeferred = window.OneSignalDeferred || [];
  window.OneSignalDeferred.push(async (OS) => {
    got = true;
    try {
      const ps = OS.User.PushSubscription;
      const ok = !!OS.Notifications.permission && !!(ps && ps.optedIn !== false && ps.id);
      if (resync && Object.keys(resync).length) {
        try {
          // ×”×¡×¤×§ ×™×©×§×£ ×‘×“×™×•×§ ××ª ×ž×” ×©× ×©×ž×¨ ×‘×“×¤×“×¤×Ÿ ×”×–×”: ×ª×’×™× ×™×©× ×™× (×¢×™×¨ ×©×”×•×¡×¨×”,
          // ×ª×“×™×¨×•×ª ×§×•×“×ž×ª) × ×ž×—×§×™×, ×›×œ ×”×©××¨ × ×©×œ×—×™× ×©×•×‘. ×’× ×›×©×”×”×¨×©××” ×¢×•×“ ×œ×
          // ××•×©×¨×” â€” ×”×ª×’×™× ×ž×—×›×™× ××¦×œ ×”×¡×¤×§ ×•×ª×•×¤×¡×™× ×‘×¨×’×¢ ×©×”×ž× ×•×™ × ×¤×ª×— (×©×œ×ž×” 07.09:
          // × ×ž×¦× ×ž× ×•×™ ×‘×œ×™ ×©×•× ×ª×’ ×œ×ž×¨×•×ª ×—×ž×© ×¢×¨×™× ×©×ž×•×¨×•×ª)
          const cur = (await OS.User.getTags()) || {};
          const stale = Object.keys(cur).filter((k) => !(k in resync) && TAG_RE.test(k));
          if (stale.length) OS.User.removeTags(stale);
          OS.User.addTags(resync);
        } catch (e) { /* ignore */ }
      }
      const st = { loading: false, ok, id: ps && ps.id, why: ok ? "" : (!OS.Notifications.permission ? "×¢×•×“ ×œ× ××•×©×¨×” ×”×¨×©××” ×œ×”×ª×¨××•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×” â€” ×œ×—×¦×• \"×”×¤×¢×œ×ª ×”×ª×¨××•×ª\" ×•××©×¨×•" : "×”×ž× ×•×™ ×›×‘×•×™ â€” ×œ×—×¦×• \"×”×¤×¢×œ×ª ×”×ª×¨××•×ª\"") };
      cb(st);
      // ×’× ×‘×¤×ª×™×—×ª ×”×ž×¨×›×– (×œ× ×¨×§ ×‘×©×ž×™×¨×”): "×‘×•×“×§ ×ž×•×œ ×”×©×¨×ªâ€¦" × ×©××¨ ×ª×§×•×¢ ×¢×“ ×©×©×ž×¨×• (×©×œ×ž×” 13.09)
      if (ps && ps.id && resync) cb({ ...st, srv: Object.keys(resync).length ? await syncServer(OS, resync) : { have: {}, ok: true, fixed: false, empty: true } });
    } catch (e) { cb({ loading: false, ok: false, why: "×©×™×¨×•×ª ×”×”×ª×¨××•×ª ×œ× × ×˜×¢×Ÿ (×—×•×¡× ×¤×¨×¡×•×ž×•×ª?)" }); }
  });
  setTimeout(() => { if (!got) cb({ loading: false, ok: false, why: "×©×™×¨×•×ª ×”×”×ª×¨××•×ª ×œ× × ×˜×¢×Ÿ (×—×•×¡× ×¤×¨×¡×•×ž×•×ª ××• ×—×•×¡× ×ª×•×›×Ÿ ×‘×“×¤×“×¤×Ÿ)" }); }, 8000);
}
// ×›×œ ×”×”×¨×©×ž×” ×©×œ ×”×“×¤×“×¤×Ÿ ×”×–×” ×‘×ª×’ ××—×“. ×¡×¤×§ ×”×”×ª×¨××•×ª ×ž×’×‘×™×œ ××ª ×ž×¡×¤×¨ ×”×ª×’×™× ×œ×ž×©×ª×ž×©,
// ×•×¢× ×ª×’ ×œ×›×œ ×¢×™×¨ ×”×”×¨×©×ž×” ×©×œ ×©×œ×ž×” (5 ×¢×¨×™× + ×ª×“×™×¨×•×ª + 4 ×¡×•×’×™× = 10 ×ª×’×™×) × ×“×—×ª×”
// ×‘-409 ×‘×œ×™ ×©× ×©×ž×¨ ×›×œ×•× (07.09). kb = "f=<×ª×“×™×¨×•×ª>|g=<×¡×•×’×™×>|c=<×¢×¨×™ ×”×ž×¨×›×–>|
// w=<×¢×¨×™× ×ž×›×¤×ª×•×¨ ×¢×§×•×‘>|l=<×ž×§"×˜×™×>" â€” ×”×¢×¨×™× ×›×ª×’×™× ×ž×’×•×‘×‘×™× ×›×ž×• ×§×•×“×; ×”×¦×“ ×”×©×•×œ×—
// (tools/send_push.py, parse_sub) ×§×•×¨× ××ª ××•×ª×• ×ž×‘× ×”.
function savedTags() {
  const parts = [];
  try {
    const n = JSON.parse(localStorage.kbNotify || "{}");
    const cl = n.cities || (n.city ? [n.city] : []);
    if (cl.length) {
      parts.push("f=" + (n.freq || "1"));
      parts.push("g=" + (n.gs || KIND_GROUPS_N.map((g) => g.tag)).map((g) => String(g).replace(/^kg_/, "")).join(","));
      parts.push("c=" + cl.slice(0, 40).map(cityTag).join(","));
    }
  } catch (e) { /* ignore */ }
  try {
    const m = JSON.parse(localStorage.kbFollow || "{}");
    const w = Object.keys(m).filter((k) => /^c[0-9a-z]+$/.test(k)), l = Object.keys(m).filter((k) => /^l\d+$/.test(k)).map((k) => k.slice(1));
    if (w.length) parts.push("w=" + w.slice(0, 40).join(","));
    if (l.length) parts.push("l=" + l.slice(0, 60).join(","));
  } catch (e) { /* ignore */ }
  return parts.length ? { kb: parts.join("|") } : {};
}
// ×©×•×œ×— ×œ×¡×¤×§ ××ª ×ž×” ×©× ×©×ž×¨ ×‘×“×¤×“×¤×Ÿ ×”×–×”, ××• ×ž×•×—×§ ××ª ×”×ª×’ ×›×©××™×Ÿ ×”×¨×©×ž×”
const pushTags = (done) => { const t = savedTags(); osTags(Object.keys(t).length ? t : { kb: null }, done); };
function FollowBtn({ tag, label, title }) {
  const [on, setOn] = useState(() => { try { return !!JSON.parse(localStorage.kbFollow || "{}")[tag]; } catch (e) { return false; } });
  const [st, setSt] = useState("");
  if (!PUSH_ON) return null;
  const toggle = () => {
    const n = !on; setOn(n); setSt("");
    try { const m = JSON.parse(localStorage.kbFollow || "{}"); if (n) m[tag] = label || "×ž×¢×§×‘"; else delete m[tag]; localStorage.kbFollow = JSON.stringify(m); } catch (e) {}
    pushTags((r) => setSt(n ? (r.ok ? (r.srv ? (r.srv.ok ? "âœ“ ×¢×•×§×‘ â€” ×¨×©×•× ××¦×œ ×¡×¤×§ ×”×”×ª×¨××•×ª" : "âœ— ×œ× × ×¨×©× ××¦×œ ×¡×¤×§ ×”×”×ª×¨××•×ª" + (r.srv.err ? ": " + r.srv.err : "")) : "âœ“ ×”×”×ª×¨××•×ª ×¤×¢×™×œ×•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”") : "âœ— " + r.why) : ""));
  };
  return <><button className="sharebtn" title={title || "×”×ª×¨××ª ×“×¤×“×¤×Ÿ ×›×©× ×¨×©× ×©×™× ×•×™ ×ž×”×•×ª×™ (×ž×¡×œ×•×œ, ×ª×—× ×•×ª, ×‘×™×˜×•×œ â€” ×œ× ×œ×•\u05f4×–)"}
    onClick={toggle}>{on ? "ðŸ”” ×¢×•×§×‘ âœ“" : "ðŸ”” " + (label || "×§×‘×œ ×”×ª×¨××•×ª")}</button>
    {st && <small style={{ color: st[0] === "âœ“" ? "#166534" : "#b91c1c", fontWeight: 700, maxWidth: 380, display: "inline-block", marginInlineStart: 6 }}>{st}</small>}</>;
}
// ×¢×¨×™ ×”×§×¦×” ×ž×”×™×¢×“ ("×ž×•×¦×-×¢×™×¨<->×™×¢×“-×¢×™×¨") â€” ×œ×ž×¢×§×‘ ×‘×¨×ž×ª ×¢×™×¨
const destCities = (dest) => {
  // ×”×¢×™×¨ = ×”×ž×§×˜×¢ ×”×¢×‘×¨×™ ×”××—×¨×•×Ÿ; ×¡×™×•×ž×•×ª ×˜×›× ×™×•×ª ("2#") ×ž×“×•×œ×’×•×ª â€” ×–×”×” ×œ×¦×“ ×”×©×•×œ×—
  const out = [];
  String(dest || "").split("<->").forEach((side) => {
    const parts = side.trim().split("-");
    for (let i = parts.length - 1; i >= 0; i--) {
      const p = parts[i].trim();
      if (p.length >= 2 && !p.includes("#") && /[×-×ª]/.test(p) && !/[0-9]/.test(p)) {
        if (!out.includes(p)) out.push(p);
        break;
      }
    }
  });
  return out.slice(0, 2);
};

// ×”×’×“×¨×ª ×ª×’×™× ×‘×‘×ª ××—×ª â€” ×”×¨×©××” ×ž×ª×‘×§×©×ª ×¨×§ ×× ××™×Ÿ; done(r) ×ž×§×‘×œ {ok, why, id}
const osTags = (map, done) => {
  const sup = pushSupport();
  if (!sup.ok) { if (done) done(sup); return; }
  window.OneSignalDeferred = window.OneSignalDeferred || [];
  window.OneSignalDeferred.push(async (OS) => {
    let r;
    try {
      r = await ensurePush(OS);
      // ×”×ª×’×™× × ×©×ž×¨×™× ×’× ×‘×œ×™ ×ž× ×•×™ ×¤×¢×™×œ â€” ×›×“×™ ×©×™×—×–×™×§×• ×›×©×”×”×¨×©××” ×ª××•×©×¨
      const add = {}, rem = [];
      for (const k in map) { if (map[k] == null) rem.push(k); else add[k] = map[k]; }
      if (Object.keys(add).length) OS.User.addTags(add);
      if (rem.length) OS.User.removeTags(rem);
    } catch (e) { r = { ok: false, why: "×©×’×™××” ×‘×”×¨×©×ž×”: " + (e && e.message ? e.message : e) }; }
    if (done) done(r);
    // ××™×ž×•×ª ×ž×•×œ ×”×©×¨×ª ×•×ª×™×§×•×Ÿ ×™×©×™×¨: ×ž×” ×©× ×©×ž×¨ ×‘×“×¤×“×¤×Ÿ ×”×–×” ×”×•× ×”××ž×ª
    if (r && r.id) { try { const srv = await syncServer(OS, savedTags()); if (done) done({ ...r, srv }); } catch (e) { /* ignore */ } }
  });
};
// ×©× ×¢×™×¨ ×›×¤×™ ×©×”×•× ×‘×§×‘×¦×™ ×”×§×•×•×™× â€” "×§×¨×™×™×ª ×ž×œ××›×™" ×©×”×•×§×œ×“ ×”×•×¤×š ×œ"×§×¨×™×ª ×ž×œ××›×™" ×©×œ
// ×”×¨×™×©×•×, ××—×¨×ª ×”×ª×’ ×œ× ×™×ª××™× ×œ×©×•× ×§×• ×•×”×ª×¨××” ×œ× ×ª×’×™×¢ (×©×œ×ž×” 07.09)
const normCity = (s) => String(s || "").trim().replace(/["'×³×´]/g, "").replace(/[-â€“]/g, " ").replace(/\s+/g, " ").replace(/×™×™/g, "×™").replace(/×•×•/g, "×•");
const canonCity = (name, all) => { const n = normCity(name); if (!n) return null; return (all || []).find((c) => c === name.trim()) || (all || []).find((c) => normCity(c) === n) || null; };
// ×§×‘×•×¦×•×ª ×¡×•×’×™-×©×™× ×•×™ ×œ×”×¨×©×ž×” (×‘×§×©×ª ×©×œ×ž×”) â€” ×”×ª×’ ×‘×¦×“ ×”×©×•×œ×— ×–×”×”
const KIND_GROUPS_N = [
  { tag: "kg_rem", label: "×‘×™×˜×•×œ×™ ×§×•×•×™×", kinds: ["removed"] },
  { tag: "kg_new", label: "×§×•×•×™× ×—×“×©×™×", kinds: ["new"] },
  { tag: "kg_route", label: "×ž×¡×œ×•×œ ×•×ª×—× ×•×ª", kinds: ["route", "redraw", "extend", "shorten", "terminal", "stops", "stops-add", "stops-del"] },
  { tag: "kg_ident", label: "×™×¢×“, ×ž×¡×¤×¨ ×•×ž×¤×¢×™×œ", kinds: ["dest", "renum", "renamed", "operator", "mode"] },
];
// ×‘×›×œ ×›× ×™×¡×” ×œ××ª×¨: ×”×”×¨×©×ž×•×ª ×©× ×©×ž×¨×• ×‘×“×¤×“×¤×Ÿ ×”×–×” × ×©×œ×—×•×ª ×©×•×‘ ×œ×¡×¤×§ (×©×œ×ž×” 07.09: ××¦×œ
// ×”×¡×¤×§ × ×ž×¦× ×ª×’ ×¢×™×¨ ××—×“ ×ž×ª×•×š ×—×ž×©). ×‘×œ×™ ×‘×§×©×ª ×”×¨×©××” â€” ×¨×§ ×× ×›×‘×¨ ××•×©×¨×”.
if (PUSH_ON) { try { const t0 = savedTags(); if (Object.keys(t0).length) readPushState(() => {}, t0); } catch (e) { /* ignore */ } }
// "×”×”×¨×©×ž×•×ª ×©×œ×™": ×›×œ ×ž×” ×©× ×¨×©× ×ž×”×“×¤×“×¤×Ÿ ×”×–×” (×¦'×™×¤×™× + ×”×ž×¨×›×–), ×¢× âœ– ×œ×”×¡×¨×”
function MyFollows({ bump, onChange }) {
  const read = () => {
    // ×¢×¨×™ ×”×ž×¨×›×– ×œ× ×›××Ÿ â€” ×”×Ÿ ×›×‘×¨ ×ž×•×¦×’×•×ª ×›×¦'×™×¤×™× ×ž×¢×œ (×”×•×¤×™×¢×• ×©×œ×•×© ×¤×¢×ž×™×; ×©×œ×ž×” 13.09).
    // × ×©××¨×• ×¨×§ ×ž×¢×§×‘×™× ×™×©× ×™× ×ž×›×¤×ª×•×¨ "×¢×§×•×‘" â€” ×›×“×™ ×©××¤×©×¨ ×™×”×™×” ×œ×”×¡×™×¨ ××•×ª×
    const out = [];
    try {
      const m = JSON.parse(localStorage.kbFollow || "{}");
      for (const t in m) out.push({ tag: t, label: typeof m[t] === "string" ? m[t] : (t[0] === "l" ? "×§×• (×ž×§×˜ " + t.slice(1) + ")" : "×ž×¢×§×‘ ×™×©×Ÿ") });
    } catch (e) {}
    return out;
  };
  const [items, setItems] = useState(read);
  useEffect(() => { setItems(read()); }, [bump]);
  const drop = (it) => {
    if (it.center) {
      try {
        const n = JSON.parse(localStorage.kbNotify || "{}");
        const cl = (n.cities || (n.city ? [n.city] : [])).filter((c) => c !== it.cityName);
        if (!cl.length) delete localStorage.kbNotify;
        else localStorage.kbNotify = JSON.stringify({ ...n, cities: cl, city: undefined });
      } catch (e) {}
    } else {
      try { const m = JSON.parse(localStorage.kbFollow || "{}"); delete m[it.tag]; localStorage.kbFollow = JSON.stringify(m); } catch (e) {}
    }
    pushTags();
    setItems(read());
    // ×”×ž×¨×›×– ×©×ž×¢×œ ×ž×—×–×™×§ ××ª ×¨×©×™×ž×ª ×”×¢×¨×™× ×‘×ž×¦×‘ ×ž×©×œ×• â€” ×‘×œ×™ ×¢×“×›×•×Ÿ, "×©×ž×™×¨×”" ×”×‘××” ×”×™×™×ª×”
    // ×ž×—×–×™×¨×” ××ª ×”×¢×™×¨ ×©×”×•×¡×¨×” ×›××Ÿ (×©×œ×ž×” 07.09: "×‘×ž×§×•× ×œ×”×•×¨×™×“ ×”×•× ×ž×—×–×™×¨ ××ª ×”×¢×™×¨")
    if (onChange) onChange();
  };
  if (!items.length) return null;
  return (
    <div style={{ borderTop: "1px solid #e2e8f0", paddingTop: 8 }}>
      <b>×”×”×¨×©×ž×•×ª ×”×¤×¢×™×œ×•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”:</b>
      <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginTop: 6 }}>
        {items.map((it) => (
          <span key={it.tag + (it.center ? "c" : "")} style={{ display: "inline-flex", alignItems: "center", gap: 6, border: "1px solid #e2e8f0", borderRadius: 999, padding: "3px 10px" }}>
            ðŸ”” {it.label}
            <button title="×”×¡×¨×ª ×”×”×¨×©×ž×” ×”×–×•" onClick={() => drop(it)}
              style={{ border: "none", background: "none", cursor: "pointer", color: "#b91c1c", fontWeight: 900 }}>âœ–</button>
          </span>
        ))}
      </div>
    </div>
  );
}
// ×”×¨×©×ž×” ×ž×“×£ ×”×§×• (×©×œ×ž×” 13.09): ×œ×•×—×¦×™× ×¢×œ ×¢×™×¨, ×‘×•×—×¨×™× ×¡×•×’×™ ×©×™× ×•×™ ×•×ª×“×™×¨×•×ª, ×•× ×¨×©×ž×™× â€”
// ×–×• ××•×ª×” ×”×¨×©×ž×” ×›×ž×• ×‘×ž×¨×›×– ×”×”×ª×¨××•×ª ×‘×¢×ž×•×“ ×”×¨××©×™: ×”×¢×™×¨ ×ž×¦×˜×¨×¤×ª ×œ×¨×©×™×ž×ª ×”×¢×¨×™× ×©×,
// ×•×”×ª×“×™×¨×•×ª ×•×¡×•×’×™ ×”×©×™× ×•×™ ×ž×©×•×ª×¤×™× ×œ×›×œ ×”×¢×¨×™×.
function LineFollow({ cities }) {
  const readN = () => { try { const n = JSON.parse(localStorage.kbNotify || "{}"); if (n.city && !n.cities) n.cities = [n.city]; return n; } catch (e) { return {}; } };
  const [openC, setOpenC] = useState(null);
  const [gs, setGs] = useState(() => new Set(readN().gs || KIND_GROUPS_N.map((g) => g.tag)));
  const [freq, setFreq] = useState(() => readN().freq || "1");
  const [st, setSt] = useState("");
  const [, setBump] = useState(0);
  if (!PUSH_ON || !cities.length) return null;
  const isOn = (c) => (readN().cities || []).includes(c);
  const save = (c) => {
    if (!gs.size) { setSt("âœ— ×¡×ž× ×• ×œ×¤×—×•×ª ×¡×•×’ ×©×™× ×•×™ ××—×“"); return; }
    try { const n = readN(); const cl = n.cities || []; if (!cl.includes(c)) cl.push(c); localStorage.kbNotify = JSON.stringify({ cities: cl, freq, gs: [...gs] }); } catch (e) { /* ignore */ }
    setSt("×©×•×ž×¨â€¦"); setOpenC(null);
    pushTags((r) => { setSt(r.ok ? (r.srv ? (r.srv.ok ? "âœ“ × ×¨×©×ž×ª× ×‘×”×¦×œ×—×”" : "âœ— ×”×”×¨×©×ž×” ×œ× × ×§×œ×˜×” ×‘×©×¨×ª ×”×”×ª×¨××•×ª â€” × ×¡×• ×©×•×‘") : "âœ“ × ×¨×©×ž×ª× â€” ×”×”×ª×¨××•×ª ×¤×¢×™×œ×•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”") : "âœ— " + r.why); setBump((b) => b + 1); });
  };
  const remove = (c) => {
    try { const n = readN(); const cl = (n.cities || []).filter((x) => x !== c); if (cl.length) localStorage.kbNotify = JSON.stringify({ ...n, cities: cl, city: undefined }); else delete localStorage.kbNotify; } catch (e) { /* ignore */ }
    pushTags(); setSt("×”×”×¨×©×ž×” ×œ" + c + " ×”×•×¡×¨×”"); setBump((b) => b + 1);
  };
  const pill = { display: "flex", alignItems: "center", gap: 5, border: "1px solid #e2e8f0", borderRadius: 999, padding: "4px 10px", fontWeight: 400 };
  return <>
    {cities.map((c) => isOn(c)
      ? <button key={c} className="sharebtn" title={"×¨×©×•×ž×™× ×œ×”×ª×¨××•×ª ×¢×œ ×§×•×•×™ " + c + " â€” ×œ×—×™×¦×” ×ž×¡×™×¨×” ××ª ×”×¢×™×¨ ×ž×”×”×¨×©×ž×”"} onClick={() => remove(c)}>ðŸ”” ×¨×©×•×ž×™×: {c} âœ“</button>
      : <button key={c} className="sharebtn" title={"×”×¨×©×ž×” ×œ×”×ª×¨××•×ª ×¢×œ ×›×œ ×©×™× ×•×™ ×ž×”×•×ª×™ ×‘×§×•×•×™× ×©×œ " + c} aria-expanded={openC === c} onClick={() => setOpenC(openC === c ? null : c)}>ðŸ”” ×”×¨×©×ž×”: {c}</button>)}
    {st && <small style={{ color: st[0] === "âœ“" ? "#166534" : "#b91c1c", fontWeight: 700, display: "inline-block", marginInlineStart: 6 }}>{st}</small>}
    {openC && (
      <div style={{ width: "100%", border: "1px solid #e2e8f0", borderRadius: 10, padding: "8px 12px", marginTop: 6, display: "flex", flexDirection: "column", gap: 8 }}>
        <b>×”×¨×©×ž×” ×œ×”×ª×¨××•×ª ×¢×œ ×§×•×•×™ {openC}</b>
        <div style={{ fontWeight: 700 }}>××™×œ×• ×©×™× ×•×™×™×:
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 4 }}>
            {KIND_GROUPS_N.map((g) => (
              <label key={g.tag} style={pill}><input type="checkbox" checked={gs.has(g.tag)} onChange={() => setGs((p) => { const n = new Set(p); if (n.has(g.tag)) n.delete(g.tag); else n.add(g.tag); return n; })} /> {g.label}</label>
            ))}
          </div>
        </div>
        <div style={{ fontWeight: 700 }}>×‘××™×–×• ×ª×“×™×¨×•×ª:
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 4 }}>
            {[["1", "×›×œ ×™×•× ×©×™×© ×©×™× ×•×™"], ["3", "×¡×™×›×•× ×›×œ 3 ×™×ž×™×"], ["7", "×¡×™×›×•× ×©×‘×•×¢×™"]].map(([v, l]) => (
              <label key={v} style={pill}><input type="radio" name="lffreq" checked={freq === v} onChange={() => setFreq(v)} /> {l}</label>
            ))}
          </div>
        </div>
        <div className="mut">×¡×•×’×™ ×”×©×™× ×•×™ ×•×”×ª×“×™×¨×•×ª ×ž×©×•×ª×¤×™× ×œ×›×œ ×”×¢×¨×™× ×©× ×¨×©×ž×ª× ××œ×™×”×Ÿ â€” ××¤×©×¨ ×œ×©× ×•×ª ×‘×ž×¨×›×– ×”×”×ª×¨××•×ª ×‘×¢×ž×•×“ ×”×¨××©×™.</div>
        <div style={{ display: "flex", gap: 8 }}>
          <button className="kathead" style={{ width: "auto", padding: "6px 14px" }} onClick={() => save(openC)}>×©×ž×™×¨×” ×•×”×¨×©×ž×”</button>
          <button className="sharebtn" onClick={() => setOpenC(null)}>×‘×™×˜×•×œ</button>
        </div>
      </div>
    )}
  </>;
}
function NotifyCenter({ cities: allCities }) {
  const st0 = (() => { try { const s = JSON.parse(localStorage.kbNotify || "{}"); if (s.city && !s.cities) s.cities = [s.city]; return s; } catch (e) { return {}; } })();
  const [open, setOpen] = useState(false);
  const [city, setCity] = useState("");
  const [cities, setCities] = useState(st0.cities || []);
  const [freq, setFreq] = useState(st0.freq || "1");
  const [gs, setGs] = useState(() => new Set(st0.gs || KIND_GROUPS_N.map((g) => g.tag)));
  const [saved, setSaved] = useState(!!(st0.cities || []).length);
  const [msg, setMsg] = useState("");
  const [ps, setPs] = useState({ loading: true });
  useEffect(() => { if (open && PUSH_ON) { setPs({ loading: true }); readPushState((s) => setPs((p) => (typeof s === "function" ? s(p) : s)), savedTags()); } }, [open]);
  if (!PUSH_ON) return null;
  const toggleG = (t) => setGs((p) => { const n = new Set(p); if (n.has(t)) n.delete(t); else n.add(t); return n; });
  // ×¨×§ ×©×ž×•×ª ×¢×¨×™× ×›×¤×™ ×©×”× ×‘×¨×™×©×•× (××—×¨×ª ×”×ª×’ ×œ× ×™×ª××™× ×œ×©×•× ×§×•)
  const pick = (raw) => {
    const c = raw.trim(); if (!c) return null;
    const canon = canonCity(c, allCities);
    if (!canon) { setMsg(`"${c}" ×œ× × ×ž×¦××” ×‘×™×Ÿ ×¢×¨×™ ×”×§×¦×” ×©×œ ×”×§×•×•×™× â€” ×‘×—×¨×• ×©× ×ž×”×¨×©×™×ž×” ×©× ×¤×ª×—×ª ×‘×”×§×œ×“×”`); return null; }
    return canon;
  };
  const addCity = () => { const c = pick(city); if (!c) return; if (!cities.includes(c)) setCities([...cities, c]); setCity(""); setMsg(""); };
  const save = () => {
    const list = [...cities];
    if (city.trim()) { const c = pick(city); if (!c) return; if (!list.includes(c)) list.push(c); }   // ×ž×” ×©×”×•×§×œ×“ ×•×œ× × ×œ×—×¥ "×”×•×¡×£"
    if (!list.length) { setMsg("×”×•×¡×™×¤×• ×œ×¤×—×•×ª ×¢×™×¨ ××—×ª"); return; }
    if (!gs.size) { setMsg("×¡×ž× ×• ×œ×¤×—×•×ª ×¡×•×’ ×©×™× ×•×™ ××—×“"); return; }
    setMsg("×©×•×ž×¨â€¦");
    try { localStorage.kbNotify = JSON.stringify({ cities: list, freq, gs: [...gs] }); } catch (e) {}
    pushTags((r) => {
      setPs({ loading: false, ok: !!r.ok, why: r.why || "", id: r.id, srv: r.srv });
      setMsg(r.ok ? "âœ“ × ×¨×©×ž×ª× ×‘×”×¦×œ×—×”. ×”×”×ª×¨××•×ª ×¤×¢×™×œ×•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”" : `âœ— × ×©×ž×¨, ××‘×œ ×”×”×ª×¨××•×ª ×œ× ×™×’×™×¢×•: ${r.why}`);
    });
    setCities(list); setCity(""); setSaved(true);
  };
  // ×§×¨×™××” ×ž×—×“×© ×©×œ ×ž×” ×©×©×ž×•×¨ ×‘×“×¤×“×¤×Ÿ â€” ××—×¨×™ ×”×¡×¨×” ×ž×¨×©×™×ž×ª "×”×”×¨×©×ž×•×ª ×”×¤×¢×™×œ×•×ª" ×©×ž×ª×—×ª
  const reload = () => {
    try {
      const s = JSON.parse(localStorage.kbNotify || "{}");
      const cl = s.cities || (s.city ? [s.city] : []);
      setCities(cl); setSaved(!!cl.length); setCity("");
      if (s.freq) setFreq(s.freq);
      if (s.gs) setGs(new Set(s.gs));
    } catch (e) { setCities([]); setSaved(false); }
  };
  // "×”×¤×¢×œ×ª ×”×ª×¨××•×ª" ×©×•×œ×— ×’× ××ª ×›×œ ×ž×” ×©× ×©×ž×¨ ×‘×“×¤×“×¤×Ÿ ×”×–×” â€” ×œ× ×¨×§ ×ž×‘×§×© ×”×¨×©××”
  const enable = () => { setMsg("â€¦"); pushTags((r) => { setPs({ loading: false, ok: !!r.ok, why: r.why || "", id: r.id, srv: r.srv }); setMsg(r.ok ? "âœ“ ×”×”×ª×¨××•×ª ×¤×¢×™×œ×•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”" : "âœ— " + r.why); }); };
  // ×ž×¦×‘ ×”×¡× ×›×¨×•×Ÿ ×ž×•×œ ×©×¨×ª ×”×”×ª×¨××•×ª â€” ×‘×œ×™ ×œ×—×–×•×¨ ×¢×œ ×©×ž×•×ª ×”×¢×¨×™× (×”×Ÿ ×›×‘×¨ ×‘×¦'×™×¤×™×; ×©×œ×ž×” 13.09)
  const srvText = (s) => {
    if (s.err) return "×œ× ×”×¦×œ×—×ª×™ ×œ×‘×“×•×§ ×ž×•×œ ×©×¨×ª ×”×”×ª×¨××•×ª: " + s.err;
    if (s.empty) return "××™×Ÿ ×¢×“×™×™×Ÿ ×”×¨×©×ž×” ×‘×“×¤×“×¤×Ÿ ×”×–×”";
    if (!s.ok) return "âœ— ×”×”×¨×©×ž×” ×œ× × ×§×œ×˜×” ×‘×©×¨×ª ×”×”×ª×¨××•×ª â€” × ×¡×• ×œ×©×ž×•×¨ ×©×•×‘";
    return s.fixed ? "âœ“ ×”×”×¨×©×ž×” ×¡×•× ×›×¨× ×” ×¢×›×©×™×• ×¢× ×©×¨×ª ×”×”×ª×¨××•×ª" : "âœ“ ×”×”×¨×©×ž×” ×¨×©×•×ž×” ×‘×©×¨×ª ×”×”×ª×¨××•×ª";
  };
  const cancel = () => {
    try { delete localStorage.kbNotify; } catch (e) {}
    pushTags();
    setCities([]); setSaved(false); setMsg("×”×”×¨×©×ž×” ×‘×•×˜×œ×”");
  };
  return (
    <div className="katbox" style={{ marginTop: 8 }}>
      <button className="kathead" style={{ fontWeight: 800 }} aria-expanded={open} onClick={() => setOpen(!open)}>
        ðŸ”” ×”×¨×©×ž×” ×œ×”×ª×¨××•×ª ×¢×œ ×©×™× ×•×™×™×{saved ? (() => { const cl = (st0.cities || [st0.city]).filter(Boolean); return cl.length > 2 ? ` â€” ×¨×©×•×ž×™× ×œ-${cl.length} ×¢×¨×™×` : ` â€” ×¨×©×•×ž×™× ×œ${cl.join(" ×•")}`; })() : " â€” ×¢×™×¨, ×¡×•×’×™ ×©×™× ×•×™×™× ×•×ª×“×™×¨×•×ª"}
      </button>
      {open && (
        <div style={{ padding: "10px 12px", display: "flex", flexDirection: "column", gap: 10 }}>
          {/* ×ž×¦×‘ ×”×”×ª×¨××•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×” â€” ×‘×œ×™ ×–×” ×”×”×¨×©×ž×” × ×©×ž×¨×ª ××‘×œ ×©×•× ×”×•×“×¢×” ×œ× ×ž×’×™×¢×” */}
          <div style={{ border: "1px solid " + (ps.loading ? "#e2e8f0" : ps.ok ? "#bbf7d0" : "#fecaca"), background: ps.loading ? "#f8fafc" : ps.ok ? "#f0fdf4" : "#fef2f2", borderRadius: 10, padding: "8px 12px", fontWeight: 700, display: "flex", gap: 10, alignItems: "center", flexWrap: "wrap" }}>
            {ps.loading ? "×‘×•×“×§ ××ª ×ž×¦×‘ ×”×”×ª×¨××•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”â€¦" : ps.ok ? "âœ“ ×”×”×ª×¨××•×ª ×¤×¢×™×œ×•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”" : "âœ— ×”×”×ª×¨××•×ª ×œ× ×¤×¢×™×œ×•×ª ×‘×“×¤×“×¤×Ÿ ×”×–×”: " + ps.why}
            {!ps.loading && !ps.ok && !/××™×™×¤×•×Ÿ|×œ× ×ª×•×ž×š/.test(ps.why || "") && <button className="kathead" style={{ width: "auto", padding: "5px 12px" }} onClick={enable}>×”×¤×¢×œ×ª ×”×ª×¨××•×ª</button>}
            {!ps.loading && ps.ok && !ps.srv && <small style={{ width: "100%", fontWeight: 400, color: "var(--muted, #64748b)" }}>×‘×•×“×§ ×ž×•×œ ×”×©×¨×ª ×ž×” ×¨×©×•× ×©×â€¦</small>}
            {!ps.loading && ps.srv && <div style={{ width: "100%", fontWeight: 600, fontSize: 13, color: ps.srv.ok ? "#166534" : "#b91c1c" }}>{srvText(ps.srv)}</div>}
          </div>
          <label style={{ fontWeight: 700 }}>×¢×¨×™× (××¤×©×¨ ×›×ž×”):
            <div style={{ display: "flex", gap: 6, marginTop: 4 }}>
              <input list="kbcities" dir="rtl" value={city} onChange={(e) => setCity(e.target.value)}
                onKeyDown={(e) => { if (e.key === "Enter") { e.preventDefault(); addCity(); } }}
                placeholder="×”×§×œ×™×“×• ×©× ×¢×™×¨â€¦" style={{ flex: 1, padding: "8px 10px", borderRadius: 8, border: "1px solid #cbd5e1", font: "inherit" }} />
              <button className="kathead" style={{ width: "auto", padding: "6px 14px" }} onClick={addCity}>+ ×”×•×¡×£</button>
            </div>
            <datalist id="kbcities">{(allCities || []).map((c) => <option key={c} value={c} />)}</datalist>
            {cities.length > 0 && (
              <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginTop: 6, fontWeight: 400 }}>
                {cities.map((ct) => (
                  <span key={ct} style={{ display: "inline-flex", alignItems: "center", gap: 5, background: "#ede9fe", borderRadius: 999, padding: "3px 10px" }}>
                    {ct} <button onClick={() => setCities(cities.filter((x) => x !== ct))}
                      style={{ border: "none", background: "none", cursor: "pointer", color: "#b91c1c", fontWeight: 900 }}>âœ–</button>
                  </span>
                ))}
              </div>
            )}
          </label>
          <div style={{ fontWeight: 700 }}>××™×œ×• ×©×™× ×•×™×™× ×ž×¢× ×™×™× ×™× ××ª×›×:
            <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 6, fontWeight: 400 }}>
              {KIND_GROUPS_N.map((g) => (
                <label key={g.tag} style={{ display: "flex", alignItems: "center", gap: 5, border: "1px solid #e2e8f0", borderRadius: 999, padding: "4px 10px" }}>
                  <input type="checkbox" checked={gs.has(g.tag)} onChange={() => toggleG(g.tag)} /> {g.label}
                </label>
              ))}
            </div>
            <div className="mut" style={{ fontWeight: 400 }}>×©×™× ×•×™×™ ×œ×•×´×– ×•×ª×“×™×¨×•×ª ××™× × × ×©×œ×—×™× â€” ×¨×§ ×©×™× ×•×™×™× ×ž×”×•×ª×™×™×.</div>
          </div>
          <div style={{ fontWeight: 700 }}>×‘××™×–×• ×ª×“×™×¨×•×ª:
            <div style={{ display: "flex", gap: 8, marginTop: 6, flexWrap: "wrap", fontWeight: 400 }}>
              {[["1", "×›×œ ×™×•× ×©×™×© ×©×™× ×•×™"], ["3", "×¡×™×›×•× ×›×œ 3 ×™×ž×™×"], ["7", "×¡×™×›×•× ×©×‘×•×¢×™"]].map(([v, l]) => (
                <label key={v} style={{ display: "flex", alignItems: "center", gap: 5, border: "1px solid #e2e8f0", borderRadius: 999, padding: "4px 10px" }}>
                  <input type="radio" name="kbfreq" checked={freq === v} onChange={() => setFreq(v)} /> {l}
                </label>
              ))}
            </div>
            <div className="mut" style={{ fontWeight: 400 }}>×‘×¡×™×›×•×, ×”×”×ª×¨××” × ×¤×ª×—×ª ×œ×¢×ž×•×“ ×¢× ×›×œ ×”×©×™× ×•×™×™× ×‘×ª×§×•×¤×” â€” ×ž×§×•×‘×¦×™× ×œ×¤×™ ×¡×•×’.</div>
          </div>
          <div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}>
            <button className="kathead" style={{ background: "#4c1d95", color: "#fff", borderRadius: 10, width: "auto", padding: "8px 18px" }} onClick={save}>×©×ž×™×¨×” ×•×”×¨×©×ž×”</button>
            {saved && <button className="kathead" style={{ width: "auto", padding: "8px 14px" }} onClick={cancel}>×‘×™×˜×•×œ ×”×”×¨×©×ž×”</button>}
            {msg && <span style={{ fontWeight: 700 }}>{msg}</span>}
          </div>
          <MyFollows bump={msg} onChange={reload} />
        </div>
      )}
    </div>
  );
}
// ×¢×ž×•×“ ×”×¡×™×›×•× ×©× ×¤×ª×— ×ž×”×ª×¨××ª ×¡×™×›×•×: ×›×œ ×”×©×™× ×•×™×™× ×‘×ª×§×•×¤×”, ×ž×§×•×‘×¦×™× ×œ×¤×™ ×¡×•×’ â€”
// ×§×•×•×™× ×¢× ××•×ª×• ×©×™× ×•×™ ×‘××•×ª×” ×©×•×¨×”, ×›×œ ××—×“ ×‘×§×™×©×•×¨ ×ž×©×œ×• (×‘×§×©×ª ×©×œ×ž×”)
function DigestPage({ city, days, onBack, openLine }) {
  const [data, setData] = useState(null);
  useEffect(() => {
    fetch("data/digest.json", { cache: "no-cache" }).then((r) => r.json()).then(setData)
      .catch(() => setData({ items: [] }));
  }, []);
  const since = new Date(Date.now() - days * 864e5).toISOString().slice(0, 10);
  const cityList = String(city || "").split(",").map((c) => c.trim()).filter(Boolean);
  const items = ((data && data.items) || []).filter((x) => x.d >= since && (x.ct || []).some((c) => cityList.includes(c)));
  const byKind = {};
  items.forEach((x) => { (byKind[x.k] = byKind[x.k] || []).push(x); });
  return (
    <div className="card">
      <button className="back" onClick={onBack}>â†’ ×œ×—×™×¤×•×©</button>
      <h2 style={{ margin: "6px 0" }}>ðŸ”” ×¡×™×›×•× ×”×©×™× ×•×™×™× ×‘×§×•×•×™ {String(city || "").split(",").join(", ")} â€” {days === 7 ? "×”×©×‘×•×¢ ×”××—×¨×•×Ÿ" : `${days} ×”×™×ž×™× ×”××—×¨×•× ×™×`}</h2>
      {!data ? <div>×˜×•×¢×Ÿâ€¦</div> : !items.length ? <div className="mut">××™×Ÿ ×©×™× ×•×™×™× ×ž×”×•×ª×™×™× ×‘×ª×§×•×¤×” ×”×–×•.</div> : (
        Object.keys(byKind).map((k) => {
          const seen = new Set();
          const rows = byKind[k].filter((x) => !seen.has(x.mk) && seen.add(x.mk));
          return (
            <div key={k} style={{ margin: "10px 0", padding: "8px 12px", border: "1px solid #e2e8f0", borderRadius: 10 }}>
              <b style={{ color: (KINDS[k] || {}).color || "#0f172a" }}>{(KINDS[k] || {}).label || k}</b> Â· {rows.length} ×§×•×•×™×:{" "}
              {rows.map((x, i) => (
                <React.Fragment key={x.rd + x.d}>
                  {i > 0 && " Â· "}
                  <a href={"#" + encodeURIComponent(x.rd) + "@" + x.d} onClick={(e) => { e.preventDefault(); openLine(x.rd, x.d); }}
                    style={{ fontWeight: 700 }}>×§×• {x.line || x.mk}</a>
                </React.Fragment>
              ))}
            </div>
          );
        })
      )}
      <div className="mut">×©×™× ×•×™×™ ×œ×•×´×– ×•×ª×“×™×¨×•×ª ××™× × × ×›×œ×œ×™× ×‘×¡×™×›×•×.</div>
    </div>
  );
}

function LinePage({ rd, lineGone, sibs, onSwitch, onBack, initDate, initCats }) {
  // "Â· ×¨×¦×™×£ N" ×œ×™×“ ×ª×—× ×” ×‘×ž×¡×•×£ â€” ×”×¨×¦×™×£ ×©×œ ×”×•×•×¨×™×× ×˜ ×”×–×” ×œ×¤×™ ×©×•×¨×ª ×”×¨×¦×™×£ ×‘×§×•×‘×¥
  // ×”×ª×—× ×•×ª (platforms.json rd), ×ž×ª×¢×“×›×Ÿ ×ž×“×™ ×™×•×
  const plats = usePlatforms();
  const rdPlat = (plats && plats.rd && plats.rd[rd]) || {};
  const withPlat = (str, c) => (c != null && rdPlat[String(c)]) ? `${str} Â· ×¨×¦×™×£ ${rdPlat[String(c)]}` : str;
  const [lf, setLf] = useState(null);
  const [err, setErr] = useState(null);
  const [sel, setSel] = useState(null);   // ××™× ×“×§×¡ ×’×¨×¡×” × ×‘×—×¨×ª
  const [mon, setMon] = useState("");
  const [offK, setOffK] = useState(() => new Set());   // ×§×˜×’×•×¨×™×•×ª ×©×›×•×‘×• ×‘×¢×ž×•×“ ×”×§×•
  const [cmpI, setCmpI] = useState(null);              // ×’×¨×¡×ª ×‘×¡×™×¡ ×œ×”×©×•×•××” ×—×•×¤×©×™×ª
  const [onlyCur, setOnlyCur] = useState(false);       // ×ž×¤×” ×‘×œ×™ ×©×›×‘×ª ×”×¢×‘×¨
  const [only12, setOnly12] = useState(true);          // ×—×œ×•× ×™×ª 2012 ×¤×ª×•×—×”: ×‘×ž×¤×” ×¨×§ ×ž×¡×œ×•×œ 2012 (×©×œ×ž×” 05.09), ××• ×™×—×“ ×¢× ×”×™×•×
  const [show12, setShow12] = useState(false);
  const [altRd, setAltRd] = useState(null);   // ×—×œ×•×¤×” ×©× ×‘×—×¨×” ×œ×”×©×•×•××”
  const [d12, setD12] = useState(null);   // ×§×•×‘×¥ ×”×§×• ×©×œ 2012 (× ×˜×¢×Ÿ ×‘×¤×ª×™×—×”)
  const [r12, setR12] = useState(0);      // ×•×¨×™×× ×˜ 2012 × ×‘×—×¨
  const [s12, setS12] = useState(null);   // ×ž×¡×œ×•×œ×™× ×ž×©×•×¢×¨×™× ×©×œ 2012 ×¢×œ ×”×›×‘×™×©×™× (×× ×—×•×©×‘×•)
  const [rty, setRty] = useState(0);      // ×ž×•× ×” "× ×¡×• ×©×•×‘" ××—×¨×™ ×›×©×œ ×¨×©×ª
  // ×”×¢×•×’×Ÿ ×©×œ 2012 ×ž×•×˜×ž×¢ ×‘×§×•×‘×¥ ×”×§×• ×¢×¦×ž×• (lf.anc, ×ž×•×–×¨×§ ×‘×¦×™× ×•×¨ ×”×œ×™×œ×™) â€”
  // ×‘×¢×‘×¨ ×›×œ ×¤×ª×™×—×ª ×¢×ž×•×“ ×§×• ×”×•×¨×™×“×” ××ª ×§×•×‘×¥ ×”×¢×•×’× ×™× ×”×ž×œ× (1.2MB) ×¨×§ ×›×“×™
  // ×œ×©×œ×•×£ ×©×•×¨×” ××—×ª, ×›×•×œ×œ ×‘×§×•×•×™ ×¨×›×‘×ª ×•×ž×•× ×™×•×ª ×©××™×Ÿ ×œ×”× ×¢×•×’×Ÿ ×‘×›×œ×œ.
  const anc = (lf && lf.anc) || null;
  useEffect(() => {
    setShow12(false); setD12(null); setR12(0); setS12(null); setAltRd(null);
  }, [rd]);
  // ×”×ž×¡×œ×•×œ ×”×ž×©×•×¢×¨ ×¢×œ ×”×›×‘×™×©×™× × ×©×ž×¨ ×‘×§×•×‘×¥ × ×¤×¨×“ ×œ×›×œ ×§×• 2012 â€” ×§×™×™× ×¨×§ ×œ×ž×” ×©×—×•×©×‘
  useEffect(() => {
    if (!show12 || s12 || !anc) return;
    dfetch("../magihim-2012/data/shapes/l" + anc.k + ".json")
      .then((r) => (r.ok ? r.json() : { routes: {} }))
      .then(setS12).catch(() => setS12({ routes: {} }));
  }, [show12, anc, s12]);
  useEffect(() => {
    if (!show12 || d12 || !anc) return;
    dfetch("../magihim-2012/data/l" + anc.k + ".json")
      .then((r) => r.json())
      .then((d) => {
        // ×‘×—×™×¨×ª ×•×¨×™×× ×˜ 2012 ×©×ª×•×× ××ª ×›×™×•×•×Ÿ ×”×•×•×¨×™×× ×˜ ×”×¤×ª×•×— â€” ×œ×¤×™ ×“×ž×™×•×Ÿ
        // ×ª×—× ×•×ª ×”×§×¦×” (×•×œ× ×¡×ª× ×”×•×•×¨×™×× ×˜ ×”×¨××©×•×Ÿ ×‘×§×•×‘×¥)
        const routes = d.routes || [];
        const cur = ((lf || {}).versions || []).slice(-1)[0] || {};
        const st = cur.stops || [];
        let best = 0;
        if (st.length && routes.length > 1) {
          const tok = (x) => new Set(String(x || "").split(/[^×-×ª0-9]+/).filter((w) => w.length >= 3));
          const f0 = tok(st[0][1]), l0 = tok(st[st.length - 1][1]);
          let bs = -1;
          routes.forEach((r, i) => {
            const sc = [...tok(r.f)].filter((w) => f0.has(w)).length * 2 +
              [...tok(r.l)].filter((w) => l0.has(w)).length * 2 + (r.n || 0) / 1000;
            if (sc > bs) { bs = sc; best = i; }
          });
        }
        setR12(best);
        setD12(d);
      })
      .catch(() => setD12({ routes: [] }));
  }, [show12, anc, d12, lf]);
  useEffect(() => {
    // ×ž×¢×‘×¨ ×ž×”×™×¨ ×‘×™×Ÿ ×§×•×•×™× ×ž×™×™×¦×¨ ×©×ª×™ ×‘×§×©×•×ª, ×•×”×ª×©×•×‘×” ×”××™×˜×™×ª ×™×•×ª×¨ ×¢×œ×•×œ×”
    // ×œ× ×—×•×ª ××—×¨×•× ×” ×•×œ×¢×¨×‘×‘ ×§×• ××—×“ ×¢× ×ž×¦×‘ ×©×œ ××—×¨. ××•×ª×• ×©×ž×™×¨×” ×›×ž×• ×‘×”×©×•×•××ª
    // ×”×—×œ×•×¤×•×ª.
    let ok = true;
    setLf(null); setErr(null); setSel(null); setMon(""); setOffK(new Set()); setCmpI(null); setOnlyCur(false);
    dfetch("data/lines/" + fsafe(rd) + ".json")
      .then((r) => { if (!r.ok) throw new Error(r.status); return r.json(); })
      .then((d) => { if (!ok) return; const m = materializeLf(d); setLf(m);
        // ×§×™×©×•×¨ ×ž×¦×™×¨ ×ª×—× ×” ×ž×’×™×¢ ×¢× ×ª××¨×™×š (â€Ž#×ž×§×˜@×ª××¨×™×šâ€Ž) â€” × ×•×—×ª×™× ×™×©×¨ ×¢×œ
        // ×”×’×¨×¡×” ×©×œ ××•×ª×• ×©×™× ×•×™, ×œ× ×¢×œ ×”×’×¨×¡×” ×”××—×¨×•× ×”
        let s = d.versions.length - 1;
        if (initDate) {
          const i = d.versions.findIndex((v) => v.d === initDate);
          if (i >= 0) s = i;
        }
        setSel(s);
        // × ×›× ×¡×• ×ž×ª×•×š ×—×™×¤×•×© ×œ×¤×™ ×§×˜×’×•×¨×™×”: ×”×¡×¨×’×œ × ×“×œ×§ ×¨×§ ×¢×œ ×¡×•×’×™ ×”××™×¨×•×¢×™×
        // ×©×ž×ª××™×ž×™× ×œ×” (×©×œ×ž×” 06.09). ×”×¡×™× ×•×Ÿ ×”×•× ×œ×¤×™ ×¡×•×’ ×”××™×¨×•×¢, ×›×ž×• ×‘×¡×¨×’×œ â€”
        // ×¡×•×’ ×©×™×© ×‘×• ×œ×¤×—×•×ª ××™×¨×•×¢ ××—×“ ×ž×ª××™× × ×©××¨ ×“×œ×•×§. ×× ×©×•× ××™×¨×•×¢ ×œ×
        // ×ž×ª××™× â€” ×ž×¦×™×’×™× ×”×›×•×œ, ×œ× ×¢×ž×•×“ ×¨×™×§.
        const cats = String(initCats || "").split(",").filter(Boolean);
        if (cats.length) {
          const vs2 = m.versions || [], keep = new Set(), all = new Set();
          vs2.forEach((x, i) => { if (x.hid) return; const dk = dispKind(x, i, vs2); all.add(dk); if (cats.some((c) => evInCat(x, i, vs2, c))) keep.add(dk); });
          if (keep.size && keep.size < all.size) setOffK(new Set([...all].filter((k) => !keep.has(k))));
        } })
      .catch((e) => { if (ok) setErr(e); });
    return () => { ok = false; };
  }, [rd, rty, initDate, initCats]);
  // ×”×•×“×¢×ª ×©×’×™××” ××—×ª ×œ×©× ×™ ×ž×¦×‘×™× ×©×•× ×™× ×”×˜×¢×ª×”: ×›×©×œ ×¨×©×ª ×¨×’×¢×™ ×‘× ×™×™×“ ×”×•×¦×’
  // ×›"×œ× × ×ž×¦××• × ×ª×•× ×™×" ×•×”×’×•×œ×© ×”×¡×™×§ ×©××™×Ÿ ×ž×” ×œ×¨××•×ª. ×¡×˜×˜×•×¡ HTTP (404) ×”×•×
  // ×‘××ž×ª ×§×• ×©××™×Ÿ ×œ×• ×§×•×‘×¥; ×›×œ ×”×©××¨ â€” ×ª×§×œ×”, ×¢× ×›×¤×ª×•×¨ ×œ× ×¡×•×ª ×©×•×‘.
  if (err) return (
    <div className="card"><button className="back" onClick={onBack}>â†’ ×—×–×¨×”</button>
      {/^\d+$/.test(String(err.message)) ? <div className="empty">×œ× × ×ž×¦××• × ×ª×•× ×™× ×œ×•×¨×™×× ×˜ ×”×–×”.</div>
        : <div className="empty">ðŸ“¡ ×”× ×ª×•× ×™× ×œ× ×™×¨×“×• â€” ×›× ×¨××” ×ª×§×œ×ª ×¨×©×ª ×¨×’×¢×™×ª.{" "}
          <button className="morebtn" onClick={() => setRty((n) => n + 1)}>â†» × ×¡×• ×©×•×‘</button></div>}
    </div>);
  if (!lf) return <div className="card">×˜×•×¢×Ÿâ€¦</div>;
  const vs = lf.versions;
  // ×ž×¡×¤×¨ ×”× ×¡×™×¢×•×ª ×ž×’×™×¢ ×ž×”××™× ×“×§×¡ ×•×œ× ×ž×§×•×‘×¥ ×”×§×•: ×”×•× ×ž×©×ª× ×” ×ž×™×•× ×œ×™×•×, ×•×‘×§×•×‘×¥
  // ×”×§×• ×”×•× ×”×™×” ×ž×©× ×” ××ª ×›×œ 13,000 ×”×§×‘×¦×™× ×‘×›×œ ×¨×™×¦×” ×™×•×ž×™×ª
  const ntr = ((sibs || []).find((x) => x.rd === rd) || {}).ntr || 0;
  const months = [...new Set(vs.filter((v) => !v.hid).map((v) => v.d.slice(0, 7)))].reverse();
  const shown = vs.map((v, i) => ({ v, i }))
    .filter((x) => !x.v.hid && (!mon || x.v.d.slice(0, 7) === mon) && !offK.has(dispKind(x.v, x.i, vs))).reverse();
  // ×”×§×˜×’×•×¨×™×•×ª ×©×§×™×™×ž×•×ª ×‘×§×• ×”×–×” ×‘×¤×•×¢×œ, ×œ×¤×™ ×©×›×™×—×•×ª â€” ×¡×¨×’×œ ×›×™×‘×•×™/×”×“×œ×§×”.
  // ×©×œ×•×© ×§×‘×•×¦×•×ª ×ž××•×—×“×•×ª ×›××Ÿ ×•×œ× ×‘×ª×•×•×™×ª ×©×¢×œ ×”××™×¨×•×¢: ×‘×¡×¨×’×œ ×”×Ÿ ×©××œ×” ××—×ª
  // ("×œ×”×¦×™×’ ×©×™× ×•×™×™ ×ª×—× ×•×ª?") ×•××™×œ×• ×¢×œ ×”××™×¨×•×¢ ×¢×¦×ž×• ×”×”×‘×—× ×” ×›×Ÿ × ×•×©××ª ×ž×™×“×¢.
  const kindsHere = (() => {
    // ×”×ª×•×•×™×ª ×©×¢×œ ×”××™×¨×•×¢ × ×’×–×¨×ª ×ž-dispKind ×•×œ× ×ž-k ×”×’×•×œ×ž×™; ×¡×™× ×•×Ÿ ×œ×¤×™ k ×”×™×”
    // ×™×•×¦×¨ ××™-×”×ª××ž×” â€” ×›×¤×ª×•×¨ "×©×™× ×•×™ ×ž×¡×¤×¨ ×”×¨×›×‘×™×" ×©×ž×©××™×¨ ××™×¨×•×¢×™× ×ž×ª×•×™×’×™×
    // "×©×™× ×•×™ ×œ×•×´×–". ×”×¡×™× ×•×Ÿ ×•×”×ª×•×•×™×•×ª ×—×™×™×‘×™× ×œ×“×‘×¨ ×‘××•×ª×” ×©×¤×”.
    const c = new Map();
    vs.forEach((x, i) => {
      if (x.hid) return;
      const dk = dispKind(x, i, vs), g = KGROUP[dk] || dk;
      const e = c.get(g) || { n: 0, kinds: new Set() };
      e.n += 1; e.kinds.add(dk); c.set(g, e);
    });
    return [...c.entries()].sort((a, b) => b[1].n - a[1].n);
  })();
  const toggleK = (ks, wasOff) => setOffK((prev) => {
    const n = new Set(prev);
    ks.forEach((k) => (wasOff ? n.delete(k) : n.add(k)));
    return n;
  });
  // ×ž×¡' ×ª×—× ×” ×œ×¨×©×™×ž×•×ª âž•/âž–: ×‘×”×•×¡×¤×” ×ž×—×¤×©×™× ×‘×’×¨×¡×” ×¢×¦×ž×”, ×‘×”×•×¨×“×” ×‘×’×¨×¡××•×ª ×©×œ×¤× ×™×”
  const codeOf = (name, i, isAdd) => {
    const scan = (l) => { const h = (l || []).find((s) => s && s[1] === name); return h ? h[0] : null; };
    // ×”××™× ×“×§×¡ ×¢×œ×•×œ ×œ×”×¦×‘×™×¢ ×ž×—×•×¥ ×œ×ž×¢×¨×š ×›×©×ž×¢×‘×¨ ×‘×™×Ÿ ×§×•×•×™× ×ž×•×ª×™×¨ ×ž×¦×‘ ×ž×’×¨×¡×”
    // ×§×•×“×ž×ª. ×”×’×‘×•×œ ×”×ª×—×ª×•×Ÿ × ×©×ž×¨ ×›××Ÿ ×ž××– ×•×ž×ª×ž×™×“, ××‘×œ ×”×¢×œ×™×•×Ÿ ×œ× â€” ×•×‘×§×• ×©×§×™×‘×œ
    // ×ª×—× ×•×ª ×‘×’×¨×¡×” ×”××—×¨×•× ×” ×–×” ×”×¤×™×œ ××ª ×›×œ ×”×¢×ž×•×“.
    const top = Math.min(i, vs.length - 1);
    if (isAdd && vs[top]) { const c = scan(vs[top].stops); if (c) return c; }
    for (let j = top - (isAdd ? 0 : 1); j >= 0; j--) { const c = scan(vs[j].stops); if (c) return c; }
    for (let j = i + 1; j < vs.length; j++) { const c = scan(vs[j].stops); if (c) return c; }
    return null;
  };
  // ×¨×©×•×ž×ª âž– ×©×”×™× ×ž×§"×˜ ×—×©×•×£: ×”×¡×¨×™×§×” ×œ× ×ž×¦××” ×©× ×œ×ª×—× ×” ×©×™×¨×“×” (×”×ž×§"×˜ ×›×‘×¨
  // × ×ž×—×§ ×ž×”×¨×™×©×•× ×”××¨×¦×™) ×•×©×ž×¨×” ××ª ×”×ž×¡×¤×¨ ×¢×¦×ž×•. ×”×©× ×§×™×™× ×‘×ª×™×¢×•×“ ×”×™×©×Ÿ ×©×œ
  // ×”×§×• â€” ×ž××ª×¨×™× ××•×ª×• ×©×. ×ž×—×–×™×¨ ××ª ×¨×©×•×ž×ª ×”×ª×—× ×” ×”×ž×œ××” (×ž×§"×˜, ×©×, ×ž×™×§×•×)
  const stopByCode = (code, i) => {
    const top = Math.min(i, vs.length - 1);
    for (let j = top; j >= 0; j--) {
      const h = (vs[j].stops || []).find((s) => s && String(s[0]) === code);
      if (h) return h;
    }
    for (let j = top + 1; j < vs.length; j++) {
      const h = (vs[j].stops || []).find((s) => s && String(s[0]) === code);
      if (h) return h;
    }
    return null;
  };
  // âž– ×©× ×©××¨ ×‘×œ×™ ×ž×§"×˜: ×©× ×”×ª×—× ×” ×‘×¤×™×“ ×”×©×ª× ×” ×ž××– ×”×ª×™×¢×•×“ ×©×œ× ×• (×œ×ž×©×œ
  // "×“×•×“ ×¨×ž×–/×”×›×¨×" ×©×©×ž×•×¨ ××¦×œ× ×• ×‘×©×ž×• ×”×™×©×Ÿ "×ž×¡×•×£ ×¨×ž×–/×“×•×“ ×¨×ž×–"), ×•×œ×›×Ÿ ×—×™×¤×•×©
  // ×œ×¤×™ ×©× ×œ× ×ž×•×¦× ×›×œ×•×. ×ž×©×œ×™×ž×™× ×ž×•×œ ×”×”×¤×¨×© ×‘×¤×•×¢×œ ×‘×™×Ÿ ×”×’×¨×¡××•×ª: ×× ×‘×“×™×•×§
  // ×ž×§"×˜ ××—×“ ×™×¨×“ ×•×œ× × ×ª×‘×¢ ×¢×œ ×™×“×™ ×©× ××—×¨ ×‘×¨×©×™×ž×” â€” ×”×•×-×”×•× ×”×ª×—× ×” ×”×—×¡×¨×”
  const remFix = (i) => {
    const out = {};
    const top = Math.min(i, vs.length - 1);
    const v = vs[top];
    if (!v || !(v.stops || []).length || !(v.rem || []).length) return out;
    let prevS = null;
    for (let j = top - 1; j >= 0; j--) { if ((vs[j].stops || []).length) { prevS = vs[j].stops; break; } }
    if (!prevS) return out;
    const curC = new Set(v.stops.map((s) => String(s[0])));
    const claimed = new Set();
    const unresolved = [];
    for (const n of v.rem || []) {
      const e = v.nc && v.nc[n];
      const c = Array.isArray(e) ? String(e[0]) : (e || (/^\d{3,}$/.test(n) ? n : codeOf(n, i, false)));
      if (c) claimed.add(String(c)); else unresolved.push(n);
    }
    const free = prevS.filter((s) => !curC.has(String(s[0])) && !claimed.has(String(s[0])));
    if (unresolved.length === 1 && free.length === 1) out[unresolved[0]] = free[0];
    return out;
  };
  // v.nc â€” ×ž×§"×˜×™× ×©×”×¦× ×¨×ª ×¤×¢× ×—×” ×ž×¨××© ×œ×©×ž×•×ª ×©××™× × ×‘×§×•×‘×¥ (×ª×—× ×” ×©×™×¨×“×”
  // ×‘×’×¨×¡×” ×”×¨××©×•× ×” ×©×œ ×§×• ××¨×›×™×•× ×™, ×œ×ž×©×œ) â€” × ×‘×“×§ ×œ×¤× ×™ ×”×—×™×¤×•×© ×”×¨×’×™×œ.
  // ×”×¢×¨×š: ×ž×—×¨×•×–×ª ×ž×§"×˜, ××• [×ž×§"×˜, lat, lon] ×›×©×’× ×”×ž×™×§×•× ×™×“×•×¢ (×œ×ž×¤×”)
  const ncOf = (nv, name) => {
    const e = nv && nv.nc && nv.nc[name];
    return Array.isArray(e) ? e[0] : e;
  };
  // ×›×œ ×”×ž×§"×˜×™× ×©×©×™×™×›×™× ×œ×©× â€” ×‘×¡×“×¨ ×”×¡×¨×™×§×” ×©×œ codeOf. ×©× ×©×ž×•×¤×™×¢ ×¤×¢×ž×™×™×
  // ×‘×¨×©×™×ž×” ×”×•× ×©×ª×™ ×ª×—× ×•×ª ×©×•× ×•×ª (×©× ×™ ×¦×™×“×™ ×¨×—×•×‘), ×•×›×œ ×ž×•×¤×¢ ×¦×¨×™×š ××ª ×”×ž×¡×¤×¨
  // ×©×œ×• â€” ×œ× "Ã—2" ×¢× ×ž×§"×˜ ××—×“ ×œ×©×ª×™×”×Ÿ (×“×™×•×•×— ×©×œ×ž×”: ×™×©×¢×™×”×• ×”× ×‘×™×)
  const codesFor = (name, i, isAdd) => {
    const out = []; const seen = new Set();
    const take = (l) => (l || []).forEach((s) => {
      if (s && s[1] === name && !seen.has(String(s[0]))) { seen.add(String(s[0])); out.push(String(s[0])); }
    });
    const top = Math.min(i, vs.length - 1);
    if (isAdd && vs[top]) take(vs[top].stops);
    for (let j = top - (isAdd ? 0 : 1); j >= 0; j--) take(vs[j].stops);
    for (let j = i + 1; j < vs.length; j++) take(vs[j].stops);
    return out;
  };
  // ×¨×©×™×ž×ª âž•/âž– ×ž×•×›× ×” ×œ×”×¦×’×”. ×”×›×œ×œ: ×”×–×™×”×•×™ ×œ×¤×™ ×ž×¡×¤×¨ ×ª×—× ×”, ×”×©× ×¨×§ ×ª×¦×•×’×”.
  // ××™×¨×•×¢×™× ×—×“×©×™× × ×•×©××™× ××ª ×”×ž×§"×˜ ×”×ž×“×•×™×§ ×œ×¦×“ ×›×œ ×©× (e.c, ×ž×™×•×©×¨ ×ž×”×¡×•×¨×§);
  // ×”× ×™×—×•×©-×œ×¤×™-×©× × ×©××¨ ×¨×§ ×œ×¨×©×•×ž×•×ª ×™×©× ×•×ª ×©× ×©×ž×¨×• ×‘×œ×™ ×ž×¡×¤×¨×™×.
  const labelList = (entries, i, isAdd) => {
    const cnt = {};
    const strs = (entries || []).map((e) => {
      const name = e.n;
      if (e.c) {
        if (/^\d{3,}$/.test(name)) { const h = stopByCode(e.c, i); return h ? withPlat(`${h[1]} (${e.c})`, e.c) : name; }
        return withPlat(`${name} (${e.c})`, e.c);
      }
      if (!isAdd && /^\d{3,}$/.test(name)) {
        const h = stopByCode(name, i);
        return h ? withPlat(`${h[1]} (${name})`, name) : name;
      }
      const k = (cnt[name] = (cnt[name] || 0) + 1) - 1;
      const nv = vs[Math.min(i, vs.length - 1)];
      const cs = codesFor(name, i, isAdd);
      const c = cs[k] != null ? cs[k] : (k === 0 ? ncOf(nv, name) : null);
      if (c) return withPlat(`${name} (${c})`, c);
      if (!isAdd) { const f = remFix(i)[name]; if (f) return withPlat(`${name} (${f[0]})`, f[0]); }
      return name;
    });
    return dedupCount(strs).map(({ x, n: c }) => x + (c > 1 ? ` Ã—${c}` : "")).join(", ");
  };
  // ×ª×—× ×” ×©×™×¨×“×” ×•×ª×—× ×” ×©× ×•×¡×¤×” ×¢× ××•×ª×• ×©× ×•×¨×§ ×¨×¦×™×£ ×©×•× ×” â€” ×–×” ×ž×¢×‘×¨ ×¨×¦×™×£,
  // ×œ× "×ª×—× ×” ×—×“×©×”": ×ž×–×•×•×’×™× ××•×ª×Ÿ ×•×ž×¦×™×’×™× ×©×•×¨×ª ×ž×¢×‘×¨ ××—×ª ×‘×¨×•×¨×”
  const platOf = (n) => {
    const m = /×¨×¦×™×£\s*([^\s/,]+)/.exec(n || "");
    return m ? { plat: m[1], base: (n.replace(/\s*\/?\s*×¨×¦×™×£\s*[^\s/,]+/, "").trim() || n) } : null;
  };
  const splitPlatformMoves = (add, rem) => {
    const a = (add || []).map((e) => ({ ...e, p: platOf(e.n), used: false }));
    const r = (rem || []).map((e) => ({ ...e, p: platOf(e.n), used: false }));
    const moves = [];
    for (const x of a) {
      if (!x.p) continue;
      const y = r.find((y2) => y2.p && !y2.used && y2.p.base === x.p.base);
      if (y) { x.used = y.used = true; moves.push({ base: x.p.base, from: y.p.plat, to: x.p.plat }); }
    }
    return { moves,
      add: a.filter((x) => !x.used).map((x) => ({ n: x.n, c: x.c })),
      rem: r.filter((y) => !y.used).map((y) => ({ n: y.n, c: y.c })) };
  };
  // ×ª×—× ×” ×©×™×¨×“×” ×•×ª×—× ×” ×©× ×•×¡×¤×” ×¢× ××•×ª×• ×©×, ×ž×§"×˜ ×©×•× ×” ×•××•×ª×• ×ž×™×§×•× â€” ×ž×©×¨×“
  // ×”×ª×—×‘×•×¨×” ×”×—×œ×™×£ ×œ×ª×—× ×” ××ª ×”×ž×¡×¤×¨, ×œ× ×‘×™×˜×œ ××•×ª×”: ×©×•×¨×ª ×”×—×œ×¤×” ××—×ª ×‘×ž×§×•×
  // "× ×•×¡×¤×”"+"×™×¨×“×”" ×©× ×¨××•×ª ×›×¡×ª×™×¨×” (×§×• 80 ×›×¤×¨ ×—×‘"×“: 33300 â† 33486)
  // ×¡×™×›×•×ª "×ª×—× ×” ×©×™×¨×“×”" ×œ×ž×¤×” + ×ž×™ ×©× ×©××¨×” ×‘×œ×™ ×ž×™×§×•×. ×ª×—× ×” ×©××™ ××¤×©×¨ ×œ×ž×§×
  // ××¡×•×¨ ×©×ª×™×¢×œ× ×‘×©×§×˜ (×“×¨×™×©×ª ×©×œ×ž×”) â€” ×”×™× ×—×•×–×¨×ª ×‘-unplaced ×•×ž×•×¦×’×ª ×‘×›×™×ª×•×‘
  const remPinsOf = (v2, vi2, gv2) => {
    const pins = [];
    const unplaced = [];
    (v2.rem || []).forEach((n, j) => {
      // ×”×ž×§"×˜ ×”×ž×“×•×™×§ ×©× ×©×ž×¨ ×¢× ×”××™×¨×•×¢ (v.rc) ×§×•×“× ×œ×›×œ × ×™×—×•×©
      const rc = v2.rc && v2.rc[j] != null ? String(v2.rc[j]) : null;
      if (rc) {
        const h = stopByCode(rc, vi2);
        if (h) { pins.push([rc, /^\d{3,}$/.test(n) ? h[1] : n, h[2], h[3]]); return; }
      }
      const e = v2.nc && v2.nc[n];
      if (Array.isArray(e)) { pins.push([String(e[0]), n, e[1], e[2]]); return; }
      // ×¨×©×•×ž×” ×©×”×™× ×ž×§"×˜ ×—×©×•×£ â€” ×”×ª×—× ×” ×ž××•×ª×¨×ª ×œ×¤×™ ×”×ž×¡×¤×¨ ×‘×ª×™×¢×•×“ ×”×™×©×Ÿ
      if (/^\d{3,}$/.test(n)) {
        const h = stopByCode(n, vi2);
        if (h) pins.push([n, h[1], h[2], h[3]]); else unplaced.push(n);
        return;
      }
      // ×”×ª×—× ×” ×©×™×¨×“×” ×§×™×™×ž×ª ×¢× ×§×•××•×¨×“×™× ×˜×•×ª ×‘×¨×©×™×ž×•×ª ×©×œ ×’×¨×¡××•×ª ××—×¨×•×ª â€”
      // ×‘×œ×™ ×”× ×¤×™×œ×” ×”×–×• ×œ××—×•×¨ ×”×™× ×”×•×¤×™×¢×” ×‘×˜×§×¡×˜ ××š ×œ× ×¢×œ ×”×ž×¤×”
      for (let k = vi2 - 1; k >= 0; k--) {
        const h = (vs[k].stops || []).find((s) => s && s[1] === n);
        if (h) { pins.push([String(h[0]), n, h[2], h[3]]); return; }
      }
      for (let k = vi2 + 1; k < vs.length; k++) {
        const h = (vs[k].stops || []).find((s) => s && s[1] === n);
        if (h) { pins.push([String(h[0]), n, h[2], h[3]]); return; }
      }
      // ×”×©× ×‘×¤×™×“ ×”×©×ª× ×” ×ž××– ×”×ª×™×¢×•×“ ×©×œ× ×• â€” ×”×–×™×•×•×’ ×ž×•×œ ×”×”×¤×¨×© ×‘×¤×•×¢×œ
      const f = remFix(vi2)[n];
      if (f) pins.push([String(f[0]), n, f[2], f[3]]); else unplaced.push(n);
    });
    return { unplaced,
      pins: pins.filter((p) => p[2] != null && p[3] != null).filter((p) =>
        // ×ž×§"×˜ ×©×”×•×—×œ×£: ×ª×—× ×” ×‘××•×ª×• ×©× ×•××•×ª×• ×ž×™×§×•× ×¢×“×™×™×Ÿ ×‘×§×• â€” ×œ× ×ž×¦×™×™×¨×™×
        // ×¢×œ×™×” ×¡×™×›×ª "×™×¨×“×”" ××“×•×ž×” ×©× ×¨××™×ª ×›××™×œ×• ×”×ª×—× ×” ×‘×•×˜×œ×”
        !((gv2 && gv2.stops) || []).some((s) => s && s[1] === p[1] &&
          Math.abs(s[2] - p[2]) < 0.005 && Math.abs(s[3] - p[3]) < 0.005)) };
  };
  const splitRenumbers = (add, rem, i) => {
    const a = (add || []).map((e) => ({ ...e, used: false }));
    const moves = []; const rest = [];
    const oldByName = (n2) => {
      const top = Math.min(i, vs.length - 1);
      for (let j = top - 1; j >= 0; j--) { const h = (vs[j].stops || []).find((s) => s && s[1] === n2); if (h) return h; }
      return remFix(i)[n2] || null;
    };
    const curStops = vs[Math.min(i, vs.length - 1)].stops || [];
    for (const e of rem || []) {
      const bare = !e.c && /^\d{3,}$/.test(e.n);
      // ×§×•×“× ×œ×¤×™ ×”×ž×¡×¤×¨ ×”×ž×“×•×™×§ ×›×©× ×©×ž×¨; ×—×™×¤×•×©-×©× ×¨×§ ×œ×¨×©×•×ž×•×ª ×™×©× ×•×ª ×‘×œ×¢×“×™×•
      const oldStop = (e.c && stopByCode(e.c, i)) || (bare ? stopByCode(e.n, i) : oldByName(e.n));
      const oldName = bare || (e.c && /^\d{3,}$/.test(e.n)) ? (oldStop && oldStop[1]) : e.n;
      const x = oldName && a.find((y) => !y.used && y.n === oldName);
      const newStop = x && ((x.c && curStops.find((s) => s && String(s[0]) === x.c)) ||
        curStops.find((s) => s && s[1] === oldName));
      const near = oldStop && newStop && String(oldStop[0]) !== String(newStop[0]) &&
        Math.abs(oldStop[2] - newStop[2]) < 0.005 && Math.abs(oldStop[3] - newStop[3]) < 0.005;
      if (near) { x.used = true; moves.push({ name: oldName, from: String(e.c || oldStop[0]), to: String(newStop[0]) }); }
      else rest.push(e);
    }
    return { moves, add: a.filter((x) => !x.used).map((x) => ({ n: x.n, c: x.c })), rem: rest };
  };
  const v = vs[sel] || vs[vs.length - 1];
  // ×‘×¨×™×¨×ª ×”×ž×—×“×œ ×”×™× ×”×’×¨×¡×” ×”×§×•×“×ž×ª ×”×¡×ž×•×›×”; ×‘×ž×¦×‘ ×”×©×•×•××” ×—×•×¤×©×™×ª ×”×ž×©×ª×ž×© ×‘×•×—×¨
  // ×’×¨×¡×ª ×‘×¡×™×¡ ××—×¨×ª ×•×›×œ ×”×”×¤×¨×© â€” ×”×¨×©×™×ž×”, ×”×ž×¤×” ×•×”×ž×¡×œ×•×œ â€” × ×ž×“×“ ×ž×•×œ×”.
  const vi = vs.indexOf(v);
  const cmpOn = cmpI != null && cmpI !== vi && cmpI >= 0 && cmpI < vs.length;
  const pi = cmpOn ? cmpI : vi - 1;
  const pv = pi >= 0 ? vs[pi] : null;
  // ×”×¤×¨×© ×”×ª×—× ×•×ª ×ž×—×•×©×‘ ×ž×”×¨×©×™×ž×•×ª ×¢×¦×ž×Ÿ â€” ×‘×ž×¦×‘ ×”×©×•×•××” ××™ ××¤×©×¨ ×œ×”×¡×ª×ž×š ×¢×œ
  // add/rem ×©×©×ž×•×¨×™× ×¢×œ ×”×’×¨×¡×”, ×›×™ ×”× × ×ž×“×“×• ×ž×•×œ ×”×’×¨×¡×” ×”×§×•×“×ž×ª ×•×œ× ×ž×•×œ ×”×‘×¡×™×¡.
  const stopsAt = (idx, dir) => {
    for (let j = idx; dir < 0 ? j >= 0 : j < vs.length; j += dir) {
      if ((vs[j].stops || []).length) return vs[j].stops;
    }
    return null;
  };
  const cmpDiff = (() => {
    if (!cmpOn) return null;
    const a = stopsAt(pi, -1), b = stopsAt(vi, -1);
    if (!a || !b) return null;
    const ac = new Set(a.map((x) => x[0])), bc = new Set(b.map((x) => x[0]));
    return {
      add: b.filter((x) => !ac.has(x[0])),
      rem: a.filter((x) => !bc.has(x[0])),
      from: vs[pi].d, to: vs[vi].d,
    };
  })();
  // ×’×¨×¡×ª ××¨×›×™×•×Ÿ ×‘×œ×™ ×’××•×ž×˜×¨×™×” ××š ×¢× ×¨×¦×£ ×ª×—× ×•×ª (×©×œ×‘ ×‘') â€” ×§×• ×ž×§×•×¨×‘ ×‘×™×Ÿ ×”×ª×—× ×•×ª.
  const toPts = (x) => (x.shp ? decodeShape(x.shp) : ((x.stops || []).length > 1 ? x.stops.map((s) => [s[2], s[3]]) : null));
  // ×‘×ž×¦×‘ ×”×©×•×•××” ×ž×¢× ×™×™×Ÿ ×ž×¦×‘ ×”×§×• ×‘×©× ×™ ×”×ª××¨×™×›×™×, ×œ× ×”××™×¨×•×¢ ×¢×¦×ž×•: ××™×¨×•×¢ ×œ×•"×–
  // ××™× ×• × ×•×©× ×’××•×ž×˜×¨×™×”, ×•×‘×œ×™ ×–×” ×”×ž×¤×” ×œ× ×”×™×™×ª×” × ×¤×ª×—×ª ×›×œ×œ ×‘×”×©×•×•××”.
  const geoAt = (idx) => {
    for (let j = idx; j >= 0; j--) if ((vs[j].stops || []).length || vs[j].shp) return vs[j];
    return null;
  };
  // ××™×¨×•×¢ ×‘×œ×™ ×’××•×ž×˜×¨×™×” ×ž×©×œ×• â€” "×”×•×•×¨×™×× ×˜ ×”×•×¤×™×¢ ×‘×¨×™×©×•×", "×‘×•×˜×œ" â€” ×”×¦×™×’
  // "××™×Ÿ ×¤×™×¨×•×˜" ×’× ×›×©×œ×§×• ×™×© ×ž×¡×œ×•×œ ×ž×ª×•×¢×“. ×‘×§×©×ª ×©×œ×ž×”: ×©×ª×™×¤×ª×— ×ž×¤×” ×’× ×‘×ª×™×¢×•×“
  // ×”×¨××©×•×Ÿ ×•×’× ×‘××—×¨×•×Ÿ. ×œ×•×§×—×™× ××ª ×”×’×¨×¡×” ×”×’×™××•×ž×˜×¨×™×ª ×”×§×¨×•×‘×” â€” ××—×•×¨×” ×•×× ××™×Ÿ
  // ××– ×§×“×™×ž×” (×”×ª×™×¢×•×“ ×”×¨××©×•×Ÿ ×©×œ ×§×• ×ž×”××¨×›×™×•×Ÿ ×”×•× ×œ×¨×•×‘ ××™×¨×•×¢-×¨×™×©×•×, ×•×”×¦×™×œ×•×
  // ×¢× ×”×ž×¡×œ×•×œ × ×•×¡×£ ××—×¨×™×•) â€” ×•××•×ž×¨×™× ×ž××™×–×” ×ª××¨×™×š ×”×ž×¤×”.
  const geoNear = (idx) => {
    const back = geoAt(idx);
    if (back) return back;
    for (let j = idx + 1; j < vs.length; j++) if ((vs[j].stops || []).length || vs[j].shp) return vs[j];
    return null;
  };
  const ownGeo = !!(v.shp || (v.stops || []).length > 1);
  // ×©×™× ×•×™ ×©×ª×•×›× ×Ÿ ×•×œ× ×™×¦× ×œ×¤×•×¢×œ (×©×œ×ž×” 03.09: "×›×œ ×©×™× ×•×™ ×©×œ× × ×›× ×¡ ××ž×•×¨ ×œ×”×™×•×ª
  // ×ž×¤×”"): ×”×ž×¤×” ×ž×¦×™×’×” ××ª ×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ (pstops) ×‘×ž×§×•×•×§×•, ×•××ª ×”×ž×¡×œ×•×œ ×‘×¤×•×¢×œ
  // ×©×œ ×”×•×•×¨×™×× ×˜, ×× ×”×™×” ×›×–×”, ×›×¨×§×¢ ××¤×•×¨ â€” ×‘×œ×™ ×¡×™×ž×•× ×™ × ×•×¡×¤×•/×™×¨×“×•, ×›×™ ×–×” ×œ×
  // ×©×™× ×•×™ ×©×§×¨×” ××œ× ×ª×•×›× ×™×ª ×©×™×¨×“×”.
  const plannedV = !cmpOn && v.k === "planned-dropped" && (v.pstops || []).length > 1;
  const actualV = plannedV ? geoNear(vi) : null;
  // ×ž×” ×‘×“×™×•×§ ×œ× ×™×¦× ×œ×¤×•×¢×œ (×©×œ×ž×” 03.09: "×—×•×¥ ×ž×ª×—× ×•×ª ×× ×™ ×œ× ×ž×‘×™×Ÿ ×ž×” ×”×™×” ×©×•× ×”"):
  // 'new' = ×”×•×•×¨×™×× ×˜ ×›×•×œ×• ×¤×•×¨×¡× ×¢× ×ª××¨×™×š ×”×ª×—×œ×” ×•×œ× ×”×ª×—×™×œ ×‘×• (×”×ž×¡×œ×•×œ ×¢×¦×ž×•
  // ×™×›×•×œ ×œ×”×™×•×ª ×–×”×” ×œ×ž×” ×©× ×¡×¢ ××—×¨ ×›×š â€” ××– ×ž×” ×©× ×¤×œ ×”×•× ×”×ž×•×¢×“); 'route' = ×ª×•×›× ×™×ª
  // ×œ×©× ×•×ª ×ž×¡×œ×•×œ ×§×™×™×. ×”×”×‘×“×œ ×ž×•×œ ×”×ž×¡×œ×•×œ ×‘×¤×•×¢×œ × ×ž×“×“ ×‘×¨×¦×£ ×”×ª×—× ×•×ª.
  const plKind = plannedV ? (v.pk || (/×”×•×•×¨×™×× ×˜/.test(v.note || "") ? "new" : "route")) : null;
  const plDiff = plannedV && actualV && (actualV.stops || []).length ? (() => {
    const pc = new Set(v.pstops.map((s) => String(s[0]))), ac = new Set(actualV.stops.map((s) => String(s[0])));
    return { add: v.pstops.filter((s) => !ac.has(String(s[0]))), rem: actualV.stops.filter((s) => !pc.has(String(s[0]))) };
  })() : null;
  const plSame = !!plDiff && !plDiff.add.length && !plDiff.rem.length;
  const plInfo = plannedV ? plannedInfo(v, vi, vs) : null;
  const gv = plannedV ? { d: v.d, stops: v.pstops, shp: v.pshp || "" } : (cmpOn ? (geoAt(vi) || v) : (ownGeo ? v : (geoNear(vi) || v)));
  const borrowed = !cmpOn && !ownGeo && !plannedV && gv !== v;
  // "×ž×§×•×¨×‘" × ×ž×“×“ ×¢×œ ×”×’×¨×¡×” ×©×ž×¦×•×™×¨×ª ×‘×¤×•×¢×œ â€” ×›×©×”×ž×¤×” ×©××•×œ×” ×ž×’×¨×¡×” ××—×¨×ª,
  // ×”×“×™×•×§ ×©×œ×” ×”×•× ×©×œ ××•×ª×” ×’×¨×¡×” ×•×œ× ×©×œ ×”××™×¨×•×¢ ×©× ×‘×—×¨
  const approx = !gv.shp && (gv.stops || []).length > 1;
  const cur = toPts(gv) || [];
  // ×”×ž×¡×œ×•×œ ×”×§×•×“× ×ž×•×¦×’ ×¨×§ ×›×©×™×© ×‘××ž×ª ×ž×” ×œ×”×©×•×•×ª: ×”×’×¨×¡×” ×ž×ª×¢×“×ª ×©×™× ×•×™ ×ª×—× ×•×ª
  // (×›×•×œ×œ ×”×¤×¨×©-×¤×¢×¨ ×ž×•×œ "×ª×™×¢×•×“ ×¨××©×•×Ÿ") ××• ×©×™× ×•×™ ×ž×¡×œ×•×œ, ××• ×©×©×ª×™ ×”×’×¨×¡××•×ª
  // ×ž×“×•×™×§×•×ª. ×‘×œ×™ ×–×”, ×§×™×¨×•×‘-×œ×¤×™-×ª×—× ×•×ª ×ž×•×œ ×’××•×ž×˜×¨×™×” ×ž×œ××” ×ž×¦×™×™×¨ "×ž×¡×œ×•×œ ×™×©×Ÿ"
  // ××“×•× ×©× ×¨××” ×›×ž×• ×©×™× ×•×™ ××ž×™×ª×™ ×›×©×”×ž×¡×œ×•×œ ×‘×›×œ×œ ×œ× ×”×©×ª× ×” (×‘×§×©×ª ×©×œ×ž×”).
  const ROUTE_KINDS = new Set(["route", "redraw", "extend", "shorten", "terminal", "stops", "stops-add", "stops-del"]);
  // ×¦×™×œ×•× ×ž×”××¨×›×™×•×Ÿ / ×ª×™×¢×•×“-×¨××©×•×Ÿ ××™× ×• ×©×™× ×•×™: ×”×ž×¤×” × ×¤×ª×—×ª × ×§×™×™×” â€” ×‘×œ×™
  // ×©×›×‘×ª "×œ×¤× ×™" ×•×‘×œ×™ ×¡×™×ž×•× ×™ × ×•×¡×¤×•/×™×¨×“×• ×©× ×¨××™× ×›××™×œ×• ×§×¨×” ×ž×©×”×• (×“×™×•×•×—
  // ×©×œ×ž×”, ×§×• 26). ×›×¤×ª×•×¨ "×”×©×•×•×”" ×¢×“×™×™×Ÿ ×–×ž×™×Ÿ ×œ×ž×™ ×©×¨×•×¦×” ×œ×”×©×•×•×ª ×‘×ž×¤×•×¨×©.
  const snapOnly = (v.k === "snapshot" || v.k === "baseline" || v.k === "times")
    && !(v.add || []).length && !(v.rem || []).length;
  // ×”×©×•×•××” ×ž×¤×•×¨×©×ª ("×”×©×•×•×”") ×¢×•×‘×“×ª ×ª×ž×™×“ â€” ×—×¡×™×ž×ª ×”×¦×™×œ×•×ž×™× ×—×œ×” ×¨×§ ×¢×œ
  // ×”×”×©×•×•××” ×”××•×˜×•×ž×˜×™×ª ×©×œ ×”×›×¨×˜×™×¡ (×”×ª×™×§×•×Ÿ ×”×§×•×“× ×©×‘×¨ ××ª ×”×”×©×•×•××”, ×©×œ×ž×”)
  const comparable = !plannedV && (cmpOn || (!!pv && !snapOnly &&
    (!!(v.add || v.rem) || ROUTE_KINDS.has(v.k) || !!(v.shp && pv.shp))));
  // ×”×’×¨×¡×” ×”×¡×ž×•×›×” ×¢×©×•×™×” ×œ×”×™×•×ª ××™×¨×•×¢-×¨×™×©×•× ×‘×œ×™ ×¨×¦×£ ×ª×—× ×•×ª â€” ×•××– "×”×ž×¡×œ×•×œ
  // ×”×§×•×“×" ×•×”×ª×—× ×•×ª ×©×™×¨×“×• ×œ× ×¦×•×™×¨×• ×›×œ×œ (×”×‘××’ ×©×¦×™×œ× ×©×œ×ž×” ×‘×§×• 35 ××©×“×•×“).
  // ×©×•××œ×™× ××ª ×”×ª×™×¢×•×“ ×”×’×™××•×ž×˜×¨×™ ×”××—×¨×•×Ÿ ×©×œ×¤× ×™ ×”××™×¨×•×¢, ×›×ž×• ×‘×ž×¦×‘ ×”×©×•×•××”.
  const pgv = plannedV ? actualV : (cmpOn ? (geoAt(pi) || pv)
    : (pv && !(pv.stops || []).length && !pv.shp ? (geoAt(pi) || pv) : pv));
  const prev = plannedV ? (actualV ? toPts(actualV) : null) : (comparable ? toPts(pgv) : null);
  const prevApprox = !!(pgv && prev && !pgv.shp);
  // ×§×•×‘×¥ 2012 ×ž×§×‘×¥ ××ª ×›×œ ×”×•×•×¨×™×× ×˜×™× ×”××¨×¦×™×™× ×©×œ ×”×ž×¡×¤×¨ â€” ×ž×¦×™×’×™× ×¨×§ ××ª
  // ×”×¨×œ×•×•× ×˜×™×™× ×œ×§×• ×”×¤×ª×•×— (×—×¤×™×¤×ª ×ž×™×œ×™× ×¢× ×”×ª×—× ×•×ª/×”×™×¢×“), ×¢× ××¤×©×¨×•×ª ×œ×—×©×•×£ ×”×›×œ
  const rel12 = (() => {
    const routes = (d12 && d12.routes) || [];
    if (routes.length <= 1) return routes.map((_, i) => i);
    // ×”×ª××ž×ª ×¢×™×¨: ×•×¨×™×× ×˜ 2012 ×¨×œ×•×•× ×˜×™ ×¨×§ ×× ×¢×™×¨ ×©×œ ×ª×—× ×ª ×§×¦×” ×©×œ×• ("×©× - ×¢×™×¨")
    // ×ž×•×¤×™×¢×” ×‘×™×¢×“ ××• ×‘×ª×—× ×•×ª ×©×œ ×”×§×• ×”×¤×ª×•×—. ×ž×™×œ×™× ×’× ×¨×™×•×ª ("×ª×—× ×” ×ž×¨×›×–×™×ª",
    // "×”×¨×¦×œ") ×”×˜×¢×• ××ª ×”×¡×™× ×•×Ÿ ×”×§×•×“× ×•×”×›× ×™×¡×• ×•×¨×™×× ×˜×™× ×ž×¢×¨×™× ××—×¨×•×ª.
    const hay = String(lf.dest || "") + " " +
      (([...vs].reverse().find((x) => (x.stops || []).length) || {}).stops || []).map((s) => s[1]).join(" ");
    const cityOf = (x) => { const p = String(x || "").split(" - "); return p.length > 1 ? p[p.length - 1].trim() : null; };
    const passCity = (r) => { const a = cityOf(r.f), b = cityOf(r.l); return !!((a && hay.includes(a)) || (b && hay.includes(b))); };
    let rel = routes.map((r, i) => [r, i]).filter((x) => passCity(x[0])).map((x) => x[1]);
    if (!rel.length) {
      const GEN = new Set(["×ª×—× ×”", "×ª×—× ×ª", "×ž×¨×›×–×™×ª", "×¨×›×‘×ª", "×¦×•×ž×ª", "×ž×¡×•×£", "×ž×¨×›×–",
        "×§× ×™×•×Ÿ", "×©×•×§", "×‘×™×ª", "×©×“×¨×•×ª", "×›×‘×™×©", "×›×™×›×¨", "×›×›×¨", "×“×¨×š", "×¨×—×•×‘"]);
      const tokset = new Set(hay.split(/[^×-×ª0-9]+/).filter((w) => w.length >= 3 && !GEN.has(w)));
      const sc = (r) => String((r.f || "") + " " + (r.l || "")).split(/[^×-×ª0-9]+/)
        .filter((w) => w.length >= 3 && tokset.has(w)).length;
      rel = routes.map((r, i) => [sc(r), i]).filter((x) => x[0] > 0).map((x) => x[1]);
    }
    return rel.length ? rel : routes.map((_, i) => i);
  })();
  const vis12 = rel12;
  const sel12 = vis12.includes(r12) ? r12 : (vis12[0] ?? 0);
  // ×ž×¡×œ×•×œ 2012 ×œ×ž×¤×”: ×¨×§ ×›×©×”×¤×× ×œ ×¤×ª×•×—, ×•×¨×§ ×ª×—× ×•×ª ×©×”×•×¦×œ×‘×• (×™×© ×œ×”×Ÿ ×§×•××•×¨×“×™× ×˜×•×ª)
  const stops12 = (show12 && d12 && (d12.routes || []).length)
    ? ((d12.routes[sel12] || d12.routes[0]).stops || []).filter((s) => s.length >= 7).map((s) => [s[1], s[5], s[6], s[7] === 1, s[4] && s[4][0]])
    : null;
  const sh12 = (stops12 && s12 && s12.routes && s12.routes[String(sel12)]) || null;
  const shape12 = sh12 ? decodeShape(sh12.pl) : null;
  // ×›×©×¤×•×ª×—×™× ××ª 2012 ×”×ž×¤×” ×ž×¦×™×’×” ×¨×§ ××ª ×ž×¡×œ×•×œ 2012; ×›×¤×ª×•×¨ ×ž×—×–×™×¨ ××ª ×©×ª×™ ×”×©×›×‘×•×ª ×™×—×“
  const m12only = !!(show12 && only12 && stops12 && stops12.length);
  return (
    <div className="linewrap">
      <div className="card side">
        <button className="back" title="×—×–×¨×” ×œ×ž×¡×š ×”×—×™×¤×•×© â€” ×”×˜×§×¡×˜ ×©×—×™×¤×©×ª× × ×©×ž×¨" onClick={onBack}>â†’ ×—×–×¨×” ×œ×—×™×¤×•×©</button>
        {/* ×œ×§×•×•×™ ×”×¨×›×‘×ª ××™×Ÿ ×ž×¡×¤×¨ ×§×• ×‘-GTFS â€” ×”×¡×ž×œ ×ž×ž×œ× ××ª ×ž×§×•×ž×• ×›×“×™ ×©×”×ª×’ ×œ× ×™×•×¤×™×¢ ×¨×™×§ */}
        <div className="linehead"><span className="badge">{lf.line || TT_ICON[lf.tt] || "â€”"}</span><span className="dest">{lf.dest}</span>
          {/* ×©×™×ª×•×£ ×›×ž×• ×‘×”×§×• ×”×ž×“×œ×’: ×’×™×œ×™×•×Ÿ ×”×©×™×ª×•×£ ×©×œ ×”×˜×œ×¤×•×Ÿ, ×•×‘× ×¤×™×œ×” â€” ×”×¢×ª×§×” */}
          <button className="sharebtn" title="×©×™×ª×•×£ ×”×§×™×©×•×¨ ×œ×¢×ž×•×“ ×”×§×• ×”×–×” â€” ×›×œ ×”×”×™×¡×˜×•×¨×™×” ×©×œ×•"
            onClick={(e) => {
              // ×“×£-×©×™×ª×•×£ ×™×™×¢×•×“×™: ×ž×¦×™×’ ×‘×•×•××˜×¡××¤ ××ª ×©× ×”×§×• ×•×¡×ž×œ ×”××ª×¨, ×•×ž×§×¤×™×¥ ×œ×“×£ (×‘×§×©×ª ×©×œ×ž×”)
              const url = location.origin + location.pathname.replace(/line-history\/?[^/]*$/, "") + "s/l-" + fsafe(rd) + ".html";
              const b = e.currentTarget;
              // "×”×•×¢×ª×§" ×¨×§ ××—×¨×™ ×©×”×”×¢×ª×§×” ×‘××ž×ª ×”×¦×œ×™×—×”; ×‘×’×™×œ×™×•×Ÿ ×”×©×™×ª×•×£ ×©×œ
              // ×”×˜×œ×¤×•×Ÿ ××™×Ÿ ×ž×” ×œ×”×›×¨×™×– (×¦×™×“ ×”×‘××’×™×, ×¡×‘×‘ ×‘)
              if (navigator.share) { navigator.share({ title: "×”×§×• ×‘×–×ž×Ÿ â€” " + (lf.line ? "×§×• " + lf.line : lf.dest), url }).catch(() => {}); return; }
              const t = b.textContent;
              const done = () => { b.textContent = "âœ“ ×”×•×¢×ª×§"; setTimeout(() => { b.textContent = t; }, 1500); };
              if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(url).then(done, () => {});
            }}>ðŸ”— ×©×™×ª×•×£</button>
          {/* ×ž×¢×§×‘ ×œ×¤×™ ×§×• ×‘×•×“×“ ×”×•×¡×¨ (×‘×§×©×ª ×©×œ×ž×” 25.08) â€” ×”×¨×©×ž×” ×‘×¨×ž×ª ×¢×™×¨ ×‘×œ×‘×“;
              ×ž×“×£ ×”×§×• ×‘×•×—×¨×™× ×¡×•×’×™ ×©×™× ×•×™ ×•×ª×“×™×¨×•×ª, ×›×ž×• ×‘×ž×¨×›×– (×©×œ×ž×” 13.09) */}
          <LineFollow cities={destCities(lf.dest)} />
        </div>
        {/* "×¢×™×¨×•× ×™" ×¤×¢× ××—×ª ×‘×œ×‘×“ (×©×œ×ž×” 06.09): ×›×©×”×•× ×ž×•×¤×™×¢ ×œ×™×“ "× ×’×™×©" â€” ×”×ª×’ ×”× ×¤×¨×“ ×œ× ×ž×•×¦×’ */}
        {/* ×¡×•×’ ×”×§×• ×ž×¨×©×™×ž×ª ×”××©×›×•×œ×•×ª ×©×œ ×”×ž×©×¨×“ (ltc), ×•×× ××™×Ÿ â€” ×ž×§×•×‘×¥ ×”× ×•×¡×¢×™× (ty); ×™×™×—×•×“×™×•×ª
            (×ª×œ×ž×™×“×™×/×œ×™×œ×”/×ž×–×™×Ÿ) ×•××©×›×•×œ ×”×ž×›×¨×– â€” tools/linehistory_ltype.py (×©×œ×ž×” 07.09) */}
        <div className="facts">{lf.op}{(() => { const t = lf.ltc || lf.ty; return t && !(lf.vt && lf.vt.startsWith(t)) ? " Â· " + t : ""; })()}
          {lf.un && lf.un !== "×¡×“×™×¨" ? " Â· " + ({ "×ª×œ×ž×™×“×™×": "×§×• ×ª×œ×ž×™×“×™×", "×œ×™×œ×”": "×§×• ×œ×™×œ×”", "×§×•×•×™× ×ž×–×™× ×™×": "×§×• ×ž×–×™×Ÿ" }[lf.un] || lf.un) : ""}
          {lf.clu ? " Â· ××©×›×•×œ " + lf.clu : ""}{lf.tt ? " Â· " + (TT_LABEL[lf.tt] || "") : ""}
          {/* ×¨×¦×™×£ ×”×ž×•×¦× ×œ× ×‘×©×•×¨×ª ×”×¤×¨×˜×™× â€” ×¨×§ ×œ×™×“ ×”×ª×—× ×” ×‘×¨×©×™×ž×” ×•×‘×ž×¤×” (×©×œ×ž×” 13.09) */}
          {/* × ×’×™×©×•×ª ×œ×›×™×¡× ×’×œ×’×œ×™× ×ž×’×™×¢×” ×ž-wheelchair_accessible ×‘×¤×™×“, ×•×”×™×
              ××—×™×“×” ×œ×›×œ × ×¡×™×¢×•×ª ×”×§×• â€” ×•×œ×›×Ÿ ×ª×›×•× ×” ×©×œ ×”×§×•. ×× ×ª×•×¢×“ ××™×¨×•×¢ ×©×™× ×•×™
              × ×’×™×©×•×ª, ×”×ª×’ ×ž×¦×™×™×Ÿ ×ž××™×–×” ×ª××¨×™×š ×”×ž×¦×‘ ×”× ×•×›×—×™; ×©×™× ×•×™ ×©×§×¨×” ×™×—×“ ×¢×
              ×©×™× ×•×™ ×ž×¡×œ×•×œ ×ž×¡×•×•×’ route/stops ×•×œ× access, ×•×œ×›×Ÿ ×”×–×™×”×•×™ × ×¢×–×¨ ×’×
              ×‘×”×¢×¨×”. ×œ×§×•×•×™× ×©×œ× × ×¦×¤×” ×‘×”× ×©×™× ×•×™ â€” ×ª×’ ×‘×œ×™ ×ª××¨×™×š, ×œ× ×ª××¨×™×š ×ž×•×ž×¦×. */}
          {/* ×’×•×“×œ ×”×¨×›×‘ ×•×¡×•×’ ×”×§×• ×ž×”×¨×™×©×•×™ × ×›×ª×‘×™× ×‘×‘×™×˜×•×™ ××—×“ ×œ×™×“ ×”× ×’×™×©×•×ª â€”
              "×ž×™× ×™×‘×•×¡ ×¢×™×¨×•× ×™ × ×’×™×©" (×©×œ×ž×” 06.09); ×©×™× ×•×™×™× ×‘×”× â€” ×§×˜×’×•×¨×™×” ×‘×¦×™×¨ ×”×–×ž×Ÿ */}
          {(() => {
            // ×‘×™×˜×•×™ ××—×“: "×ž×™× ×™×‘×•×¡ ×¢×™×¨×•× ×™ × ×’×™×©" â€” ×’×•×“×œ ×”×¨×›×‘ ×•×¡×•×’ ×”×§×• ×ž×¨×™×©×•×™ ×ž×©×¨×“
            // ×”×ª×—×‘×•×¨×”, ×•×”× ×’×™×©×•×ª ×ž×”×¤×™×“ ×”××¨×¦×™ (×©×œ×ž×” 06.09: "×¤×©×•×˜ ×œ×¨×©×•× ×ž×™× ×™×‘×•×¡ ×¢×™×¨×•× ×™
            // × ×’×™×©"). "××•×˜×•×‘×•×¡" × ×©××¨ ×›×ž×™×œ×” ×›×™ "××•×˜×•×‘×•×¡ ×¢×™×¨×•× ×™ × ×’×™×©" ×§×¨×™×; "×œ× ×ž×•×’×“×¨"
            // ×œ× × ×›×ª×‘ â€” × ×©××¨ ×¨×§ ×¡×•×’ ×”×§×•.
            const VSZ = { "××•×˜×•×‘×•×¡": "××•×˜×•×‘×•×¡", "×ž×¤×¨×§×™": "××•×˜×•×‘×•×¡ ×ž×¤×¨×§×™", "×ž×™× ×™×‘×•×¡": "×ž×™× ×™×‘×•×¡", "×ž×™×“×™×‘×•×¡": "×ž×™×“×™×‘×•×¡" };
            const what = [VSZ[lf.vsz] || "", lf.vt || ""].filter(Boolean).join(" ");
            if (lf.wa !== "1" && lf.wa !== "2")
              return what ? <span className="vsz" title="×’×•×“×œ ×”×¨×›×‘ ×•×¡×•×’ ×”×§×• ×©× ×§×‘×¢×• ×œ×§×• ×‘×¨×™×©×•×™ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×”"> Â· ðŸšŒ {what}</span> : null;
            const chg = [...vs].reverse().find((v) => v.k === "access" || String(v.note || "").includes("×”× ×’×™×©×•×ª ×©×•Ö¼× ×ª×”"));
            const since = chg ? " ×ž××– " + fmtD(chg.d) : "";
            const tip = (lf.wa === "1" ? "×œ×¤×™ ×”×¤×™×“ ×”××¨×¦×™, ×”×§×• ×ž×•× ×’×© ×œ×›×™×¡× ×’×œ×’×œ×™×" : "×œ×¤×™ ×”×¤×™×“ ×”××¨×¦×™, ×”×§×• ××™× ×• ×ž×•× ×’×© ×œ×›×™×¡× ×’×œ×’×œ×™×")
              + (chg ? " â€” ×”×©×™× ×•×™ × ×§×œ×˜ ×‘×¤×™×“ ×‘-" + fmtD(chg.d) : "") + (what ? " Â· ×’×•×“×œ ×”×¨×›×‘ ×•×¡×•×’ ×”×§×• ×œ×¤×™ ×¨×™×©×•×™ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×”: " + what : "");
            // ×¡×ž×œ ×”× ×’×™×©×•×ª ×¨×§ ×›×©×”×§×• × ×’×™×©; ×œ×§×• ×©××™× ×• × ×’×™×© â€” ×¡×ž×œ ××•×˜×•×‘×•×¡ ×•×”×ž×™×œ×™× (×©×œ×ž×” 06.09)
            if (lf.wa === "1") return <span className="wa yes" title={tip}> Â· â™¿ {what ? what + " " : ""}× ×’×™×©{since}</span>;
            return <span className="wa no" title={tip}> Â· {what ? "ðŸšŒ " + what + " Â· " : ""}××™× ×• × ×’×™×©{since}</span>;
          })()}
          {/* ×›×ž×” × ×¡×™×¢×•×ª ×ž×ª×•×›× × ×•×ª ×™×© ×œ×—×œ×•×¤×” ×”×™×•×. "×§×™×™× ×‘×¤×™×“" ××™× ×• "×¤×•×¢×œ":
              ×”×¤×™×“ ×ž×¤×¨×¡× ×§×•×•×™× ×œ×¤× ×™ ×”×¤×ª×™×—×”, ×•×”×§×• ×”×™×¨×•×§ ×‘×™×¨×•×©×œ×™× × ×›× ×¡ ×¢×
              × ×¡×™×¢×” ××—×ª ×‘×›×™×•×•×Ÿ ×ž×•×œ 680 ×©×œ ×”×§×• ×”×™×¨×•×§ ×‘×ª×œ ××‘×™×‘. ×”×ž×¡×¤×¨ ×ž×•×¦×’
              ×›×¢×•×‘×“×” ×•×œ× ×›×ž×¡×§× ×” â€” ×©×œ×™×© ×ž×”×—×œ×•×¤×•×ª × ×•×¡×¢×•×ª ××¨×‘×¢ ×¤×¢×ž×™× ×‘×™×•× ××•
              ×¤×—×•×ª (×§×•×•×™ ×ª×œ×ž×™×“×™×, ×—×œ×•×¤×•×ª ×ž×©× ×”), ×•××–×”×¨×” ×¢×œ×™×”×Ÿ ×”×™×™×ª×” ×¨×¢×©. */}
          {ntr > 0 && (
            <span title="×ž×¡×¤×¨ ×”× ×¡×™×¢×•×ª ×”×ž×ª×•×›× × ×•×ª ×œ×—×œ×•×¤×” ×”×–×• ×‘×¤×™×“ ×©×œ ×”×™×•×, ×œ×¤×™ ×œ×•×—×•×ª ×”×–×ž× ×™× ×©×‘×ª×•×§×£">
              {" Â· "}{ntr === 1 ? "× ×¡×™×¢×” ××—×ª ×‘×™×•×" : `${ntr.toLocaleString()} × ×¡×™×¢×•×ª ×‘×™×•×`}</span>
          )}
          {" Â· ×ž×§×´×˜ "}<span className="rdnum" dir="ltr">{rdTxt(lf.rd)}</span> Â· {vs.length} ×’×¨×¡××•×ª ×ž×ª×•×¢×“×•×ª</div>
        {/* ×ª×§×•×¤×•×ª ×©×‘×”×Ÿ ×”×§×• ×œ× ×”×™×” ×‘×¨×™×©×•× ×•×—×–×¨ â€” ×›×¨×˜×™×¡ "×‘×•×˜×œ ×•×—×–×¨" ×‘×¦×™×¨ ×”×–×ž×Ÿ
            (materializeLf), ×œ× ×¤×¡ ×˜×§×¡×˜ ×›××Ÿ (×©×œ×ž×” 06.09). ×‘×™×˜×•×œ ×©×¢×“×™×™×Ÿ ×œ× × ×’×ž×¨
            ×ž×•×¦×’ ×‘×”×•×“×¢×ª ×”×¡×˜×˜×•×¡ ×œ×ž×˜×”. */}
        {/* ×¨×§ "×©×™×¨×•×ª ×œ×¤×™ ×“×¨×™×©×”" ×ž×§×‘×œ ×”×¢×¨×”, ×›×™ ×”×™× × ×•×©××ª ×ž×™×“×¢ ×©××™× ×• ×‘×ž×§×•×
            ××—×¨: ×”×§×• ×™×•×©×‘ ×‘×™×Ÿ ×§×•×•×™ ×”××•×˜×•×‘×•×¡ ×•× ×¨××” ×¨×’×™×œ ×œ×—×œ×•×˜×™×Ÿ, ×•××™ ××¤×©×¨
            ×œ×“×¢×ª ×ž×ž× ×• ×©×”× ×¡×™×¢×” ×ž×•×ª× ×™×ª ×‘×”×–×ž× ×”. ×œ×©××¨ ×”×¡×•×’×™× ×”×ª×•×•×™×ª ×‘×©×•×¨×ª
            ×”×¤×¨×˜×™× ×›×‘×¨ ××•×ž×¨×ª ×”×›×œ, ×•×”×¢×¨×” × ×•×¡×¤×ª ×”×™× ×¨×¢×©. */}
        {lf.tt === "demand" && (
          <div className="ttnote">
            {/* ×”× ×™×¡×•×— ×”×§×•×“× ×§×‘×¢ ×©"×”× ×¡×™×¢×” ×ž×‘×•×¦×¢×ª ×œ×¤×™ ×”×–×ž× ×” ×ž×¨××©". ×–×• ×¤×¨×©× ×•×ª
                ×©×œ route_type 715, ×•×”× ×ª×•× ×™× ×©×œ× ×• ×¡×•×ª×¨×™× ××•×ª×”: ×œ-22 ×ž×ª×•×š 61
                ×”×§×•×•×™× ×”××œ×” ×ž×¤×•×¨×¡× ×œ×•×— ×–×ž× ×™× ×¢× ×©×¢×•×ª ×™×¦×™××” ×ž×ž×©. ×ž×” ×©×™×“×•×¢
                ×”×•× ×”×¡×™×•×•×’ ×‘×¤×™×“, ×œ× ××•×¤×Ÿ ×”×”×–×ž× ×” ×‘×¤×•×¢×œ. */}
            ðŸš <b>×©×™×¨×•×ª ×œ×¤×™ ×“×¨×™×©×”</b> â€” ×›×š ×”×§×• ×ž×¡×•×•×’ ×‘×¤×™×“ ×”××¨×¦×™ (route_type 715),
            ×¡×™×•×•×’ ×©× ×•×¢×“ ×œ×©×™×¨×•×ª ×©××™× ×• ×™×•×¦× ×‘×©×¢×” ×§×‘×•×¢×”. ×™×© ×§×•×•×™× ×‘×¡×™×•×•×’ ×”×–×” ×©×›×Ÿ
            ×ž×¤×•×¨×¡× ×œ×”× ×œ×•×— ×–×ž× ×™×, ×•×œ×›×Ÿ ×›×“××™ ×œ×‘×“×•×§ ×ž×•×œ ×”×ž×¤×¢×™×œ ××™×š ×”× ×¡×™×¢×” ×ž×•×–×ž× ×ª ×‘×¤×•×¢×œ.
          </div>
        )}
        <SchedBox rd={rd} vs={vs} selD={sel != null && vs[sel] ? vs[sel].d : null}
          isLast={sel == null || sel >= vs.length - 1} />
        {anc && !NO_2012.has(lf.tt || "") && (
          <div className="a2012">
            <b>2012</b> Â· {anc.f} â† {anc.l} Â· {anc.n} ×ª×—× ×•×ª
            {/* ×ž×¡×¤×¨ ×”×ª×—× ×•×ª ×”×ž×©×•×ª×¤×•×ª ×”×•× ×ž×” ×©×§×•×©×¨ ××ª ×”×§×• ×©×œ ××– ×œ×§×• ×©×œ ×”×™×•×.
                ×›×©×”×”×ª××ž×” × ×¢×©×ª×” ×œ×¤×™ ×©× ×•×ž×¡×¤×¨ ×§×• ×‘×œ×‘×“ ×”×•× ×œ× ×§×™×™×. */}
            {anc.ov && <TipTag cls="a2012ov" tip="×ž×¡×¤×¨ ×”×ª×—× ×•×ª ×©×ž×•×¤×™×¢×•×ª ×’× ×‘×ž×¡×œ×•×œ ×©×œ 2012 ×•×’× ×‘×ž×¡×œ×•×œ ×”×™×©×Ÿ ×‘×™×•×ª×¨ ×©×™×“×•×¢ ×œ× ×• â€” ×¢×œ ×¡×ž×š ×–×” × ×§×‘×¢ ×©×ž×“×•×‘×¨ ×‘××•×ª×• ×§×•">Â· {anc.ov} ×ª×—× ×•×ª ×ž×©×•×ª×¤×•×ª</TipTag>}
            <button className="a2012btn" title="×”×¦×’×ª ×¨×©×™×ž×ª ×”×ª×—× ×•×ª ×©×œ ×”×§×• ×›×¤×™ ×©×”×™×™×ª×” ×‘-2012 â€” ×›×•×œ×œ ×”×ž×¡×œ×•×œ ×¢×œ ×”×ž×¤×” ×‘×§×• ×ž×§×•×•×§×• ×—×•×" aria-expanded={show12} onClick={() => setShow12(!show12)}>
              {show12 ? "×”×¡×ª×¨ â–²" : "×¨×¦×£ ×”×ª×—× ×•×ª â–¼"}
            </button>
            {show12 && (d12 ? (d12.routes || []).length ? (
              <div>
                {vis12.length > 1 && (
                  <div className="s12chips">{vis12.map((i) => (
                    <button key={i} className={"rchip12" + (i === sel12 ? " on" : "")} title="×ž×¡×œ×•×œ 2012 × ×•×¡×£ ×©×œ ××•×ª×• ×§×• â€” ×œ×—×™×¦×” ×ž×¦×™×’×” ××•×ª×•"
                      onClick={() => setR12(i)}>{d12.routes[i].f} â† {d12.routes[i].l} ({d12.routes[i].n})</button>
                  ))}</div>
                )}
                <ol className="s12">
                  {((d12.routes[sel12] || d12.routes[0]).stops || []).map((s) => (
                    <li key={s[0]}>{s[1]}{" "}
                      {s[4] && s[4].length === 1 ? <span className="pcode">×ž×§×´×˜ {s[4][0]}</span>
                        : s[4] && s[4].length > 1 ? <span className="pcode">{s[4].length} ×ž×§×´×˜×™× ××¤×©×¨×™×™×</span>
                          : <span className="pcode">×œ× ×”×•×¦×œ×‘×”</span>}
                    </li>
                  ))}
                </ol>
              </div>
            ) : <div className="mut">×”× ×ª×•× ×™× ×œ× × ×˜×¢× ×• â€” × ×¡×• ×œ×¨×¢× ×Ÿ.</div>
              : <div className="mut">×˜×•×¢×Ÿâ€¦</div>)}
          </div>
        )}
        {sibs && sibs.length > 1 && (
          <div className="sibs">
            <span className="sibt">×—×œ×•×¤×•×ª ×•×›×™×•×•× ×™×:</span>
            {sibs.map((s) => {
              const alt0 = (x) => x.rd.split("-").slice(2).join("-");
              const dir0 = (x) => x.rd.split("-")[1] || "";
              const dir = dir0(s), alt = alt0(s);
              const isBase = alt === "" || alt === "#" || alt === "0";
              const dupDir = sibs.filter((x) => dir0(x) === dir).length > 1;
              const dupBase = dupDir && isBase && sibs.some((x) => x.rd !== s.rd && dir0(x) === dir && ["", "#", "0"].includes(alt0(x)));
              const lbl = "×›×™×•×•×Ÿ " + dir + (isBase
                ? (dupDir ? " Â· ×¨××©×™×ª" + (dupBase ? " (" + (alt || "Ö¾") + ")" : "") : "")
                : " Â· ×—×œ×•×¤×” " + alt);
              return (
                <span key={s.rd} className="sibwrap">
                  <a className={"sib" + (s.rd === rd ? " on" : "")} title={s.dest}
                    href={lineHref(s.rd)}
                    onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); if (s.rd !== rd) onSwitch(s.rd); }}>
                    {lbl}
                    {s.lk === "removed" && <span className="sibx">âœ–</span>}
                  </a>
                  {/* ×”×©×•×•××” ×‘×™×Ÿ ×—×œ×•×¤×•×ª: ×¢×“ ×¢×›×©×™×• ××¤×©×¨ ×”×™×” ×¨×§ ×œ×¢×‘×•×¨ ×‘×™× ×™×”×Ÿ,
                      ×•×œ×”×—×–×™×§ ××ª ×”×”×‘×“×œ ×‘×¨××© */}
                  {s.rd !== rd && (
                    <button className="sibcmp" title={"×”×©×•×•××ª ×”×ª×—× ×•×ª ×•×”×ž×¡×œ×•×œ ×ž×•×œ " + lbl}
                      onClick={() => setAltRd(altRd === s.rd ? null : s.rd)}>
                      {altRd === s.rd ? "âœ•" : "â‡„"}</button>
                  )}
                </span>
              );
            })}
          </div>
        )}
        {altRd && (
          <AltCompare rd={rd} altRd={altRd} onClose={() => setAltRd(null)}
            label={(sibs.find((x) => x.rd === altRd) || {}).dest || altRd} />
        )}
        {/* ×”×¡×˜×˜×•×¡ × ×’×–×¨ ×ž×”×¨×©×•×ž×” ×”××—×¨×•× ×” ×©××™× ×” "×ª×•×›× ×Ÿ ×•×œ× × ×›× ×¡ ×œ×ª×•×§×£": ×ª×•×›× ×™×ª
            ×œ×”×—×–×™×¨ ×§×• ×ž×‘×•×˜×œ ×©×œ× ×”×ª×ž×ž×©×” ××™× ×” ×ž×‘×˜×œ×ª ××ª ×”×‘×™×˜×•×œ */}
        {vs.length > 0 && (() => {
          // ×•×’× ×œ× ××™×¨×•×¢ × ×’×–×¨ (×¡×•×’ ×¨×›×‘ ×‘×¨×™×©×•×™) ×©×ª××¨×™×›×• ×ž××•×—×¨ ×ž×”×‘×™×˜×•×œ
          let li = vs.length - 1;
          while (li > 0 && (vs[li].k === "planned-dropped" || vs[li].syn)) li--;
          const lv = vs[li];
          return lv.k === "removed" && (
          <div className="facts" style={{ color: lineGone ? (KINDS[dispKind(lv, li, vs)] || {}).color : "#c2410c", fontWeight: 700 }}>
            {lineGone
              ? <>âŒ ×”×§×• ×‘×•×˜×œ â€” ××™×Ÿ ×—×œ×•×¤×•×ª ×¤×¢×™×œ×•×ª â€” ×ž××– {fmtD(lv.d)}</>
              : <>âš ï¸ ×”×—×œ×•×¤×” ×”×–×• ×ž×‘×•×˜×œ×ª ×ž××– {fmtD(lv.d)} (×œ×§×• ×™×© ×—×œ×•×¤×•×ª ×¤×¢×™×œ×•×ª)</>}
            {dispKind(lv, li, vs) === "removed-year" ? " â€” ×ž×¢×œ ×©× ×” ×•×œ× ×—×–×¨×”" : ""}
          </div>);
        })()}
        {cmpOn && (
          <div className="cmpbar">
            <b>×”×©×•×•××”</b> Â· {String(vs[pi].d).split("-").reverse().join(".")} â† {String(v.d).split("-").reverse().join(".")}
            {cmpDiff && (
              <span className="cmpsum">
                {cmpDiff.add.length ? ` Â· âž• ${cmpDiff.add.length} ×ª×—× ×•×ª` : ""}
                {cmpDiff.rem.length ? ` Â· âž– ${cmpDiff.rem.length} ×ª×—× ×•×ª` : ""}
                {!cmpDiff.add.length && !cmpDiff.rem.length ? " Â· ××•×ª×Ÿ ×ª×—× ×•×ª ×‘×“×™×•×§" : ""}
              </span>
            )}
            <button className="cmpx" title="×¡×™×•× ×”×”×©×•×•××” â€” ×—×–×¨×” ×œ×”×¤×¨×© ×ž×•×œ ×”×’×¨×¡×” ×”×§×•×“×ž×ª" onClick={() => setCmpI(null)}>âœ• ×¡×™×•×</button>
            {cmpDiff && (cmpDiff.add.length || cmpDiff.rem.length) ? (
              <div className="cmplist">
                {cmpDiff.add.length ? <div className="ad">âž• {dedupCount(cmpDiff.add).map(({ x, n: c }) => `${x[1]} (${x[0]})` + (c > 1 ? ` Ã—${c}` : "")).join(", ")}</div> : null}
                {cmpDiff.rem.length ? <div className="rm">âž– {dedupCount(cmpDiff.rem).map(({ x, n: c }) => `${x[1]} (${x[0]})` + (c > 1 ? ` Ã—${c}` : "")).join(", ")}</div> : null}
              </div>
            ) : null}
          </div>
        )}
        {kindsHere.length > 1 && initCats && offK.size > 0 && (
          <div className="khint">ðŸ”Ž ×ž×•×¦×’×™× ×¨×§ ×”×©×™× ×•×™×™× ×ž×”×§×˜×’×•×¨×™×” ×©×‘×—×¨×ª ×‘×—×™×¤×•×©. "×”×›×•×œ" ×ž×¦×™×’ ××ª ×›×œ ×”×©×™× ×•×™×™× ×‘×§×•.</div>
        )}
        {kindsHere.length > 1 && (
          <div className="kfilter">
            <button className={"kchip" + (offK.size ? "" : " on")}
              title="×”×¦×’×ª ×›×œ ×¡×•×’×™ ×”×©×™× ×•×™×™× ×‘×§×• ×”×–×”" onClick={() => setOffK(new Set())}>×”×›×•×œ</button>
            {kindsHere.map(([g, e]) => {
              const off = [...e.kinds].every((k) => offK.has(k));
              return (
                <button key={g} className={"kchip" + (off ? " off" : " on")}
                  style={off ? null : { borderColor: catColor(g), color: catColor(g) }}
                  title={off ? "×”×“×œ×§×” â€” ×”××™×¨×•×¢×™× ×”××œ×” ×™×—×–×¨×• ×œ×¨×©×™×ž×”" : "×›×™×‘×•×™ â€” ×”××™×¨×•×¢×™× ×”××œ×” ×™×™×¢×œ×ž×• ×ž×”×¨×©×™×ž×”"}
                  onClick={() => toggleK([...e.kinds], off)}>
                  {KGLABEL[g] || (KINDS[g] || {}).label || g} <b>{e.n}</b>
                </button>
              );
            })}
          </div>
        )}
        {months.length > 1 && (
          <div className="months">
            <button className={"mchip" + (!mon ? " on" : "")} aria-pressed={!mon} title="×›×œ ×”×ª×§×•×¤×” â€” ×‘×œ×™ ×¡×™× ×•×Ÿ ×œ×—×•×“×©" onClick={() => setMon("")}>×”×›×•×œ</button>
            {months.map((m) => (
              <button key={m} className={"mchip" + (mon === m ? " on" : "")} aria-pressed={mon === m} onClick={() => setMon(m)}>
                {m.split("-").reverse().join(".")} <b>{vs.filter((x) => x.d.slice(0, 7) === m).length}</b>
              </button>
            ))}
          </div>
        )}
        <div className="tl">
          {/* ×‘×—×™×¨×ª ××™×¨×•×¢ ×—×™×™×‘×ª ×œ×¢×‘×•×“ ×’× ×‘×ž×§×œ×“×ª ×•×‘×§×•×¨× ×ž×¡×š (×¡×¢×™×£ 4) â€”
              ××‘×œ ×‘×œ×™ ×›×¤×ª×•×¨-×‘×ª×•×š-×›×¤×ª×•×¨: ×”×©×•×¨×” × ×©××¨×ª ×œ×—×™×¦×” ×œ×¢×›×‘×¨ ×‘×œ×‘×“,
              ×•×ª×’×™×ª ×”×¡×•×’ ×”×™× ×”×›×¤×ª×•×¨ ×”××ž×™×ª×™ (nested-interactive ×ž×”×‘×™×§×•×¨×ª) */}
          {shown.map(({ v: x, i }) => (
            <div key={x.d + x.k + i} className={"ev" + (i === vs.indexOf(v) ? " sel" : "")}
              onClick={() => setSel(i)}>
              <div className="d">
                {(() => { const ed = evDate(x); return ed.tip
                  ? <TipTag cls={ed.exact ? "" : "approxd"} tip={ed.tip}>{ed.txt}{ed.exact ? "" : " â‰ˆ"}</TipTag>
                  : <span>{ed.txt}</span>; })()}
                {(x.shp || (x.stops || []).length > 1 || (x.pstops || []).length > 1) ? " Â· ðŸ—ºï¸" : ""}
                {(x.shp || (x.stops || []).length > 1) && (
                <button className={"cmpbtn" + (cmpI === i ? " on" : "")}
                  title={cmpI === i ? "×–×• ×’×¨×¡×ª ×”×‘×¡×™×¡ ×œ×”×©×•×•××” â€” ×œ×—×™×¦×” ×ž×‘×˜×œ×ª" : "×§×‘×™×¢×ª ×”×’×¨×¡×” ×”×–×• ×›×‘×¡×™×¡, ×•××– ×œ×—×™×¦×” ×¢×œ ××™×¨×•×¢ ××—×¨ ×ª×©×•×•×” ×ž×•×œ×”"}
                  onClick={(e) => {
                    e.stopPropagation();
                    if (cmpI === i) { setCmpI(null); return; }
                    setCmpI(i);
                    // ×œ×—×™×¦×” ××—×ª ×ž×¡×¤×™×§×”: ×× ×”××™×¨×•×¢ ×”×¤×ª×•×— ×”×•× ×”×‘×¡×™×¡ ×¢×¦×ž×• ××™×Ÿ ×ž×”
                    // ×œ×”×©×•×•×ª, ×•×œ×›×Ÿ × ×¤×ª×—×ª ×ž×•×œ×• ×”×’×¨×¡×” ×”×¢×“×›× ×™×ª ×‘×™×•×ª×¨.
                    if (vs.indexOf(v) === i) {
                      const last = vs.length - 1;
                      setSel(last === i ? Math.max(0, i - 1) : last);
                    }
                  }}>
                  {cmpI === i ? "â‡„ ×‘×¡×™×¡ ×”×”×©×•×•××”" : "â‡„ ×”×©×•×•×”"}</button>)}
              </div>
              <div className="t">
                <button className="k kbtn" style={{ background: (KINDS[dispKind(x, i, vs)] || {}).color || "#64748b" }}
                  aria-current={i === vs.indexOf(v)}
                  aria-label={"×‘×—×™×¨×ª ×”××™×¨×•×¢ ×ž-" + evDate(x).txt + ": " + ((KINDS[dispKind(x, i, vs)] || { label: x.k }).label)}
                  onClick={(e) => { e.stopPropagation(); setSel(i); }}>{(KINDS[dispKind(x, i, vs)] || { label: x.k }).label}</button>
                {x.k === "redraw" && " ×”×’××•×ž×˜×¨×™×” ×ª×•×§× ×” â€” ×¨×¦×£ ×”×ª×—× ×•×ª ×œ× ×”×©×ª× ×”"}
                {/* ×©×™× ×•×™ ×©×¨×¦×£ ×”×ª×—× ×•×ª ×—×–×¨ ×ž×ž× ×• ×ž×™×“. ×‘×œ×™ ×”×¡×™×ž×•×Ÿ ×”×–×” ×”×©×•×¨×”
                    ××•×ž×¨×ª ×©×ª×—× ×•×ª ×™×¨×“×•, ×‘×¢×•×“ ×”×§×• ×¢×•×¦×¨ ×‘×”×Ÿ ×¢×“ ×”×™×•×. */}
                {x.rv ? (
                  <TipTag cls="rvflag" tip="×¨×¦×£ ×”×ª×—× ×•×ª ×—×–×¨ ×‘×“×™×•×§ ×œ×ž×” ×©×”×™×” ×œ×¤× ×™ ×”×©×™× ×•×™ ×”×–×”. ×©×™× ×•×™ ×©×ž×ª×‘×˜×œ ×ž×™×“ ×”×•× ×›×ž×¢×˜ ×ª×ž×™×“ ×ª× ×•×“×” ×‘×¤×¨×¡×•× ×•×œ× ×©×™× ×•×™ ×‘×ž×¡×œ×•×œ">
                    â†© ×—×–×¨ ×›×¢×‘×•×¨ {x.rv === 1 ? "×™×•×" : x.rv + " ×™×ž×™×"}</TipTag>
                ) : x.rvb ? (
                  <TipTag cls="rvflag" tip="×”×©×™× ×•×™ ×”×§×•×“× ×”×ª×‘×˜×œ ×›××Ÿ â€” ×¨×¦×£ ×”×ª×—× ×•×ª ×—×–×¨ ×œ×ž×” ×©×”×™×” ×œ×¤× ×™×•">
                    â†© ×”×—×–×¨×ª ×”×ž×¦×‘ ×”×§×•×“×</TipTag>
                ) : null}
                {/* ×”×“: ××•×ª×Ÿ ×ª×—× ×•×ª × ×•×¡×¤×• ×•××•×ª×Ÿ ×™×¨×“×• ×›×‘×¨ ×§×•×“× ×‘×—×œ×•×¤×” ××—×¨×ª ×©×œ ×”×§×• (×§×•×•×™ ××©×“×•×“: 19.07 ×‘×—×œ×•×¤×•×ª
                    ×”×¨××©×™×•×ª, 13.09 ×‘×—×œ×•×¤×” 6). ×–×” ×©×™× ×•×™ ××ž×™×ª×™ ×‘×—×œ×•×¤×” ×”×–××ª â€” × ×©××¨ ×•×ž×“×•×•×— â€” ××‘×œ ××•×ž×¨×™×
                    ×©×”×•× ×›×‘×¨ × ×›× ×¡ ×§×•×“× ×‘×—×œ×•×¤×” ××—×¨×ª (×©×œ×ž×” 16.09) */}
                {x.echo ? (
                  <TipTag cls="rvflag" tip={"××•×ª×Ÿ ×ª×—× ×•×ª × ×•×¡×¤×• ×•××•×ª×Ÿ ×ª×—× ×•×ª ×™×¨×“×• ×›×‘×¨ ×‘-" + fmtD(x.echo.d) + " ×‘×—×œ×•×¤×” " + x.echo.rd + " ×©×œ ×”×§×•. ×¢×›×©×™×• ×”×©×™× ×•×™ ×”×’×™×¢ ×’× ×œ×—×œ×•×¤×” ×”×–××ª"}>
                    â†» ×”×©×™× ×•×™ ×›×‘×¨ × ×›× ×¡ ×‘-{fmtD(x.echo.d)} ×‘×—×œ×•×¤×” {x.echo.rd}; ×¢×›×©×™×• ×’× ×‘×—×œ×•×¤×” ×”×–××ª</TipTag>
                ) : null}
                {x.note && x.k !== "planned-dropped" && <span className="evnote"> {noteFix(x.note)}</span>}
              </div>
              {/* ×ž××™×¤×” ×”××™×¨×•×¢ ×”×–×” ×”×’×™×¢. ×”×”×¢×¨×•×ª ××ž×¨×• "×ž××¨×›×™×•×Ÿ ×”×¤×™×“ ×”××¨×¦×™"
                  ×‘×œ×™ ×œ× ×§×•×‘ ×‘×©×, ×•××™ ××¤×©×¨ ×”×™×” ×œ×“×¢×ª ×ž×” × ×ž×“×“ ×•×ž×™ ×ž×“×“. */}
              <div className="evsrc">{x.k === "vehicle" ? SRC_LABEL.rishui : x.k === "ltype" ? SRC_LABEL.ctl : (SRC_LABEL[x.src] || SRC_LABEL._daily)}</div>
              {/* ×©×™× ×•×™ ×©×ª×•×›× ×Ÿ ×•×œ× × ×›× ×¡ ×œ×ª×•×§×£: ×ž×” ×§×¨×” ×‘×¡×•×£, ×©× ×™ ×”×ª××¨×™×›×™× (×ž×ª×™ ×”×™×”
                  ××ž×•×¨ ×œ×”×™×›× ×¡, ×ž×ª×™ ×™×¨×“), ×•×ž×” ×”×ª×•×›× ×™×ª ×”×™×™×ª×” ×ž×©× ×” â€” ×‘×ž×§×•× ×ž×¡×¤×¨
                  ×”×ª×—× ×•×ª (×©×œ×ž×” 05.09) */}
              {x.k === "planned-dropped" && (x.ps || x.pc) && (() => { const p = plannedInfo(x, i, vs); return (
                <div className="sub">
                  <div><PlanStatus p={p} /> Â· ðŸ“… ×”×™×” ××ž×•×¨ ×œ×”×™×›× ×¡ ×‘-<b>{fmtD(x.ps)}</b>
                    {x.pc && x.ps && x.pc >= x.ps ? <> Â· ×™×¨×“ ×ž×”×¨×™×©×•× ×‘-<b>{fmtD(x.pc)}</b></> : <> Â· ×‘×•×˜×œ ×‘-<b>{fmtD(x.pc || x.d)}</b>, ×œ×¤× ×™ ×”×ž×•×¢×“</>}
                    {x.sd && gapDays(x.sd, x.pc || x.d) > 1 ? <> (× ×¨××” ×œ××—×¨×•× ×” ×‘-{fmtD(x.sd)})</> : null}
                    {x.pf ? <> Â· ×¤×•×¨×¡× ×œ×¨××©×•× ×” ×‘-{fmtD(x.pf)}</> : null}</div>
                  <PlanLines p={p} max={8} />
                </div>); })()}
              {(x.add || x.rem) && (() => {
                // ×”×–×™×”×•×™ ×œ×¤×™ ×ž×¡×¤×¨ ×ª×—× ×” (x.ac/x.rc, ×ž×™×•×©×¨×™× ×œ×©×ž×•×ª) â€” ×”×©× ×ª×¦×•×’×”
                const addE = (x.add || []).map((n, j) => ({ n, c: x.ac && x.ac[j] != null ? String(x.ac[j]) : null }));
                const remE = (x.rem || []).map((n, j) => ({ n, c: x.rc && x.rc[j] != null ? String(x.rc[j]) : null }));
                const pm = splitPlatformMoves(addE, remE);
                // ×”×—×œ×¤×ª ×ž×§"×˜ (××•×ª×• ×©×, ××•×ª×• ×ž×™×§×•×) ×”×™× ××™×¨×•×¢ ×©×œ ×¨×™×©×•×
                // ×”×ª×—× ×•×ª, ×œ× ×©×œ ×”×§×• â€” ×œ× ×ž×•×¦×’×ª ×›××Ÿ ×‘×›×œ×œ (×‘×§×©×ª ×©×œ×ž×”)
                const rn = splitRenumbers(pm.add, pm.rem, i);
                if (!pm.moves.length && !rn.add.length && !rn.rem.length) return null;
                return (
                  <div className="sub">
                    {pm.moves.map((m, k) => <div key={k}>ðŸ”€ ×ž×¢×‘×¨ ×¨×¦×™×£: {m.base} â€” ×ž×¨×¦×™×£ {m.from} ×œ×¨×¦×™×£ {m.to}</div>)}
                    {rn.add.length > 0 && <div>âž• × ×•×¡×¤×•: {labelList(rn.add, i, true)}</div>}
                    {rn.rem.length > 0 && <div>âž– ×™×¨×“×•: {labelList(rn.rem, i, false)}</div>}
                  </div>
                );
              })()}
            </div>
          ))}
        </div>
      </div>
      <div className="card main">
        <div className="vhead">
          {plannedV ? (plKind === "new" ? <>×§×• ×©×¤×•×¨×¡× ×œ×”×ª×—×œ×” ×‘-<b>{fmtD(v.ps)}</b> ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ</> : <>×©×™× ×•×™ ×ª×—× ×•×ª ×©×¤×•×¨×¡× ×œ-<b>{fmtD(v.ps)}</b> ×•×œ× × ×›× ×¡ ×œ×¤×¢×•×œ</>)
            : cmpOn ? <>×”×©×•×•××” ×©×‘×™×§×©×ª: <b>{evDate(v).txt}</b> ×ž×•×œ <b>{evDate(pv).txt}</b></>
            : <>×’×¨×¡×ª <b>{evDate(v).txt}</b>{prev ? <> ×ž×•×œ ×”×’×¨×¡×” ×©×œ×¤× ×™×” (<b>{evDate(pv).txt}</b>)</> : pv ? "" : " â€” ×”×’×¨×¡×” ×”×ž×ª×•×¢×“×ª ×”×¨××©×•× ×”"}</>}
        </div>
        {plannedV && (
          <div className="mut">
            {v.pc && v.ps && v.pc >= v.ps ? <>×™×¨×“ ×ž×”×¨×™×©×•× ×‘-<b>{fmtD(v.pc)}</b></> : <>×‘×•×˜×œ ×‘-<b>{fmtD(v.pc || v.d)}</b>, ×œ×¤× ×™ ×”×ž×•×¢×“</>}
            {v.pf ? <> Â· ×¤×•×¨×¡× ×œ×¨××©×•× ×” ×‘-{fmtD(v.pf)}</> : null}
            {" Â· "}×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ ×‘×ž×§×•×•×§×•{actualV
              ? (actualV === plInfo.base
                ? <>; ×”×ž×¡×œ×•×œ ×©×”×§×• × ×¡×¢ ×‘×• ×œ×¤× ×™ ×”×ª×•×›× ×™×ª ({plInfo.removedAt ? <>×¢×“ {fmtD(plInfo.removedAt)}</> : <>×ž-{fmtD(actualV.d)}</>}) ×‘××¤×•×¨</>
                : <>; ×”×ž×¡×œ×•×œ ×©× ×›× ×¡ ×‘×¤×•×¢×œ, ×ž-{fmtD(actualV.d)}, ×‘××¤×•×¨</>)
              : <>; ×”×•×•×¨×™×× ×˜ ×œ× × ×¡×¢ ×ž×¢×•×œ×</>}.
          </div>
        )}
        {plannedV && (
          <div className="mut" style={{ fontWeight: 700 }}>
            {plKind === "new"
              ? (plInfo.entered ? <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×ž×•×¢×“ ×”×”×ª×—×œ×” ×‘×œ×‘×“. ×”×•×•×¨×™×× ×˜ ×”×ª×—×™×œ ×‘-{fmtD(plInfo.entered.d)} ×‘××•×ª×• ×ž×¡×œ×•×œ, ×•×œ×›×Ÿ ×©×ª×™ ×”×©×›×‘×•×ª ×‘×ž×¤×” ×—×•×¤×¤×•×ª.</>
                : plInfo.changed ? <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×”×•×•×¨×™×× ×˜ ×‘×ž×¡×œ×•×œ ×”×–×”. ×‘×¤×•×¢×œ ×”×ª×—×™×œ ×‘-{fmtD(plInfo.changed.d)} ×‘×ž×¡×œ×•×œ ×©×•× ×”.</>
                : plInfo.base ? <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×—×–×¨×ª ×”×•×•×¨×™×× ×˜. {plInfo.removedAt ? <>× ×¡×¢ ×¢×“ {fmtD(plInfo.removedAt)}</> : <>×ª×•×¢×“ × ×•×¡×¢ ×œ××—×¨×•× ×” ×‘-{fmtD(plInfo.base.d)}</>}, ×¤×•×¨×¡× ×œ×—×–×•×¨ ×‘-{fmtD(v.ps)} ×•×œ× ×—×–×¨.</>
                : <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×”×•×•×¨×™×× ×˜ ×›×•×œ×•. ×¤×•×¨×¡× ×¢× ×ª××¨×™×š ×”×ª×—×œ×”, ×™×¨×“ ×œ×¤× ×™ ×©×”×ª×—×™×œ ×•×œ× × ×¡×¢ ×ž×¢×•×œ×.</>)
              : (plInfo.entered ? <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×”×ž×•×¢×“ ×‘×œ×‘×“. ×”×©×™× ×•×™ × ×›× ×¡ ×‘×¡×•×£ ×‘-{fmtD(plInfo.entered.d)}.</>
                : plInfo.changed ? <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×”×©×™× ×•×™ ×©×ž×¤×•×¨×˜ ×›××Ÿ. ×ž×” ×©× ×›× ×¡ ×‘-{fmtD(plInfo.changed.d)} ×”×™×” ×©×•× ×” ×ž×ž× ×•.</>
                : plInfo.goneAt ? <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×”×©×™× ×•×™ ×©×ž×¤×•×¨×˜ ×›××Ÿ. ×”×§×• ×¢×¦×ž×• ×™×¨×“ ×ž×”×¨×™×©×•× ×‘-{fmtD(plInfo.goneAt)}.</>
                : <>×ž×” ×œ× ×™×¦× ×œ×¤×•×¢×œ: ×”×©×™× ×•×™ ×©×ž×¤×•×¨×˜ ×›××Ÿ. {plInfo.base ? "×”×ž×¡×œ×•×œ × ×©××¨ ×›×¤×™ ×©×”×™×”." : "×¢×“ ×”×™×•× ×œ× × ×›× ×¡."}</>)}
            <div className="sub" style={{ fontWeight: 400 }}>
              <PlanLines p={plInfo} max={20} />
            </div>
          </div>
        )}
        {/* ××™-×”×•×•×“××•×ª ××™× ×” ×‘×¤×¢×¨ ×©×‘×™×Ÿ ×©×ª×™ ×”×’×¨×¡××•×ª: ×”×ž× ×•×¢ ×¢×•×‘×¨ ×¢×œ ×›×œ ×¦×™×œ×•×ž×™
            ×”××¨×›×™×•×Ÿ, ×•×œ×›×Ÿ ×¤×¢×¨ ××¨×•×š ×‘×™×Ÿ ×’×¨×¡××•×ª ×¤×™×¨×•×©×• ×©×”×ž×¡×œ×•×œ ×‘××ž×ª ×œ× ×”×©×ª× ×”
            ×œ××•×¨×›×•. ××™-×”×•×•×“××•×ª ×”×™×—×™×“×” ×”×™× ×”×ž×¨×•×•×— ×‘×™×Ÿ ×©× ×™ ×¦×™×œ×•×ž×™× ×¡×ž×•×›×™×,
            ×•×–×” ×‘×“×™×•×§ ×ž×” ×©-'sd' ×ž×•×“×“. */}
        {!cmpOn && !evDate(v).exact && (
          <div className="gapwarn">
            â„¹ï¸ ×”×™×•× ×”×ž×“×•×™×§ ××™× ×• ×™×“×•×¢: {evDate(v).tip}.
          </div>
        )}
        {v.k === "times" && v.tb ? (
          /* ×”×œ×•"×– ×”××—×¨×•×Ÿ ×©×œ ×§×• ×ž×‘×•×˜×œ â€” ×¦×™×œ×•× ×ž×”××¨×›×™×•×Ÿ (×‘×§×©×ª ×”×ž×©×ª×ž×©): ×§×•
             ×©×‘×•×˜×œ ×‘×œ×™ ×©×•× ××™×¨×•×¢ ×œ×•"×– ×ž×§×‘×œ, ×©× ×” ××—×¨×™ ×”×‘×™×˜×•×œ, ××ª ×©×¢×•×ª-×”×™×¦×™××”
             ×©×œ×• ×ž×™×•×-×”××¨×›×™×•×Ÿ ×”××—×¨×•×Ÿ ×©×‘×• ×¤×¢×œ */
          <div className="tsnap-wrap">
            <div className="mut">â„¹ï¸ {noteFix(v.note)} â€” × ×›×•×Ÿ ×œ-{fmtD(v.d)}, ×”×™×•× ×”××—×¨×•×Ÿ ×©×‘×• ×”×§×• ×ž×•×¤×™×¢ ×‘××¨×›×™×•×Ÿ.</div>
            <table className="tsnap"><tbody>
              {v.tb.map(([label, ts]) => (
                <tr key={label}>
                  <th>{label}</th>
                  <td>{ts.split(",").map((t, ti) => <span key={ti} className="tt">{t}</span>)}</td>
                </tr>
              ))}
            </tbody></table>
          </div>
        ) : !gv.shp && !((gv.stops || []).length > 1) ? (
          <div className="nogeo">
            â„¹ï¸ {noteFix(v.note) || "××™×Ÿ ×¤×™×¨×•×˜ ×œ×’×¨×¡×” ×–×•"}<br />
            <span className="mut">×¨×©×•×ž×ª-×¢×‘×¨ ×ž××¨×›×™×•×Ÿ ××•×¤×Ÿ ×‘××¡ (×”×¡×“× × ×œ×™×“×¢ ×¦×™×‘×•×¨×™) â€” ×”×ž×¡×œ×•×œ ×”×ž×“×•×™×§ ×œ× ×–×ž×™×Ÿ ×œ×ª×§×•×¤×” ×–×•. ×¨×¦×£ ×”×ª×—× ×•×ª ×™×•×©×œ× ×‘×ž×™×œ×•×™ ×”×œ×™×œ×™ ×ž×©×œ×‘ ×‘×³.</span>
          </div>
        ) : (<>
        {/* ×”×”×©×•×•××” ×¢×•× ×” ×¢×œ "×ž×” ×”×©×ª× ×”", ××‘×œ ×œ× ×¢×œ "××™×š ×”×§×• × ×¨××” ×¢×›×©×™×•" â€”
            ×©×ª×™ ×”×©×›×‘×•×ª ×™×—×“ ×ž×§×©×•×ª ×œ×§×¨×•× ××ª ×”×ž×¡×œ×•×œ ×¢×¦×ž×•. ×”×›×¤×ª×•×¨ ×ž×¡×™×¨ ××ª
            ×©×›×‘×ª ×”×¢×‘×¨ ×•×ž×©××™×¨ ××ª ×”×ž×¡×œ×•×œ ×”×ž×œ× ×›×¤×™ ×©×”×•× ××—×¨×™ ×”×©×™× ×•×™. */}
        {!m12only && prev && !plannedV && (
          <div className="onlycur">
            <button className={"kchip" + (onlyCur ? "" : " on")}
              style={onlyCur ? {} : { borderColor: "#7c3aed", color: "#5b21b6" }}
              onClick={() => setOnlyCur(false)}>â‡„ ×ž×” ×”×©×ª× ×”</button>
            <button className={"kchip" + (onlyCur ? " on" : "")}
              style={onlyCur ? { borderColor: "#7c3aed", color: "#5b21b6" } : {}}
              onClick={() => setOnlyCur(true)}>ðŸšŒ ×”×ž×¡×œ×•×œ ×”×ž×œ× ××—×¨×™ ×”×©×™× ×•×™</button>
          </div>
        )}
        {/* ×—×œ×•× ×™×ª 2012 ×¤×ª×•×—×”: ×‘×¨×™×¨×ª ×”×ž×—×“×œ ×”×™× ×ž×¡×œ×•×œ 2012 ×œ×‘×“, ×›×™ ×©×ª×™ ×”×©×›×‘×•×ª
            ×™×—×“ ×ž×§×©×•×ª ×œ×§×¨×•× ××™×š ×”×§×• × ×¡×¢ ××–; ×”×›×¤×ª×•×¨ ×”×©× ×™ ×ž×¦×™×’ ××ª ×©×ª×™×”×Ÿ. */}
        {show12 && stops12 && stops12.length > 0 && (
          <div className="onlycur">
            <button className={"kchip" + (only12 ? " on" : "")}
              style={only12 ? { borderColor: "#78350f", color: "#78350f" } : {}}
              onClick={() => setOnly12(true)}>ðŸ•°ï¸ ×¨×§ ×ž×¡×œ×•×œ 2012</button>
            <button className={"kchip" + (only12 ? "" : " on")}
              style={only12 ? {} : { borderColor: "#78350f", color: "#78350f" }}
              onClick={() => setOnly12(false)}>â‡„ 2012 ×•×”×™×•× ×™×—×“</button>
          </div>
        )}
        {!m12only && borrowed && (
          <div className="mut">â„¹ï¸ ×”××™×¨×•×¢ ×¢×¦×ž×• ××™× ×• × ×•×©× ×ž×¡×œ×•×œ â€” ×”×ž×¤×” ×ž×¦×™×’×” ××ª ×”×ž×¡×œ×•×œ ×”×ž×ª×•×¢×“
            {gv.d < v.d ? " ×”××—×¨×•×Ÿ ×œ×¤× ×™ ×”××™×¨×•×¢" : " ×”×¨××©×•×Ÿ ××—×¨×™ ×”××™×¨×•×¢"}, ×ž-{fmtD(gv.d)}.</div>
        )}
        {!m12only && !cmpOn && !onlyCur && !plannedV && prev && pgv !== pv && (
          <div className="mut">â„¹ï¸ ×œ×’×¨×¡×” ×”×§×•×“×ž×ª ×”×¡×ž×•×›×” ××™×Ÿ ×¨×¦×£ ×ª×—× ×•×ª ×ž×ª×•×¢×“ â€” "×”×ž×¡×œ×•×œ ×”×§×•×“×" ×•×”×ª×—× ×•×ª ×©×™×¨×“×• ×ž×•×¦×’×™× ×ž×”×ª×™×¢×•×“ ×”××—×¨×•×Ÿ ×©×œ×¤× ×™ ×”××™×¨×•×¢, ×ž-{fmtD(pgv.d)}.</div>
        )}
        {/* addedCodes: codeOf ×ž×¦×¤×” ×œ××™× ×“×§×¡ ×’×¨×¡×” (vi) â€” ×”××™× ×“×§×¡ ×‘×ª×•×š ×¨×©×™×ž×ª
            âž• ×’×¨× ×œ×¡×¨×™×§×” ×ž×”×’×¨×¡××•×ª ×”×™×©× ×•×ª ×‘×™×•×ª×¨, ×•×ª×—× ×” ×¢× ×©× ×–×”×” ×•×ž×§"×˜ ××—×¨
            ×ž×”×¢×‘×¨ ×§×™×‘×œ×” ××ª ×”×¡×™×ž×•×Ÿ ×”×™×¨×•×§ ×‘×ž×§×•× ×”×ª×—× ×” ×©×‘××ž×ª × ×•×¡×¤×” */}
        <DiffMap key={v.d + v.k + (cmpOn ? "c" + cmpI : "") + (onlyCur ? "o" : "") + (m12only ? "12" : "")}
          cur={m12only ? [] : cur} prev={onlyCur || m12only ? null : prev} planned={plannedV && !m12only}
          approx={cmpOn ? !gv.shp : approx} prevApprox={prevApprox} curStops={m12only ? [] : gv.stops}
          prevStops={m12only ? null : plannedV ? (plDiff && !plSame ? actualV.stops : null) : (!onlyCur && comparable && pgv && (pgv.stops || []).length ? pgv.stops : null)}
          addedCodes={!m12only && !onlyCur && (!pv || !(pv.stops || []).length) && (v.add || []).length
            ? new Set((v.add || []).map((n, j) => (v.ac && v.ac[j] != null ? String(v.ac[j]) : codeOf(n, vi, true))).filter(Boolean)) : null}
          stops12={onlyCur ? null : stops12} shape12={onlyCur ? null : shape12}
          sg={onlyCur || cmpOn || m12only ? null : (v.sg || null)}
          plats={m12only ? null : rdPlat}
          remPins={onlyCur || cmpOn || m12only ? null : remPinsOf(v, vi, gv).pins} />
        {/* ×ª×—× ×” ×©×™×¨×“×” ×•××™×Ÿ ×œ×” ×ž×™×§×•× ×‘××£ ×ž×§×•×¨ â€” × ××ž×¨×ª ×‘×ž×¤×•×¨×©, ×œ× × ×¢×œ×ž×ª */}
        {!cmpOn && !onlyCur && !m12only && (() => {
          const un = remPinsOf(v, vi, gv).unplaced
            .filter((n) => !(((pgv && pgv.stops) || []).some((s) => s && s[1] === n)));
          return un.length ? (
            <div className="mut">â„¹ï¸ ×ª×—× ×•×ª ×©×™×¨×“×• ×©××™×Ÿ ×œ×”×Ÿ ×ž×™×§×•× ×‘××£ ×ª×™×¢×•×“, ×•×œ×›×Ÿ ××™× ×Ÿ ×ž×¡×•×ž× ×•×ª ×‘×ž×¤×”: {un.join(", ")}</div>
          ) : null;
        })()}
        <div className="legend">
          {m12only ? null : plannedV ? <>
            <span><i style={{ borderColor: "#9f1239", borderStyle: "dashed" }} /> ×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ ×•×œ× ×™×¦× ×œ×¤×•×¢×œ</span>
            {prev && <span><i style={{ borderColor: "#475569" }} /> ×”×ž×¡×œ×•×œ ×‘×¤×•×¢×œ ×©×œ ×”×•×•×¨×™×× ×˜</span>}
            {plDiff && !plSame && <span><span className="dot" style={{ background: "#16a34a" }} /> ×ª×—× ×” ×©×ª×•×›× × ×” ×•××™× ×” ×‘×ž×¡×œ×•×œ ×‘×¤×•×¢×œ</span>}
            {plDiff && !plSame && <span><span className="dot" style={{ background: "#fff", border: "3px solid #dc2626" }} /> ×ª×—× ×” ×‘×ž×¡×œ×•×œ ×‘×¤×•×¢×œ ×©×œ× ×”×™×™×ª×” ×‘×ª×•×›× ×™×ª</span>}
          </> : <>
          {prev && !onlyCur && <span><i style={{ borderColor: "#dc2626", borderStyle: "dashed" }} /> ×”×ž×¡×œ×•×œ ×”×§×•×“×{prevApprox ? " (×ž×§×•×¨×‘ ×œ×¤×™ ×ª×—× ×•×ª)" : ""}</span>}
          <span><i style={{ borderColor: prev && !onlyCur ? "#16a34a" : "#4c1d95" }} /> {prev && !onlyCur ? "×”×ž×¡×œ×•×œ ×”×—×“×©" : "×”×ž×¡×œ×•×œ"}</span>
          <span><span className="dot" style={{ background: "#16a34a" }} /> ×ª×—× ×” ×©× ×•×¡×¤×”</span>
          <span><span className="dot" style={{ background: "#fff", border: "3px solid #dc2626" }} /> ×ª×—× ×” ×©×™×¨×“×”</span>
          </>}
          {stops12 && stops12.length > 1 && <span><i style={{ borderColor: "#78350f", borderStyle: "dashed" }} /> {sh12
            ? `×ž×¡×œ×•×œ ×ž×©×•×¢×¨ 2012 â€” ×—×™×©×•×‘ ×¢×œ ×›×‘×™×©×™ ×”×™×•× ×“×¨×š ${sh12.n} ×ž-${sh12.tot} ×”×ª×—× ×•×ª ×©×ž×™×§×•×ž×Ÿ ×™×“×•×¢`
            : "×ž×¡×œ×•×œ 2012 (×§×• ×™×©×¨ ×“×¨×š ×”×ª×—× ×•×ª ×©×”×•×¦×œ×‘×•)"}</span>}
        </div>
        {!cmpOn && !onlyCur && !m12only && v.sg && ((v.sg.n || []).length + (v.sg.o || []).length > 0) && (
          <div className="mut">ðŸ” ×”×§×˜×¢ ×©×©×•× ×” ×©×•×¨×˜×˜ ×‘×ž×“×•×™×§ ×ž×¦×™×œ×•×ž×™ ×”××¨×›×™×•×Ÿ â€” ××“×•× ×ž×§×•×•×§×• = ×”×§×˜×¢ ×”×™×©×Ÿ, ×™×¨×•×§ = ×”×—×“×©. ×©××¨ ×”×ž×¡×œ×•×œ ×¢×©×•×™ ×œ×”×™×•×ª ×ž×§×•×¨×‘.</div>
        )}
        {v.shpref
          ? <div className="mut">â„¹ï¸ ×¨×¦×£ ×”×ª×—× ×•×ª ×‘×¦×™×œ×•× ×–×”×” ×œ×’×¨×¡×” ×”×¡×ž×•×›×” â€” ×ž×•×¦×’ ×”×ž×¡×œ×•×œ ×”×ž×œ× ×©×œ×” ×‘×ž×§×•× ×§×• ×ž×§×•×¨×‘. {(v.stops || []).length} ×ª×—× ×•×ª ×‘×’×¨×¡×” ×–×•.</div>
          : plannedV
          ? <div className="mut">â„¹ï¸ {gv.shp ? "×”×©×¨×˜×•×˜ ×”×ž×œ× ×©×œ ×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ, ×›×¤×™ ×©×¤×•×¨×¡× ×‘×¤×™×“" : "×”×ž×¡×œ×•×œ ×©×ª×•×›× ×Ÿ ×ž×¦×•×™×¨ ×›×§×• ×™×©×¨ ×‘×™×Ÿ ×”×ª×—× ×•×ª â€” ×œ×ª×•×›× ×™×ª ×”×–×• ×œ× × ×©×ž×¨ ×©×¨×˜×•×˜"}.</div>
          : approx
          ? <div className="mut">â„¹ï¸ ×ž×¡×œ×•×œ ×ž×§×•×¨×‘ â€” ×§×• ×™×©×¨ ×‘×™×Ÿ ×”×ª×—× ×•×ª ×œ×¤×™ ×¨×¦×£ ×ž××¨×›×™×•×Ÿ ××•×¤×Ÿ ×‘××¡; ×”×’××•×ž×˜×¨×™×” ×”×ž×œ××” ×œ× ×–×ž×™× ×” ×œ×ª×§×•×¤×” ×–×•. {(gv.stops || []).length} ×ª×—× ×•×ª{borrowed ? " ×‘×’×¨×¡×” ×”×ž×•×¦×’×ª" : " ×‘×’×¨×¡×” ×–×•"}.</div>
          : <div className="mut">ðŸ” ×”×’××•×ž×˜×¨×™×” × ×©×ž×¨×ª ×‘×ž×œ×•××”, ×‘×œ×™ ×“×™×œ×•×œ â€” ×’× ×ª×™×§×•×Ÿ ×©×¨×˜×•×˜ ×©×œ ×›×ž×” ×ž×˜×¨×™× ×™×™×¨××” ×›××Ÿ. {(gv.stops || []).length} ×ª×—× ×•×ª{borrowed ? " ×‘×’×¨×¡×” ×”×ž×•×¦×’×ª" : " ×‘×’×¨×¡×” ×–×•"}.</div>}
        </>)}
        {(v.tl || v.tn) && <TimesDiff tl={v.tl} tn={v.tn} />}
      </div>
    </div>
  );
}

/* ---------- ×ž×¤×ª ××™×¨×•×¢ ×ª×—× ×”: ×ž×™×§×•× ×™×©×Ÿ ×ž×•×œ ×—×“×© ---------- */
function StopEvMap({ ev }) {
  const ref = useRef(null);
  useEffect(() => {
    if (!ref.current) return;
    const map = L.map(ref.current, { scrollWheelZoom: false });
    L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">OpenStreetMap</a>', maxZoom: 19,
    }).addTo(map);
    const pts = [[ev.la, ev.lo]];
    if (ev.k === "moved" && ev.ola != null) {
      pts.push([ev.ola, ev.olo]);
      L.polyline([[ev.ola, ev.olo], [ev.la, ev.lo]], { color: "#2563eb", weight: 3, dashArray: "5 7", opacity: 0.9 }).addTo(map);
      L.circleMarker([ev.ola, ev.olo], { radius: 9, color: "#dc2626", weight: 3, fillColor: "#fff", fillOpacity: 1 })
        .addTo(map).bindPopup(`<b>×”×ž×™×§×•× ×”×™×©×Ÿ</b><br><span class="pcode" dir="ltr">(${ev.ola}, ${ev.olo})</span>`, { className: "lh-pop" });
    }
    L.circleMarker([ev.la, ev.lo], { radius: 9, color: "#fff", weight: 2,
      fillColor: ev.k === "moved" ? "#16a34a" : ((SKINDS[ev.k] || {}).color || "#2563eb"), fillOpacity: 1 })
      .addTo(map).bindPopup(`<b>${esc(ev.n || ev.nn || "")}</b>${ev.k === "moved" ? "<br>×”×ž×™×§×•× ×”×—×“×©" : ev.k === "gtfs2012" ? "<br>×”×ž×™×§×•× ×‘×¨×™×©×•× 2012" : ""}<br><span class="pcode" dir="ltr">(${ev.la}, ${ev.lo})</span>`, { className: "lh-pop" });
    map.fitBounds(L.latLngBounds(pts).pad(0.6), { maxZoom: 17 });
    return () => map.remove();
  }, [ev]);
  return <div className="smap" ref={ref} role="img"
    aria-label={ev.k === "moved" ? "×ž×¤×”: ×”×–×–×ª ×”×ª×—× ×” ×ž×”×ž×™×§×•× ×”×™×©×Ÿ ×œ×—×“×©, " + (ev.dist || ev.m || "") + " ×ž×˜×¨×™×" : ev.k === "gtfs2012" ? "×ž×¤×”: ×ž×™×§×•× ×”×ª×—× ×” ×‘×¨×™×©×•× 2012" : "×ž×¤×”: ×ž×™×§×•× ×”×ª×—× ×” " + (ev.n || ev.nn || "")} />;
}

/* ---------- ×¢×ž×•×“ ×§×• ×©×œ 2012 ---------- */
// ×§×• ×©×œ 2012 × ×¤×ª×— ×§×•×“× ×›×¨×©×™×ž×ª ×ª×—× ×•×ª ×‘×ª×•×š ×©×•×¨×” ×‘×¤×™×“, ×‘×œ×™ ×ž×¤×” ×•×‘×œ×™ ×›×ª×•×‘×ª
// ×ž×©×œ×•. ×–×” ×¢×ž×•×“ ×œ×›×œ ×“×‘×¨: ×ž×¡×œ×•×œ ×¢×œ ×”×ž×¤×” ×“×¨×š ×”×ª×—× ×•×ª ×©×”×•×¦×œ×‘×• ×œ×ž×§"×˜, ×¨×¦×£
// ×”×ª×—× ×•×ª, ×•×ž×¢×‘×¨ ×œ×§×• ×©×œ ×”×™×•× ×›×©×™×© ×›×–×”.
function Map2012({ stops, shape }) {
  const ref = useRef(null);
  useEffect(() => {
    if (!ref.current) return;
    const map = L.map(ref.current, { scrollWheelZoom: false });
    L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">OpenStreetMap</a>', maxZoom: 19,
    }).addTo(map);
    // ×ž×™×§×•× ×”×ª×—× ×”: ×ž×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ 2012 ×¢×¦×ž×• (GTFS ×™×•× ×™ 2012 ×“×¨×š OpenStreetMap,
    // changeset 12028672 â€” s[7] === 1), ×•×× ×”×ª×—× ×” ×œ× × ×ž×¦××” ×©×, ×ž×”×¨×™×©×•× ×©×œ ×”×™×•×. ×ª×—× ×” ×©×œ×
    // ×”×•×¦×œ×‘×” ×œ×ž×§"×˜ ××™× ×” ×¢×œ ×”×ž×¤×”. ×›×©×—×•×©×‘ ×ž×¡×œ×•×œ ×ž×©×•×¢×¨ ×¢×œ ×”×›×‘×™×©×™× (shape,
    // tools/shape_2012.py) ×”×•× ×ž×¦×•×™×¨; ××—×¨×ª ×§×• ×™×©×¨ ×‘×™×Ÿ ×”×ª×—× ×•×ª, ×ž×§×•×•×§×•.
    const pts = stops.filter((s) => s[5] != null && s[6] != null).map((s) => [s[5], s[6]]);
    const road = shape && shape.length > 1 ? shape : null;
    if (road) L.polyline(road, { color: "#78350f", weight: 5, opacity: 0.85, dashArray: "10 7" }).addTo(map);
    else if (pts.length > 1) L.polyline(pts, { color: "#78350f", weight: 4, opacity: 0.85, dashArray: "6 8" }).addTo(map);
    stops.forEach((s, i) => {
      if (s[5] == null) return;
      const last = i === stops.length - 1;
      L.circleMarker([s[5], s[6]], { radius: i === 0 || last ? 8 : 5, weight: 2,
        color: i === 0 || last ? "#fff" : "#78350f",
        fillColor: i === 0 ? "#16a34a" : last ? "#dc2626" : "#fff", fillOpacity: 1 })
        .addTo(map).bindPopup(`<b>${esc(s[1])}</b><br><span class="pst">×ª×—× ×” ${s[0]} ×‘×ž×¡×œ×•×œ 2012</span>` +
          (s[4] && s[4].length === 1 ? `<br><span class="pcode">×ž×§×´×˜ ${esc(String(s[4][0]))}</span>` : ""),
          { className: "lh-pop", offset: [0, -4] });
    });
    const all = pts.concat(road || []);
    if (all.length) map.fitBounds(L.latLngBounds(all).pad(0.15), { maxZoom: 16 });
    else map.setView([31.5, 34.9], 8);
    return () => map.remove();
  }, [stops, shape]);
  return <div className="map" ref={ref} role="img"
    aria-label={"×ž×¤×ª ×ž×¡×œ×•×œ 2012 ×“×¨×š " + (stops || []).filter((x) => x[5] != null).length + " ×ª×—× ×•×ª ×©×”×•×¦×œ×‘×• ×œ×ž×™×§×•×"} />;
}

function Line2012Page({ k12, anchorRd, openLine, onBack }) {
  const [d, setD] = useState(null);
  const [sh, setSh] = useState(null);     // ×ž×¡×œ×•×œ×™× ×ž×©×•×¢×¨×™× ×¢×œ ×”×›×‘×™×©×™×, ×× ×—×•×©×‘×•
  const [ri, setRi] = useState(0);
  useEffect(() => {
    setD(null); setSh(null); setRi(0);
    fetch("../magihim-2012/data/l" + k12 + ".json?v=" + BUILD)
      .then((r) => (r.ok ? r.json() : null)).then(setD).catch(() => setD(false));
    dfetch("../magihim-2012/data/shapes/l" + k12 + ".json")
      .then((r) => (r.ok ? r.json() : { routes: {} })).then(setSh).catch(() => setSh({ routes: {} }));
  }, [k12]);
  if (d === null) return <div className="card">×˜×•×¢×Ÿâ€¦</div>;
  if (!d) return <div className="card"><button className="back" onClick={onBack}>â†’ ×—×–×¨×”</button>
    <div className="empty">×”×§×• ×œ× × ×ž×¦× ×‘×¦×™×œ×•× 2012.</div></div>;
  const r = (d.routes || [])[ri] || (d.routes || [])[0] || { stops: [] };
  const stops = r.stops || [];
  const matched = stops.filter((s) => s[5] != null).length;
  const rsh = (sh && sh.routes && sh.routes[String((d.routes || []).indexOf(r))]) || null;
  const shape = rsh ? decodeShape(rsh.pl) : null;
  return (
    <div className="card">
      <button className="back" onClick={onBack}>â†’ ×—×–×¨×”</button>
      <div className="linehead">
        <span className="badge">{d.no}</span>
        <span className="dest">{d.dest}</span>
        <span className="k" style={{ background: "#78350f" }}>×¦×™×œ×•× 2012</span>
        {/* ×©×™×ª×•×£ ×›×ž×• ×‘×¢×ž×•×“ ×§×• ×©×œ ×”×™×•×: ×“×£-×©×™×ª×•×£ ×™×™×¢×•×“×™ (s/k-*.html) ×©×ž×¦×™×’ ×‘×•×•××˜×¡××¤
            ××ª ×ž×¡×¤×¨ ×”×§×• ×•××ª ×¡×ž×œ ×”××ª×¨ â€” ×œ×§×•×•×™ 2012 ×œ× ×”×™×” ×›×–×” (×©×œ×ž×” 07.09) */}
        <button className="sharebtn" title="×©×™×ª×•×£ ×”×§×™×©×•×¨ ×œ×§×• ×”×–×” ×›×¤×™ ×©×”×™×” ×‘-2012"
          onClick={(e) => {
            const url = location.origin + location.pathname.replace(/line-history\/?[^/]*$/, "") + "s/k-" + fsafe(k12) + ".html";
            const b = e.currentTarget;
            if (navigator.share) { navigator.share({ title: "×”×§×• ×‘×–×ž×Ÿ â€” ×§×• " + (d.no || k12) + " (2012)", url }).catch(() => {}); return; }
            const t = b.textContent;
            const done = () => { b.textContent = "âœ“ ×”×•×¢×ª×§"; setTimeout(() => { b.textContent = t; }, 1500); };
            if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(url).then(done, () => {});
          }}>ðŸ”— ×©×™×ª×•×£</button>
      </div>
      <div className="facts">{d.an} Â· {stops.length} ×ª×—× ×•×ª Â· {(d.routes || []).length} ×ž×¡×œ×•×œ×™× Â·
        {" "}{matched} ×ª×—× ×•×ª ×”×•×¦×œ×‘×• ×œ×ž×§"×˜ Â· {stops.filter((s) => s[7] === 1).length} ×ž×”×Ÿ ×ž×¨×™×©×•× 2012 ×¢×¦×ž×•
        {anchorRd && <> Â· <a className="totoday" href={lineHref(anchorRd)}
          onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); openLine(anchorRd); }}>×”×§×• ×©×œ ×”×™×•× â†</a></>}
      </div>
      {(d.routes || []).length > 1 && (
        <div className="s12chips">{d.routes.map((x, i) => (
          <button key={x.rid} className={"rchip12" + (i === ri ? " on" : "")} onClick={() => setRi(i)}>
            {x.f} â† {x.l} ({x.n})</button>
        ))}</div>
      )}
      <Map2012 stops={stops} shape={shape} />
      <div className="legend">
        <span><i style={{ borderColor: "#78350f", borderStyle: "dashed" }} /> {rsh
          ? `×ž×¡×œ×•×œ ×ž×©×•×¢×¨ â€” ×—×™×©×•×‘ ×¢×œ ×›×‘×™×©×™ ×”×™×•× ×“×¨×š ${rsh.n} ×ž-${rsh.tot} ×”×ª×—× ×•×ª, ×›-${(rsh.m / 1000).toFixed(1)} ×§"×ž`
          : "×ž×¡×œ×•×œ 2012 (×§×• ×™×©×¨ ×“×¨×š ×”×ª×—× ×•×ª ×©×”×•×¦×œ×‘×•)"}</span>
        <span><i className="dot" style={{ background: "#16a34a" }} /> ×¨××©×•× ×”</span>
        <span><i className="dot" style={{ background: "#dc2626" }} /> ××—×¨×•× ×”</span>
      </div>
      <ol className="s12">
        {stops.map((s) => (
          <li key={s[0]}>{s[1]}{" "}
            {s[4] && s[4].length === 1 ? <span className="pcode">×ž×§×´×˜ {s[4][0]}</span>
              : s[4] && s[4].length > 1 ? <span className="pcode">{s[4].length} ×ž×§×´×˜×™× ××¤×©×¨×™×™×</span>
                : <span className="pcode">×œ× ×”×•×¦×œ×‘×”</span>}
          </li>
        ))}
      </ol>
      <div className="katnote">â„¹ï¸ ×ª×—× ×•×ª ×”×ž×¡×•×ž× ×•×ª "2012": ×”×ž×§"×˜ ×•×”×ž×™×§×•× ×ž×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×ž×™×•× ×™ 2012 (×§×•×‘×¥ GTFS),
        ×›×¤×™ ×©×™×•×‘× ×œ-<a href="https://www.openstreetmap.org/changeset/12028672" target="_blank" rel="noopener">OpenStreetMap ×‘-26.06.2012</a> (33,055 ×ª×—× ×•×ª, ×¨×™×©×™×•×Ÿ ODbL).
        ×¨×™×©×•× 2012 ×§×•×“× ×œ×›×œ ×”×¦×œ×‘×” ××—×¨×ª; ×ª×—× ×” ×©×œ× × ×ž×¦××” ×‘×• ×”×•×¦×œ×‘×” ×œ×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ ×”×™×•×. ×ª×—× ×” ×©×œ× ×”×•×¦×œ×‘×” ×›×œ×œ ××™× ×” ×ž×•×¤×™×¢×” ×¢×œ ×”×ž×¤×”{rsh
          ? ", ×•×”×ž×¡×œ×•×œ ×‘×™× ×™×”×Ÿ ×”×•× ×”×¢×¨×›×”: × ×¡×™×¢×ª ××•×˜×•×‘×•×¡ ×‘×›×‘×™×©×™× ×©×œ ×”×™×•× ×“×¨×š ×”×ª×—× ×•×ª ×”×™×“×•×¢×•×ª, ×œ×¤×™ ×”×¡×“×¨. ×›×‘×™×© ×©× ×¤×ª×— ×ž××– 2012 ××• ×ª×—× ×” ×©×œ× ×”×•×¦×œ×‘×” ×™×›×•×œ×™× ×œ×¢×§× ××•×ª×•."
          : ", ×•×œ×›×Ÿ ×”×§×• ×ž×§×•×•×§×•."}</div>
    </div>
  );
}

/* ---------- ×©×™× ×•×™×™× ×œ×¤×™ ×™×•× (×›×œ ×”×§×•×•×™×) ---------- */
/* ---------- ×ª×•×¦××•×ª ×ž×¦×™×œ×•× 2012 ---------- */
// ×—×™×¤×•×© "548" ×”×—×–×™×¨ ××ª ×”×§×• ×›×¤×™ ×©×”×•× ×”×™×•× â€” ×§×¨×™×ª ×ž×œ××›×™ ×œ×›×¤×¨ ×—×‘"×“ â€” ×•×œ× ×¨×ž×–
// ×©×‘-2012 ×”×™×” 548 ××—×¨, ×©×œ ××’×“, ×ž×§×¨×™×ª ×ž×œ××›×™ ×œ×‘× ×™ ×‘×¨×§. ×”×•× ×œ× × ×¢×œ× ×ž×”××ª×¨:
// ×”×•× × ×ž×¦× ×¨×§ ×‘×¦×™×œ×•× 2012, ×›×™ ×”××¨×›×™×•×Ÿ ×©×œ ×”×¤×™×“ ×ž×ª×—×™×œ ×‘×ž×¨×¥ 2017 ×•×”×§×• ×›×‘×¨ ×œ×
// ×”×™×” ×©×. ×ž×™ ×©×ž×—×¤×© ×ž×¡×¤×¨ ×§×• ×¦×¨×™×š ×œ×¨××•×ª ×’× ××ª ×–×”.
// ×—×™×¤×•×© ×‘×§×•×•×™ 2012: "1 ×§×¨×™×ª ×ž×œ××›×™" = ×ž×¡×¤×¨ ×§×• + ×˜×§×¡×˜ (×©×œ×ž×” 06.09: ×”×—×™×¤×•×© ×œ×
// ×ž×¦× ×›×œ×•×, ×›×™ ×”×ž×—×¨×•×–×ª ×›×•×œ×” ×”×•×©×•×•×ª×” ×œ×ž×¡×¤×¨ ×”×§×•). ×”×ª×•×¦××•×ª ×ž×ž×•×™× ×•×ª ×œ×¤×™ ×ž×¡×¤×¨
// ×”×§×•, ×•×ž×¡×¤×¨ ×ž×“×•×™×§ ×œ×¤× ×™ ×ž×¡×¤×¨ ×©×¨×§ ×ž×ª×—×™×œ ×›×š (×—×™×¤×•×© "×§×¨×™×ª ×ž×œ××›×™" ×”×—×–×™×¨ ××ª 301 ×¨××©×•×Ÿ).
function parse12(needle) {
  const m = /^\s*(\d+[×-×ª]?)?\s*(.*)$/.exec(needle || "");
  return { no: (m && m[1]) || "", text: ((m && m[2]) || "").trim() };
}
function match12(v, needle) {
  const { no, text } = parse12(needle);
  if (!no && !text) return true;
  const vno = String(v.no || "");
  if (no && !(vno === no || (!text && vno.startsWith(no)))) return false;
  if (text && !((v.dest || "").includes(text) || (v.an || "").includes(text))) return false;
  return true;
}
function lineNum(no) { const m = /^(\d+)/.exec(String(no || "")); return m ? parseInt(m[1], 10) : 1e9; }
function sort12(list, needle) {
  const { no } = parse12(needle);
  return list.slice().sort((a, b) => {
    if (no) { const ea = String(a.no) === no ? 0 : 1, eb = String(b.no) === no ? 0 : 1; if (ea !== eb) return ea - eb; }
    return lineNum(a.no) - lineNum(b.no) || String(a.no).localeCompare(String(b.no), "he") || (a.dest || "").localeCompare(b.dest || "", "he");
  });
}
function Res2012({ needle, onOpen }) {
  const [idx12, setIdx12] = useState(null);
  useEffect(() => {
    if (!needle || idx12) return;
    fetch("../magihim-2012/data/index.json?v=" + BUILD)
      .then((r) => (r.ok ? r.json() : { lines: [] }))
      .then(setIdx12).catch(() => setIdx12({ lines: [] }));
  }, [needle, idx12]);
  if (!needle || !idx12) return null;
  const list = sort12((idx12.lines || []).filter((v) => match12(v, needle)), needle).slice(0, 12);
  if (!list.length) return null;
  return (
    <div className="r12">
      <div className="r12head">×‘×¦×™×œ×•× 2012 × ×ž×¦××• ×’× <b>{list.length}</b> ×§×•×•×™× ×ª×•××ž×™× â€”
        ×¨×©×ª ×ž×’×™×¢×™×, ×—×ž×© ×©× ×™× ×œ×¤× ×™ ×ª×—×™×œ×ª ×”××¨×›×™×•×Ÿ</div>
      {list.map((v) => (
        <a key={v.k} className="lrow" href={"#2012/" + v.k}
          onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); onOpen(v.k); }}>
          <span className="badge sm">{v.no}</span>
          <span className="k" style={{ background: "#78350f" }}>2012</span>
          <span className="ldest">{v.dest}</span>
          <span className="lmeta">{v.an} Â· {v.ns} ×ª×—× ×•×ª Â· ×¨×¦×£ ×”×ª×—× ×•×ª â†</span>
        </a>
      ))}
    </div>
  );
}

/* ---------- ×©×™× ×•×™×™× ×œ×¤×™ ×™×•× (×›×œ ×”×§×•×•×™×) ---------- */
/* ---------- ×‘×™×˜×•×œ×™× ---------- */
// ×”×™×” ×›××Ÿ ×ž×¡×š ×ž×§×•×‘×¥ ×ž×©×œ×• â€” ×›×•×ª×¨×ª, ×ž×•× ×™× ×•×©×•×¨×ª ×©× ×™×. ×–×• ×”×™×™×ª×” ×§×˜×’×•×¨×™×” ×‘×ª×•×š
// ×§×˜×’×•×¨×™×”: ×ž×™ ×©×ž×¡×ž×Ÿ "×ž×‘×•×˜×œ" ×ž×¦×¤×” ×œ×¨×©×™×ž×ª ×”×§×•×•×™× ×”×¨×’×™×œ×”, ×›×ž×• ×‘×›×œ ×§×˜×’×•×¨×™×”
// ××—×¨×ª, ×•×œ× ×œ×ž×¡×š ××—×¨ ×¢× ×—×•×§×™× ××—×¨×™×. ×”×‘×™×˜×•×œ×™× ×ž×•×¦×’×™× ×‘×¨×©×™×ž×” ×”×¨×’×™×œ×”.

function DayFeed({ idx, openLine, open12, onBack, kats, embedded }) {
  const [months, setMonths] = useState(null);
  // ×”×©× ×” ×•×”×—×•×“×© ×©× ×‘×—×¨×• × ×©×ž×¨×™× ×œ×œ×©×•× ×™×ª: ×¤×ª×™×—×ª ×§×• ×•×—×–×¨×” ×”×—×–×™×¨×• ×œ-2026 (×©×œ×ž×” 13.09)
  const [yr, setYr] = useState(() => { try { return sessionStorage.getItem("lh-day-yr") || ""; } catch (e) { return ""; } });
  const [mon, setMon] = useState(() => { try { return sessionStorage.getItem("lh-day-mon") || ""; } catch (e) { return ""; } });
  useEffect(() => { try { sessionStorage.setItem("lh-day-yr", yr); sessionStorage.setItem("lh-day-mon", mon); } catch (e) { /* ignore */ } }, [yr, mon]);
  const [chs, setChs] = useState(null);
  const [q, setQ] = usePersistedQ("lh-q-day");
  const [lim, setLim] = useState(300);
  const [dayF, setDayF] = useState(null);   // ×œ×—×™×¦×” ×¢×œ ×›×•×ª×¨×ª ×™×•× ×ž×¦×™×’×” ×¨×§ ××•×ª×•
  useEffect(() => setLim(300), [q, mon]);
  useEffect(() => setDayF(null), [mon]);
  // ×™×¢×“ ×•×ž×¤×¢×™×œ ×œ× ×ž×©×•×›×¤×œ×™× ×‘×§×•×‘×¦×™ ×”×—×•×“×© â€” × ×©×œ×¤×™× ×ž×”××™× ×“×§×¡ ×œ×¤×™ ×ž×§"×˜
  const meta = useMemo(() => { const m = {}; ((idx && idx.lines) || []).forEach((l) => { m[l.rd] = l; }); return m; }, [idx]);
  // ×œ×©×•× ×™×ª 2012: ×›×œ ×§×•×•×™ ×”×¦×™×œ×•×. ×›×œ ×©×•×¨×” ×ž×•×‘×™×œ×” ×œ×¢×ž×•×“ ×©×œ ××•×ª×• ×§×• ×›×¤×™
  // ×©×”×™×” ×‘-2012 â€” ×ž×¤×” ×•×¨×¦×£ ×ª×—× ×•×ª â€” ×•×œ× × ×¤×ª×—×ª ×‘×ª×•×š ×”×©×•×¨×”
  const [a12, setA12] = useState(null);
  const [idx12, setIdx12] = useState(null);
  useEffect(() => { getAnchors2012().then((d) => setA12(d.anchors || {})); }, []);
  useEffect(() => {
    dfetch("../magihim-2012/data/index.json")
      .then((r) => (r.ok ? r.json() : { lines: [] }))
      .then(setIdx12).catch(() => setIdx12({ lines: [] }));
  }, []);
  const k2rd = useMemo(() => {
    const m = {};
    for (const [rd, v] of Object.entries(a12 || {})) if (!(v.k in m)) m[v.k] = rd;
    return m;
  }, [a12]);
  const rows12 = useMemo(() =>
    (((idx12 && idx12.lines) || []).map((l) => ({ ...l, rd: k2rd[l.k] || null }))),
  [idx12, k2rd]);
  const [mErr, setMErr] = useState(false);
  const [chErr, setChErr] = useState(false);
  const [rty, setRty] = useState(0);
  useEffect(() => {
    let ok = true;
    setMErr(false);
    getMonths()
      .then((d) => {
        if (!ok) return;
        // ×ž×ž×™×™× ×™× ×›××Ÿ ×‘×ž×§×•× ×œ×¡×ž×•×š ×¢×œ ×¡×“×¨ ×”×§×•×‘×¥: ×¨×™×¦×ª ×©×œ×‘ ×‘' ×”×¤×›×” ××ª
        // months ×œ×™×•×¨×“, ×•×”×”× ×—×” "×”××™×‘×¨ ×”××—×¨×•×Ÿ ×”×•× ×”× ×•×›×—×™" ×©×œ×—×” ××ª ×”×¤×™×“
        // ×œ×ž×¨×¥ 2017. ×ž×™×•×Ÿ ×ž×§×•×ž×™ ×¢×•×œ×” ×ž× ×ª×§ ××ª ×”×ª×œ×•×ª ×‘×›×™×•×•×Ÿ ×©×‘×“×™×¡×§.
        const ms = (d.months || []).slice().sort(); setMonths(ms);
        // ×œ× ×“×•×¨×¡×™× ×‘×—×™×¨×” ×©×›×‘×¨ × ×¢×©×ª×” â€” ×›× ×™×¡×” ×ž×›×ª×•×‘×ª â€Ž#2012/<k>â€Ž ×§×•×‘×¢×ª
        // ××ª ×”×©× ×” ×œ×¤× ×™ ×©×”×—×•×“×©×™× × ×˜×¢× ×™×. ××—×¨×™ ×”×ž×™×•×Ÿ ×”××™×‘×¨ ×”××—×¨×•×Ÿ ×”×•×
        // ×”×—×•×“×© ×”× ×•×›×—×™.
        const last = ms[ms.length - 1] || "";
        if (ms.length) { setYr((cur) => cur || last.slice(0, 4)); setMon((cur) => cur || last); }
      })
      .catch(() => { if (ok) { setMErr(true); setMonths([]); } });
    return () => { ok = false; };
  }, [rty]);
  useEffect(() => {
    if (!mon) return;
    // ×ž×¢×‘×¨ ×ž×”×™×¨ ×‘×™×Ÿ ×—×•×“×©×™× ×‘×¨×©×ª ××™×˜×™×ª: ×‘×œ×™ ×”×‘×™×˜×•×œ, ×ª×©×•×‘×” ××™×˜×™×ª ×©×œ ×”×—×•×“×©
    // ×”×§×•×“× ×¢×œ×•×œ×” ×œ× ×—×•×ª ××—×¨×•× ×” ×•×œ×”×¦×™×’ ×¨×©×™×ž×” ×©×œ ×—×•×“×© ××—×“ ×ª×—×ª ×›×•×ª×¨×ª ×©×œ ××—×¨
    let ok = true;
    setChs(null); setChErr(false);
    dfetch("data/changes/" + mon + ".json")
      .then((r) => (r.ok ? r.json() : { changes: [] }))
      .then((d) => { if (ok) setChs((d.changes || []).filter((c) => !hiddenEv(c))); })
      .catch(() => { if (ok) { setChErr(true); setChs([]); } });
    return () => { ok = false; };
  }, [mon, rty]);
  const wrap = embedded ? "" : "card";
  if (months === null) return <div className={wrap}>×˜×•×¢×Ÿâ€¦</div>;
  if (mErr) return <div className={wrap}><NetErr onRetry={() => { setMonths(null); setRty((n) => n + 1); }} /></div>;
  const needle = q.trim();
  // ×”×¤×™×“ ×™×•×©×‘ ×‘×˜××‘ "×§×•×•×™×", ×©×”×•× ×˜××‘ ×”××•×˜×•×‘×•×¡×™×. ×¨×›×‘×ª ×•×ž×•× ×™×•×ª ×©×™×¨×•×ª ×”×Ÿ
  // ×˜××‘×™× ×ž×©×œ×”×Ÿ, ×•×›×©×”×Ÿ ×”×•×¤×™×¢×• ×›××Ÿ ×”×Ÿ ×’× ×”×’×™×¢×• ×‘×œ×™ ×ž×¡×¤×¨ ×§×• â€” ×ª×’ ×¨×™×§.
  // ×¡×™× ×•×Ÿ ×œ×¤×™ ×”×§×˜×’×•×¨×™×•×ª ×”×ž×¡×•×ž× ×•×ª ×‘×¢×ž×•×“ (×©×œ×ž×” 13.09: "×©×™× ×•×™×™× ×œ×¤×™ ×™×•×" ×‘××•×ª×• ×¢×ž×•×“)
  const inKats = (c) => !kats || !kats.size || kats.has(c.k) ||
    (kats.has("stops") && /^stops/.test(c.k)) || (kats.has("terminal") && (c.k === "extend" || c.k === "shorten")) ||
    (kats.has("removed") && c.k === "removed");
  const list = (chs || []).filter((c) => {
    const m = meta[c.rd] || {};
    if (m.tt && m.tt !== "demand") return false;
    if (!inKats(c)) return false;
    return !needle || c.line.includes(needle) || sQ(m.dest).includes(sQ(needle)) ||
      sQ(m.op).includes(sQ(needle)) || c.rd.includes(needle);
  });
  const days = []; const byd = new Map();
  for (const c of list) { let g = byd.get(c.d); if (!g) { g = []; byd.set(c.d, g); days.push(c.d); } g.push(c); }
  days.sort().reverse();
  const WD = ["×¨××©×•×Ÿ", "×©× ×™", "×©×œ×™×©×™", "×¨×‘×™×¢×™", "×—×ž×™×©×™", "×©×™×©×™", "×©×‘×ª"];
  let shown = 0;
  return (
    <div className={wrap}>
      {!embedded && <button className="back" title="×—×–×¨×” ×œ×ž×¡×š ×”×—×™×¤×•×© ×”×¨××©×™ â€” ×”×˜×§×¡×˜ ×©×—×™×¤×©×ª× × ×©×ž×¨" onClick={onBack}>â†’ ×—×–×¨×” ×œ×—×™×¤×•×© ×”×§×•×•×™×</button>}
      <div className="months">
        {/* ×ž×”×—×“×©×” ×œ×™×©× ×”, ×›×ž×• ×”×—×•×“×©×™× ×‘×ª×•×š ×›×œ ×©× ×”. 2012 ××™× ×” ×ž×’×™×¢×”
            ×ž-months.json ××œ× ×ž×¦×™×œ×•× × ×¤×¨×“, ×•×œ×›×Ÿ ×”×™× × ×›×ª×‘×ª ×‘× ×¤×¨×“ â€” ×•×‘×¡×“×¨
            ×™×•×¨×“ ×ž×§×•×ž×” ×”× ×›×•×Ÿ ×”×•× ×‘×¡×•×£, ×›×©× ×” ×”×™×©× ×” ×ž×›×•×œ×Ÿ. */}
        {[...new Set(months.map((m) => m.slice(0, 4)))].sort().reverse().map((y) => (
          <button key={y} className={"mchip" + (yr === y ? " on" : "")} aria-pressed={yr === y}
            title={"×”×¦×’×ª ×”×©×™× ×•×™×™× ×©×œ ×©× ×ª " + y} onClick={() => { setYr(y); const ms = months.filter((m) => m.startsWith(y)); if (!ms.includes(mon)) setMon(ms[ms.length - 1]); }}>{y}</button>
        ))}
        <button className={"mchip" + (yr === "2012" ? " on" : "")} aria-pressed={yr === "2012"} title="×¨×©×ª ×”×§×•×•×™× ×”×ž×œ××” ×›×¤×™ ×©×¦×•×œ×ž×” ×‘-2012 â€” 3,214 ×§×•×•×™× ×ž××ª×¨ ×ž×’×™×¢×™×"
          onClick={() => { setYr("2012"); setMon(""); }}>2012</button>
      </div>
      {yr && yr !== "2012" && (
        <div className="months">
          {months.filter((m) => m.startsWith(yr)).slice().reverse().map((m) => (
            <button key={m} className={"mchip" + (mon === m ? " on" : "")} aria-pressed={mon === m} title="×”×¦×’×ª ×”×©×™× ×•×™×™× ×©×œ ×”×—×•×“×© ×”×–×” ×‘×œ×‘×“" onClick={() => setMon(m)}>{m.split("-").reverse().join(".")}</button>
          ))}
        </div>
      )}
      <input className="search" type="search" placeholder="×¡×™× ×•×Ÿ: ×ž×¡×¤×¨ ×§×•, ×™×¢×“, ×ž×¤×¢×™×œ ××• ×ž×§×´×˜â€¦" value={q} onChange={(e) => setQ(e.target.value)} />
      {yr === "2012" ? (() => {
        if (!a12 || !idx12) return "×˜×•×¢×Ÿâ€¦";
        const list12 = sort12(rows12.filter((v) => match12(v, needle)), needle);
        if (!list12.length) return <div className="empty">××™×Ÿ ×§×•×•×™ 2012 ×ª×•××ž×™×.</div>;
        const linked = list12.filter((v) => v.rd).length;
        return (
          <div>
            <div className="dayhead">×¦×™×œ×•× ×ž×¦×‘ 2012 Â· {list12.length.toLocaleString()} ×§×•×•×™× Â·
              {" "}{linked.toLocaleString()} ×ž×§×•×©×¨×™× ×œ×§×•×•×™× ×©×œ ×”×™×•×</div>
            {list12.slice(0, lim).map((v) => (
              <a key={v.k} className="lrow" href={"#2012/" + encodeURIComponent(v.k)}
                onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); open12(v.k); }}>
                <span className="badge sm">{v.no}</span>
                <span className="k" style={{ background: v.rd ? "#78350f" : "#57534e" }}>
                  {v.rd ? "2012" : "×œ× ×§×™×™× ×”×™×•×"}</span>
                <span className="ldest">{v.dest || "â€”"}</span>
                <span className="lmeta">{v.an} Â· {v.nr} ×ž×¡×œ×•×œ×™× Â· {v.ns} ×ª×—× ×•×ª Â· ×ž×¤×” ×•×¨×¦×£ â†</span>
              </a>
            ))}
            {list12.length > lim && (
              <button className="morebtn" onClick={() => setLim(lim + 500)}>
                âŒ„ ×”×¦×’ ×¢×•×“ â€” ×ž×•×¦×’×™× {Math.min(lim, list12.length).toLocaleString()} ×ž×ª×•×š {list12.length.toLocaleString()}
              </button>
            )}
            <div className="katnote">â„¹ï¸ "×œ× ×§×™×™× ×”×™×•×" â€” ×œ× × ×ž×¦× ×œ×§×• ×”×–×” ×§×• ×ª×•×× ×‘×¨×©×ª ×”× ×•×›×—×™×ª: ×‘×•×˜×œ,
              ××• ×©×©×•× ×” ×¢×“ ×œ×œ× ×”×™×›×¨. ×œ×—×™×¦×” ×¢×œ ×”×©×•×¨×” ×¤×•×ª×—×ª ××ª ×¨×¦×£ ×”×ª×—× ×•×ª ×©×œ×• ×ž-2012.</div>
          </div>
        );
      })() : chs === null ? "×˜×•×¢×Ÿâ€¦" : chErr ? <NetErr onRetry={() => setRty((n) => n + 1)} />
        : days.length === 0 ? <div className="empty">××™×Ÿ ×©×™× ×•×™×™× ×ª×•××ž×™× ×‘×—×•×“×© ×”×–×”.</div> : (
        <div>
          {/* ××™× ×“×§×¡ ×”×™×ž×™× ×©×œ ×”×—×•×“×©: ×¨×•××™× ×ž×¨××© ×‘××™×œ×• ×ª××¨×™×›×™× ×™×© ×©×™× ×•×™×™×,
              ×•×œ×—×™×¦×” ×§×•×¤×¦×ª ×™×©×¨ ×œ×™×•× â€” ×‘×œ×™ ×œ×’×œ×•×œ ×•×œ×’×œ×•×ª ××•×ª× ××—×“-××—×“ */}
          {days.length > 1 && (
            <div className="months">
              <button className={"mchip" + (!dayF ? " on" : "")} aria-pressed={!dayF} title="×›×œ ×™×ž×™ ×”×—×•×“×© ×‘×¨×¦×£" onClick={() => setDayF(null)}>×›×œ ×”×—×•×“×©</button>
              {days.map((d) => (
                <button key={d} className={"mchip" + (dayF === d ? " on" : "")} aria-pressed={dayF === d}
                  title={"×™×•× " + WD[new Date(d).getDay()] + " Â· " + byd.get(d).length.toLocaleString() + " ×©×™× ×•×™×™×"}
                  onClick={() => setDayF(dayF === d ? null : d)}>{d.slice(8, 10) + "." + d.slice(5, 7)}</button>
              ))}
            </div>
          )}
          {days.filter((d) => !dayF || d === dayF).map((d) => shown >= lim ? null : (
            <React.Fragment key={d}>
              <div className="dayhead" role="button" tabIndex={0} style={{ cursor: "pointer" }}
                title={dayF === d ? "×œ×—×™×¦×” ×—×•×–×¨×ª ×ž×¦×™×’×” ×©×•×‘ ××ª ×›×œ ×”×—×•×“×©" : "×œ×—×™×¦×” ×ž×¦×™×’×” ×¨×§ ××ª ×”×™×•× ×”×–×”"}
                onClick={() => setDayF(dayF === d ? null : d)}
                onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); setDayF(dayF === d ? null : d); } }}>
                {fmtD(d)} Â· ×™×•× {WD[new Date(d).getDay()]} Â· {byd.get(d).length.toLocaleString()} ×©×™× ×•×™×™× {dayF === d ? "Â· ðŸ“Œ" : ""}</div>
              {byd.get(d).map((c, i) => {
                if (shown >= lim) return null;
                shown++;
                const m = meta[c.rd] || {};
                return (
                  <a key={c.rd + c.k + i} className="lrow" href={lineHref(c.rd)}
                    onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); openLine(c.rd); }}>
                    <span className="badge sm">{c.line || TT_ICON[m.tt] || "â€”"}</span>
                    <span className="k" style={{ background: (KINDS[evKind(c)] || {}).color || "#64748b" }}>{(KINDS[evKind(c)] || { label: c.k }).label}</span>
                    <span className="ldest">{m.dest || rdTxt(c.rd)}</span>
                    <span className="lmeta">{m.op || ""} Â· ×ž×§×´×˜ <span className="rdnum" dir="ltr">{rdTxt(c.rd)}</span></span>
                    {c.sd && gapDays(c.sd, c.d) > 3 ? <TipTag cls="approxd" tip={"××•×ª×¨ ×‘×™×Ÿ " + fmtD(c.sd) + " ×œ-" + fmtD(c.d) + " â€” ×”×™×•× ×”×ž×“×•×™×§ ××™× ×• ×™×“×•×¢"}>â‰ˆ ×ª××¨×™×š ×ž×§×•×¨×‘</TipTag> : null}
                    {c.k === "planned-dropped" && c.ps ? <span className="lnote">ðŸ“… ×ª×•×›× ×Ÿ ×œ-{fmtD(c.ps)} Â· ×‘×•×˜×œ ×‘-{fmtD(c.pc || c.d)}</span> : null}
                    {c.note ? <span className="lnote">{noteFix(c.note)}</span> : null}
                  </a>
                );
              })}
            </React.Fragment>
          ))}
          {list.length > lim && (
            <button className="morebtn" onClick={() => setLim(lim + 500)}>
              âŒ„ ×”×¦×’ ×¢×•×“ â€” ×ž×•×¦×’×™× {Math.min(lim, list.length).toLocaleString()} ×ž×ª×•×š {list.length.toLocaleString()}
            </button>
          )}
        </div>
      )}
    </div>
  );
}

/* ---------- ×ž×§"×˜ ×©×”×•× ×’× ×›×¤×ª×•×¨ ×”×¢×ª×§×” ---------- */
// ×œ×—×™×¦×” ×¢×œ ×”×ž×§"×˜ ×ž×¢×ª×™×§×” ××ª ×”×§×™×©×•×¨ ×œ×ª×—× ×” ×™×©×¨ ×œ×œ×•×—, ×‘×œ×™ ×ª×¤×¨×™×˜ ×•×‘×œ×™ ×©×œ×‘
// × ×•×¡×£. ×”×•× × ×©××¨ ×§×™×©×•×¨ ××ž×™×ª×™, ×›×š ×©×¤×ª×™×—×” ×‘×œ×©×•× ×™×ª ×—×“×©×” (×œ×—×™×¦×” ××ž×¦×¢×™×ª ××•
// ×œ×—×™×¦×” ××¨×•×›×”) ×ž×ž×©×™×›×” ×œ×¢×‘×•×“ ×›×¨×’×™×œ.
function StopCode({ code }) {
  const [ok, setOk] = useState(false);
  useEffect(() => {
    if (!ok) return;
    const t = setTimeout(() => setOk(false), 1600);
    return () => clearTimeout(t);
  }, [ok]);
  const copy = (e) => {
    e.stopPropagation();
    if (!plainClick(e)) return;      // Ctrl/××ž×¦×¢×™×ª â€” ×¤×ª×™×—×” ×‘×œ×©×•× ×™×ª ×—×“×©×”
    e.preventDefault();
    const url = location.origin + location.pathname + stopHref(code);
    const done = () => setOk(true);
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(url).then(done, () => fallback(url, done));
    } else fallback(url, done);
  };
  const fallback = (url, done) => {
    const ta = document.createElement("textarea");
    ta.value = url; ta.style.position = "fixed"; ta.style.opacity = "0";
    document.body.appendChild(ta); ta.select();
    try { document.execCommand("copy"); done(); } catch (err) { /* ××™×Ÿ ×œ×•×— â€” ×”×§×™×©×•×¨ ×¢×“×™×™×Ÿ ×‘-href */ }
    document.body.removeChild(ta);
  };
  return (
    <a className={"code slink" + (ok ? " copied" : "")} href={stopHref(code)} onClick={copy}
      title="×œ×—×™×¦×” ×ž×¢×ª×™×§×” ××ª ×”×§×™×©×•×¨ ×œ×ª×—× ×” ×”×–×• â€” ×›×œ ×”×©×™× ×•×™×™× ×©×œ×”, ×ž×›×œ ×”×©× ×™×">
      {" "}({code}) {ok ? "âœ“ ×”×•×¢×ª×§" : "ðŸ”—"}</a>
  );
}

/* ---------- ×ž×” ×”×©×ª× ×” ×œ××—×¨×•× ×” ---------- */
// ×”×ž×¡×š ×”×¨××©×•×Ÿ ×”×¦×™×’ ×©×•×¨×ª ×—×™×¤×•×© ×•×”×•×¨××” ×œ×”×§×œ×™×“, ×•×›×œ 58 ××œ×£ ×”×©×™× ×•×™×™× ×”×™×•
// ×ž×•×¡×ª×¨×™× ×ž××—×•×¨×™ ×¤×¢×•×œ×” ×©×”×ž×‘×§×¨ ×¦×¨×™×š ×œ×™×–×•×. ×›××Ÿ ×ž×•×¦×’×™× ×”×™×ž×™× ×”××—×¨×•× ×™×
// ×©×‘×”× ×§×¨×” ×ž×©×”×•, ×›×ž×• ×‘×˜××‘ ×”×ª×—× ×•×ª ×©×›×‘×¨ × ×¤×ª×— ×¢×œ ×”×—×•×“×© ×”××—×¨×•×Ÿ.
const WDR = ["×¨××©×•×Ÿ", "×©× ×™", "×©×œ×™×©×™", "×¨×‘×™×¢×™", "×—×ž×™×©×™", "×©×™×©×™", "×©×‘×ª"];
function RecentChanges({ idx, openLine, onAll }) {
  const [rows, setRows] = useState(null);
  const [nerr, setNerr] = useState(false);
  const [rty, setRty] = useState(0);
  const meta = useMemo(() => { const m = {}; ((idx && idx.lines) || []).forEach((l) => { m[l.rd] = l; }); return m; }, [idx]);
  useEffect(() => {
    let ok = true;
    setNerr(false);
    getMonths()
      .then(async (d) => {
        const ms = (d.months || []).slice().sort();
        if (!ms.length) { if (ok) setRows([]); return; }
        const get = (m) => dfetch("data/changes/" + m + ".json")
          .then((r) => (r.ok ? r.json() : { changes: [] }))
          .then((d) => ({ changes: (d.changes || []).filter((c) => !hiddenEv(c)) }));
        let list = (await get(ms[ms.length - 1])).changes || [];
        // ×‘-1 ×‘×—×•×“×© ×”×§×•×‘×¥ ×”×—×“×© ×›×ž×¢×˜ ×¨×™×§, ×•×”×ž×¡×š ×”×¨××©×™ × ×¨××” ×›××™×œ×• ×”××ª×¨ ×ž×ª â€”
        // ×›×©×—×¡×¨×™× ×™×ž×™× ×ž×©×œ×™×ž×™× ×ž×”×—×•×“×© ×”×§×•×“× ×›×“×™ ×©×ª×ž×™×“ ×™×•×¦×’×• ×”×™×ž×™× ×”××—×¨×•× ×™×
        const daysIn = new Set(list.map((c) => c.d));
        if (daysIn.size < 3 && ms.length > 1) {
          list = list.concat((await get(ms[ms.length - 2])).changes || []);
        }
        if (ok) setRows(list.slice().sort((a, b) => b.d.localeCompare(a.d)));
      })
      .catch(() => { if (ok) { setNerr(true); setRows([]); } });
    return () => { ok = false; };
  }, [rty]);
  if (rows === null) return <div className="empty">×˜×•×¢×Ÿ ××ª ×”×©×™× ×•×™×™× ×”××—×¨×•× ×™×â€¦</div>;
  if (nerr) return <NetErr onRetry={() => { setRows(null); setRty((n) => n + 1); }} />;
  if (!rows.length) return <div className="empty">××™×Ÿ ×¢×“×™×™×Ÿ ×©×™× ×•×™×™× ×‘×—×•×“×© ×”×–×”.</div>;
  const days = [];
  const byd = new Map();
  for (const c of rows) {
    if (!byd.has(c.d)) { byd.set(c.d, []); days.push(c.d); }
    byd.get(c.d).push(c);
  }
  const top = days.slice(0, 3);
  return (
    <div className="recent">
      {/* ×‘×œ×™ ×›×¤×ª×•×¨ "×›×œ ×”×©×™× ×•×™×™× ×œ×¤×™ ×™×•×" â€” ×”×•× ×©×›×¤×œ ××ª ×”×›×¤×ª×•×¨ ×”×’×“×•×œ
          ×©×›×‘×¨ ×™×•×©×‘ ×ž×ž×© ×ž×¢×œ (×©×œ×ž×” ×¡×™×ž×Ÿ ××ª ×”×›×¤×™×œ×•×ª ×‘×¦×™×œ×•× ×ž×¡×š) */}
      <div className="rechead">
        <b>×ž×” ×”×©×ª× ×” ×œ××—×¨×•× ×”</b>
      </div>
      {/* ××•×ª×• ×¡×™× ×•×Ÿ ×›×ž×• ×‘×¤×™×“ ×”×™×•×ž×™: ×¨×›×‘×ª/×¨×§"×œ/×ž×•× ×™×•×ª ×”×Ÿ ×˜××‘×™× ×ž×©×œ×”×Ÿ */}
      {top.map((d) => (
        <React.Fragment key={d}>
          <div className="dayhead">{fmtD(d)} Â· ×™×•× {WDR[new Date(d).getDay()]} Â· {byd.get(d).length.toLocaleString()} ×©×™× ×•×™×™×</div>
          {byd.get(d).filter((c) => { const m = meta[c.rd] || {}; return !m.tt || m.tt === "demand"; }).slice(0, 8).map((c, i) => {
            const m = meta[c.rd] || {};
            return (
              <a key={c.rd + c.k + i} className="lrow" href={lineHref(c.rd)}
                onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); openLine(c.rd); }}>
                <span className="badge sm">{c.line}</span>
                <span className="k" style={{ background: (KINDS[evKind(c)] || {}).color || "#64748b" }}>{(KINDS[evKind(c)] || { label: c.k }).label}</span>
                <span className="ldest">{m.dest || rdTxt(c.rd)}</span>
                <span className="lmeta">{m.op || ""} Â· ×ž×§×´×˜ <span className="rdnum" dir="ltr">{rdTxt(c.rd)}</span></span>
                {c.sd && gapDays(c.sd, c.d) > 3 ? <TipTag cls="approxd" tip={"××•×ª×¨ ×‘×™×Ÿ " + fmtD(c.sd) + " ×œ-" + fmtD(c.d) + " â€” ×”×™×•× ×”×ž×“×•×™×§ ××™× ×• ×™×“×•×¢"}>â‰ˆ ×ª××¨×™×š ×ž×§×•×¨×‘</TipTag> : null}
                    {c.k === "planned-dropped" && c.ps ? <span className="lnote">ðŸ“… ×ª×•×›× ×Ÿ ×œ-{fmtD(c.ps)} Â· ×‘×•×˜×œ ×‘-{fmtD(c.pc || c.d)}</span> : null}
                    {c.note ? <span className="lnote">{noteFix(c.note)}</span> : null}
              </a>
            );
          })}
          {byd.get(d).length > 8 && (
            <button className="recmore" onClick={onAll}>
              ×•×¢×•×“ {(byd.get(d).length - 8).toLocaleString()} ×©×™× ×•×™×™× ×‘-{fmtD(d)} â†
            </button>
          )}
        </React.Fragment>
      ))}
      <div className="katnote">
        â„¹ï¸ ×”×§×œ×™×“×• ×ž×¡×¤×¨ ×§×• ×›×“×™ ×œ×¨××•×ª ××ª ×”×”×™×¡×˜×•×¨×™×” ×©×œ×•, ××• ×¤×ª×—×• ××ª "×§×˜×’×•×¨×™×•×ª ×œ×‘×—×™×¨×”"
        ×•×¡×ž× ×• ××™×œ×• ×¡×•×’×™ ×©×™× ×•×™×™× ×œ×”×¦×™×’. ×”×ª×™×¢×•×“ ×ž×ª×—×™×œ ×‘-16.03.2017.
      </div>
    </div>
  );
}

/* ---------- ×˜××‘ ×ª×—× ×•×ª ---------- */
// ×¦×™×¨ ×”×§×•×•×™× ×©×œ ×ª×—× ×” ×‘×•×“×“×ª: ××™×œ×• ×§×•×•×™× ×¢×¦×¨×• ×‘×”, ×ž×™ × ×•×¡×£ ×•×ž×™ ×™×¨×“ ×•×ž×ª×™
// (×‘×§×©×ª ×”×ž×©×ª×ž×©). ×”× ×ª×•× ×™×: data/stopev/XX.json â€” × ×’×–×¨×™× ×™×•×ž×™×ª ×ž×§×•×‘×¦×™
// ×”×§×•×•×™×, ×›×š ×©×©×™× ×•×™ ××¦×œ ×§×• × ×¨×©× ××•×˜×•×ž×˜×™×ª ×’× ××¦×œ ×›×œ ×ª×—× ×” ×©×”×•×©×¤×¢×”.
function LinesAtStop({ code, onClose }) {
  const plats = usePlatforms();   // ×”×¨×¦×™×¤×™× ×”×¤×¢×™×œ×™× ×‘×ª×—× ×” ×•×ž×™ ×¢×•×¦×¨ ×‘×›×œ ××—×“ (×©×œ×ž×” 07.09)
  const [d, setD] = useState(null);
  const [err, setErr] = useState(false);
  const [all, setAll] = useState(false);
  useEffect(() => {
    setD(null); setErr(false); setAll(false);
    dfetch("data/stopev/" + (code.length >= 2 ? code.slice(0, 2) : "0x") + ".json")
      .then((r) => (r.ok ? r.json() : {}))
      .then((m) => setD(m[code] || { ev: [] }))
      .catch(() => setErr(true));
  }, [code]);
  if (err || (d && !(d.ev || []).length)) return null;
  if (!d) return <div className="lat"><div className="empty">â³ ×˜×•×¢×Ÿ ××ª ×§×•×•×™ ×”×ª×—× ×”â€¦</div></div>;
  const ev = d.ev;
  // ×ž×¦×‘ × ×•×›×—×™: ×”××™×¨×•×¢ ×”××—×¨×•×Ÿ ×©×œ ×›×œ ×•×¨×™×× ×˜ ×§×•×‘×¢ ×× ×”×•× ×¢×•×¦×¨ ×›××Ÿ ×”×™×•×
  const lastByRd = {};
  ev.forEach((e) => { lastByRd[e[2]] = e; });
  // "×”×™×•×" = ×™×© ×œ×•"×– ×œ×©×‘×•×¢ ×”×§×¨×•×‘ (d.a, ×ž×¤×¨×¡×•× ×”×¨×™×©×•×™ ×œ-10 ×”×™×ž×™×; × ×‘× ×” ×‘×¦×™× ×•×¨).
  // ×•×¨×™×× ×˜ ×©×”×ª×™×¢×•×“ ××•×ž×¨ ×©×”×•× ×¢×•×¦×¨ ×›××Ÿ ××‘×œ ××™×Ÿ ×œ×• ×œ×•"×– ××™× ×• ×¤×¢×™×œ (×©×œ×ž×” 07.09)
  const active = Array.isArray(d.a) ? new Set(d.a) : null;
  const nowMap = new Map();
  Object.values(lastByRd).forEach((e) => { if (e[3] !== "out" && e[3] !== "mvout" && e[1] && (!active || active.has(e[2]))) nowMap.set(e[1], e[2]); });
  const now = [...nowMap.entries()].sort((a, b) => (parseInt(a[0]) || 9e9) - (parseInt(b[0]) || 9e9) || String(a[0]).localeCompare(b[0]));
  // ×¦×™×¨ ×”×–×ž×Ÿ: ××™×¨×•×¢×™ "×”×ª×™×¢×•×“ ×”×¨××©×•×Ÿ" ×©×œ ××•×ª×• ×ª××¨×™×š ×ž×§×•×‘×¦×™× ×œ×©×•×¨×” ××—×ª,
  // ×•×—×œ×•×¤×•×ª ×©×œ ××•×ª×• ×§×• ×‘××•×ª×• ×™×•× ×œ× ×ž×•×¦×’×•×ª ×¤×¢×ž×™×™×
  const rows = [];
  const baseByDate = {};
  ev.forEach((e) => { if (e[3] === "base" && e[1]) (baseByDate[e[0]] = baseByDate[e[0]] || new Map()).set(e[1], e[2]); });
  Object.entries(baseByDate).forEach(([dte, lines]) => rows.push({ d: dte, k: "base", lines: [...lines.entries()] }));
  // mvin/mvout (×”×¦×™× ×•×¨ ×ž×–×”×” ×§×• ×©×”×¤×¡×™×§ ×‘×ª×—× ×” ××—×ª ×•×”×ª×—×™×œ ×‘×ª×—× ×” ×©×›× ×” ×‘××•×ª×• ×™×•×)
  // ×ž×•×¦×’×™× ×›×ž×• in/out ×¨×’×™×œ×™×: "×§×• 38 ×”×¤×¡×™×§ ×œ×¢×¦×•×¨ ×‘×ª×—× ×”" â€” ×”× ×™×¡×•×— "×¢×‘×¨ ×œ×¢×¦×•×¨
  // ×‘â€¦" ×œ× ×”×ª×‘×§×© (×©×œ×ž×” 07.09). ×ž×¢×‘×¨ ×¨×¦×™×£ ××ž×™×ª×™ (×¨×¦×™×£ 2 â† ×¨×¦×™×£ 1) ×ž×’×™×¢
  // ×ž× ×ª×•× ×™ ×”×¨×¦×™×¤×™× (platforms.json), ×œ× ×ž× ×™×—×•×© ×œ×¤×™ ×©×ž×•×ª.
  const seen = new Set();
  ev.forEach((e) => {
    if (e[3] === "base") return;
    const k = e[3] === "mvin" ? "in" : e[3] === "mvout" ? "out" : e[3];
    const key = e[0] + "|" + e[1] + "|" + k;
    if (seen.has(key)) return;
    seen.add(key);
    rows.push({ d: e[0], k, line: e[1] || "â€”", rd: e[2] });
  });
  rows.sort((a, b) => b.d.localeCompare(a.d));
  // ××•×ª×• ×ª××¨×™×š ×•××•×ª×• ×¡×•×’ â€” ×©×•×¨×” ××—×ª ×¢× ×›×œ ×”×§×•×•×™×, ×‘×ž×§×•× ×¢×©×¨ ×©×•×¨×•×ª ×–×”×•×ª (×©×œ×ž×” 07.09)
  const grouped = [], gk = {};
  rows.forEach((r) => {
    if (r.k === "base") { grouped.push(r); return; }
    const key = r.d + "|" + r.k;
    if (gk[key]) { gk[key].items.push([r.line, r.rd]); return; }
    gk[key] = { d: r.d, k: r.k, items: [[r.line, r.rd]] };
    grouped.push(gk[key]);
  });
  const shown = all ? grouped : grouped.slice(0, 30);
  // ×¨×¦×™×¤×™× ×¤×¢×™×œ×™× = ×¨×¦×™×¤×™× (×©×•×¨×•×ª ×‘×§×•×‘×¥ ×”×ª×—× ×•×ª ×©×œ ×”×ž×©×¨×“, ××•×ª×• ×ž×§"×˜) ×©×™×© ×‘×”×
  // × ×¡×™×¢×•×ª ×‘×œ×•×— ×”×–×ž× ×™× ×©×‘×ª×•×§×£ â€” ×œ× ×›×œ ×”×¨×¦×™×¤×™× ×”×‘× ×•×™×™× (×©×œ×ž×” 07.09: "×‘×ª×—× ×” ×–×•
  // ×™×© 16 ×¨×¦×™×¤×™× ×¤×¢×™×œ×™×"; ×‘×ž×¨×›×–×™×ª ×¨××©×œ"×¦ ×™×© ×’× ×¨×¦×™×¤×™× ×‘×œ×™ ×§×•×•×™×)
  const pst = plats && plats.st && plats.st[code];
  const platNos = pst ? Object.keys(pst.p || {}).sort(platSort) : [];
  return (
    <div className="lat">
      <div className="lathead">ðŸšŒ ×”×§×•×•×™× ×‘×ª×—× ×” ×”×–×• ×œ××•×¨×š ×–×ž×Ÿ
        {onClose && <button className="latx" title="×¡×’×™×¨×ª ×¦×™×¨ ×”×§×•×•×™×" onClick={onClose}>âœ•</button>}
      </div>
      {platNos.length > 0 && (
        <details className="latplat">
          <summary title="×œ×¤×™ ×©×•×¨×•×ª ×”×¨×¦×™×¤×™× ×‘×§×•×‘×¥ ×”×ª×—× ×•×ª ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×”: ×¨×¦×™×£ × ×—×©×‘ ×¤×¢×™×œ ×›×©×™×© ×‘×• × ×¡×™×¢×•×ª ×‘×œ×•×— ×”×–×ž× ×™× ×©×‘×ª×•×§×£ â€” ×ž×ª×¢×“×›×Ÿ ×ž×“×™ ×™×•×">
            ðŸ›¤ï¸ {platNos.length === 1
              ? <>×‘×ª×—× ×” ×–×• ×¨×¦×™×£ ×¤×¢×™×œ ××—×“: <b>×¨×¦×™×£ {platNos[0]}</b></>
              : <>×‘×ª×—× ×” ×–×• ×™×© <b>{platNos.length} ×¨×¦×™×¤×™× ×¤×¢×™×œ×™×</b> (×¨×¦×™×¤×™× ×©×™×© ×‘×”× ×§×•×•×™× ×”×™×•×)</>}
          </summary>
          <div className="platlist">
            {platNos.map((p) => {
              // ×›×ž×” ×—×œ×•×¤×•×ª ×©×œ ××•×ª×• ×§×• ×‘××•×ª×• ×¨×¦×™×£ â€” ×ž×¡×¤×¨ ×”×§×• ×¤×¢× ××—×ª
              const seenL = new Set();
              const items = (pst.p[p] || []).filter(([rd2, l]) => { const k = l || rd2; if (seenL.has(k)) return false; seenL.add(k); return true; });
              return (
                <div key={p} className="platrow"><b>×¨×¦×™×£ {p}</b>:{" "}
                  {items.map(([rd2, l], j) => (
                    <React.Fragment key={rd2}>{j > 0 ? ", " : ""}<a href={lineHref(rd2)} title={rd2}>{l || rd2}</a></React.Fragment>
                  ))}
                </div>
              );
            })}
          </div>
        </details>
      )}
      {now.length > 0 && (
        <div className="latnow" title="×§×•×•×™× ×©×¢×•×¦×¨×™× ×‘×ª×—× ×” ×œ×¤×™ ×”×ª×™×¢×•×“ ×•×™×© ×œ×”× ×œ×•×´×– ×œ×©×‘×•×¢ ×”×§×¨×•×‘ (×¤×¨×¡×•× ×”×¨×™×©×•×™ ×œ-10 ×”×™×ž×™×)">×¢×•×¦×¨×™× ×‘×” ×”×™×•× (×™×© ×œ×”× ×œ×•×´×– ×œ×©×‘×•×¢ ×”×§×¨×•×‘):{" "}
          {now.slice(0, 40).map(([l, rd2]) => <a key={l} className="badge sm latb" href={lineHref(rd2)}>{l}</a>)}
        </div>
      )}
      <div className="latlist">
        {shown.map((r, i) => r.k === "base"
          ? <div className="latrow" key={i}><span className="latd">{fmtD(r.d)}</span> {r.lines.length === 1
              ? <>ðŸš ×§×• <a href={lineHref(r.lines[0][1]) + "@" + r.d}><b>{r.lines[0][0]}</b></a> ×ª×•×¢×“ ×‘×ª×—× ×” ×œ×¨××©×•× ×”</>
              : <>ðŸš ×‘×ª×™×¢×•×“ ×”×¨××©×•×Ÿ ×¢×¦×¨×• ×›××Ÿ {r.lines.length} ×§×•×•×™×: {r.lines.slice(0, 25).map(([l, rd2], j) =>
                  <React.Fragment key={l + rd2}>{j > 0 ? ", " : ""}<a href={lineHref(rd2) + "@" + r.d}><b>{l}</b></a></React.Fragment>)}{r.lines.length > 25 ? "â€¦" : ""}</>}</div>
          : r.items.length === 1
            ? <div className="latrow" key={i}><span className="latd">{fmtD(r.d)}</span> {r.k === "in"
                ? <>ðŸ†• ×§×• <a href={lineHref(r.items[0][1]) + "@" + r.d}><b>{r.items[0][0]}</b></a> ×”×ª×—×™×œ ×œ×¢×¦×•×¨ ×‘×ª×—× ×”</>
                : <>âž– ×§×• <a href={lineHref(r.items[0][1]) + "@" + r.d}><b>{r.items[0][0]}</b></a> ×”×¤×¡×™×§ ×œ×¢×¦×•×¨ ×‘×ª×—× ×”</>}</div>
            : <div className="latrow" key={i}><span className="latd">{fmtD(r.d)}</span> {r.k === "in" ? "ðŸ†•" : "âž–"} {r.items.length} ×§×•×•×™× {r.k === "in" ? "×”×ª×—×™×œ×• ×œ×¢×¦×•×¨ ×‘×ª×—× ×”" : "×”×¤×¡×™×§×• ×œ×¢×¦×•×¨ ×‘×ª×—× ×”"}: {r.items.map(([l, rd2], j) =>
                <React.Fragment key={l + rd2}>{j > 0 ? ", " : ""}<a href={lineHref(rd2) + "@" + r.d}><b>{l}</b></a></React.Fragment>)}</div>)}
      </div>
      {grouped.length > shown.length && <button className="morebtn" onClick={() => setAll(true)}>âŒ„ ×›×œ {grouped.length.toLocaleString()} ×”××™×¨×•×¢×™×</button>}
      <div className="latnote">×ž×—×•×©×‘ ×ž×”×©×•×•××ª ×¨×¦×¤×™ ×”×ª×—× ×•×ª ×©×œ ×›×œ ×”×§×•×•×™× ×œ××•×¨×š ×”×ª×§×•×¤×”. ×ž×¢×‘×¨ ×¨×¦×™×£ × ×¨××” ×›××Ÿ ×›×§×• ×©×™×¨×“ â€” ×•×¢×œ×” ×‘××•×ª×• ×ª××¨×™×š ×‘×¨×¦×™×£ ×”×©×›×Ÿ.</div>
    </div>
  );
}

function StopsTab({ sel, selN }) {
  const [months, setMonths] = useState(null);
  const [mon, setMon] = useState("");
  const [yr, setYr] = useState("");   // ×©× ×” × ×‘×—×¨×ª ×‘×‘×•×—×¨ ×”×—×•×“×©×™×
  const [chs, setChs] = useState(null);
  const [hist, setHist] = useState(null);   // ×§×•×¨×•×ª-×—×™×™× ×ž×¦×˜×‘×¨×™× ×œ×›×œ ×ª×—× ×”
  const [kinds, setKinds] = useState(() => new Set());   // ×¡×™×ž×•×Ÿ ×ž×¨×•×‘×”, ×›×ž×• ×‘×§×•×•×™×
  const [onlyNs, setOnlyNs] = useState(false);           // ×¨×§ ×ª×—× ×•×ª ×©×”×™×• ×‘×¨×™×©×•× ×•×œ× ×‘×©×™×¨×•×ª
  const [katOpen, setKatOpen] = useState(false);
  const [q, setQ] = usePersistedQ("lh-q-stops");
  const [openKey, setOpenKey] = useState(null);   // ×©×•×¨×ª ×ª×—× ×” ×¤×ª×•×—×” ×¢× ×ž×¤×”
  // ×¦×™×¨ ×”×§×•×•×™× × ×¡×’×¨ ×‘-âœ• ××• ×‘×¨×’×¢ ×©×”×—×™×¤×•×© ×›×‘×¨ ×œ× ×ž×¦×™×’ ××ª ×”×ª×—× ×” ×©×œ×•;
  // ×”×¡×’×™×¨×” ×ž× ×§×” ×’× ××ª ×”×›×ª×•×‘×ª, ×›×“×™ ×©×¨×™×¢× ×•×Ÿ ×œ× ×™×—×–×™×¨ ××ª ×”×¤×× ×œ
  const [latHide, setLatHide] = useState(false);
  useEffect(() => setLatHide(false), [sel, selN]);
  const closeLat = () => {
    setLatHide(true);
    if ((location.hash || "").includes("stop=")) history.replaceState(null, "", "#t=stops");
  };
  // ×’× ×¡×’×™×¨×” ×¢×§×™×¤×” â€” ×©×™× ×•×™ ××• ×ž×—×™×§×” ×©×œ ×”×—×™×¤×•×© â€” ×ž× ×§×” ××ª ×”×›×ª×•×‘×ª, ×›×“×™
  // ×©×œ×—×™×¦×” ×—×•×–×¨×ª ×¢×œ ×§×™×©×•×¨ ×”×ª×—× ×” ×ª×™×—×©×‘ ×œ× ×™×•×•×˜ ×—×“×© ×•×ª×¤×ª×— ××ª ×”×¤×× ×œ
  // selDone: ×¨×§ ××—×¨×™ ×©×”×—×™×¤×•×© ×›×‘×¨ ×§×™×‘×œ ××ª ×ž×§"×˜ ×”×§×™×©×•×¨. ×‘×¡×‘×‘ ×”×¨××©×•×Ÿ ×”×—×™×¤×•×©
  // ×¢×“×™×™×Ÿ ×¨×™×§, ×•×”×›×ª×•×‘×ª â€Ž#stop=274â€Ž × ×ž×—×§×” ×œ-â€Ž#t=stopsâ€Ž ×¢×•×“ ×œ×¤× ×™ ×©×”×ª×—× ×” × ×¤×ª×—×” â€”
  // ×¨×™×¢× ×•×Ÿ (××• "××ª×¨ ×œ×ž×—×©×‘") ××™×‘×“ ××ª ×”×ª×—× ×” (×”×‘×“×™×§×” ×”××•×˜×•×ž×˜×™×ª, 18.09)
  useEffect(() => {
    if (sel && selDone.current && q.trim() !== sel && (location.hash || "").includes("stop="))
      history.replaceState(null, "", "#t=stops");
  }, [q, sel]);
  // 100 ×ª×—× ×•×ª ×‘×˜×¢×™× ×” (×”×™×• 250): ×‘"×›×œ ×”×ª×§×•×¤×”" ×›×œ ×§×‘×•×¦×” ×”×™× ×›×ž×” ×©×•×¨×•×ª, ×•×ž××•×ª
  // ×©×•×¨×•×ª ×‘×›×œ ×”×§×œ×“×” ×”×¨×’×™×©×• ×›×§×™×¤××•×Ÿ ×‘×˜×œ×¤×•×Ÿ. "×”×¦×’ ×¢×•×“" ×ž×¨×—×™×‘.
  const [lim, setLim] = useState(100);   // "×”×¦×’ ×¢×•×“" ×ž×¨×—×™×‘; ×¡×™× ×•×Ÿ ×—×“×© ×ž××¤×¡
  const dq = useDebounced(q, 220);       // ×”×¡×™× ×•×Ÿ ×¨×¥ ××—×¨×™ ×”×¤×¡×§×” ×‘×”×§×œ×“×”, ×œ× ×¢×œ ×›×œ ×ª×•
  useEffect(() => setLim(100), [dq, mon, kinds, onlyNs]);
  // ×ª×—× ×” ×©×”×’×™×¢×” ×ž×”×›×ª×•×‘×ª: ×›×œ ×§×•×¨×•×ª ×”×—×™×™× ×©×œ×”, ×•×œ× ×¨×§ ×”×—×•×“×© ×©× ×‘×—×¨
  useEffect(() => {
    if (!sel) return;
    setMon("all"); setYr(""); setQ(sel); setKinds(new Set()); setOnlyNs(false);
  }, [sel, selN]);
  const toggleKind = (k) => setKinds((s) => { const n = new Set(s); if (n.has(k)) n.delete(k); else n.add(k); return n; });
  // ×§×•×¨×•×ª ×”×—×™×™× ×©×œ ×›×œ ×”×ª×—× ×•×ª ×”× 4.5 ×ž×’×”, ×•×”× × ×“×¨×©×™× ×¨×§ ×œ"×›×œ ×”×ª×§×•×¤×”".
  // ×ª×¦×•×’×ª ×—×•×“×© ×‘×•×“×“ ×ž×¡×ª×“×¨×ª ×¢× ×§×•×‘×¥ ×©×œ ×¢×©×¨×•×ª ×§×™×œ×•×‘×™×™×˜ ×›×™ ×”×›×œ×œ×™× ×©×“×¨×©×•
  // ××•×ª× ×ž×¡×•×ž× ×™× ×ž×¨××© ×¢×œ ×”××™×¨×•×¢ (k1/xb), ×•×§×™×©×•×¨ ×œ×ª×—× ×” ×ž×¡×ª×¤×§ ×‘×©×‘×¨ ×©×œ
  // ×”×§×™×“×•×ž×ª ×©×œ×” â€” ×›×ž××ª×™×™× ×§×™×œ×•×‘×™×™×˜ ×‘×ž×§×•× ××¨×‘×¢×” ×•×—×¦×™ ×ž×’×”.
  const [shard, setShard] = useState(null);   // ×”×ž×§"×˜ ×©×”×©×‘×¨ ×©× ×˜×¢×Ÿ ×©×™×™×š ×œ×•
  const [snap12, setSnap12] = useState(null); // ×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ 2012 (×›×•×œ×• ×‘"×›×œ ×”×ª×§×•×¤×”", ×©×‘×¨ ×œ×ª×—× ×” ×ž×§×™×©×•×¨)
  const needHist = mon === "all" || !!sel;
  // ×”×”×©×•×•××” ×œ-q ×”×™×™×ª×” ×ž×•×§×“×ž×ª ×ž×“×™: ×”×—×™×¤×•×© × ×§×‘×¢ ×œ-sel ×‘××¤×§×˜ ××—×¨, ×•×‘×¡×‘×‘
  // ×”×¨××©×•×Ÿ ×”×•× ×¢×“×™×™×Ÿ ×”×¢×¨×š ×”×™×©×Ÿ â€” ×•××– × ×˜×¢×Ÿ ×”×§×•×‘×¥ ×”×ž×œ× ×‘×ž×§×•× ×”×©×‘×¨.
  // shardLeft: ××—×¨×™ ×©×”×—×™×¤×•×© ×¢×–×‘ ××ª ×ª×—× ×ª ×”×§×™×©×•×¨, ×”×“×’×œ × ×©××¨ ×“×œ×•×§ ×•×”×©×‘×¨
  // × ×˜×¢×Ÿ ×©×•×‘ ×•×©×•×‘ â€” ×—×™×¤×•×© ×ª×—× ×” ××—×¨×ª ×¨×¥ ×‘×©×§×˜ ×¢×œ ~1% ×ž×”×ª×—× ×•×ª ×‘×œ×‘×“
  // ×•×”×¦×™×’ "××™×Ÿ ×ª×•×¦××•×ª" ×¢×œ ×ª×—× ×•×ª ×§×™×™×ž×•×ª (×¦×™×“ ×”×‘××’×™×, ×¡×‘×‘ ×‘).
  const shardLeft = useRef(false);
  useEffect(() => { shardLeft.current = false; }, [sel]);
  const wantShard = !!sel && !shardLeft.current;
  useEffect(() => {
    if (!needHist || hist) return;
    const done = (d) => setHist(d);
    const done12 = (d) => setSnap12(d && d.stops ? d.stops : d || {});
    setSnap12(null);
    if (wantShard) {
      setShard(sel);
      const pre = (sel.slice(0, 2) || "0").padStart(2, "0");
      dfetch("data/stops/" + pre + ".json")
        .then((r) => (r.ok ? r.json() : {})).then(done).catch(() => done({}));
      dfetch("../magihim-2012/data/stops-2012/" + pre + ".json")
        .then((r) => (r.ok ? r.json() : {})).then(done12).catch(() => done12({}));
      return;
    }
    setShard(null);
    dfetch("data/stops-hist.json")
      .then((r) => (r.ok ? r.json() : {})).then(done).catch(() => done({}));
    dfetch("../magihim-2012/data/stops-2012.json")
      .then((r) => (r.ok ? r.json() : {})).then(done12).catch(() => done12({}));
  }, [needHist, hist, wantShard, sel]);
  // ×—×™×¤×•×© ×©×™×¦× ×ž×”×ª×—× ×” ×©×œ ×”×§×™×©×•×¨ â€” ×”×©×‘×¨ ×›×‘×¨ ×œ× ×ž×¡×¤×™×§, ×•×¦×¨×™×š ××ª ×”×›×œ.
  // ×”×“×’×œ × ×—×•×¥ ×›×™ ×‘×¡×‘×‘ ×”×¨××©×•×Ÿ ×”×—×™×¤×•×© ×¢×“×™×™×Ÿ ×ž×—×–×™×§ ×¢×¨×š ×§×•×“×, ×•×‘×œ×¢×“×™×• ×”×©×‘×¨
  // ×”×™×” × ×–×¨×§ ×•× ×˜×¢×Ÿ ×©×•×‘ ×‘×œ×•×œ××” ××™× ×¡×•×¤×™×ª.
  const selDone = useRef(false);
  useEffect(() => { selDone.current = false; }, [sel]);
  useEffect(() => {
    if (sel && q.trim() === sel) selDone.current = true;
    if (shard && selDone.current && q.trim() !== shard) { shardLeft.current = true; setHist(null); setShard(null); }
  }, [q, shard, sel]);
  // ×›×œ×œ×™ ×”×ª×¦×•×’×” (×‘×§×©×ª ×”×ž×©×ª×ž×©, ×‘×¢×§×‘×•×ª ×ª×—× ×•×ª ×¢×•× ×ª×™×•×ª ×›×ž×• ×ª×—× ×•×ª ×”×”×ª×¨×¢× × ×•×ª):
  // "×—×“×©×”" â€” ×¨×§ ×”×¨×™×©×•× ×”×¨××©×•×Ÿ ××™-×¤×¢× ×©×œ ×”×ª×—× ×”; ×”×¨×©×ž×•×ª ×—×•×–×¨×•×ª ×œ× ×ž×•×¦×’×•×ª.
  // "×‘×•×˜×œ×”" â€” ×¨×§ ×× ×¢×‘×¨×” ×©× ×” ×‘×œ×™ ×©×—×–×¨×”; ×•×ž×™ ×©×ž×•×¤×™×¢×” ×‘×¨×™×©×•× ×”× ×•×›×—×™
  // (×’× ×‘×œ×™ ×§×•×•×™× ×©×¢×•×¦×¨×™× ×‘×”) ××™× ×” ×ž×‘×•×˜×œ×ª.
  const keepEvent = (c) => {
    // ×‘×œ×™ ×§×•×¨×•×ª ×”×—×™×™× ×ž×›×¨×™×¢×™× ×œ×¤×™ ×”×¡×™×ž×•× ×™× ×©×—×•×©×‘×• ×ž×¨××©: k1 â€” ×–×” ×”×¨×™×©×•×
    // ×”×¨××©×•×Ÿ ×©×œ ×”×ª×—× ×”, xb â€” ×”×‘×™×˜×•×œ ×”×–×” ×›×‘×¨ ×œ× ×‘×ª×•×§×£. ×›×œ×œ ×”×©× ×” × ×©××¨ ×›××Ÿ,
    // ×›×™ ×”×•× ×ª×œ×•×™ ×‘×ª××¨×™×š ×©×œ ×”×™×•×.
    if (!hist) {
      if (c.k === "new") return !!c.k1;
      if (c.k === "del") return !c.xb && Date.now() - new Date(c.d) >= 365 * 864e5;
      return true;
    }
    const evs = hist[c.c] || [];
    if (c.k === "new") {
      // "×—×“×©×”" = ×œ×™×“×ª ×”×ª×—× ×”: ×”××™×¨×•×¢ ×”×¨××©×•×Ÿ ×‘×›×œ×œ ×‘×§×•×¨×•×ª-×”×—×™×™× ×©×œ×”. ×ª×—× ×”
      // ×©×”×”×™×¡×˜×•×¨×™×” ×©×œ×” ×ž×ª×—×™×œ×” ×‘×‘×™×˜×•×œ ×§×™×™×ž×ª ×ž×œ×¤× ×™ ×”×ª×™×¢×•×“ â€” ×”"×—×“×©×”" ×©××—×¨×™×•
      // ×”×™× ×—×–×¨×” ×œ×¨×™×©×•×, ×œ× ×œ×™×“×”
      return !evs.length || (evs[0].k === "new" && evs[0].d === c.d);
    }
    if (c.k === "del") {
      if (evs.some((e) => e.d > c.d && e.k === "new")) return false;   // ×—×–×¨×” ×œ×¤×¢×•×œ
      const last = evs[evs.length - 1];
      if (last && last.k === "del" && last.now) return false;          // ×¢×“×™×™×Ÿ ×‘×¨×™×©×•×
      return Date.now() - new Date(c.d) >= 365 * 864e5;                // ×ž×‘×•×˜×œ×ª = ×ž×¢×œ ×©× ×”
    }
    return true;
  };
  const [mErr, setMErr] = useState(false);
  const [chErr, setChErr] = useState(false);
  const [rty, setRty] = useState(0);
  useEffect(() => {
    let ok = true;
    setMErr(false);
    getMonths()
      .then((d) => { if (!ok) return;
        // ×ž×™×•×Ÿ ×™×•×¨×“ ×ž×¤×•×¨×© â€” ×œ× ×¡×•×ž×›×™× ×¢×œ ×”×¡×“×¨ ×©×‘×“×™×¡×§ (×¨××• ×”×¤×™×“ ×©×œ ×”×§×•×•×™×)
        const ms = (d.stopMonths || []).slice().sort().reverse();
        setMonths(ms);
        // ×‘×¨×™×¨×ª ×”×ž×—×“×œ: ×”×—×•×“×© ×”××—×¨×•×Ÿ â€” × ×˜×¢×Ÿ ×ž×™×™×“×™×ª. "×›×œ ×”×ª×§×•×¤×”" (×¤×™×¨×•×§
        // ×•×ž×™×•×Ÿ ×©×œ ×›×œ ×§×•×¨×•×ª-×”×—×™×™×, ×ž××•×ª ××œ×¤×™ ××™×¨×•×¢×™×) ×¨×§ ×‘×‘×—×™×¨×” ×ž×¤×•×¨×©×ª
        // ×›×©×”×’×¢× ×• ×ž×§×™×©×•×¨ ×œ×ª×—× ×”, "×›×œ ×”×ª×§×•×¤×”" ×›×‘×¨ × ×‘×—×¨ â€” ×•×˜×¢×™× ×ª ×”×—×•×“×©×™×
        // ×“×¨×¡×” ××•×ª×• ×‘×—×•×“×© ×”××—×¨×•×Ÿ, ×›×š ×©×”×§×™×©×•×¨ × ×—×ª ×¢×œ ×ž×¡×š ×¨×™×§
        if (ms.length && !sel) { setMon(ms[0]); setYr(ms[0].slice(0, 4)); } })
      .catch(() => { if (ok) { setMErr(true); setMonths([]); } });
    return () => { ok = false; };
  }, [rty]);
  useEffect(() => {
    // "×›×œ ×”×ª×§×•×¤×”" × ×‘× ×™×ª ×ž×§×•×¨×•×ª ×”×—×™×™× ×•×œ× ×ž×§×•×‘×¥ ×—×•×“×© â€” ×‘×œ×™ ×”×ª× ××™ ×”×–×” × ×©×œ×—×”
    // ×‘×§×©×” ×œ-stops-all.json ×©×ª×ž×™×“ ×—×•×–×¨×ª 404
    if (!mon || mon === "all") return;
    // ×‘×™×˜×•×œ ×›×©×¢×•×‘×¨×™× ×—×•×“×©: ×ª×©×•×‘×” ××™×˜×™×ª ×©×œ ×—×•×“×© ×§×•×“× ×œ× ×“×•×¨×¡×ª ××ª ×”×—×“×©
    let ok = true;
    setChs(null); setChErr(false);
    dfetch("data/changes/stops-" + mon + ".json")
      .then((r) => (r.ok ? r.json() : { changes: [] }))
      .then((d) => { if (ok) setChs(d.changes || []); })
      .catch(() => { if (ok) { setChErr(true); setChs([]); } });
    return () => { ok = false; };
  }, [mon, rty]);
  // ×ž×ž×•××: ×‘×œ×™ ×–×” ×›×œ ×”×§×œ×“×” ×‘×—×™×¤×•×© ×‘× ×ª×” ×•×ž×™×™× ×” ×ž×—×“×© ××ª ×›×œ ×”××™×¨×•×¢×™× (×œ××’×™×).
  // "×›×œ ×”×ª×§×•×¤×”": ×›×œ ×”××™×¨×•×¢×™× ×ž×›×œ ×”×–×ž× ×™× ×ž×ª×•×š ×§×•×¨×•×ª-×”×—×™×™×, ×¢× ×ª××¨×™×š ×œ×™×“ ×›×œ ××—×“
  const source = useMemo(() => {
    // ×ž×—×¨×•×–×ª ×—×™×¤×•×© ××—×ª ×œ×›×œ ××™×¨×•×¢, ×ž×—×•×©×‘×ª ×¤×¢× ××—×ª â€” ×‘×ž×§×•× ××¨×‘×¢ ×”×—×œ×¤×•×ª-regex
    // ×œ×›×œ ××™×¨×•×¢ ×‘×›×œ ×”×§×œ×“×”; ×”×ž×™×•×Ÿ ×‘×”×©×•×•××ª ×ž×—×¨×•×–×•×ª ×¨×’×™×œ×” (localeCompare ××™×˜×™ ×¤×™ 2)
    const withS = (e) => ({ ...e, s: sQ(e.n) + "|" + sQ(e.nn) + "|" + sQ(e.on) + "|" + sQ(e.t) });
    // ×§×™×©×•×¨ ×œ×ª×—× ×”: ×¢×“ ×©×”×—×•×“×© ×”×¤×š ×œ"×›×œ ×”×ª×§×•×¤×”" ×•×”×©×‘×¨ ×©×œ×” × ×˜×¢×Ÿ, ×”×¨×©×™×ž×”
    // ×”×¨××ª×” ×œ×¨×’×¢ ×ª×—× ×•×ª ××—×¨×•×ª ×ž×”×—×•×“×© ×”× ×•×›×—×™ ×©×ž×¡×¤×¨×Ÿ ×ž×›×™×œ ××ª ×”×ž×§"×˜ â€” ×ž×¡×š
    // "×˜×•×¢×Ÿ" ×‘×ž×§×•× ×–×” (×”×‘×“×™×§×” ×”××•×˜×•×ž×˜×™×ª ×ª×¤×¡×”: â€Ž#stop=274â€Ž "× ×¤×ª×—×” ×¢×œ ×ª×—× ×” ××—×¨×ª")
    if (sel && !shardLeft.current && (mon !== "all" || !hist)) return null;
    const raw = mon === "all"
      ? (hist ? Object.entries(hist).flatMap(([c, evs]) => evs.map((e) => withS({ ...e, c })))
          // ×¨×™×©×•× 2012: ××™×¨×•×¢ ×œ×›×œ ×ª×—× ×” ×©×”×™×™×ª×” ××– â€” ××—×¨×™ ×›×œ ×”×©××¨, ×›×™ ×”×•× ×”×™×©×Ÿ ×‘×™×•×ª×¨
          .concat(snap12 ? events2012(snap12).map(withS) : []).sort((a, b) => (a.d < b.d ? 1 : a.d > b.d ? -1 : 0)) : null)
      : (chs ? chs.map(withS) : chs);
    // ×›×©×ž×’×™×¢×™× ×œ×ª×—× ×” ×ž×§×™×©×•×¨ ×ž×¦×™×’×™× ××ª ×›×œ ×ž×” ×©×™×“×•×¢ ×¢×œ×™×”. ×›×œ×œ×™ ×”×ª×¦×•×’×”
    // × ×•×¢×“×• ×œ×¤×™×“ ×”×—×•×“×©×™, ×•×‘×ª×—× ×” ×ž×¡×•×™×ž×ª ×”× ×”×¡×ª×™×¨×• ×’× ××ª ×ž×” ×©×‘×™×§×©×• ×œ×¨××•×ª:
    // â€Ž#stop=48â€Ž ×”×¨××” ×ž×¡×š ×¨×™×§, ×›×™ ×©× ×™ ×”××™×¨×•×¢×™× ×©×œ×” × ×—×©×‘×™× "×—×•×–×¨×™×".
    return raw === null ? null
      : raw.filter((c) => !hiddenEv(c) && ((sel && c.c === sel) || keepEvent(c)));
  }, [mon, hist, chs, sel, snap12]);
  const counts = useMemo(() => {
    const cn = {};
    (source || []).forEach((c) => { cn[c.k] = (cn[c.k] || 0) + 1; });
    return cn;
  }, [source]);
  // ×‘-useMemo â€” ×”×¡×™× ×•×Ÿ ×¢×œ ×¢×©×¨×•×ª ××œ×¤×™ ××™×¨×•×¢×™× ×œ× ×¨×¥ ×ž×—×“×© ×‘×¤×¢×•×œ×•×ª ×©××™× ×Ÿ
  // ×—×™×¤×•×© (×¡×¢×™×£ 14); ×”×¢×¨×” ×§×™×™×ž×ª ×¢×œ source ×ž×¡×‘×™×¨×” ××ª ××•×ª×• ×œ××’
  const { nsCount, list } = useMemo(() => {
    const needle = dq.trim(); const sNeedle = sQ(needle);
    const ls = (source || []).filter((c) => (!kinds.size || kinds.has(c.k)) && (!onlyNs || c.ns) &&
      (!needle || c.s.includes(sNeedle) || c.c === needle));
    return { nsCount: (source || []).filter((c) => c.ns).length, list: ls };
  }, [source, dq, kinds, onlyNs]);
  if (months === null) return <div className="card">×˜×•×¢×Ÿâ€¦</div>;
  if (mErr) return <div className="card"><NetErr onRetry={() => { setMonths(null); setRty((n) => n + 1); }} /></div>;
  if (!months.length) return <div className="card"><div className="empty">×¢×“×™×™×Ÿ ××™×Ÿ × ×ª×•× ×™ ×©×™× ×•×™×™ ×ª×—× ×•×ª â€” ×”× ×™×¦×˜×‘×¨×• ×ž×”×¨×™×¦×•×ª ×”×™×•×ž×™×•×ª ×”×§×¨×•×‘×•×ª.</div></div>;
  return (
    <div className="card">
      {/* ×‘×•×—×¨ ×œ×¤×™ ×©× ×”: slice(0,18) ×”×™×©×Ÿ ×”×¡×ª×™×¨ ××ª ×›×œ ×ž×” ×©×œ×¤× ×™ 02.2025 â€”
          ×¢×›×©×™×• ×›×œ ×©× ×” × ×’×™×©×” ×‘×œ×—×™×¦×”, ×•×”×—×•×“×©×™× ×©×œ×” × ×¤×ª×—×™× ×ž×ª×—×ª×™×” */}
      <div className="months">
        <button className={"mchip" + (mon === "all" ? " on" : "")} aria-pressed={mon === "all"} title="×›×œ ×”××™×¨×•×¢×™× ×ž×›×œ ×”×©× ×™× ×‘×¨×¦×£ ××—×“" onClick={() => { setYr(""); setMon("all"); }}>ðŸ—“ï¸ ×›×œ ×”×ª×§×•×¤×”</button>
        {/* ×ž×”×—×“×©×” ×œ×™×©× ×”, ×‘××•×ª×• ×›×™×•×•×Ÿ ×©×œ ×”×—×•×“×©×™× ×‘×ª×•×š ×›×œ ×©× ×”.
            stopMonths ×ž×ž×•×™×Ÿ ×™×•×¨×“ (×‘× ×™×’×•×“ ×œ-months ×©×œ ×”×§×•×•×™×) â€” ×”×—×•×“×©
            ×”×—×“×© ×©×œ ×©× ×” ×”×•× ms[0], ×•×”-reverse ×”×™×” ×”×•×¤×š ××ª ×¡×“×¨ ×”×ª×¦×•×’×” */}
        {[...new Set(months.map((m) => m.slice(0, 4)))].sort().reverse().map((y) => (
          <button key={y} className={"mchip" + (yr === y ? " on" : "")} aria-pressed={yr === y}
            title={"×”×¦×’×ª ×”×©×™× ×•×™×™× ×©×œ ×©× ×ª " + y} onClick={() => { setYr(y); const ms = months.filter((m) => m.startsWith(y)); if (!ms.includes(mon)) setMon(ms[0]); }}>{y}</button>
        ))}
      </div>
      {yr && (
        <div className="months">
          {months.filter((m) => m.startsWith(yr)).map((m) => (
            <button key={m} className={"mchip" + (mon === m ? " on" : "")} aria-pressed={mon === m} title="×”×¦×’×ª ×”×©×™× ×•×™×™× ×©×œ ×”×—×•×“×© ×”×–×” ×‘×œ×‘×“" onClick={() => setMon(m)}>{m.split("-").reverse().join(".")}</button>
          ))}
        </div>
      )}
      <input className="search" type="search" placeholder="×—×™×¤×•×© ×ª×—× ×” / ×¢×™×¨ / ×ž×§×´×˜â€¦" value={q} onChange={(e) => setQ(e.target.value)} />
      <div className="katbox">
        <button className="kathead" aria-expanded={katOpen} onClick={() => setKatOpen(!katOpen)}>
          <span className="katarrow" aria-hidden="true">{katOpen ? "â–¼" : "â—€"}</span>
          ðŸ—‚ï¸ ×§×˜×’×•×¨×™×•×ª ×œ×‘×—×™×¨×”
          {kinds.size > 0 && <b className="katn">{kinds.size} ×ž×¡×•×ž× ×•×ª</b>}
        </button>
        {katOpen && (
          <div className="katlist">
            {Object.entries(SKINDS).map(([k, v]) => (
              <label key={k} className="katrow">
                <input type="checkbox" checked={kinds.has(k)} onChange={() => toggleKind(k)} />
                <i className="katdot" style={{ background: v.color }} />
                <span className="katlab">{v.label}</span>
                {/* ×‘×–×ž×Ÿ ×”×—×œ×¤×ª ×ª×§×•×¤×” ×”×ž×•× ×” ×”×¨××” 0 ×ž×˜×¢×” ×œ×›×ž×” ×©× ×™×•×ª (×‘×§×©×ª
                    ×©×œ×ž×”) â€” ×¢×“ ×©×”× ×ª×•× ×™× × ×˜×¢× ×™× ×›×ª×•×‘ ×‘×• "×˜×•×¢×Ÿ" */}
                <b className="katc">{chs === null ? "×˜×•×¢×Ÿâ€¦" : (counts[k] || 0).toLocaleString()}</b>
              </label>
            ))}
            {/* ×ª×—× ×•×ª ×©×œ× × ×ž×¦××• ×‘×ž×¡×œ×•×œ ×©×œ ××£ ×§×• ×ž×”×ž×ª×•×¢×“×™× ××¦×œ× ×•. ×—×œ×§×Ÿ
                ××•×©×¨×• ×‘×¨×™×©×•×™ ×•×œ× × ×¤×ª×—×• ×œ×¦×™×‘×•×¨ â€” ×•×œ×›×Ÿ ×“×•×•×§× ×”×Ÿ ×ž×¢× ×™×™× ×•×ª.
                ××™ ××¤×©×¨ ×œ×§×‘×•×¢ ×©××£ ×§×• ×œ× ×¢×¦×¨ ×‘×”×Ÿ: ×”×›×™×¡×•×™ ×©×œ× ×• ×—×œ×§×™, ×•×–×”
                ×ž×” ×©×”×¡×™× ×•×Ÿ ××•×ž×¨ ×•×œ× ×™×•×ª×¨. */}
            <div className="katgrp">×¨×™×©×•× ×ž×•×œ ×©×™×¨×•×ª</div>
            <label className="katrow">
              <input type="checkbox" checked={onlyNs} onChange={() => setOnlyNs(!onlyNs)} />
              <i className="katdot" style={{ background: "#64748b" }} />
              <span className="katlab">×¨×§ ×ª×—× ×•×ª ×©×œ× × ×ž×¦××• ×‘×ž×¡×œ×•×œ ×©×œ ××£ ×§×•</span>
              <b className="katc">{chs === null ? "×˜×•×¢×Ÿâ€¦" : nsCount.toLocaleString()}</b>
            </label>
            {kinds.size > 0 && (
              <button className="katclear" onClick={() => setKinds(new Set())}>âœ– × ×§×” ××ª ×”×‘×—×™×¨×”</button>
            )}
          </div>
        )}
      </div>
      {sel && !latHide && q.trim() === sel && <LinesAtStop code={sel} onClose={closeLat} />}
      {source === null ? "×˜×•×¢×Ÿâ€¦" : (
        <div className="slist">
          {(() => {
            // ×›×œ ×”×©×™× ×•×™×™× ×©×œ ××•×ª×” ×ª×—× ×” ×ž×§×•×‘×¦×™× ×™×—×“ (×‘×§×©×ª ×”×ž×©×ª×ž×©)
            const groups = [];
            const byCode = new Map();
            for (const c of list) {
              let g = byCode.get(c.c);
              if (!g) { g = { code: c.c, evs: [] }; byCode.set(c.c, g); groups.push(g); }
              g.evs.push(c);
            }
            const shown = groups.slice(0, lim);
            const evRow = (c, one) => {
              const k0 = c.c + c.k + c.d;
              return (
                <React.Fragment key={k0}>
                {/* ×”×©×•×¨×” ×œ×—×™×¦×” ×œ×¢×›×‘×¨; ×”×›×¤×ª×•×¨ ×”××ž×™×ª×™ ×œ×ž×§×œ×“×ª/×§×•×¨× ×ž×¡×š ×”×•×
                    ×¡×ž×œ ×”×ž×¤×” ×‘×¡×•×¤×” â€” ×‘×œ×™ ×›×¤×ª×•×¨-×‘×ª×•×š-×›×¤×ª×•×¨ (×”×‘×™×§×•×¨×ª) */}
                <div className={"srow" + (one ? "" : " sub") + (c.la != null ? " clk" : "")}
                  onClick={() => { if (c.la != null) setOpenKey(openKey === k0 ? null : k0); }}>
                  <span className="k" style={{ background: (SKINDS[c.k] || {}).color }}>{c.k === "platform" && c.pv ? (c.st === "add" ? "×¨×¦×™×£ × ×•×¡×£" : "×¨×¦×™×£ ×‘×•×˜×œ") : (SKINDS[c.k] || { label: c.k }).label}</span>
                  {one ? (
                    <span className="nm">
                      {c.k === "renamed" ? <><s>{c.on}</s> â† <b>{c.nn}</b></> : <b>{c.n}</b>}
                      <StopCode code={c.c} />
                      <a className="latlink" href={"#stop=" + c.c} title="××™×œ×• ×§×•×•×™× ×¢×¦×¨×• ×‘×ª×—× ×” ×•×ž×” ×”×©×ª× ×”"
                        onClick={(e) => e.stopPropagation()}>ðŸšŒ ×§×•×•×™×</a>
                    </span>
                  ) : (c.k === "renamed" && <span className="nm"><s>{c.on}</s> â† <b>{c.nn}</b></span>)}
                  {/* ×¨×©×•×ž×” ×‘×¨×™×©×•× ×©××£ ×§×• ×œ× ×¢×¦×¨ ×‘×”. ×‘×œ×™ ×”×¡×™×ž×•×Ÿ "×ª×—× ×” ×—×“×©×”"
                      × ×§×¨× ×›××™×œ×• × ×•×¡×¤×” ×ª×—× ×” ×©××¤×©×¨ ×œ×—×›×•×ª ×‘×” â€” ×•×–×” ×œ× × ×›×•×Ÿ. */}
                  {c.ns && <TipTag cls="nsflag" tip="×”×ª×—× ×” ×œ× × ×ž×¦××” ×‘×ž×¡×œ×•×œ ×©×œ ××£ ×§×• ×ž×”×§×•×•×™× ×©×ž×ª×•×¢×“×™× ××¦×œ× ×•. ×™×™×ª×›×Ÿ ×©×”×™× ××•×©×¨×” ×‘×¨×™×©×•×™ ×•×œ× × ×¤×ª×—×” ×œ×¦×™×‘×•×¨, ×•×™×™×ª×›×Ÿ ×©×¢×¦×¨ ×‘×” ×§×• ×©×¨×¦×£ ×”×ª×—× ×•×ª ×©×œ×• ×—×¡×¨ ×œ× ×•">×‘×¨×™×©×•× ×‘×œ×‘×“</TipTag>}
                  {/* ×¨×§ ×¢×œ "×ª×—× ×” ×—×“×©×”", ×©×©× ×–×” ×ª×™×§×•×Ÿ ×©×œ ×ž×” ×©×›×ª×•×‘: ×”×ª×—× ×”
                      ×—×–×¨×” ×œ×¨×™×©×•×, ×œ× × ×‘× ×ª×”. ×¢×œ ×©××¨ ×¡×•×’×™ ×”××™×¨×•×¢×™× ×–×• ×”×¢×¨×ª
                      ×¨×§×¢ ×©×œ× ×ž×•×¡×™×¤×” ×›×œ×•×, ×•×œ×›×Ÿ ×”×™× ×œ× ×ž×•×¦×’×ª. */}
                  {c.m12 && c.k === "new" && <TipTag cls="m12flag"
                    tip="×”×ª×—× ×” ×›×‘×¨ ×”×•×¤×™×¢×” ×‘×ž×¡×œ×•×œ ×©×œ ×§×• ×‘×ž××’×¨ ×ž×’×™×¢×™× ×ž-2012, ×•×œ×›×Ÿ ××™×Ÿ ×ž×“×•×‘×¨ ×‘×ª×—× ×” ×©× ×‘× ×ª×” ×¢×›×©×™×• ××œ× ×‘×—×–×¨×” ×œ×¨×™×©×•× ×”××¨×¦×™">
                    ×”×™×™×ª×” ×›×‘×¨ ×‘-2012</TipTag>}
                  <span className="meta">
                    {one && c.t ? c.t + " Â· " : ""}{c.b12 ? <span title="×”×©×•×•××” ×‘×™×Ÿ ×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ 2012 ×œ×¨×™×©×•× ×”×¨××©×•×Ÿ ×©×™×© ×œ× ×•, ×ž×ž×¨×¥ 2017 â€” ×”×ž×•×¢×“ ×”×ž×“×•×™×§ ×œ× ×™×“×•×¢">×‘×™×Ÿ 2012 ×œ-2017</span> : fmtD(c.d)}
                    {/* ×‘×œ×™ ×”×ž×™×§×•× ×”×§×•×“× ×”×©×•×¨×” ×™×¦××” "×”×•×–×–×” ×ž×³ Â· (, ) â† (â€¦)" â€”
                        ×—×¦×™× ×¨×™×§×™× ×ž×©× ×™ ×”×¦×“×“×™×. ×›×©×”×•× ×—×¡×¨ ×ž×•×¦×’ ×”×™×¢×“ ×‘×œ×‘×“. */}
                    {/* dir=ltr ×¢×œ ×–×•×’ ×”×§×•××•×¨×“×™× ×˜×•×ª: ×‘×˜×§×¡×˜ ×¢×‘×¨×™ ×”×¤×¡×™×§ ×•×”×¨×•×•×—
                        ×ž×§×‘×œ×™× ×›×™×•×•×Ÿ RTL ×•×¡×“×¨ lat/lon ×”×ª×”×¤×š ×•×™×–×•××œ×™×ª */}
                    {c.k === "city" && <> Â· <s>{c.oc}</s> â† <b>{c.nc}</b></>}
                    {c.reused && <> Â· ×”×ž×§×´×˜ ×©×™×ž×© ××—×¨ ×›×š ×ª×—× ×” ××—×¨×ª: <b>{c.reused}</b>, ×‘×ž×¨×—×§ {c.rdist >= 1000 ? (c.rdist / 1000).toFixed(1) + " ×§×´×ž" : c.rdist + " ×ž×³"} â€” ×œ× ××•×ª×” ×ª×—× ×”</>}
                    {c.k === "gtfs2012" && <> Â· <span dir="ltr">({c.la}, {c.lo})</span>
                      {" "}Â· <a href="https://www.openstreetmap.org/changeset/12028672" target="_blank" rel="noopener" title="×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×ž×™×•× ×™ 2012 (GTFS), ×›×¤×™ ×©×™×•×‘× ×œ-OpenStreetMap ×‘-26.06.2012" onClick={(e) => e.stopPropagation()}>×ž×§×•×¨: GTFS 06.2012 ×“×¨×š OSM</a></>}
                    {/* ×¨×¦×™×£ (pv=2, tools/platforms.py): ×¨×¦×™×£ ×‘×ž×¡×•×£ ×©×§×™×‘×œ ×§×•×•×™× / × ×©××¨ ×‘×œ×™ ×§×•×•×™× â€”
                        ×¨×§ ×–×” "×©×™× ×•×™ ×‘×¨×¦×™×£ ×”×ª×—× ×”" (×©×œ×ž×” 07.09), ×œ× ×§×¤×™×¦×” ×©×œ ×ž×¡×¤×¨ ×‘×¨×™×©×•× */}
                    {/* ××™×¨×•×¢×™ ×”××¨×›×™×•×Ÿ (src=tf/ob, tools/backfill_platform_rows.py) × ×’×–×¨×™× ×ž×§×•×‘×¥
                        ×”×ª×—× ×•×ª ×‘×œ×‘×“ â€” ×”×¨×¦×™×£ ×”×•×¤×™×¢/× ×¢×œ× ×ž×”×¨×™×©×•×, ×•×ž×™ ×¢×¦×¨ ×‘×• ×œ× ×™×“×•×¢ */}
                    {c.k === "platform" && c.pv && (c.st === "add"
                      ? <> Â· ðŸ†• <b>×¨×¦×™×£ {c.pl}</b> × ×•×¡×£{(c.lines || []).length ? <> â€” ×¢×•×¦×¨×™× ×‘×•: {c.lines.join(", ")}</> : c.src ? " (×œ×¤×™ ×§×•×‘×¥ ×”×ª×—× ×•×ª ×‘××¨×›×™×•×Ÿ)" : ""}</>
                      : <> Â· âž– <b>×¨×¦×™×£ {c.pl}</b> ×‘×•×˜×œ{(c.lines || []).length ? <> (× ×©××¨ ×‘×œ×™ ×§×•×•×™×) â€” ×¢×¦×¨×• ×‘×•: {c.lines.join(", ")}</> : c.src ? " (× ×¢×œ× ×ž×§×•×‘×¥ ×”×ª×—× ×•×ª ×‘××¨×›×™×•×Ÿ)" : ""}</>)}
    {/* × ×™×¡×•×— ×¤×©×•×˜ (×‘×§×©×ª ×©×œ×ž×”): "×”×©× ×”×™×©×Ÿ ×”×™×”â€¦ ×”×©× ×”×—×“×© ×”×•×â€¦". ××™×¨×•×¢×™
                        "×”×¤×›×”/×—×“×œ×” ×œ×”×™×•×ª ×ª×—× ×ª ×™×¢×“" ×”×•×¡×¨×• ×›×œ×™×œ â€” ×¨×§ ×©×™× ×•×™×™ ×©× */}
                    {c.k === "pubdest" && c.st === "ren" &&
                      <> Â· ×ª×—× ×ª ×”×™×¢×“ ×œ×¤×¨×¡×•× ×©×•Ö¼× ×ª×” Â· ×”×©× ×”×™×©×Ÿ ×”×™×”: <b>{c.oh}</b> Â· ×”×©× ×”×—×“×© ×”×•×: <b>{c.nh}</b></>}
                    {c.k === "moved" && (c.ola != null
                      ? <> Â· ×”×•×–×–×” <b>{c.dist || c.m} ×ž×³</b> Â· <s dir="ltr">({c.ola}, {c.olo})</s> â† <b dir="ltr">({c.la}, {c.lo})</b></>
                      : <> Â· ×”×•×–×–×” <b>{c.dist || c.m} ×ž×³</b> Â· ××œ <b dir="ltr">({c.la}, {c.lo})</b></>)}
                    {c.lines && c.lines.length > 0 && c.k !== "platform" && <> Â· {c.k === "new" ? "×§×•×•×™× ×©×¢×¦×¨×• ×‘×” ×ž×”×¤×ª×™×—×”" : "×§×•×•×™× ×©×¢×¦×¨×• ×‘×” ××–"}: {c.lines.slice(0, 10).join(", ")}</>}
                    {c.la != null && <> Â· <button className="mapbtn" aria-expanded={openKey === k0}
                      aria-label={"×ž×¤×ª ×”×ª×—× ×” " + (c.n || c.nn || c.c)}
                      onClick={(e) => { e.stopPropagation(); setOpenKey(openKey === k0 ? null : k0); }}>ðŸ—ºï¸</button></>}
                  </span>
                </div>
                {openKey === k0 && c.la != null && <StopEvMap ev={c} />}
                </React.Fragment>
              );
            };
            const rows = shown.map((g) => {
              if (g.evs.length === 1) return evRow(g.evs[0], true);
              const head = g.evs[0];
              const nm = head.nn || head.n || (g.evs.find((e) => e.n || e.nn) || {}).n || "";
              return (
                <div className="sgroup" key={g.code}>
                  <div className="srow ghead">
                    <span className="nm"><b>{nm}</b>
                      <StopCode code={g.code} />
                      <a className="latlink" href={"#stop=" + g.code} title="××™×œ×• ×§×•×•×™× ×¢×¦×¨×• ×‘×ª×—× ×” ×•×ž×” ×”×©×ª× ×”"
                        onClick={(e) => e.stopPropagation()}>ðŸšŒ ×§×•×•×™×</a></span>
                    <span className="meta">{head.t ? head.t + " Â· " : ""}{g.evs.length} ×©×™× ×•×™×™×</span>
                  </div>
                  {g.evs.map((c) => evRow(c, false))}
                </div>
              );
            });
            return (
              <React.Fragment>
                {rows}
                {groups.length > lim && (
                  <button className="morebtn" onClick={() => setLim(lim + 150)}>
                    âŒ„ ×”×¦×’ ×¢×•×“ â€” ×ž×•×¦×’×•×ª {shown.length.toLocaleString()} ×ž×ª×•×š {groups.length.toLocaleString()} ×ª×—× ×•×ª
                  </button>
                )}
              </React.Fragment>
            );
          })()}
          {list.length === 0 && (chErr
            ? <NetErr onRetry={() => setRty((n) => n + 1)} />
            : <div className="empty">××™×Ÿ ×©×™× ×•×™×™× ×ª×•××ž×™× ×‘×—×•×“×© ×”×–×”.</div>)}
        </div>
      )}
    </div>
  );
}


/* ---------- ××¤×œ×™×§×¦×™×” ---------- */
// ×¡×•×’×™ ×ª×—×‘×•×¨×” ×©××™× × ××•×˜×•×‘×•×¡. ×¢×“ ×™×•×œ×™ 2026 ×”×¡×•×¨×§ ×¡×™× ×Ÿ ×›×œ route_type ×©××™× ×• 3,
// ×•×œ×›×Ÿ ×”×¨×›×‘×ª, ×ž×•× ×™×•×ª ×”×©×™×¨×•×ª ×•×”×¨×›×‘×ª ×”×§×œ×” ×œ× ×”×™×• ×‘××ª×¨ ×›×œ×œ â€” ×œ×ž×¨×•×ª ×©×”× ×™×•×©×‘×™×
// ×‘××•×ª×• ×¤×™×“ ×•× ×•×©××™× route_desc ×‘××•×ª×• ×¤×•×¨×ž×˜. ×”×¨×›×‘×ª ×”×§×œ×” ×•×”×›×¨×ž×œ×™×ª ×ž×•×¦×’×•×ª
// ×›×§×‘×•×¦×” ××—×ª (×‘×§×©×ª ×”×ž×©×ª×ž×©).
// ×›×œ ×¡×•×’ ×ª×—×‘×•×¨×” ×”×•× ×§×˜×’×•×¨×™×” ×¢×•×ž×“×ª ×‘×¤× ×™ ×¢×¦×ž×” â€” ×¨×›×‘×ª ×•×ž×•× ×™×•×ª ×©×™×¨×•×ª ××™× ×Ÿ
// ××•×ª×• ×“×‘×¨ ×•××™× ×Ÿ ×—×•×œ×§×•×ª ×ž×¡×š (×‘×§×©×ª ×”×ž×©×ª×ž×©). "×©×™×¨×•×ª ×œ×¤×™ ×“×¨×™×©×”" ××™× ×• ×›××Ÿ:
// ×”×•× ×ž×•×¤×¢×œ ×‘×™×“×™ ×—×‘×¨×•×ª ×”××•×˜×•×‘×•×¡ ×•×™×•×©×‘ ×ª×—×ª "×§×•×•×™×".
/* ---------- ×ž×¤×ª ×”×¢×™×¨ ×œ×¤×™ ×—×•×“×© (×©×œ×ž×” 18.09) ---------- */
// ×‘×•×—×¨×™× ×¢×™×¨, ×©× ×” ×•×—×•×“×© â€” ×•×¨×•××™× ×¢×œ ×”×ž×¤×” ××™×š ×–×” × ×¨××” ××–: ×ª×—× ×•×ª ×©×”×©×ª× ×•
// ×‘××•×ª×• ×—×•×“×© (×œ×—×™×¦×” ×¢×œ ×¡×™×ž×Ÿ = ×ž×” ×§×¨×” ×œ×ª×—× ×”), ××• ×§×•×•×™× â€” ×ž×¡×•×ž× ×™× ×¨×§
// ×”×§×•×•×™× ×©×‘×”× ×‘×•×¦×¢×• ×©×™× ×•×™×™× ×ž×”×¡×•×’×™× ×©× ×‘×—×¨×•, ×¢×œ ×”×ž×¡×œ×•×œ ×›×¤×™ ×©×”×™×” ××–.
// ×¦×‘×¢ ×œ×›×œ ×§×• (×œ×¤×™ ×ž×¡×¤×¨×•) â€” ×›×œ ×§×•×•×™ ×”×¢×™×¨ ×‘×¦×‘×¢×™×, ×›×ž×• ×‘×ž×¤×ª ×§×•×•×™× (×©×œ×ž×” 18.09)
const LINE_PALETTE = ["#2563eb", "#059669", "#d97706", "#7c3aed", "#db2777", "#0891b2", "#65a30d", "#ea580c", "#4f46e5", "#0d9488", "#c026d3", "#b45309"];
const lineHash = (str) => { let h = 0; for (const ch of String(str)) h = (h * 31 + ch.charCodeAt(0)) >>> 0; return h; };
const STOP_KIND_LIST = ["new", "del", "renamed", "moved", "city", "pubdest", "platform", "gtfs2012"];
const LINE_KIND_SKIP = new Set(["baseline", "snapshot"]);
function MapTab({ idx, openLine, cities }) {
  const [months, setMonths] = useState(null);
  const [city, setCity] = usePersistedQ("lh-map-city");
  const [yr, setYr] = useState("");
  const [mon, setMon] = useState("");
  const [mode, setMode] = useState("stops");
  const [kinds, setKinds] = useState(() => new Set());
  const [stopChs, setStopChs] = useState(null);
  const [lineChs, setLineChs] = useState(null);
  const [routes, setRoutes] = useState({});       // rd â†’ [[lat,lon],â€¦] ×©×œ ×”×’×¨×¡×” ×©×”×™×™×ª×” ×‘×ª×•×§×£ ××–
  const [err, setErr] = useState(null);
  // "2012" ×‘×ž×¤×” (×©×œ×ž×” 22.09): ×›×œ ×”×ª×—× ×•×ª ×©×”×™×• ×‘×¢×™×¨ ×‘×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ ×™×•× ×™ 2012
  // (GTFS ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×“×¨×š OpenStreetMap, changeset 12028672) â€” × ×˜×¢×Ÿ ×¤×¢× ××—×ª
  const [snap12, setSnap12] = useState(null);
  const is2012 = yr === "2012";
  useEffect(() => {
    if (!is2012 || snap12) return;
    dfetch("../magihim-2012/data/stops-2012.json").then((r) => (r.ok ? r.json() : { stops: {} })).then((d) => setSnap12(d.stops || {})).catch(() => setSnap12({}));
  }, [is2012, snap12]);
  const mapRef = useRef(null), mapObj = useRef(null), layer = useRef(null);
  const cache = useRef({});
  useEffect(() => { getMonths().then((d) => {
    const ms = (d.months || []).slice().sort();
    setMonths(ms);
    if (ms.length) { const last = ms[ms.length - 1]; setYr((c) => c || last.slice(0, 4)); setMon((c) => c || last); }
  }).catch(() => setErr("months")); }, []);
  const canon = useMemo(() => {
    const c = (city || "").trim();
    if (!c) return "";
    return (cities || []).find((x) => x === c) || (cities || []).find((x) => x.includes(c) || c.includes(x)) || c;
  }, [city, cities]);
  const monthEnd = mon ? mon + "-31" : "";
  // ×ž×™×§×•×“ ×¢×œ ×”×¢×™×¨ ×‘×¨×’×¢ ×”×‘×—×™×¨×” (×©×œ×ž×” 18.09) â€” ×’×‘×•×œ×•×ª ×”×ª×—× ×•×ª ×©×œ ×›×œ ×¢×™×¨, data/cities.json
  const [cityBox, setCityBox] = useState(null);
  useEffect(() => { dfetch("data/cities.json").then((r) => r.json()).then(setCityBox).catch(() => {}); }, []);
  useEffect(() => {
    const map = mapObj.current, b = cityBox && canon && cityBox[canon];
    if (!map || !b) return;
    map.fitBounds([[b[0], b[1]], [b[2], b[3]]], { padding: [20, 20], maxZoom: 14 });
  }, [canon, cityBox]);
  // ××™×¨×•×¢×™ ×ª×—× ×•×ª / ×©×™× ×•×™×™ ×§×•×•×™× ×©×œ ×”×—×•×“×©
  useEffect(() => {
    if (!mon) return;
    setStopChs(null); setLineChs(null); setErr(null);
    const a = dfetch("data/changes/stops-" + mon + ".json").then((r) => r.json()).then((d) => setStopChs(d.changes || [])).catch(() => setStopChs([]));
    const b = dfetch("data/changes/" + mon + ".json").then((r) => r.json()).then((d) => setLineChs(d.changes || [])).catch(() => setLineChs([]));
    Promise.all([a, b]).catch(() => {});
  }, [mon]);
  const lineOf = useMemo(() => { const m = {}; (((idx || {}).lines) || []).forEach((l) => { m[l.rd] = l; }); return m; }, [idx]);
  // ×ª×—× ×•×ª ×©×œ ×”×¢×™×¨ ×©×”×©×ª× ×• ×‘×—×•×“×© â€” ×ž×§×•×‘×¦×•×ª ×œ×¤×™ ×ž×§"×˜
  const stopGroups = useMemo(() => {
    if (is2012) {
      if (!snap12 || !canon) return [];
      // ×”×¢×™×¨ ×”×™× ×¡×•×£ ×”×›×ª×•×‘×ª ×‘×¨×™×©×•× 2012; ×©×ž×•×ª ×¢×¨×™× ×©× ×›×ª×‘×• ××– ××—×¨×ª
      const ALIAS = { "×ª×œ ××‘×™×‘ ×™×¤×•": ["×ª×œ ××‘×™×‘"], "× ×•×£ ×”×’×œ×™×œ": ["× ×¦×¨×ª ×¢×™×œ×™×ª"], "×ž×¢×œ×•×ª ×ª×¨×©×™×—×": ["×ž×¢×œ×•×ª"], "×™×”×•×“ ×ž×•× ×•×¡×•×Ÿ": ["×™×”×•×“"], "×“×™×™×¨ ×—× ×": ["×“×™×¨ ×—× ×"] };
      const names = [canon].concat(ALIAS[canon] || []);
      const out = [];
      for (const code in snap12) {
        const [n, la, lo, addr] = snap12[code];
        const a = (addr || "").trim();
        if (!names.some((c) => a === c || a.endsWith(" " + c))) continue;
        out.push({ code, evs: [{ c: code, d: "2012-06-26", k: "gtfs2012", n, t: a, la, lo }] });
      }
      return out;
    }
    if (!stopChs || !canon) return [];
    const g = {};
    stopChs.forEach((c) => {
      if (c.la == null || (c.t || "") !== canon) return;
      if (c.k === "platform" && !c.pv) return;
      if (kinds.size && !kinds.has(c.k)) return;
      (g[c.c] = g[c.c] || []).push(c);
    });
    return Object.entries(g).map(([code, evs]) => ({ code, evs: evs.sort((a, b) => a.d < b.d ? -1 : 1) }));
  }, [stopChs, canon, kinds, is2012, snap12]);
  const stopKindCounts = useMemo(() => {
    const n = {};
    (stopChs || []).forEach((c) => { if (c.la != null && (c.t || "") === canon && !(c.k === "platform" && !c.pv)) n[c.k] = (n[c.k] || 0) + 1; });
    return n;
  }, [stopChs, canon]);
  // ×§×•×•×™× ×©×œ ×”×¢×™×¨ (×œ×¤×™ ×¢×¨×™ ×”×§×¦×”) ×©×”×©×ª× ×• ×‘×—×•×“×© â€” ×œ×¤×™ ×¡×•×’×™ ×”×©×™× ×•×™ ×©× ×‘×—×¨×•
  const lineGroups = useMemo(() => {
    if (!lineChs || !canon) return [];
    const g = {};
    lineChs.forEach((c) => {
      const l = lineOf[c.rd];
      if (!l || LINE_KIND_SKIP.has(c.k)) return;
      if (!destCities(l.dest).includes(canon)) return;
      (g[c.rd] = g[c.rd] || { rd: c.rd, l, chs: [] }).chs.push(c);
    });
    return Object.values(g).sort((a, b) => lineNum(a.l.line) - lineNum(b.l.line));
  }, [lineChs, canon, lineOf]);
  const lineKindCounts = useMemo(() => {
    const n = {};
    lineGroups.forEach((x) => x.chs.forEach((c) => { n[c.k] = (n[c.k] || 0) + 1; }));
    return n;
  }, [lineGroups]);
  const shownLines = useMemo(() => kinds.size ? lineGroups.filter((x) => x.chs.some((c) => kinds.has(c.k))) : [], [lineGroups, kinds]);
  // ×›×œ ×§×•×•×™ ×”×¢×™×¨ ×©×™×›×œ×• ×œ×”×ª×§×™×™× ×‘××•×ª×• ×—×•×“×© (×•×¨×™×× ×˜ ×©×‘×•×˜×œ ×œ×¤× ×™ ×”×—×•×“×© ×ž×“×•×œ×’);
  // ×ž×” ×©×‘××ž×ª ×”×™×” ×‘×ª×•×§×£ ××– × ×§×‘×¢ ×ž×§×•×‘×¥ ×”×§×• â€” ×”×’×¨×¡×” ×”××—×¨×•× ×” ×¢×“ ×¡×•×£ ×”×—×•×“×©
  const cityLines = useMemo(() => {
    if (!canon || !mon) return [];
    return (((idx || {}).lines) || []).filter((l) => destCities(l.dest).includes(canon) && !(l.lk === "removed" && (l.ld || "") < mon + "-01"));
  }, [idx, canon, mon]);
  useEffect(() => setKinds(new Set()), [mode, canon]);
  const toggleKind = (k) => setKinds((s) => { const n = new Set(s); if (n.has(k)) n.delete(k); else n.add(k); return n; });
  // ×ž×¡×œ×•×œ×™ ×”×§×•×•×™× ×”×ž×¡×•×ž× ×™× â€” ×”×’×¨×¡×” ×”××—×¨×•× ×” ×¢× ×©×¨×˜×•×˜ ×¢×“ ×¡×•×£ ×”×—×•×“×©.
  // ×›×œ ×”×§×•×•×™× × ×˜×¢× ×™× (×‘×œ×™ ×ª×§×¨×”, ×‘×§×©×ª ×©×œ×ž×”): ×‘×ž× ×•×ª ×©×œ 8 ×‘×ž×§×‘×™×œ, ×”×ž×¤×”
  // ×ž×ª×ž×œ××ª ×ª×•×š ×›×“×™ ×•×¤×¡ ×”×ª×§×“×ž×•×ª ×ž×¨××” ×›×ž×” × ×©××¨.
  const [prog, setProg] = useState(null);   // {done, total} ×‘×–×ž×Ÿ ×˜×¢×™× ×”
  useEffect(() => {
    if (mode !== "lines" || !monthEnd) return;
    const want = cityLines.map((l) => l.rd).filter((rd) => !(rd + "@" + mon in cache.current));
    if (!want.length) { setProg(null); return; }
    let alive = true, i = 0, done = 0;
    setProg({ done: 0, total: want.length });
    const flush = () => setRoutes((r) => { const o = { ...r }; want.forEach((rd) => { if (rd + "@" + mon in cache.current) o[rd + "@" + mon] = cache.current[rd + "@" + mon]; }); return o; });
    const one = (rd) => dfetch("data/lines/" + fsafe(rd) + ".json").then((r) => r.json()).then((lf) => {
      const m = materializeLf(lf);
      const vs = (m.versions || []).filter((v) => v.d <= monthEnd && !hiddenEv(v));
      const last = vs[vs.length - 1];
      // ×œ× ×”×™×” ×‘×ª×•×§×£ ××–: ×¢×•×“ ×œ× ×ª×•×¢×“, ××• ×©×”×’×¨×¡×” ×”××—×¨×•× ×” ×¢×“ ××– ×”×™× ×‘×™×˜×•×œ
      if (!last || last.k === "removed" || last.k === "planned-dropped") { cache.current[rd + "@" + mon] = null; return; }
      const v = [...vs].reverse().find((x) => typeof x.shp === "string" && x.shp.length > 2);
      cache.current[rd + "@" + mon] = v ? decodeShape(v.shp) : null;
    }).catch(() => { cache.current[rd + "@" + mon] = null; });
    const worker = () => {
      if (!alive) return Promise.resolve();
      if (i >= want.length) return Promise.resolve();
      const rd = want[i++];
      return one(rd).then(() => {
        done++;
        if (!alive) return;
        if (done % 5 === 0 || done === want.length) setProg({ done, total: want.length });   // ×”×ž×¤×” ×ž×¦×•×™×¨×ª ×¤×¢× ××—×ª, ×‘×¡×•×£
        return worker();
      });
    };
    Promise.all(Array.from({ length: 8 }, worker)).then(() => { if (alive) { flush(); setProg(null); } });
    return () => { alive = false; };
  }, [cityLines, mode, monthEnd, mon]);
  // ×”×ž×¤×”
  useEffect(() => {
    if (!mapRef.current || mapObj.current) return;
    const map = L.map(mapRef.current, { scrollWheelZoom: true, preferCanvas: true });   // canvas â€” ×ž××•×ª ×ž×¡×œ×•×œ×™× ×‘×œ×™ ×œ×”×™×—× ×§
    L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">OpenStreetMap</a>', maxZoom: 19,
    }).addTo(map);
    map.setView([31.9, 35.0], 8);
    mapObj.current = map;
    layer.current = L.layerGroup().addTo(map);
    return () => { map.remove(); mapObj.current = null; layer.current = null; };
  }, []);
  const stopLabel = (c) => c.k === "platform" ? (c.st === "add" ? "×¨×¦×™×£ " + c.pl + " × ×•×¡×£" : "×¨×¦×™×£ " + c.pl + " ×‘×•×˜×œ")
    : c.k === "renamed" ? "×©×™× ×•×™ ×©×: " + esc(c.on || "") + " â† " + esc(c.nn || c.n || "")
    : c.k === "moved" ? "×”×•×–×–×” " + (c.dist || c.m || "") + " ×ž×³"
    : c.k === "city" ? "×©×™× ×•×™ ×¢×™×¨: " + esc(c.oc || "") + " â† " + esc(c.nc || "")
    : c.k === "pubdest" ? "×ª×—× ×ª ×”×™×¢×“ ×œ×¤×¨×¡×•× ×©×•Ö¼× ×ª×”"
    : c.k === "gtfs2012" ? "×‘×¨×™×©×•× 2012"
    : c.k === "new" ? "×ª×—× ×” ×—×“×©×”" + (c.lines && c.lines.length ? " Â· ×§×•×•×™×: " + c.lines.slice(0, 8).join(", ") : "")
    : c.k === "del" ? "×‘×•×˜×œ×”" + (c.reused ? " Â· ×”×ž×§×´×˜ ×”×•×§×¦×” ×ž×—×“×© ×œ×ª×—× ×” ××—×¨×ª" : "") + (c.lines && c.lines.length ? " Â· ×¢×¦×¨×• ×‘×”: " + c.lines.slice(0, 8).join(", ") : "")
    : (SKINDS[c.k] || { label: c.k }).label;
  useEffect(() => {
    const map = mapObj.current, lg = layer.current;
    if (!map || !lg) return;
    lg.clearLayers();
    const pts = [];
    if (mode === "stops") {
      stopGroups.forEach((g) => {
        const last = g.evs[g.evs.length - 1];
        const color = (SKINDS[last.k] || {}).color || "#2563eb";
        pts.push([last.la, last.lo]);
        const html = `<b>${esc(last.nn || last.n || "")}</b> <span class="pcode" dir="ltr">${esc(g.code)}</span><br>` +
          (last.k === "gtfs2012" ? `<span class="pst">${esc(last.t || "")}</span><br><span class="pst">×¨×™×©×•× 2012 Â· GTFS 06.2012 ×“×¨×š OSM</span><br>` : "") +
          g.evs.map((c) => `<span class="pst"><i style="display:inline-block;width:9px;height:9px;border-radius:50%;background:${(SKINDS[c.k] || {}).color || "#64748b"};margin-inline-end:5px"></i>${c.b12 ? "×‘×™×Ÿ 2012 ×œ-2017" : fmtD(c.d)} Â· ${stopLabel(c)}</span>`).join("<br>") +
          `<br><a href="#stop=${encodeURIComponent(g.code)}" class="plink">×›×œ ×”×”×™×¡×˜×•×¨×™×” ×©×œ ×”×ª×—× ×” â†</a>`;
        L.circleMarker([last.la, last.lo], { radius: 8, color: "#fff", weight: 2, fillColor: color, fillOpacity: 0.95 })
          .bindPopup(html, { className: "lh-pop", maxWidth: 320 }).addTo(lg);
      });
    } else {
      const sel = new Set(shownLines.map((x) => x.rd));
      // ×›×œ ×§×•×•×™ ×”×¢×™×¨ ×›×¤×™ ×©×”×™×• ××– â€” ××¤×•×¨ ×“×§; ×”×§×•×•×™× ×©×”×©×ª× ×• (×œ×¤×™ ×”×¡×•×’×™× ×©× ×‘×—×¨×•) ×ž×¢×œ×™×”×, ×¦×‘×•×¢×™×
      cityLines.forEach((l) => {
        const r = routes[l.rd + "@" + mon];
        if (!r || !r.length || sel.has(l.rd)) return;
        r.forEach((p) => pts.push(p));
        const html = `<b>×§×• ${esc(l.line || "")}</b> Â· ${esc(l.op || "")}<br><span class="pcode">${esc(l.dest || "")}</span><br><a href="#${encodeURIComponent(l.rd)}" class="plink">×œ×¢×ž×•×“ ×”×§×• â†</a>`;
        L.polyline(r, { color: LINE_PALETTE[lineHash(l.line || l.rd) % LINE_PALETTE.length], weight: 2.5, opacity: 0.75 }).bindPopup(html, { className: "lh-pop", maxWidth: 320 }).addTo(lg);
      });
      shownLines.forEach((x) => {
        const r = routes[x.rd + "@" + mon];
        if (!r || !r.length) return;
        r.forEach((p) => pts.push(p));
        const k0 = x.chs.find((c) => kinds.has(c.k)) || x.chs[0];
        const color = catColor(k0.k) || (KINDS[k0.k] || {}).color || "#7c3aed";
        const html = `<b>×§×• ${esc(x.l.line || "")}</b> Â· ${esc(x.l.op || "")}<br><span class="pcode">${esc(x.l.dest || "")}</span><br>` +
          x.chs.map((c) => `<span class="pst"><i style="display:inline-block;width:9px;height:9px;border-radius:50%;background:${catColor(c.k)};margin-inline-end:5px"></i>${fmtD(c.d)} Â· <b>${esc((KINDS[c.k] || { label: c.k }).label)}</b>${c.note ? " â€” " + esc(noteFix(c.note)).slice(0, 220) : ""}</span>`).join("<br>") +
          `<br><a href="#${encodeURIComponent(x.rd)}" class="plink">×œ×¢×ž×•×“ ×”×§×• â†</a>`;
        // ×§×• ×©×”×©×ª× ×”: ×ž×¡×’×¨×ª ×œ×‘× ×” ×•×ž×¢×œ×™×” ×¦×‘×¢ ×”×§×˜×’×•×¨×™×”, ×¢×‘×” â€” ×‘×•×œ×˜ ×ž×¢×œ ×©××¨ ×”×§×•×•×™×
        L.polyline(r, { color: "#fff", weight: 9, opacity: 0.95 }).addTo(lg);
        L.polyline(r, { color, weight: 5, opacity: 1 }).bindPopup(html, { className: "lh-pop", maxWidth: 340 }).addTo(lg);
      });
    }
    if (pts.length) map.fitBounds(L.latLngBounds(pts).pad(0.15), { maxZoom: 15 });
  }, [mode, stopGroups, shownLines, cityLines, routes, mon, kinds]);
  const years = months ? [...new Set(months.map((m) => m.slice(0, 4)))].sort().reverse() : [];
  const noRoute = mode === "lines" && shownLines.filter((x) => routes[x.rd + "@" + mon] === null).length;
  return (
    <div className="card">
      <p className="maphint">×‘×•×—×¨×™× ×¢×™×¨, ×©× ×” ×•×—×•×“×© â€” ×•×”×ž×¤×” ×ž×¨××” ××™×š ×–×” × ×¨××” ××–: ×ª×—× ×•×ª ×©×”×©×ª× ×• ×‘××•×ª×• ×—×•×“×© (×œ×—×™×¦×” ×¢×œ ×¡×™×ž×Ÿ = ×ž×” ×§×¨×” ×œ×”), ××• ×›×œ ×§×•×•×™ ×”×¢×™×¨ ×¢×œ ×”×ž×¡×œ×•×œ ×›×¤×™ ×©×”×™×” ××–, ×›×œ ×§×• ×‘×¦×‘×¢ ×ž×©×œ×•; ×¡×™×ž×•×Ÿ ×¡×•×’×™ ×©×™× ×•×™ ×ž×‘×œ×™×˜ ×‘×¢×‘×” ××ª ×”×§×•×•×™× ×©×‘×”× ×–×” ×§×¨×” ×‘××•×ª×• ×—×•×“×©.</p>
      <input className="search" list="lh-map-cities" value={city} onChange={(e) => setCity(e.target.value)} placeholder="×¢×™×¨â€¦ (×œ×ž×©×œ ×—×•×œ×•×Ÿ)" aria-label="×¢×™×¨" />
      <datalist id="lh-map-cities">{(cities || []).map((c) => <option key={c} value={c} />)}</datalist>
      {months && (
        <div className="months" style={{ marginTop: 10 }}>
          <button className={"mchip" + (is2012 ? " on" : "")} aria-pressed={is2012} title="×›×œ ×”×ª×—× ×•×ª ×©×”×™×• ×‘×¢×™×¨ ×‘×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×ž×™×•× ×™ 2012"
            onClick={() => { setYr("2012"); setMode("stops"); setKinds(new Set()); }}>2012</button>
          {years.map((y) => (
            <button key={y} className={"mchip" + (yr === y ? " on" : "")} aria-pressed={yr === y}
              onClick={() => { setYr(y); const ms = months.filter((m) => m.startsWith(y)); if (!ms.includes(mon)) setMon(ms[ms.length - 1]); }}>{y}</button>
          ))}
        </div>
      )}
      {months && yr && !is2012 && (
        <div className="months">
          {months.filter((m) => m.startsWith(yr)).slice().reverse().map((m) => (
            <button key={m} className={"mchip" + (mon === m ? " on" : "")} aria-pressed={mon === m} onClick={() => setMon(m)}>{m.split("-").reverse().join(".")}</button>
          ))}
        </div>
      )}
      <div className="tabs" role="tablist" aria-label="×ž×” ×œ×”×¦×™×’ ×¢×œ ×”×ž×¤×”">
        <button role="tab" aria-selected={mode === "stops"} className={"tab" + (mode === "stops" ? " on" : "")} onClick={() => setMode("stops")}>ðŸš ×ª×—× ×•×ª ×©×”×©×ª× ×•</button>
        <button role="tab" aria-selected={mode === "lines"} className={"tab" + (mode === "lines" ? " on" : "")} onClick={() => setMode("lines")}>ðŸšŒ ×§×•×•×™× ×©×”×©×ª× ×•</button>
      </div>
      {canon && mon && !is2012 && (
        <div className="months">
          {mode === "stops"
            ? STOP_KIND_LIST.filter((k) => stopKindCounts[k]).map((k) => (
              <button key={k} className={"mchip" + (kinds.has(k) ? " on" : "")} aria-pressed={kinds.has(k)} onClick={() => toggleKind(k)}>
                <i className="katdot" style={{ background: SKINDS[k].color, display: "inline-block", width: 9, height: 9, borderRadius: "50%", marginInlineEnd: 5 }} />{SKINDS[k].label} <b>{stopKindCounts[k]}</b></button>))
            : Object.keys(lineKindCounts).sort((a, b) => lineKindCounts[b] - lineKindCounts[a]).map((k) => (
              <button key={k} className={"mchip" + (kinds.has(k) ? " on" : "")} aria-pressed={kinds.has(k)} onClick={() => toggleKind(k)}>
                <i className="katdot" style={{ background: catColor(k), display: "inline-block", width: 9, height: 9, borderRadius: "50%", marginInlineEnd: 5 }} />{(KINDS[k] || { label: k }).label} <b>{lineKindCounts[k]}</b></button>))}
          {kinds.size > 0 && <button className="katclear" onClick={() => setKinds(new Set())}>âœ– × ×§×”</button>}
        </div>
      )}
      <div className="mapstat">
        {is2012 ? (!canon ? "×‘×—×¨×• ×¢×™×¨ ×›×“×™ ×œ×”×ª×—×™×œ" : !snap12 ? "×˜×•×¢×Ÿâ€¦" : <>{stopGroups.length.toLocaleString()} ×ª×—× ×•×ª ×‘{canon} ×‘×¨×™×©×•× 2012 Â· ×”×ž×§×•×¨: ×¨×™×©×•× ×”×ª×—× ×•×ª ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×ž×™×•× ×™ 2012 (GTFS), ×›×¤×™ ×©×™×•×‘× ×œ-<a href="https://www.openstreetmap.org/changeset/12028672" target="_blank" rel="noopener">OpenStreetMap ×‘-26.06.2012</a> (33,055 ×ª×—× ×•×ª ×‘××¨×¥, ×¨×™×©×™×•×Ÿ ODbL) Â· ×œ×—×™×¦×” ×¢×œ ×ª×—× ×”: ×©×, ×ž×§×´×˜ ×•×›×ª×•×‘×ª ×©×œ ××–</>)
          : !canon ? "×‘×—×¨×• ×¢×™×¨ ×›×“×™ ×œ×”×ª×—×™×œ" : !mon ? "×‘×—×¨×• ×—×•×“×©" : (mode === "stops" ? (stopChs === null ? "×˜×•×¢×Ÿâ€¦" : stopGroups.length ? `${stopGroups.length} ×ª×—× ×•×ª ×‘${canon} ×”×©×ª× ×• ×‘-${fmtM(mon)}` : `××™×Ÿ ×ª×—× ×•×ª ×‘${canon} ×©×”×©×ª× ×• ×‘-${fmtM(mon)}`)
          : (lineChs === null || prog ? "×˜×•×¢×Ÿâ€¦" : `${cityLines.filter((l) => routes[l.rd + "@" + mon]).length} ×§×•×•×™× ×©×œ ${canon} ×”×™×• ×‘×ª×•×§×£ ×‘-${fmtM(mon)}` + (lineGroups.length ? ` Â· ${lineGroups.length} ×ž×”× ×”×©×ª× ×• ×‘××•×ª×• ×—×•×“×©` + (kinds.size ? ` Â· ${shownLines.length} ×ž×•×‘×œ×˜×™×` : " â€” ×¡×ž× ×• ×¡×•×’×™ ×©×™× ×•×™ ×›×“×™ ×œ×¦×‘×•×¢ ××•×ª×") : "") + (noRoute && shownLines.length ? ` Â· ×œ-${noRoute} ×ž×”×ž×¡×•×ž× ×™× ××™×Ÿ ×©×¨×˜×•×˜ ×ž××•×ª×• ×–×ž×Ÿ` : "")))}
      </div>
      <div className="mapwrap">
        <div className="citymap" ref={mapRef} role="application" aria-label="×ž×¤×ª ×”×©×™× ×•×™×™× ×‘×¢×™×¨ ×œ×¤×™ ×—×•×“×©" />
        {prog && <div className="mapload" role="status" aria-live="polite">
          <div className="mapprog-t">×˜×•×¢×Ÿ ××ª ×›×œ ×§×•×•×™ {canon} ×›×¤×™ ×©×”×™×• ×‘-{fmtM(mon)}â€¦ {prog.done}/{prog.total}</div>
          <div className="mapprog-s">×”×ž×¤×” ×ª×•×¦×’ ×›×©×›×œ ×”×§×•×•×™× ×™×”×™×• ×ž×•×›× ×™×</div>
          <div className="mapprog-b"><i style={{ width: Math.round(prog.done / prog.total * 100) + "%" }} /></div>
        </div>}
      </div>
    </div>
  );
}

const TABS = [
  { k: "rail", icon: "ðŸš†", label: "×¨×›×‘×ª", tts: ["rail", "lightrail", "cable"],
    tip: "×¨×›×‘×ª ×™×©×¨××œ, ×”×¨×›×‘×ª ×”×§×œ×” ×‘×™×¨×•×©×œ×™×, ×”×›×¨×ž×œ×™×ª ×•×›×‘×œ ××§×¡×¤×¨×¡ â€” ×”×™×¡×˜×•×¨×™×™×ª ×ž×¡×œ×•×œ×™× ×•×ª×—× ×•×ª",
    groups: [
      { k: "heavy", icon: "ðŸš†", label: "×¨×›×‘×ª ×™×©×¨××œ", tts: ["rail"] },
      { k: "light", icon: "ðŸšŠ", label: "×¨×›×‘×ª ×§×œ×” ×•×›×¨×ž×œ×™×ª", tts: ["lightrail", "cable"] },
    ] },
  { k: "taxi", icon: "ðŸš•", label: "×ž×•× ×™×•×ª ×©×™×¨×•×ª", tts: ["taxi"],
    tip: "×§×•×•×™ ×ž×•× ×™×•×ª ×”×©×™×¨×•×ª ×©×‘×¤×™×“ ×”××¨×¦×™ â€” ×ž×¡×œ×•×œ×™×, ×ª×—× ×•×ª ×•×”×©×™× ×•×™×™× ×‘×”×",
    groups: [] },
];
// ×¨×©×ª 2012 ×”×™× ××•×˜×•×‘×•×¡×™× ×‘×œ×‘×“ â€” ×œ×¡×•×’×™× ×”××œ×” ××™×Ÿ ×©× ×ž×§×‘×™×œ×”
const NO_2012 = new Set(["rail", "lightrail", "cable", "taxi"]);
const TT_ICON = { rail: "ðŸš†", taxi: "ðŸš•", lightrail: "ðŸšŠ", cable: "ðŸš¡", demand: "ðŸš" };
// ×ž×§×•×¨ ×”××™×¨×•×¢ â€” ×©×œ×•×©×” ×ž×§×•×¨×•×ª ×©×•× ×™× ×œ×—×œ×•×˜×™×Ÿ, ×•×›×œ ××—×“ ×¢× ×“×™×•×§ ××—×¨. ×‘×œ×™
// ×œ× ×§×•×‘ ×‘×©×, "×ž××¨×›×™×•×Ÿ ×”×¤×™×“ ×”××¨×¦×™" ×œ× ××•×ž×¨ ×ž×™ ×ž×“×“ ×•×ž×ª×™.
const SRC_LABEL = {
  tf: "×ž×§×•×¨: ××¨×›×™×•×Ÿ TransitFeeds / OpenMobilityData â€” ×¦×™×œ×•×ž×™ ×”×¤×™×“ ×”××¨×¦×™ ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” (×”××¨×›×™×•×Ÿ ×ž×›×¡×” 03.2017â€“12.2022; ×–×” ×˜×•×•×— ×”×ž×§×•×¨, ×œ× ×˜×•×•×— ×”×§×•)",
  tf17: "×ž×§×•×¨: ××¨×›×™×•×Ÿ TransitFeeds / OpenMobilityData â€” ×¦×™×œ×•× 16.3.2017, ×”×™×©×Ÿ ×‘×™×•×ª×¨ ×©×§×™×™×",
  ob: "×ž×§×•×¨: ××¨×›×™×•×Ÿ ×”×¡×“× × ×œ×™×“×¢ ×¦×™×‘×•×¨×™ (Open Bus) â€” ×¦×™×œ×•×ž×™× ×™×•×ž×™×™×, 01.2022â€“07.2026",
  v10: "×ž×§×•×¨: ×§×•×‘×¥ ×”×¨×™×©×•×™ ×”×™×•×ž×™ Gtfs_10_days ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” â€” ×”×¤×•×¨×ž×˜ ×©×ž×™×™×¦×’ ×›×ž×” ×¨×›×‘×™× ×‘××•×ª×” ×™×¦×™××”",
  _daily: "×ž×§×•×¨: ×”×¡×¨×™×§×” ×”×™×•×ž×™×ª ×©×œ× ×• â€” ×”×©×•×•××ª ×”×¤×™×“ ×”××¨×¦×™, ×™×•× ×ž×•×œ ×™×•×",
  rishui: "×ž×§×•×¨: ×ž××’×¨ \"×¨×™×©×•×™ ×ž×¢×¨×š ×”××•×˜×•×‘×•×¡×™×\" ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” (data.gov.il) â€” ×¡×•×’ ×•×’×•×“×œ ×”×¨×›×‘ ×©× ×§×‘×¢×• ×œ×§×•, ×©×•×¨×” ×œ×›×œ ×ž×§\"×˜ ×œ×›×œ ×™×•× ×ž-2022",
  ctl: "×ž×§×•×¨: ×¨×©×™×ž×ª ×”××©×›×•×œ×•×ª (ClusterToLine) ×©×ž×©×¨×“ ×”×ª×—×‘×•×¨×” ×ž×¤×¨×¡× ×œ×¦×“ ×œ×•×— ×”×–×ž× ×™× â€” ×¡×•×’ ×”×§×• ×•××©×›×•×œ ×”×ž×›×¨×–, ×¦×™×œ×•× ×™×•×ž×™ ×ž-03.2022 ×‘××¨×›×™×•×Ÿ ×”×¡×“× × ×œ×™×“×¢ ×¦×™×‘×•×¨×™; ×”×™×™×—×•×“×™×•×ª (×ª×œ×ž×™×“×™×/×œ×™×œ×”/×ž×–×™× ×™×) â€” ×§×•×‘×¥ ×”× ×•×¡×¢×™× ×©×œ ×”×ž×©×¨×“",
};

// ×¨×©×™×ž×ª ×”×ž×§×•×¨×•×ª ×”×ž×œ××”. ×”×™× ×ž×•×¦×’×ª ×œ×ž×©×ª×ž×© ×•×œ× ×¨×§ ×ž×ª×•×¢×“×ª ×‘×§×•×“: ×ž×™ ×©×§×•×¨×
// "×ª×—× ×” ×‘×•×˜×œ×” ×‘-2019" ×¦×¨×™×š ×œ×“×¢×ª ×ž××™×¤×” ×–×” ×™×“×•×¢, ×•×ž×” ×”×’×‘×•×œ ×©×œ ×ž×” ×©×™×“×•×¢.
const SOURCES = [
  { t: "×”×¡×¨×™×§×” ×”×™×•×ž×™×ª ×©×œ× ×•", d: "×ž-25.07.2026 ×•×”×œ××”",
    b: "×”×•×¨×“×” ×™×•×ž×™×ª ×©×œ ×”×¤×™×“ ×”××¨×¦×™ (israel-public-transportation.zip) ×•×”×©×•×•××” ×ž×•×œ ×”×™×•× ×”×§×•×“×. ×–×” ×”×ž×§×•×¨ ×”×—×™ â€” ×›×œ ×ž×” ×©×ž×›××Ÿ ×•×”×œ××” × ×ž×“×“ ×‘×™×•× ×©×‘×• ×§×¨×”." },
  { t: "×§×•×‘×¥ ×”×¨×™×©×•×™ ×”×™×•×ž×™ Gtfs_10_days", d: "×ž-08.2026 ×•×”×œ××”",
    b: "×”×¤×•×¨×ž×˜ ×”×—×“×© ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×”, ×”×™×—×™×“ ×©×ž×™×™×¦×’ ×©× ×™ ××•×˜×•×‘×•×¡×™× ××• ×©×œ×•×©×” ×©×™×•×¦××™× ×‘××•×ª×” ×“×§×” ×¢×œ ××•×ª×” × ×¡×™×¢×”. ×ž×ž× ×• × ×¨×©×ž×™× ×©×™× ×•×™×™ ×ª×’×‘×•×¨. ×”××¨×›×™×•× ×™× ×œ× ×©×ž×¨×• ××•×ª×•, ×•×œ×›×Ÿ ××™×Ÿ ×œ×• ×”×™×¡×˜×•×¨×™×”." },
  { t: "××¨×›×™×•×Ÿ ××•×¤×Ÿ ×‘××¡ â€” ×”×¡×“× × ×œ×™×“×¢ ×¦×™×‘×•×¨×™", d: "16.01.2022 â€“ 24.07.2026",
    b: "×¦×™×œ×•×ž×™× ×™×•×ž×™×™× ×©×œ ×”×¤×™×“ ×”××¨×¦×™. ×ž×”× × ×‘× ×ª×” ×”×™×¡×˜×•×¨×™×™×ª ×”×§×•×•×™× ×•×”×ª×—× ×•×ª ×œ×ª×§×•×¤×” ×”×–×•, ×•×ž×ž× ×• ×’× ×”×©×™× ×•×™×™× ×©×ª×•×›× × ×• ×•×œ× × ×›× ×¡×• ×œ×ª×•×§×£ ×ž×™× ×•××¨ 2023 ×•××™×œ×š." },
  { t: "××¨×›×™×•×Ÿ TransitFeeds / OpenMobilityData", d: "16.03.2017 â€“ 14.01.2022",
    b: "799 ×¦×™×œ×•×ž×™× ×©×œ ×”×¤×™×“ ×”××¨×¦×™. 16.03.2017 ×”×•× ×”×¦×™×œ×•× ×”×™×©×Ÿ ×‘×™×•×ª×¨ ×©×§×™×™× ×©×. ×ž×©× ×ž×’×™×¢×” ×›×œ ×”×”×™×¡×˜×•×¨×™×” ×©×œ×¤× ×™ 2022, ×‘×§×•×•×™× ×•×‘×ª×—× ×•×ª ×›××—×“." },
  { t: "×¨×™×©×•×™ ×ž×¢×¨×š ×”××•×˜×•×‘×•×¡×™× â€” ×ž×©×¨×“ ×”×ª×—×‘×•×¨×”", d: "×ž-01.2022 ×•×”×œ××”",
    b: "×ž××’×¨ ×‘-data.gov.il ×¢× ×©×•×¨×” ×œ×›×œ ×ž×§\"×˜ ×œ×›×œ ×™×•×: ×”×¡×•×’ (×¢×™×¨×•× ×™/×‘×™× ×¢×™×¨×•× ×™) ×•×”×’×•×“×œ (××•×˜×•×‘×•×¡/×ž×™× ×™×‘×•×¡/×ž×™×“×™×‘×•×¡/×ž×¤×¨×§×™) ×©×œ ×”×¨×›×‘ ×©× ×§×‘×¢ ×œ×§×•. ×ž×ž× ×• ×ž×’×™×¢×™× \"×©×™× ×•×™ ×¡×•×’ ×¨×›×‘\" ×‘×¦×™×¨ ×”×–×ž×Ÿ ×•×”×ª×’ ×œ×™×“ ×”× ×’×™×©×•×ª. ×”×©×“×•×ª ×§×™×™×ž×™× ×¨×§ ×ž-2022." },
  { t: "×¨×©×ª 2012 ×ž××ª×¨ ×ž×’×™×¢×™×", d: "×¦×™×œ×•× ×™×—×™×“ ×ž-2012",
    b: "×¨×¦×¤×™ ×”×ª×—× ×•×ª ×©×œ 3,214 ×§×•×•×™×, ×—×ž×© ×©× ×™× ×œ×¤× ×™ ×ª×—×™×œ×ª ×”××¨×›×™×•×Ÿ. ×ž×©×ž×© ×›×¢×•×’×Ÿ ×‘×¢×ž×•×“ ×”×§×•, ×•×›×¢×“×•×ª ×©×§×• ×¢×¦×¨ ×‘×ª×—× ×” ×ž×¡×•×™×ž×ª." },
];
const TT_LABEL = { rail: "×¨×›×‘×ª", taxi: "×ž×•× ×™×ª ×©×™×¨×•×ª", lightrail: "×¨×›×‘×ª ×§×œ×”",
                   cable: "×¨×›×‘×œ/×›×¨×ž×œ×™×ª", demand: "×©×™×¨×•×ª ×œ×¤×™ ×“×¨×™×©×”" };

function ModesTab({ idx, openLine, spec }) {
  const [sel, setSel] = useState(() => new Set());
  const [q, setQ] = usePersistedQ("lh-q-" + spec.k);
  const [lim, setLim] = useState(200);
  useEffect(() => { setLim(200); setSel(new Set()); }, [spec.k]);
  useEffect(() => setLim(200), [q, sel]);

  const mine = useMemo(() => idx.lines.filter((l) => spec.tts.includes(l.tt)), [idx, spec]);
  const counts = useMemo(() => {
    const c = {};
    spec.groups.forEach((m) => { c[m.k] = mine.filter((l) => m.tts.includes(l.tt)).length; });
    return c;
  }, [mine, spec]);
  const toggle = (k) => setSel((s) => { const n = new Set(s); n.has(k) ? n.delete(k) : n.add(k); return n; });

  // ×‘-useMemo â€” ×©×œ× ×™×¨×•×¥ ×ž×—×“×© ×¢×œ ×›×œ ×¨×™× ×“×•×¨ ×©××™× ×• ×§×©×•×¨ ×œ×—×™×¤×•×© (×¡×¢×™×£ 14)
  const { list, total } = useMemo(() => {
    const needle = q.trim();
    const allowed = sel.size ? new Set(spec.groups.filter((m) => sel.has(m.k)).flatMap((m) => m.tts)) : null;
    let ls = mine.filter((l) => !allowed || allowed.has(l.tt));
    if (needle) {
      const toks = sQ(needle).split(/\s+/).filter(Boolean);
      ls = ls.filter((l) => toks.every((t) =>
        (l.line || "").startsWith(t) || l.rd.startsWith(t) ||
        sQ(l.dest).includes(t) || sQ(l.op).includes(t)));
    }
    const lnum = (l) => parseInt(l.line) || 1e9;
    ls = ls.slice().sort((a, b) => lnum(a) - lnum(b) || (a.line || "").localeCompare(b.line || "") || a.rd.localeCompare(b.rd));
    return { total: ls.length, list: ls.slice(0, lim) };
  }, [mine, spec, sel, q, lim]);

  return (
    <div className="card">
      {spec.groups.length > 1 && (
        <div className="kfilter">
          <button className={"kchip" + (sel.size ? "" : " on")}
            style={sel.size ? {} : { borderColor: "#7c3aed", color: "#5b21b6" }}
            onClick={() => setSel(new Set())}>×”×›×œ<b>{mine.length.toLocaleString()}</b></button>
          {spec.groups.map((m) => (
            <button key={m.k} className={"kchip" + (sel.size && !sel.has(m.k) ? " off" : "")}
              style={sel.has(m.k) ? { borderColor: "#7c3aed", color: "#5b21b6" } : {}}
              onClick={() => toggle(m.k)}>
              {m.icon} {m.label}<b>{(counts[m.k] || 0).toLocaleString()}</b>
            </button>
          ))}
        </div>
      )}
      <input className="search" type="search" dir="rtl"
        placeholder="×—×™×¤×•×©: ×ž×¡×¤×¨ ×§×•, ×ž×§×´×˜, ×™×¢×“ ××• ×ž×¤×¢×™×œâ€¦"
        value={q} onChange={(e) => setQ(e.target.value)} />
      <div className="llist">
        {list.map((l) => (
          <a key={l.rd} className="lrow" href={lineHref(l.rd)}
            onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); openLine(l.rd); }}>
            <span className="badge sm">{l.line || TT_ICON[l.tt] || "â€”"}</span>
            {l.lk === "removed" && (
              <span className="k" style={{ background: isRemovedYear(l) ? "#7f1d1d" : "#dc2626" }}>
                {isRemovedYear(l) ? "×‘×•×˜×œ â€” ×ž×¢×œ ×©× ×”" : "×‘×•×˜×œ"}
              </span>
            )}
            <span className="ldest">{l.dest}</span>
            <span className="lmeta">{l.op} Â· ×ž×§×´×˜ <span className="rdnum" dir="ltr">{rdTxt(l.rd)}</span> Â· {l.v > 1 ? (l.v - 1) + " ×©×™× ×•×™×™×" : "×œ×œ× ×©×™× ×•×™×™× ×¢×“×™×™×Ÿ"}</span>
          </a>
        ))}
        {list.length === 0 && <div className="empty">×œ× × ×ž×¦× ×§×• ×ª×•××.</div>}
        {total > list.length && (
          <button className="morebtn" onClick={() => setLim(lim + 300)}>
            âŒ„ ×”×¦×’ ×¢×•×“ â€” ×ž×•×¦×’×™× {list.length.toLocaleString()} ×ž×ª×•×š {total.toLocaleString()}
          </button>
        )}
      </div>
    </div>
  );
}

/* ×”×•×“×¢×ª ×”×‘×“×™×§×”-×ž×—×“×© (×‘×§×©×ª ×©×œ×ž×”): ×”×ª×’×œ×ª×” ×ª×§×œ×” ×‘×—×œ×§ ×ž×”×ž×™×“×¢ ×”×”×™×¡×˜×•×¨×™ â€”
   ×©×¨×˜×•×˜×™× ×•×ž×§"×˜×™× ×—×¡×¨×™× ×•×©×™× ×•×™×™× ×‘×œ×™ ×ª××¨×™×š ×ž×“×•×™×§ â€” ×•×›×œ ×”××¨×›×™×•×Ÿ × ×‘×“×§
   ×ž×—×“×© ×ž×•×œ ×©× ×™ ×”×ž×§×•×¨×•×ª. ×”×˜×•×•×—×™× ×—×™×™× ×‘×§×•×‘×¥ ×¡×˜×˜×•×¡ × ×¤×¨×“ ×œ×›×œ ×ž× ×•×¢ ×¡×¨×™×§×”
   (recheck-ob / recheck-tf) â€” ×§×•×‘×¥ ×ž×©×•×ª×£ ×’×¨× ×œ×§×•× ×¤×œ×™×§×˜×™× ×‘×™×Ÿ ×”×ž× ×•×¢×™× â€”
   ×•-recheck.json ×”×™×©×Ÿ × ×©××¨ ×›×‘×¡×™×¡. ×”×ž×™×–×•×’ ×›××Ÿ, ×‘×“×¤×“×¤×Ÿ. */
function RecheckNotice() {
  const [rc, setRc] = useState(null);
  useEffect(() => {
    const grab = (u) => dfetch(u).then((r) => r.json()).catch(() => ({}));
    Promise.all([grab("data/recheck.json"), grab("data/recheck-ob.json"), grab("data/recheck-tf.json")])
      .then(([a, b, c]) => setRc({ ...a, ...b, ...c }))
      .catch(() => {});
  }, []);
  if (!rc || !rc.ob_young) return null;
  const f = (d) => (d ? `${d.slice(8, 10)}.${d.slice(5, 7)}.${d.slice(0, 4)}` : "");
  const obDone = rc.ob_pass >= 2 || rc.ob_young <= "2022-01-16";
  const tfDone = rc.tf_young && rc.tf_young <= "2017-03-20";
  if (obDone && tfDone && rc.ob_pass >= 2) return null;   // ×”×‘×“×™×§×” ×”×¡×ª×™×™×ž×” â€” ×”×”×•×“×¢×” ×™×•×¨×“×ª
  return (
    <div style={{ background: "#fffbeb", border: "1px solid #f3d9a4", borderRadius: 12,
      padding: "10px 14px", margin: "10px 0 2px", fontSize: 14, lineHeight: 1.55, color: "#57534e" }}>
      <b style={{ color: "#92400e" }}>ðŸ› ï¸ ×”×•×“×¢×”: ×”×ª×’×œ×ª×” ×ª×§×œ×” ×‘×—×œ×§ ×ž×”×ž×™×“×¢ ×”×”×™×¡×˜×•×¨×™, ×•×”××¨×›×™×•×Ÿ × ×‘×“×§ ×•×ž×ª×¢×“×›×Ÿ ×ž×—×“×© ×ž×•×œ ×©× ×™ ×”×ž×§×•×¨×•×ª.</b>
      <div>
        âœ… <b>×˜×•×•×— ×”× ×ª×•× ×™× ×”×ž×¢×•×“×›×Ÿ ×•×”×ž××•×ž×ª: {f(rc.ob_young)} â† ×”×™×•×</b>{rc.ob_pass >= 2 ? " (×ž×¢×‘×¨ ××™×ž×•×ª ×©× ×™)" : ""}.
      </div>
      <div>
        ðŸ”„ × ×ª×•× ×™× ×ž×œ×¤× ×™ {f(rc.ob_young)} (×¢×“ ×ž×¨×¥ 2017) ×¢×“×™×™×Ÿ ×‘×‘×“×™×§×” ×ž×—×“×© ×•×¢×©×•×™×™× ×œ×”×ª×¢×“×›×Ÿ ×‘×™×ž×™× ×”×§×¨×•×‘×™×.
      </div>
    </div>
  );
}

function App() {
  const [idx, setIdx] = useState(null);
  const [err, setErr] = useState(null);
  // â€Ž#t=stopsâ€Ž ×•×›×“' â€” ×”×˜××‘ × ×©×ž×¨ ×‘×›×ª×•×‘×ª, ×›×“×™ ×©×¨×™×¢× ×•×Ÿ ×œ× ×™×—×–×™×¨ ×œ×¢×ž×•×“ ×”×¨××©×™
  const [tab, setTab] = useState(() => {
    const h = decodeURIComponent((location.hash || "").slice(1));
    if (h.startsWith("stop=")) return "stops";
    if (h.startsWith("t=")) {
      const t = h.slice(2);
      if (t === "stops" || t === "map" || TABS.some((x) => x.k === t)) return t;
    }
    return "lines";
  });
  const [q, setQ] = usePersistedQ("lh-q-main");
  const [kats, setKats] = useState(() => new Set());   // ×§×˜×’×•×¨×™×•×ª ×ž×¡×•×ž× ×•×ª (×‘×—×™×¨×” ×ž×¨×•×‘×”)
  const [katOpen, setKatOpen] = useState(false);
  // ×“×£ ×§×• × ×›× ×¡ ×œ×”×™×¡×˜×•×¨×™×™×ª ×”×“×¤×“×¤×Ÿ (×•×’× ×œ×§×™×©×•×¨, ××—×¨×™ ×”-#) â€” ×›×¤×ª×•×¨ "××—×•×¨×”"
  // ×‘×˜×œ×¤×•×Ÿ ×—×•×–×¨ ×œ×¨×©×™×ž×” ×‘×ž×§×•× ×œ×¦××ª ×ž×”××ª×¨, ×•×§×™×©×•×¨ ×œ×§×• × ×¤×ª×— ×™×©×™×¨×•×ª ×¢×œ×™×•.
  // â€Ž#2012/<k>â€Ž ×”×•× ×›×ª×•×‘×ª ×©×œ ×§×• 2012 ×‘×œ×™ ×ž×§×‘×™×œ×” ×©×œ ×”×™×•× â€” × ×¤×ª×— ×‘×¤×™×“.
  const H0 = decodeURIComponent((location.hash || "").slice(1));
  const isStopH = (h) => h.startsWith("stop=");
  // â€Ž#×ž×§×˜@×ª××¨×™×šâ€Ž â€” ×§×™×©×•×¨ ×ž×¦×™×¨ ×ª×—× ×” ×©× ×•×—×ª ×™×©×¨ ×¢×œ ×’×¨×¡×ª ×”×©×™× ×•×™
  const splitRdDate = (h) => { const i = h.lastIndexOf("@"); return i > 0 ? [h.slice(0, i), h.slice(i + 1)] : [h, null]; };
  const isDigestH = (h) => h.startsWith("digest=");
  const parseDigest = (h) => { const [c, d] = h.slice(7).split("@"); return { city: c, days: parseInt(d) || 7 }; };
  const [dig, setDig] = useState(() => (isDigestH(H0) ? parseDigest(H0) : null));
  const _plainH = H0 && !H0.startsWith("2012/") && !isStopH(H0) && !isDigestH(H0) && !H0.startsWith("t=");
  const [rd, setRd] = useState(() => (_plainH ? splitRdDate(H0)[0] : null));
  const [rdDate, setRdDate] = useState(() => (_plainH ? splitRdDate(H0)[1] : null));
  const [stopSel, setStopSel] = useState(() => (isStopH(H0) ? H0.slice(5) : null));
  // ×ž×•× ×” ×¤×ª×™×—×•×ª: ×œ×—×™×¦×” ×—×•×–×¨×ª ×¢×œ ×§×™×©×•×¨ ×œ××•×ª×” ×ª×—× ×” ×—×™×™×‘×ª ×œ×¤×ª×•×— ×ž×—×“×© ×’×
  // ×›×©×”×ž×–×”×” ×¢×¦×ž×• ×œ× ×”×©×ª× ×”
  const [stopSelN, setStopSelN] = useState(0);
  const [byDay, setByDay] = useState(false);   // ×ª×¦×•×’×ª "×©×™× ×•×™×™× ×œ×¤×™ ×™×•×"
  // â€Ž#2012/<k>â€Ž ×”×•× ×¢×ž×•×“ ×œ×›×œ ×“×‘×¨, ×•×œ× ×©×•×¨×” ×©× ×¤×ª×—×ª ×‘×ª×•×š ×”×¤×™×“
  const [k12, setK12] = useState(() => (H0.startsWith("2012/") ? H0.slice(5) : null));
  const [anc12, setAnc12] = useState({});      // ×ž×¤×ª×— ×§×• 2012 -> ×ž×§"×˜ ×©×œ ×”×™×•×
  useEffect(() => {
    if (!k12) return;
    getAnchors2012().then((d) => {
      const m = {};
      for (const [rd, v] of Object.entries(d.anchors || {})) if (!(v.k in m)) m[v.k] = rd;
      setAnc12(m);
    });
  }, [k12]);
  // ×¢×¨×™× ×œ×”×¨×©×ž×” ×œ×”×ª×¨××•×ª â€” ×ž×¢×¨×™ ×”×§×¦×” ×©×œ ×›×œ ×”×§×•×•×™× ×‘×§×˜×œ×•×’
  const notifyCities = useMemo(() => {
    const cnt = {};
    (((idx || {}).lines) || []).forEach((l) => destCities(l.dest).forEach((c) => { cnt[c] = (cnt[c] || 0) + 1; }));
    return Object.keys(cnt).filter((c) => cnt[c] >= 3).sort((a, b) => cnt[b] - cnt[a]);
  }, [idx]);
  const [lim, setLim] = useState(200);   // "×”×¦×’ ×¢×•×“" ×ž×¨×—×™×‘; ×—×™×¤×•×© ×—×“×© ×ž××¤×¡
  useEffect(() => setLim(200), [q, kats]);
  useEffect(() => {
    const onPop = (e) => { setRd((e.state && e.state.rd) || null); setK12((e.state && e.state.k12) || null); };
    const onHash = () => {
      const h = decodeURIComponent((location.hash || "").slice(1));
      if (h.startsWith("2012/")) { setRd(null); setK12(h.slice(5)); return; }
      if (isStopH(h)) { setRd(null); setStopSel(h.slice(5)); setStopSelN((n) => n + 1); setTab("stops"); return; }
      if (isDigestH(h)) { setRd(null); setK12(null); setDig(parseDigest(h)); return; }
      if (h.startsWith("t=")) { setRd(null); setK12(null); const t = h.slice(2); if (t === "stops" || t === "lines" || t === "map" || TABS.some((x) => x.k === t)) setTab(t); return; }
      // ×›×ª×•×‘×ª ×©×œ ×§×• × ×§×¨××” ×¨×§ ×‘×˜×¢×™× ×” ×”×¨××©×•× ×”: ×ž×™ ×©×”×“×‘×™×§ ×§×™×©×•×¨ ×œ×§×• ×‘×©×•×¨×ª
      // ×”×›×ª×•×‘×ª ×©×œ ×œ×©×•× ×™×ª ×¤×ª×•×—×”, ××• ×¢×¨×š ××ª ×”×›×ª×•×‘×ª ×™×“× ×™×ª, × ×©××¨ ×‘×ž×¡×š ×”×§×•×“×.
      // pushState/replaceState ××™× × ×ž×¤×¢×™×œ×™× hashchange, ×•×œ×›×Ÿ ××™×Ÿ ×›××Ÿ ×œ×•×œ××”.
      if (h) { setK12(null); const [r, dt] = splitRdDate(h); setRd(r); setRdDate(dt); }
    };
    window.addEventListener("popstate", onPop);
    window.addEventListener("hashchange", onHash);
    return () => { window.removeEventListener("popstate", onPop); window.removeEventListener("hashchange", onHash); };
  }, []);
  const openLine = (r) => {
    setDig(null); setK12(null); setRdDate(null); history.pushState({ rd: r }, "", "#" + encodeURIComponent(r)); setRd(r); };
  const open12 = (k) => { history.pushState({ k12: k }, "", "#2012/" + encodeURIComponent(k)); setK12(k); };
  const switchLine = (r) => { history.replaceState({ rd: r }, "", "#" + encodeURIComponent(r)); setRd(r); };
  // ×”×›×ª×•×‘×ª ×—×•×–×¨×ª ×œ×©×•×¨×© ×¨×§ ×‘×˜××‘ "×§×•×•×™×"; ×‘×›×œ ×˜××‘ ××—×¨ × ×©××¨ â€Ž#t=<×˜××‘>â€Ž â€”
  // ×›×š ×¨×™×¢× ×•×Ÿ ×ž×—×–×™×¨ ×œ××•×ª×• ×ž×§×•× ×•×œ× ×œ×¢×ž×•×“ ×”×¨××©×™ (×‘×§×©×ª ×©×œ×ž×”)
  const clearHashKeepTab = (t) => {
    const tt = typeof t === "string" ? t : tab;
    history.replaceState(null, "", tt && tt !== "lines" ? "#t=" + tt : location.pathname + location.search);
  };
  const backToList = (t) => {
    setRd(null);
    clearHashKeepTab(t);
  };
  const toggleKat = (k) => setKats((s) => { const n = new Set(s); if (n.has(k)) n.delete(k); else n.add(k); return n; });
  const [rty, setRty] = useState(0);
  useEffect(() => {
    dfetch("data/lines.json")
      .then((r) => { if (!r.ok) throw new Error(r.status); return r.json(); })
      .then(setIdx)
      .catch(setErr);
  }, [rty]);
  const counts = useMemo(() => {
    const c = {};
    if (idx) {
      const keys = CAT_GROUPS.flatMap((g) => g.items);
      // ××•×ª×” ××•×›×œ×•×¡×™×™×” ×©×”×¨×©×™×ž×” ×ž×¦×™×’×”. ××—×¨×ª ×”×ž×¡×¤×¨ ×œ×™×“ ×”×§×˜×’×•×¨×™×” ×’×“×•×œ ×ž×ž×”
      // ×©× ×¤×ª×— ×‘×œ×—×™×¦×” ×¢×œ×™×”.
      idx.lines.filter((l) => !l.tt || l.tt === "demand").forEach((l) => {
        keys.forEach((k) => { if (catMatch(l, k)) c[k] = (c[k] || 0) + 1; });
      });
    }
    return c;
  }, [idx]);
  // ××™×œ×• ×ž×§"×˜×™× ×¢×“×™×™×Ÿ ×¤×¢×™×œ×™× â€” ×›×“×™ ×œ×”×‘×“×™×œ ×—×œ×•×¤×” ×ž×‘×•×˜×œ×ª ×ž×§×• ×©×‘×•×˜×œ ×›×•×œ×•
  // ×”×˜××‘ ××™× ×• ×—×œ×§ ×ž×”×›×ª×•×‘×ª, ×•×œ×›×Ÿ ×¨×™×¢× ×•×Ÿ ××• ×¤×ª×™×—×ª ×§×™×©×•×¨ ×™×©×™×¨ ×œ×§×• ×”×—×–×™×¨×•
  // ×ª×ž×™×“ ×œ"×§×•×•×™×" â€” ×•×›×¤×ª×•×¨ "×—×–×¨×”" ×”×•×¦×™× ××ª ×”×ž×©×ª×ž×© ×ž×§×• ×¨×›×‘×ª ××œ ×¨×©×™×ž×ª
  // ×”××•×˜×•×‘×•×¡×™×. ×”×˜××‘ × ×’×–×¨ ×ž×¡×•×’ ×”×§×• ×”×¤×ª×•×—, ×›×š ×©×”×—×–×¨×” × ×•×—×ª×ª ×‘×ž×§×•× ×©×ž×ž× ×•
  // ×”×’×™×¢ ×’× ×›×©×”×ž×¦×‘ ×‘×–×™×›×¨×•×Ÿ ××‘×“.
  useEffect(() => {
    if (!idx || !rd) return;
    const l = idx.lines.find((x) => x.rd === rd);
    const t = l && l.tt && TABS.find((x) => x.tts.includes(l.tt));
    setTab(t ? t.k : "lines");
  }, [idx, rd]);
  const mktAlive = useMemo(() => {
    const m = {};
    if (idx) idx.lines.forEach((l) => { if (l.lk !== "removed") m[l.rd.split("-")[0]] = true; });
    return m;
  }, [idx]);
  const isLineGone = (l) => l.lk === "removed" && !mktAlive[l.rd.split("-")[0]];
  // ×”×¡×™× ×•×Ÿ ×•×”×ž×™×•×Ÿ ×©×œ 13 ××œ×£ ×©×•×¨×•×ª ×‘-useMemo: ×§×•×“× ×”× ×¨×¦×• ×ž×—×“×© ×’× ×‘×¨×™× ×“×•×¨×™×
  // ×©××™× × ×§×©×•×¨×™× ×œ×—×™×¤×•×© â€” ×¤×ª×™×—×ª ×§×˜×’×•×¨×™×•×ª, "×”×¦×’ ×¢×•×“" â€” ×ª×§×™×¢×•×ª ×ž×•×¨×’×©×ª ×‘× ×™×™×“
  const searchRes = useMemo(() => {
    const needle = q.trim();
    if (!idx || (!needle && !kats.size)) return { list: [], total: 0 };
    const inKats = (l) => {
      if (!kats.size) return true;
      for (const k of kats) { if (catMatch(l, k)) return true; }
      return false;
    };
    // ×—×™×¤×•×© ×¨×‘-×ž×™×œ×™×: "13 ×§×¨×™×ª ×’×ª" â€” ×›×œ ×ž×™×œ×” ×—×™×™×‘×ª ×œ×”×ª××™× ×œ××—×“ ×”×©×“×•×ª.
    // ×”×”×©×•×•××” ×“×¨×š sQ: ×’×¨×©×™×™× ×‘×›×œ ×¦×•×¨×” (×¨×©×œ"×¦ / ×¨×©×œ''×¦ / ×¨×©×œ×´×¦) ×ž×ª××™×ž×™×
    const toks = sQ(needle).split(/\s+/).filter(Boolean);
    const tokHit = (l, t) => l.line === t || l.line.startsWith(t) || l.rd.startsWith(t) ||
      sQ(l.dest).includes(t) || sQ(l.op).includes(t);
    // ×˜××‘ "×§×•×•×™×" ×”×•× ××•×˜×•×‘×•×¡×™×. ×§×•×•×™ "×©×™×¨×•×ª ×œ×¤×™ ×“×¨×™×©×”" ×ž×•×¤×¢×œ×™× ×‘×™×“×™ ×—×‘×¨×•×ª
    // ×”××•×˜×•×‘×•×¡ ×•× ×©××¨×™× ×’× ×›××Ÿ, ×•×œ× ×¨×§ ×‘×˜××‘ ×¡×•×’×™ ×”×ª×—×‘×•×¨×” (×‘×§×©×ª ×”×ž×©×ª×ž×©).
    const buses = idx.lines.filter((l) => !l.tt || l.tt === "demand");
    let list = buses.filter((l) => inKats(l) && toks.every((t) => tokHit(l, t)));
    const onlyRemoval = kats.size > 0 && [...kats].every((k) => REMOVAL_CATS.has(k));
    // ×“×™×¨×•×’: ×§×•×“× ×ž×¡×¤×¨ ×”×§×• ×”×ž×“×•×™×§, ××—×¨×™×• ×§×•×•×™× ×©×ž×ª×—×™×œ×™× ×‘×•, ×•×¨×§ ×‘×¡×•×£
    // ×”×ª××ž×•×ª ×ž×§"×˜/×™×¢×“/×ž×¤×¢×™×œ â€” ×•×‘×ª×•×š ×›×œ ×“×¨×’×” ×œ×¤×™ ×¡×“×¨ ×ž×¡×¤×¨×™
    const numTok = toks.find((t) => /^\d/.test(t)) || toks[0] || "";
    const rank = (l) => !numTok ? 0 : l.line === numTok ? 0 : l.line.startsWith(numTok) ? 1
      : l.rd.startsWith(numTok) ? 2 : ((l.dest || "").includes(numTok) ? 3 : 4);
    const lnum = (l) => parseInt(l.line) || 1e9;
    if (needle) list.sort((a, b) => rank(a) - rank(b) || lnum(a) - lnum(b) || a.line.localeCompare(b.line) || a.rd.localeCompare(b.rd));
    else if (onlyRemoval) list.sort((a, b) => (b.ld || "").localeCompare(a.ld || ""));
    else list.sort((a, b) => lnum(a) - lnum(b) || a.line.localeCompare(b.line) || a.rd.localeCompare(b.rd));
    return { total: list.length, list: list.slice(0, lim) };
  }, [idx, q, kats, lim]);
  // ×¡×˜×˜×•×¡ HTTP = ×”×§×•×‘×¥ ×‘××ž×ª ×œ× ×§×™×™×; ×›×œ ×›×©×œ ××—×¨ ×”×•× ×ª×§×œ×ª ×¨×©×ª â€” ×¢× ×›×¤×ª×•×¨
  // × ×™×¡×™×•×Ÿ ×—×•×–×¨ ×‘×ž×§×•× ×”×•×“×¢×” ×©×’×•×¨×ž×ª ×œ×’×•×œ×© ×œ×—×©×•×‘ ×©××™×Ÿ × ×ª×•× ×™×
  if (err) return (
    <div className="boot">{/^\d+$/.test(String(err && err.message))
      ? "×”× ×ª×•× ×™× ×¢×•×“ ×œ× × ×•×¦×¨×• â€” ×”×¨×™×¦×” ×”×¨××©×•× ×” ×©×œ ×”×¦×™× ×•×¨ ×ª×™×¦×•×¨ ××•×ª×. × ×¡×• ×œ×¨×¢× ×Ÿ ×ž××•×—×¨ ×™×•×ª×¨."
      : <>ðŸ“¡ ×”× ×ª×•× ×™× ×œ× ×™×¨×“×• â€” ×›× ×¨××” ×ª×§×œ×ª ×¨×©×ª ×¨×’×¢×™×ª.{" "}
        <button className="morebtn" onClick={() => { setErr(null); setRty((n) => n + 1); }}>â†» × ×¡×• ×©×•×‘</button></>}
    </div>);
  // ×”××™× ×“×§×¡ (3.6MB) × ×“×¨×© ×¨×§ ×œ×—×™×¤×•×© ×”×§×•×•×™× â€” ×˜××‘ ×”×ª×—× ×•×ª, ×”×¤×™×“ ×”×™×•×ž×™ ×•×¢×ž×•×“×™
  // ×§×•×•×™× ×ž×§×™×©×•×¨ ×™×©×™×¨ ×¢×•×‘×“×™× ×‘×œ×¢×“×™×•, ×•×œ×›×Ÿ ×”××ª×¨ ×›×‘×¨ ×œ× ×ž×—×›×” ×œ×• ×›×“×™ ×œ×”×•×¤×™×¢
  const needle = q.trim();
  const { list, total } = searchRes;
  const changed = idx ? idx.lines.filter((l) => l.v > 1).length : 0;
  return (
    <div className="wrap">
      <RecheckNotice />
      <header>
        <h1>ðŸ•°ï¸ ×”×§×• ×‘×–×ž×Ÿ</h1>
        <p className="tag">×›×œ ×©×™× ×•×™ ×©× ×›× ×¡ ×œ×ª×•×§×£ ×‘×ž×¡×œ×•×œ×™ ×”×§×•×•×™× ×•×‘×ª×—× ×•×ª â€” ×ž×¡×œ×•×œ, ×©×¨×˜×•×˜, ×ª×—× ×•×ª ×•×©×ž×•×ª. ×ž×”×©×•×•××ª ×”-GTFS ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×”, ×™×•× ×ž×•×œ ×™×•×, ×ž×ž×¨×¥ 2017 ×•×¢×“ ×”×™×•×.</p>
        <div className="stats">
          {idx ? (<>
            <span className="stat"><b>{idx.lines.length.toLocaleString()}</b> ×•×¨×™×× ×˜×™× ×ž×ª×•×¢×“×™×</span>
            <span className="stat"><b>{changed.toLocaleString()}</b> ×¢× ×©×™× ×•×™×™×</span>
            <span className="stat mut">×¢×•×“×›×Ÿ: {idx.gen}</span>
          </>) : <span className="stat mut">×˜×•×¢×Ÿ ××ª ×¨×©×™×ž×ª ×”×§×•×•×™× ×‘×¨×§×¢â€¦</span>}
        </div>
      </header>
      <div className="tabs" role="tablist" aria-label="××–×•×¨×™ ×”××ª×¨">
        <button role="tab" aria-selected={tab === "lines"} className={"tab" + (tab === "lines" ? " on" : "")} title="×—×™×¤×•×© ×‘×›×œ ×§×•×•×™ ×”××•×˜×•×‘×•×¡ ×‘××¨×¥ ×•×”×™×¡×˜×•×¨×™×™×ª ×”×©×™× ×•×™×™× ×©×œ ×›×œ ×§×•" onClick={() => { setTab("lines"); backToList("lines"); }}>ðŸšŒ ×§×•×•×™×</button>
        <button role="tab" aria-selected={tab === "stops"} className={"tab" + (tab === "stops" ? " on" : "")} title="×—×™×¤×•×© ×ª×—× ×•×ª ×•×”×™×¡×˜×•×¨×™×™×ª ×”×©×™× ×•×™×™× ×©×œ×”×Ÿ â€” ×©×™× ×•×™ ×©×, ×”×–×–×”, ×‘×™×˜×•×œ" onClick={() => { setTab("stops"); backToList("stops"); }}>ðŸš ×ª×—× ×•×ª</button>
        <button role="tab" aria-selected={tab === "map"} className={"tab" + (tab === "map" ? " on" : "")} title="×ž×¤×” ×œ×¤×™ ×¢×™×¨ ×•×—×•×“×©: ×ª×—× ×•×ª ×©×”×©×ª× ×• ×•×§×•×•×™× ×©×”×©×ª× ×•, ×›×¤×™ ×©×”×™×• ××–" onClick={() => { setTab("map"); backToList("map"); }}>ðŸ—ºï¸ ×ž×¤×”</button>
        {TABS.map((t) => (
          <button key={t.k} role="tab" aria-selected={tab === t.k} className={"tab" + (tab === t.k ? " on" : "")} title={t.tip}
            onClick={() => { setTab(t.k); backToList(t.k); }}>{t.icon} {t.label}</button>
        ))}
      </div>
      {!rd && !k12 && !dig && <NotifyCenter cities={notifyCities} />}
      {dig ? (
        <DigestPage city={dig.city} days={dig.days} openLine={openLine}
          onBack={() => { setDig(null); clearHashKeepTab(); }} />
      ) : k12 ? (
        <Line2012Page k12={k12} anchorRd={anc12[k12] || null} openLine={openLine}
          onBack={() => { setK12(null); clearHashKeepTab(); }} />
      ) : tab === "stops" ? <StopsTab sel={stopSel} selN={stopSelN} /> : tab === "map" && !rd ? <MapTab idx={idx} openLine={openLine} cities={notifyCities} /> : (TABS.some((t) => t.k === tab) && !rd) ? (
        idx ? <ModesTab idx={idx} openLine={openLine} spec={TABS.find((t) => t.k === tab)} />
          : <div className="card">×˜×•×¢×Ÿ ××ª ×¨×©×™×ž×ª ×”×§×•×•×™×â€¦</div>
      ) : rd ? (
        /* ×§×™×©×•×¨ ×™×©×™×¨ ×œ×§×• × ×¤×ª×— ×œ×¤× ×™ ×©×”××™× ×“×§×¡ ×”×’×™×¢ â€” ×‘×œ×™ ×”×”×’× ×•×ª ×”××œ×” ×”×“×£
           ×§×¨×¡ ×œ×œ×‘×Ÿ (×”×‘××’ ×©×©×œ×ž×” ×ž×¦×): idx ×¢×“×™×™×Ÿ null ×•-idx.lines ×”×ª×¤×•×¦×¥ */
        <LinePage rd={rd} lineGone={idx ? !mktAlive[rd.split("-")[0]] : false}
          sibs={((idx && idx.lines) || []).filter((x) => x.rd.split("-")[0] === rd.split("-")[0])}
          onSwitch={switchLine} onBack={backToList} initDate={rdDate}
          initCats={[...kats].sort().join(",")} />
      ) : (
        <div className="card">
          {/* "×©×™× ×•×™×™× ×œ×¤×™ ×™×•×" ×‘××•×ª×• ×¢×ž×•×“, ×ž×ª×—×ª ×œ×§×˜×’×•×¨×™×•×ª, ×•×”×§×˜×’×•×¨×™×•×ª ×”×ž×¡×•×ž× ×•×ª
              ×ž×¡× × ×•×ª ×’× ××•×ª×• (×©×œ×ž×” 13.09) */}
          {!byDay && <input className="search" type="search" dir="rtl" autoFocus
            placeholder="×—×™×¤×•×© ×§×•: ×ž×¡×¤×¨ ×§×•, ×ž×§×´×˜, ×™×¢×“ ××• ×ž×¤×¢×™×œâ€¦"
            value={q} onChange={(e) => setQ(e.target.value)} />}
          <div className="katbox">
            <button className="kathead" aria-pressed={byDay} title={byDay ? "×—×–×¨×” ×œ×—×™×¤×•×© ×”×§×•×•×™×" : "×¤×™×“ ×›×¨×•× ×•×œ×•×’×™: ×‘×—×™×¨×ª ×©× ×” ×•×—×•×“×© ×•×¨×•××™× ×›×œ ×©×™× ×•×™ ×©×§×¨×”, ×‘×›×œ ×§×• ×‘××¨×¥, ×œ×¤×™ ×ª××¨×™×š"} onClick={() => setByDay(!byDay)}>
              {byDay ? "ðŸ”Ž ×—×–×¨×” ×œ×—×™×¤×•×© ×”×§×•×•×™×" : "ðŸ—“ï¸ ×©×™× ×•×™×™× ×œ×¤×™ ×™×•× â€” ×ž×” ×”×©×ª× ×” ×‘×›×œ ×ª××¨×™×š, ×‘×›×œ ×”×§×•×•×™×"}
            </button>
          </div>
          <div className="katbox">
            <button className="kathead" aria-expanded={katOpen} onClick={() => setKatOpen(!katOpen)}>
              <span className="katarrow" aria-hidden="true">{katOpen ? "â–¼" : "â—€"}</span>
              ðŸ—‚ï¸ ×§×˜×’×•×¨×™×•×ª ×œ×‘×—×™×¨×”
              {kats.size > 0 && <b className="katn">{kats.size} ×ž×¡×•×ž× ×•×ª</b>}
            </button>
            {katOpen && (
              <div className="katlist">
                {CAT_GROUPS.map((g) => (
                  <React.Fragment key={g.title}>
                    <div className="katgrp">{g.title}</div>
                    {g.items.map((k) => (
                      <label key={k} className="katrow">
                        <input type="checkbox" checked={kats.has(k)} onChange={() => toggleKat(k)} />
                        <i className="katdot" style={{ background: catColor(k) }} />
                        <span className="katlab">{CAT_LABELS[k]}</span>
                        {/* ×¢×“ ×©××™× ×“×§×¡ ×”×§×•×•×™× × ×˜×¢×Ÿ ×”×ž×•× ×” ×”×¨××” 0 ×ž×˜×¢×” */}
                        <b className="katc">{idx === null ? "×˜×•×¢×Ÿâ€¦" : (counts[k] || 0).toLocaleString()}</b>
                      </label>
                    ))}
                  </React.Fragment>
                ))}
                <div className="katnote">
                  â„¹ï¸ ×›×œ ×¡×•×’×™ ×”×©×™× ×•×™×™× ×ž×—×•×©×‘×™× ×ž×”×©×•×•××ª ×¦×™×œ×•×ž×™ ×”×¤×™×“ â€” ×ž×ž×¨×¥ 2017 ×•×¢×“ ×”×™×•×.
                  ×”×™×•×¦× ×ž×Ÿ ×”×›×œ×œ ×”×•× ×©×™× ×•×™ ×ž×¡×¤×¨ ×”×¨×›×‘×™× ×‘××•×ª×” ×™×¦×™××”, ×©× ×¨×©× ×¨×§ ×ž-08.2026:
                  ×”×•× × ×©×¢×Ÿ ×¢×œ ×§×•×‘×¥ ×¨×™×©×•×™ ×©×”××¨×›×™×•× ×™× ×œ× ×©×ž×¨×•.
                </div>
                {kats.size > 0 && (
                  <button className="katclear" onClick={() => setKats(new Set())}>âœ– × ×§×” ××ª ×”×‘×—×™×¨×”</button>
                )}
              </div>
            )}
          </div>
          {byDay ? (
            <DayFeed idx={idx} openLine={openLine} open12={open12} kats={kats} embedded onBack={() => setByDay(false)} />
          ) : (needle || kats.size > 0) && !idx ? (
            <div className="empty">×˜×•×¢×Ÿ ××ª ×¨×©×™×ž×ª ×”×§×•×•×™× â€” ×”×—×™×¤×•×© ×™×¢×‘×•×“ ×‘×¢×•×“ ×¨×’×¢â€¦</div>
          ) : (needle || kats.size > 0) ? (
            <div className="llist">
              {list.map((l) => (
                <a key={l.rd} className="lrow" href={lineHref(l.rd)}
                  onClick={(e) => { if (!plainClick(e)) return; e.preventDefault(); openLine(l.rd); }}>
                  <span className="badge sm">{l.line || TT_ICON[l.tt] || "â€”"}</span>
                  {l.lk === "removed" && (isLineGone(l) ? (
                    <span className="k" style={{ background: isRemovedYear(l) ? "#7f1d1d" : "#dc2626" }}>
                      {isRemovedYear(l) ? "×”×§×• ×‘×•×˜×œ â€” ×ž×¢×œ ×©× ×”" : "×”×§×• ×‘×•×˜×œ"}
                    </span>
                  ) : (
                    <span className="k" style={{ background: isRemovedYear(l) ? "#9a3412" : "#ea580c" }}>
                      {isRemovedYear(l) ? "×—×œ×•×¤×” ×‘×•×˜×œ×” â€” ×ž×¢×œ ×©× ×”" : "×—×œ×•×¤×” ×‘×•×˜×œ×”"}
                    </span>
                  ))}
                  <span className="ldest">{l.dest}</span>
                  <span className="lmeta">{l.op} Â· ×ž×§×´×˜ <span className="rdnum" dir="ltr">{rdTxt(l.rd)}</span> Â· {l.v > 1 ? (l.v - 1) + " ×©×™× ×•×™×™×" : "×œ×œ× ×©×™× ×•×™×™× ×¢×“×™×™×Ÿ"}
                    {l.lk === "removed" && <> Â· ×ž×‘×•×˜×œ ×ž××– {fmtD(l.ld)}</>}</span>
                </a>
              ))}
              {list.length === 0 && (
                <div className="empty">{kats.size > 0 && !needle
                  ? "××™×Ÿ ×¢×“×™×™×Ÿ ×§×•×•×™× ×‘×§×˜×’×•×¨×™×•×ª ×©×¡×™×ž× ×ª× â€” ×§×˜×’×•×¨×™×•×ª ×©×œ ×ž×¡×œ×•×œ ×•×ª×—× ×•×ª ×ž×¦×˜×‘×¨×•×ª ×ž×”×”×©×•×•××•×ª ×”×™×•×ž×™×•×ª ×ž×›××Ÿ ×•×”×œ××”."
                  : "×œ× × ×ž×¦× ×§×• ×ª×•××."}</div>
              )}
              {total > list.length && (
                <button className="morebtn" onClick={() => setLim(lim + 300)}>
                  âŒ„ ×”×¦×’ ×¢×•×“ â€” ×ž×•×¦×’×™× {list.length.toLocaleString()} ×ž×ª×•×š {total.toLocaleString()}
                </button>
              )}
              <Res2012 needle={needle} onOpen={open12} />
            </div>
          ) : (
            <RecentChanges idx={idx} openLine={openLine} onAll={() => setByDay(true)} />
          )}
        </div>
      )}
      <details className="srcbox">
        <summary>ðŸ“š ×”×ž×§×•×¨×•×ª â€” ×ž××™×¤×” ×ž×’×™×¢ ×›×œ ×¤×¨×˜, ×•×¢×“ ×œ××Ÿ ×”×•× ×ž×’×™×¢</summary>
        <div className="srclist">
          {SOURCES.map((s) => (
            <div className="srcitem" key={s.t}>
              <div className="srct">{s.t} <span className="srcd">{s.d}</span></div>
              <div className="srcb">{s.b}</div>
            </div>
          ))}
          <div className="srcfoot">
            ×›×œ ×”×ž×§×•×¨×•×ª ×”× ×¦×™×œ×•×ž×™× ×©×œ ××•×ª×• ×¤×¨×¡×•× â€” ×§×•×‘×¦×™ ×”-GTFS ×©×œ ×ž×©×¨×“ ×”×ª×—×‘×•×¨×”. ×ž×” ×©×ž×•×¤×™×¢
            ×›××Ÿ ×›×©×™× ×•×™ ×”×•× ×”×¤×¨×© ×‘×™×Ÿ ×©× ×™ ×¦×™×œ×•×ž×™×, ×•×œ×›×Ÿ ×”×•× ×ž×©×§×£ ××ª ×ž×” ×©×¤×•×¨×¡× ×•×œ× ×‘×”×›×¨×— ××ª ×ž×”
            ×©×§×¨×” ×‘×©×˜×—. ×ª××¨×™×š ×©×œ ×©×™× ×•×™ ×ž×“×•×™×§ ×¢×“ ×œ×¦×™×œ×•× ×”×§×•×“×: ×›×©×”×¦×™×œ×•×ž×™× ××™× × ×™×•×ž×™×™×, ×ž×•×¦×’
            ×”×—×•×“×© ×‘×œ×‘×“.
          </div>
        </div>
      </details>
      <footer>
        ×‘×ž×¡×’×¨×ª <a href="../" target="_blank" rel="noopener">×”×§×• ×”×‘×•×—×Ÿ</a> Â· ×”× ×ª×•× ×™×: GTFS ×ž×©×¨×“ ×”×ª×—×‘×•×¨×” Â·
        ×”×™×¡×˜×•×¨×™×”: TransitFeeds/OpenMobilityData (2017â€“2022), ××•×¤×Ÿ ×‘××¡ â€” ×”×¡×“× × ×œ×™×“×¢ ×¦×™×‘×•×¨×™ (2022â€“2026), ×ž×’×™×¢×™× (2012)
      </footer>
    </div>
  );
}

ReactDOM.createRoot(document.getElementById("root")).render(<App />);


===== קישורים =====

